{
  "$comment": "GENERATED by ggen from the admitted mcp:MessageType graph. Do not edit.",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "mcp_resource": {
      "type": "object",
      "description": "A resource definition advertised by an MCP server.",
      "properties": {
        "uri": {
          "type": "string",
          "description": "Resource URI."
        },
        "name": {
          "type": "string",
          "description": "Resource name."
        },
        "title": {
          "type": "string",
          "description": "Human-readable resource title."
        },
        "description": {
          "type": "string",
          "description": "Resource description."
        },
        "mimeType": {
          "type": "string",
          "description": "Resource MIME type."
        },
        "size": {
          "type": "integer",
          "description": "Resource size in bytes."
        }
      },
      "required": ["uri", "name"
      ]

    },
    "mcp_resource_read_request": {
      "type": "object",
      "description": "A resources/read request payload.",
      "properties": {
        "uri": {
          "type": "string",
          "description": "URI of the resource to read."
        }
      },
      "required": ["uri"
      ]

    },
    "mcp_resource_read_result": {
      "type": "object",
      "description": "A resources/read result payload.",
      "properties": {
        "contents": {
          "type": "object",
          "description": "Wrapped array of resource content items."
        }
      },
      "required": ["contents"
      ]

    },
    "mcp_tool": {
      "type": "object",
      "description": "A tool definition advertised by an MCP server.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Tool name."
        },
        "title": {
          "type": "string",
          "description": "Human-readable tool title."
        },
        "description": {
          "type": "string",
          "description": "Tool description."
        },
        "inputSchema": {
          "type": "object",
          "description": "JSON Schema for the tool's input."
        },
        "outputSchema": {
          "type": "object",
          "description": "JSON Schema for the tool's output."
        }
      },
      "required": ["name", "inputSchema"
      ]

    },
    "mcp_tool_call_request": {
      "type": "object",
      "description": "A tools/call request payload.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the tool to invoke."
        },
        "arguments": {
          "type": "object",
          "description": "Arguments passed to the tool."
        }
      },
      "required": ["name"
      ]

    },
    "mcp_tool_call_result": {
      "type": "object",
      "description": "A tools/call result payload.",
      "properties": {
        "content": {
          "type": "object",
          "description": "Wrapped array of typed content blocks."
        },
        "isError": {
          "type": "boolean",
          "description": "Whether the tool call resulted in an error."
        },
        "structuredContent": {
          "type": "object",
          "description": "Optional structured result content."
        }
      },
      "required": ["content"
      ]

    },
    "rpc_error_response": {
      "type": "object",
      "description": "A JSON-RPC 2.0 error response message.",
      "properties": {
        "jsonrpc": {
          "type": "string",
          "description": "JSON-RPC protocol version, always \"2.0\"."
        },
        "id": {
          "type": "string",
          "description": "Correlation id matching the originating request."
        },
        "error": {
          "type": "object",
          "description": "The wrapped {code, message, data?} error object."
        }
      },
      "required": ["jsonrpc", "id", "error"
      ]

    },
    "rpc_notification": {
      "type": "object",
      "description": "A JSON-RPC 2.0 notification message (no id).",
      "properties": {
        "jsonrpc": {
          "type": "string",
          "description": "JSON-RPC protocol version, always \"2.0\"."
        },
        "method": {
          "type": "string",
          "description": "The notified method name."
        },
        "params": {
          "type": "object",
          "description": "Notification parameters."
        }
      },
      "required": ["jsonrpc", "method"
      ]

    },
    "rpc_request": {
      "type": "object",
      "description": "A JSON-RPC 2.0 request message.",
      "properties": {
        "jsonrpc": {
          "type": "string",
          "description": "JSON-RPC protocol version, always \"2.0\"."
        },
        "id": {
          "type": "string",
          "description": "Request correlation id."
        },
        "method": {
          "type": "string",
          "description": "The invoked method name."
        },
        "params": {
          "type": "object",
          "description": "Method parameters."
        }
      },
      "required": ["jsonrpc", "id", "method"
      ]

    },
    "rpc_result_response": {
      "type": "object",
      "description": "A JSON-RPC 2.0 success response message.",
      "properties": {
        "jsonrpc": {
          "type": "string",
          "description": "JSON-RPC protocol version, always \"2.0\"."
        },
        "id": {
          "type": "string",
          "description": "Correlation id matching the originating request."
        },
        "result": {
          "type": "object",
          "description": "The method's result payload."
        }
      },
      "required": ["jsonrpc", "id", "result"
      ]

    }
  }
}
