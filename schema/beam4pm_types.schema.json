{
  "$comment": "GENERATED by ggen from the admitted bpm:RecordType graph. Do not edit.",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "acceptance_criteria_nonweakening": {
      "type": "object",
      "description": "Refuses adaptive changes that weaken the pre-existing acceptance contract.",
      "properties": {
        "assessment_id": {
          "type": "string",
          "description": "Stable non-weakening assessment identity."
        },
        "acceptance_contract_id": {
          "type": "string",
          "description": "Acceptance contract protected."
        },
        "prior_digest": {
          "type": "string",
          "description": "Digest of current criteria."
        },
        "candidate_digest": {
          "type": "string",
          "description": "Digest of proposed criteria."
        },
        "strength_result": {
          "type": "string",
          "description": "Equivalent, stronger, or weaker comparison."
        },
        "refusal_code": {
          "type": "string",
          "description": "Typed refusal when weakening is detected."
        }
      },
      "required": ["assessment_id", "acceptance_contract_id", "prior_digest", "candidate_digest", "strength_result", "refusal_code"
      ]

    },
    "account_discovery": {
      "type": "object",
      "description": "Receipted enterprise account-discovery outcome that binds qualification evidence to a measurable discovery score.",
      "properties": {
        "account_discovery_id": {
          "type": "string",
          "description": "Stable identity of this discovery experiment."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose discovery state was observed."
        },
        "discovery_score": {
          "type": "number",
          "description": "Observed bounded discovery qualification score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this outcome."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the customer-value consequence was observed."
        }
      },
      "required": ["account_discovery_id", "account_id", "discovery_score", "evidence_digest", "observed_at"
      ]

    },
    "account_master_match": {
      "type": "object",
      "description": "Admits a source account only when it is bound to one canonical Fortune-5 account by immutable matching evidence; prevents pipeline value from being booked against an ambiguous customer identity.",
      "properties": {
        "source_account_id": {
          "type": "string",
          "description": "Required commercial-admission input for account master match. Missing input is a typed refusal."
        },
        "canonical_account_id": {
          "type": "string",
          "description": "Required commercial-admission input for account master match. Missing input is a typed refusal."
        },
        "match_evidence_hash": {
          "type": "string",
          "description": "Immutable evidence identity that makes this admission independently replayable."
        }
      },
      "required": ["source_account_id", "canonical_account_id", "match_evidence_hash"
      ]

    },
    "account_parent_scope": {
      "type": "object",
      "description": "Qualifies an account only when its buying scope is bound to the correct global parent, preventing subsidiary demand from being double-counted as independent enterprise pipeline.",
      "properties": {
        "account_id": {
          "type": "string",
          "description": "Required account parent scope input; omission is an executable typed refusal, never an inferred approval."
        },
        "parent_account_id": {
          "type": "string",
          "description": "Required account parent scope input; omission is an executable typed refusal, never an inferred approval."
        },
        "scope_evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["account_id", "parent_account_id", "scope_evidence_hash"
      ]

    },
    "account_value_realization": {
      "type": "object",
      "description": "Aggregates verified outcome evidence into an account-level realized-value observation.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "account_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "realization_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "account_id", "realization_hash"
      ]

    },
    "action_pin_evidence": {
      "type": "object",
      "description": "Executable workflow-supply-chain evidence proving a referenced GitHub Action resolves to an immutable commit identity.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this immutable action pin observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "action_sha": {
          "type": "string",
          "description": "Immutable commit SHA resolved for the admitted workflow action."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "action_sha", "observed_result"
      ]

    },
    "activation_event": {
      "type": "object",
      "description": "First verified activation event for an enterprise user cohort.",
      "properties": {
        "activation_event_id": {
          "type": "string",
          "description": "Stable identity of this activation event observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose activation event state was observed."
        },
        "activation_type": {
          "type": "string",
          "description": "Type of customer activation observed."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["activation_event_id", "account_id", "activation_type", "evidence_digest", "observed_at"
      ]

    },
    "add_on_bundle": {
      "type": "object",
      "description": "Optional paid add-on attachable to an edition.",
      "properties": {
        "add_on_id": {
          "type": "string",
          "description": "Stable add-on identity."
        },
        "name": {
          "type": "string",
          "description": "Buyer-facing add-on name."
        },
        "capability_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Additional capabilities."
        },
        "status": {
          "type": "string",
          "description": "Add-on lifecycle standing."
        }
      },
      "required": ["add_on_id", "name", "capability_ids", "status"
      ]

    },
    "addon_activation": {
      "type": "object",
      "description": "Receipted activation of a paid add-on.",
      "properties": {
        "addon_activation_id": {
          "type": "string",
          "description": "Stable identity of this addon activation contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "addon_id": {
          "type": "string",
          "description": "Stable identity of the activated commercial add-on."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["addon_activation_id", "account_id", "addon_id", "evidence_digest", "effective_at"
      ]

    },
    "admissible_action_set": {
      "type": "object",
      "description": "Computes all actions currently permitted by state, constraints, and authority.",
      "properties": {
        "state_id": {
          "type": "string",
          "description": "Required state_id for this bounded planner contract."
        },
        "constraint_hash": {
          "type": "string",
          "description": "Required constraint_hash for deterministic planner evaluation."
        },
        "action_set_hash": {
          "type": "string",
          "description": "Required action_set_hash preserving evidence and falsifiability."
        }
      },
      "required": ["state_id", "constraint_hash", "action_set_hash"
      ]

    },
    "adoption_milestone": {
      "type": "object",
      "description": "Receipted customer adoption milestone tied to observable use.",
      "properties": {
        "adoption_milestone_id": {
          "type": "string",
          "description": "Stable identity of this adoption milestone observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose adoption milestone state was observed."
        },
        "milestone_name": {
          "type": "string",
          "description": "Stable name of the achieved adoption milestone."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["adoption_milestone_id", "account_id", "milestone_name", "evidence_digest", "observed_at"
      ]

    },
    "agent_assignment": {
      "type": "object",
      "description": "Assigns an admitted policy to an agent without transferring authority.",
      "properties": {
        "agent_id": {
          "type": "string",
          "description": "Required agent_id for this bounded planner contract."
        },
        "policy_id": {
          "type": "string",
          "description": "Required policy_id for deterministic planner evaluation."
        },
        "assignment_hash": {
          "type": "string",
          "description": "Required assignment_hash preserving evidence and falsifiability."
        }
      },
      "required": ["agent_id", "policy_id", "assignment_hash"
      ]

    },
    "alignment_move": {
      "type": "object",
      "description": "One step of a conformance-checking alignment between log and model.",
      "properties": {
        "move_type": {
          "type": "string",
          "description": "One of: sync | model | log | silent."
        },
        "cost": {
          "type": "integer",
          "description": "Non-negative cost assigned to this move."
        }
      },
      "required": ["move_type", "cost"
      ]

    },
    "annual_subscription": {
      "type": "object",
      "description": "Annual package binding SKU, seats, and renewal date.",
      "properties": {
        "subscription_id": {
          "type": "string",
          "description": "Stable annual subscription."
        },
        "sku": {
          "type": "string",
          "description": "Subscribed SKU."
        },
        "seat_count": {
          "type": "integer",
          "description": "Contracted seats."
        },
        "renews_at": {
          "type": "string",
          "description": "Annual renewal instant."
        }
      },
      "required": ["subscription_id", "sku", "seat_count", "renews_at"
      ]

    },
    "anomaly_detection_observation": {
      "type": "object",
      "description": "Scores an observed subject against an exact baseline digest.",
      "properties": {
        "subject_id": {
          "type": "string",
          "description": "Observed subject identity."
        },
        "baseline_digest": {
          "type": "string",
          "description": "Exact anomaly baseline digest."
        },
        "observation_digest": {
          "type": "string",
          "description": "Current observation digest."
        },
        "anomaly_score": {
          "type": "number",
          "description": "Computed anomaly score."
        }
      },
      "required": ["subject_id", "baseline_digest", "observation_digest", "anomaly_score"
      ]

    },
    "anti_repeat_refusal": {
      "type": "object",
      "description": "Refuses a candidate action that recreates a confirmed failure signature.",
      "properties": {
        "refusal_id": {
          "type": "string",
          "description": "Stable anti-repeat refusal identity."
        },
        "candidate_action_id": {
          "type": "string",
          "description": "Candidate action refused."
        },
        "matching_signature_id": {
          "type": "string",
          "description": "Confirmed failure signature matched."
        },
        "recurrence_risk": {
          "type": "number",
          "description": "Measured recurrence risk."
        },
        "alternative_required": {
          "type": "boolean",
          "description": "Whether selection must produce a different option."
        }
      },
      "required": ["refusal_id", "candidate_action_id", "matching_signature_id", "recurrence_risk", "alternative_required"
      ]

    },
    "anti_repeat_signature": {
      "type": "object",
      "description": "Compiles a confirmed failure pattern into durable anti-repeat memory.",
      "properties": {
        "signature_id": {
          "type": "string",
          "description": "Stable anti-repeat signature identity."
        },
        "failure_class": {
          "type": "string",
          "description": "Confirmed failure class."
        },
        "causal_features_digest": {
          "type": "string",
          "description": "Digest of decision-relevant causal features."
        },
        "repair_family": {
          "type": "string",
          "description": "Repair family that addressed the failure."
        },
        "first_seen_episode": {
          "type": "string",
          "description": "First preserved episode exhibiting the pattern."
        }
      },
      "required": ["signature_id", "failure_class", "causal_features_digest", "repair_family", "first_seen_episode"
      ]

    },
    "approval_separation_evidence": {
      "type": "object",
      "description": "Executable segregation-of-duties evidence binding an exact subject to an approver distinct from its producer.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this approval-separation observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "approver_identity": {
          "type": "string",
          "description": "Identity of the independent approver observed by the control."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "approver_identity", "observed_result"
      ]

    },
    "architecture_readiness": {
      "type": "object",
      "description": "Measured architecture readiness for the customer's production environment.",
      "properties": {
        "architecture_readiness_id": {
          "type": "string",
          "description": "Stable identity of this architecture readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose architecture readiness state was observed."
        },
        "architecture_score": {
          "type": "number",
          "description": "Observed architecture-readiness score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["architecture_readiness_id", "account_id", "architecture_score", "evidence_digest", "observed_at"
      ]

    },
    "architecture_review_evidence": {
      "type": "object",
      "description": "Executable architecture-review evidence binding an exact subject to the decision produced by the admitted review.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this architecture-review observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "review_decision": {
          "type": "string",
          "description": "Observed architecture review decision for the subject."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "review_decision", "observed_result"
      ]

    },
    "artifact_digest_evidence": {
      "type": "object",
      "description": "Executable artifact-integrity evidence binding an exact commercial subject to the digest actually observed by the verifier.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this artifact digest observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "digest": {
          "type": "string",
          "description": "Content digest observed for the admitted commercial artifact."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "digest", "observed_result"
      ]

    },
    "artifact_digest_observation": {
      "type": "object",
      "description": "Binds a produced artifact digest to its exact producer run.",
      "properties": {
        "artifact_id": {
          "type": "string",
          "description": "Stable artifact identity."
        },
        "artifact_sha256": {
          "type": "string",
          "description": "Observed artifact content digest."
        },
        "producer_run_id": {
          "type": "string",
          "description": "Exact producer workflow run."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of artifact observation."
        }
      },
      "required": ["artifact_id", "artifact_sha256", "producer_run_id", "observed_at"
      ]

    },
    "astar_plan_candidate": {
      "type": "object",
      "description": "Represents an A-star plan with admissible heuristic and exact state lineage.",
      "properties": {
        "plan_id": {
          "type": "string",
          "description": "Required plan_id for this bounded planner contract."
        },
        "heuristic_id": {
          "type": "string",
          "description": "Required heuristic_id for deterministic planner evaluation."
        },
        "path_hash": {
          "type": "string",
          "description": "Required path_hash preserving evidence and falsifiability."
        }
      },
      "required": ["plan_id", "heuristic_id", "path_hash"
      ]

    },
    "attestation_verification_evidence": {
      "type": "object",
      "description": "Executable attestation evidence binding an exact commercial subject to the predicate that was cryptographically verified.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this attestation verification observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "predicate_type": {
          "type": "string",
          "description": "Verified in-toto predicate type carried by the attestation."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "predicate_type", "observed_result"
      ]

    },
    "audit_chain_evidence": {
      "type": "object",
      "description": "Executable audit evidence binding an exact subject receipt to its immutable predecessor.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this audit-chain observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "previous_receipt_hash": {
          "type": "string",
          "description": "Hash of the preceding receipt in the observed audit chain."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "previous_receipt_hash", "observed_result"
      ]

    },
    "authority_ceiling": {
      "type": "object",
      "description": "Caps every candidate action at the exact authority grant available to its subject.",
      "properties": {
        "action_id": {
          "type": "string",
          "description": "Required action_id for this bounded planner contract."
        },
        "grant_id": {
          "type": "string",
          "description": "Required grant_id for deterministic planner evaluation."
        },
        "ceiling": {
          "type": "string",
          "description": "Required ceiling preserving evidence and falsifiability."
        }
      },
      "required": ["action_id", "grant_id", "ceiling"
      ]

    },
    "autonomic_actuation_receipt": {
      "type": "object",
      "description": "Produces a complete deterministic receipt for one authorized actuation.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_actuation_replay": {
      "type": "object",
      "description": "Replays an actuation receipt against the same exact subject without repeating consequences.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_actuation_selection": {
      "type": "object",
      "description": "Selects one bounded actuation candidate while preserving the exact decision subject and admitted authority.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_authority_admission": {
      "type": "object",
      "description": "Admits or refuses authority for one consequential transition using an immutable authority receipt.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_authority_escalation": {
      "type": "object",
      "description": "Emits a machine-readable escalation only when the remaining boundary is irreducible by admitted authority.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_backpressure_admission": {
      "type": "object",
      "description": "Refuses new consequential work when admitted queue or inflight capacity is exhausted.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_caller_local_binding": {
      "type": "object",
      "description": "Binds actuation to the exact consumer checkout rather than a central surrogate.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_canary_admission": {
      "type": "object",
      "description": "Admits a bounded canary transition and refuses fleet-wide expansion without verified evidence.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_cancellation_receipt": {
      "type": "object",
      "description": "Produces a deterministic receipt proving cancellation and the resulting bounded state.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_canonical_repair_route": {
      "type": "object",
      "description": "Routes a generated-surface defect to its canonical semantic source or independent qualification rail.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_capability_token": {
      "type": "object",
      "description": "Scopes actuation authority to one capability, subject, and bounded transition.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_circuit_breaker_transition": {
      "type": "object",
      "description": "Opens, probes, and closes a circuit using receipted health state transitions.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_compensation_verification": {
      "type": "object",
      "description": "Verifies compensation restored the admitted recovery state without hiding partial effects.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_crash_recovery": {
      "type": "object",
      "description": "Resumes a receipted transition after process crash from the last verified state boundary.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_cross_consumer_receipt_refusal": {
      "type": "object",
      "description": "Refuses reuse of an actuation receipt across distinct caller-local consumer subjects.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_deterministic_receipt_replay": {
      "type": "object",
      "description": "Recomputes receipt identity deterministically without reapplying the original consequence.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_failure_classification": {
      "type": "object",
      "description": "Classifies execution failures as subject, authority, capsule, dependency, transient, or irreducible boundary.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_forged_receipt_refusal": {
      "type": "object",
      "description": "Refuses receipts whose claimed transition identity fails deterministic verification.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_generated_surface_refusal": {
      "type": "object",
      "description": "Refuses direct mutation of generator-owned workflows and projections.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_idempotence_fence": {
      "type": "object",
      "description": "Prevents duplicate execution of an already receipted actuation identity.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_incident_recovery": {
      "type": "object",
      "description": "Coordinates automatic incident recovery through verified transitions and deterministic receipts.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_least_authority_grant": {
      "type": "object",
      "description": "Encodes the least authority sufficient for exactly one admitted actuation.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_model_authority_refusal": {
      "type": "object",
      "description": "Refuses model-produced intent as ambient authority for a consequential transition.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_mutable_pack_refusal": {
      "type": "object",
      "description": "Refuses actuation when a marketplace pack is selected by mutable tag or branch.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_output_ownership_check": {
      "type": "object",
      "description": "Verifies that every written output is owned by the admitted manufacturing plan.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_pack_sha_authority": {
      "type": "object",
      "description": "Requires an exact immutable marketplace pack SHA before construction or execution.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_plan_construction": {
      "type": "object",
      "description": "Constructs an executable actuation plan bound to the exact caller-local subject.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_planner_authority_refusal": {
      "type": "object",
      "description": "Refuses planner output as authority unless a separate admitted authority receipt is present.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_rca_hypothesis": {
      "type": "object",
      "description": "Generates a bounded machine-actionable root-cause hypothesis from failed actuation evidence.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_receipt_authority_binding": {
      "type": "object",
      "description": "Binds a receipt to the exact authority admission used for the transition.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_receipt_chain_link": {
      "type": "object",
      "description": "Links each consequential transition receipt to its verified predecessor receipt.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_receipt_completeness_check": {
      "type": "object",
      "description": "Refuses any actuation receipt missing subject, authority, before-state, after-state, or result identity.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_receipt_subject_binding": {
      "type": "object",
      "description": "Cryptographically binds an actuation receipt to the exact caller-local subject SHA.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_repair_reexecution": {
      "type": "object",
      "description": "Re-executes a repaired actuation from the last verified state with a linked receipt.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_repair_selection": {
      "type": "object",
      "description": "Selects a least-change repair whose authority and affected surface remain explicitly bounded.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_replay_divergence_refusal": {
      "type": "object",
      "description": "Refuses replay when recomputed state or receipt identity diverges from the admitted evidence.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_retry_backoff": {
      "type": "object",
      "description": "Schedules bounded retry delay without widening the original authority or subject.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_retry_budget": {
      "type": "object",
      "description": "Bounds recoverable actuation retries by explicit count and authority scope.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_rollback_transition": {
      "type": "object",
      "description": "Restores an exact admitted prior state when forward repair cannot safely complete.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_saga_compensation": {
      "type": "object",
      "description": "Constructs a compensating transition for a partially completed multi-step actuation.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_second_run_identity": {
      "type": "object",
      "description": "Proves a second manufacture or sync produces byte-identical owned outputs.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_self_healing_completion_receipt": {
      "type": "object",
      "description": "Proves the full select, authorize, execute, verify, repair, replay, and containment loop completed.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_stale_action_refusal": {
      "type": "object",
      "description": "Refuses a queued action whose subject, authority, or expected before-state became stale.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_stale_receipt_refusal": {
      "type": "object",
      "description": "Refuses receipts whose subject or before-state no longer matches the admitted transition.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_state_vector": {
      "type": "object",
      "description": "Encodes decision-relevant observed dimensions for one exact subject.",
      "properties": {
        "state_vector_id": {
          "type": "string",
          "description": "Stable state-vector identity."
        },
        "subject_id": {
          "type": "string",
          "description": "Exact observed subject identity."
        },
        "dimension_digest": {
          "type": "string",
          "description": "Digest of ordered state dimensions."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of state-vector encoding."
        }
      },
      "required": ["state_vector_id", "subject_id", "dimension_digest", "observed_at"
      ]

    },
    "autonomic_subject_compare_and_swap": {
      "type": "object",
      "description": "Refuses an actuation unless the observed subject SHA equals the admitted expected SHA.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_supervisor_restart": {
      "type": "object",
      "description": "Restarts a failed actuation worker under bounded BEAM supervision semantics.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_timeout_budget": {
      "type": "object",
      "description": "Cancels an unfinished actuation when its admitted execution budget expires.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_transition_execution": {
      "type": "object",
      "description": "Records execution of one admitted state transition without granting ambient planner authority.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_transition_verification": {
      "type": "object",
      "description": "Verifies the exact before and after state digests for a completed consequential transition.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "autonomic_upgrade_transition": {
      "type": "object",
      "description": "Executes a version upgrade against exact capsule, pack, and subject identities with rollback evidence.",
      "properties": {
        "actuation_id": {
          "type": "string",
          "description": "Unique bounded actuation identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact caller-local subject commit SHA."
        },
        "authority_receipt_sha": {
          "type": "string",
          "description": "Immutable receipt admitting authority for this transition."
        },
        "state_digest": {
          "type": "string",
          "description": "Deterministic state digest used by verification and replay."
        }
      },
      "required": ["actuation_id", "subject_sha", "authority_receipt_sha", "state_digest"
      ]

    },
    "availability_observation": {
      "type": "object",
      "description": "Records a bounded availability observation against the paid tenant SLO.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "slo_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "observation_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "slo_id", "observation_hash"
      ]

    },
    "availability_slo_evidence": {
      "type": "object",
      "description": "Executable availability evidence binding an exact subject to the service level actually observed over the admitted window.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this availability-SLO observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "availability_percent": {
          "type": "number",
          "description": "Observed availability ratio for the exact subject and measurement window."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "availability_percent", "observed_result"
      ]

    },
    "backup_restore_evidence": {
      "type": "object",
      "description": "Executable recovery evidence binding an exact subject to the backup artifact successfully restored.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this backup-restore observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "backup_digest": {
          "type": "string",
          "description": "Digest of the backup artifact admitted by the restore operation."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "backup_digest", "observed_result"
      ]

    },
    "baseline_metric": {
      "type": "object",
      "description": "Pre-POC customer baseline used to falsify or confirm realized value.",
      "properties": {
        "baseline_metric_id": {
          "type": "string",
          "description": "Stable identity of this baseline metric observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose baseline metric state was observed."
        },
        "baseline_value": {
          "type": "number",
          "description": "Measured value before the intervention."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["baseline_metric_id", "account_id", "baseline_value", "evidence_digest", "observed_at"
      ]

    },
    "beam_search_candidate": {
      "type": "object",
      "description": "Represents a bounded beam-search plan with explicit width and frontier.",
      "properties": {
        "plan_id": {
          "type": "string",
          "description": "Required plan_id for this bounded planner contract."
        },
        "beam_width": {
          "type": "string",
          "description": "Required beam_width for deterministic planner evaluation."
        },
        "frontier_hash": {
          "type": "string",
          "description": "Required frontier_hash preserving evidence and falsifiability."
        }
      },
      "required": ["plan_id", "beam_width", "frontier_hash"
      ]

    },
    "belief_state_snapshot": {
      "type": "object",
      "description": "Captures a posterior belief state and explicit uncertainty standing.",
      "properties": {
        "belief_state_id": {
          "type": "string",
          "description": "Stable belief-state identity."
        },
        "subject_id": {
          "type": "string",
          "description": "Exact subject of belief."
        },
        "posterior_digest": {
          "type": "string",
          "description": "Digest of posterior state."
        },
        "uncertainty_status": {
          "type": "string",
          "description": "Machine-readable uncertainty standing."
        }
      },
      "required": ["belief_state_id", "subject_id", "posterior_digest", "uncertainty_status"
      ]

    },
    "belief_state_update": {
      "type": "object",
      "description": "Binds prior belief, new evidence, update rule, and posterior belief.",
      "properties": {
        "prior_belief_id": {
          "type": "string",
          "description": "Prior belief-state identity."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of newly admitted evidence."
        },
        "posterior_belief_id": {
          "type": "string",
          "description": "Resulting belief-state identity."
        },
        "update_rule": {
          "type": "string",
          "description": "Deterministic belief update rule."
        }
      },
      "required": ["prior_belief_id", "evidence_digest", "posterior_belief_id", "update_rule"
      ]

    },
    "beneficial_owner_evidence": {
      "type": "object",
      "description": "Binds beneficial-owner identity to immutable evidence for counterparties that require enhanced diligence.",
      "properties": {
        "account_id": {
          "type": "string",
          "description": "Required beneficial owner evidence input; omission is an executable typed refusal, never an inferred approval."
        },
        "owner_id": {
          "type": "string",
          "description": "Required beneficial owner evidence input; omission is an executable typed refusal, never an inferred approval."
        },
        "evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["account_id", "owner_id", "evidence_hash"
      ]

    },
    "billable_usage_identity": {
      "type": "object",
      "description": "Binds accepted usage to one immutable billable identity that prevents duplicate charging.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "billable_usage_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "identity_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "billable_usage_id", "identity_hash"
      ]

    },
    "billing_account": {
      "type": "object",
      "description": "Bill-to account with currency and invoice delivery profile.",
      "properties": {
        "billing_account_id": {
          "type": "string",
          "description": "Stable bill-to account identity."
        },
        "account_id": {
          "type": "string",
          "description": "Commercial account owner."
        },
        "currency": {
          "type": "string",
          "description": "ISO 4217 billing currency."
        },
        "invoice_profile": {
          "type": "string",
          "description": "Invoice delivery profile identity."
        }
      },
      "required": ["billing_account_id", "account_id", "currency", "invoice_profile"
      ]

    },
    "billing_reconciliation": {
      "type": "object",
      "description": "The reconciled billable total for one (entitlement_id, metric_name) pair over one half-open period [period_start, period_end). INVARIANT: total_quantity is exactly the sum of usage_event.quantity over the distinct event_ids listed in applied_event_ids, summed in that list's ascending order; applied_event_ids is sorted and duplicate-free. Consequently the same usage_event.event_id can never contribute twice, no matter how many times or in what order it appears in the input stream.",
      "properties": {
        "entitlement_id": {
          "type": "string",
          "description": "The entitlement this total is billed against. First component of the reconciliation partition key."
        },
        "metric_name": {
          "type": "string",
          "description": "The metered dimension this total is for. Second component of the reconciliation partition key; totals are never summed across metrics."
        },
        "total_quantity": {
          "type": "number",
          "description": "The billable total: the sum of quantity over the distinct events named in applied_event_ids, accumulated in that list's ascending event_id order. The fixed summation order is load-bearing, not incidental -- float addition is non-associative, so only a canonical order makes the total bit-identical across arrival orders and across replays. Recomputable by any third party from applied_event_ids plus the usage_event store, which is what makes this a receipt rather than a claim."
        },
        "applied_event_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "The exact deduplicated evidence set behind total_quantity: every usage_event.event_id counted, sorted ascending, with no duplicates. This is the dedup contract made auditable -- |applied_event_ids| is the honest count of billed occurrences, and any event_id appearing at most once here is a mechanically checkable falsifier for MP6 double-billing."
        },
        "period_start": {
          "type": "string",
          "description": "ISO8601 UTC INCLUSIVE lower bound of the billing period. An event is in-period when period_start <= occurred_at. Caller-supplied to reconcile_billing/4, never derived from the input events themselves -- see period_end's fieldDoc for why."
        },
        "period_end": {
          "type": "string",
          "description": "ISO8601 UTC EXCLUSIVE upper bound of the billing period. An event is in-period when occurred_at < period_end. The exclusive bound is what prevents an event landing exactly on a period boundary from being counted by both the closing and the opening period -- a boundary double-bill that per-period dedup alone would not catch. reconcile_billing/4 takes {PeriodStart, PeriodEnd} as an explicit argument and filters events by it before dedup, which is what makes this guarantee real."
        }
      },
      "required": ["entitlement_id", "metric_name", "total_quantity", "applied_event_ids", "period_start", "period_end"
      ]

    },
    "booking_readiness": {
      "type": "object",
      "description": "Produces an explicit booking-readiness decision after commercial, legal, security, and procurement evidence converge.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required booking readiness input; omission is an executable typed refusal, never an inferred approval."
        },
        "readiness_id": {
          "type": "string",
          "description": "Required booking readiness input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "readiness_id", "decision"
      ]

    },
    "brce_actuation_receipt": {
      "type": "object",
      "description": "Binds an actuated runtime consequence to its authorized BRCE request and receipt.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "request_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "receipt_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "request_id", "receipt_hash"
      ]

    },
    "brce_actuation_request": {
      "type": "object",
      "description": "Admits a runtime actuation request only with exact BRCE authority evidence.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "request_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "authority_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "request_id", "authority_hash"
      ]

    },
    "budget_period_alignment": {
      "type": "object",
      "description": "Qualifies whether the buying timeline lands inside an approved budget period, exposing unfunded timing risk.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required budget period alignment input; omission is an executable typed refusal, never an inferred approval."
        },
        "budget_period": {
          "type": "string",
          "description": "Required budget period alignment input; omission is an executable typed refusal, never an inferred approval."
        },
        "alignment_result": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "budget_period", "alignment_result"
      ]

    },
    "bundle_conflict": {
      "type": "object",
      "description": "Mutual exclusion between incompatible bundles.",
      "properties": {
        "bundle_conflict_id": {
          "type": "string",
          "description": "Stable identity of this bundle conflict contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "conflicting_bundle_id": {
          "type": "string",
          "description": "Bundle whose simultaneous activation must be refused."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["bundle_conflict_id", "account_id", "conflicting_bundle_id", "evidence_digest", "effective_at"
      ]

    },
    "bundle_dependency": {
      "type": "object",
      "description": "Required dependency between sellable bundles.",
      "properties": {
        "bundle_dependency_id": {
          "type": "string",
          "description": "Stable identity of this bundle dependency contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "required_bundle_id": {
          "type": "string",
          "description": "Bundle that must be entitled before this bundle can activate."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["bundle_dependency_id", "account_id", "required_bundle_id", "evidence_digest", "effective_at"
      ]

    },
    "burst_pricing_policy": {
      "type": "object",
      "description": "Bounded burst-capacity premium.",
      "properties": {
        "burst_pricing_policy_id": {
          "type": "string",
          "description": "Stable identity of this burst pricing policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "burst_multiplier": {
          "type": "number",
          "description": "Approved multiplier applied only above the included capacity."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["burst_pricing_policy_id", "account_id", "burst_multiplier", "evidence_digest", "effective_at"
      ]

    },
    "business_continuity_evidence": {
      "type": "object",
      "description": "Executable continuity evidence binding an exact subject to the fallback operating mode actually entered.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this business-continuity observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "continuity_mode": {
          "type": "string",
          "description": "Observed fallback mode used to preserve admitted operations."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "continuity_mode", "observed_result"
      ]

    },
    "business_outcome_measurement": {
      "type": "object",
      "description": "Records the observed business outcome against its admitted baseline and target.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "outcome_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "measurement_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "outcome_id", "measurement_hash"
      ]

    },
    "business_unit_allocation": {
      "type": "object",
      "description": "Showback allocation to an enterprise business unit.",
      "properties": {
        "business_unit_allocation_id": {
          "type": "string",
          "description": "Stable identity of this business unit allocation contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "business_unit_id": {
          "type": "string",
          "description": "Stable identity of the receiving business unit."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["business_unit_allocation_id", "account_id", "business_unit_id", "evidence_digest", "effective_at"
      ]

    },
    "buying_committee": {
      "type": "object",
      "description": "Measured buying-committee coverage for an enterprise decision path.",
      "properties": {
        "buying_committee_id": {
          "type": "string",
          "description": "Stable identity of this buying committee observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose buying committee state was observed."
        },
        "committee_coverage": {
          "type": "number",
          "description": "Fraction of required buying roles actively represented."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["buying_committee_id", "account_id", "committee_coverage", "evidence_digest", "observed_at"
      ]

    },
    "caller_local_checkout_observation": {
      "type": "object",
      "description": "Proves a reusable rail executed from the consumer's own exact checkout.",
      "properties": {
        "consumer_repository_id": {
          "type": "string",
          "description": "Stable consumer repository identity."
        },
        "checkout_sha": {
          "type": "string",
          "description": "Exact caller-local checkout commit."
        },
        "checkout_path": {
          "type": "string",
          "description": "Observed caller-local execution path."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest binding execution to checkout."
        }
      },
      "required": ["consumer_repository_id", "checkout_sha", "checkout_path", "evidence_digest"
      ]

    },
    "caller_local_consumer": {
      "type": "object",
      "description": "Preserves each caller-local consumer as an independently planned exact subject.",
      "properties": {
        "consumer_id": {
          "type": "string",
          "description": "Required consumer_id for this bounded planner contract."
        },
        "subject_sha": {
          "type": "string",
          "description": "Required subject_sha for deterministic planner evaluation."
        },
        "consumer_hash": {
          "type": "string",
          "description": "Required consumer_hash preserving evidence and falsifiability."
        }
      },
      "required": ["consumer_id", "subject_sha", "consumer_hash"
      ]

    },
    "caller_local_crown_identity": {
      "type": "object",
      "description": "Binds one admitted upstream crown to the exact caller-local consumer subject; cross-consumer crown reuse is refused.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "consumer_subject_sha": {
          "type": "string",
          "description": "Exact caller-local consumer commit SHA that must be requalified."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "consumer_subject_sha", "receipt_digest"
      ]

    },
    "canary_decision": {
      "type": "object",
      "description": "Receipted enterprise canary decision based on an observed rollout consequence.",
      "properties": {
        "canary_decision_id": {
          "type": "string",
          "description": "Stable identity of this canary decision observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose canary decision state was observed."
        },
        "canary_result": {
          "type": "string",
          "description": "Observed outcome of the canary qualification."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["canary_decision_id", "account_id", "canary_result", "evidence_digest", "observed_at"
      ]

    },
    "canary_evidence": {
      "type": "object",
      "description": "Executable canary evidence binding an exact subject to the traffic fraction actually admitted before promotion.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this canary-release observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "canary_percentage": {
          "type": "number",
          "description": "Observed fraction of production traffic routed to the canary subject."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "canary_percentage", "observed_result"
      ]

    },
    "cancellation_policy": {
      "type": "object",
      "description": "Contractual cancellation and notice policy.",
      "properties": {
        "cancellation_policy_id": {
          "type": "string",
          "description": "Stable identity of this cancellation policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "notice_days": {
          "type": "integer",
          "description": "Required cancellation notice period in days."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["cancellation_policy_id", "account_id", "notice_days", "evidence_digest", "effective_at"
      ]

    },
    "canonical_source_authority_observation": {
      "type": "object",
      "description": "Encodes whether a repository path is an authorized canonical manufacturing input.",
      "properties": {
        "source_path": {
          "type": "string",
          "description": "Observed repository path."
        },
        "authority_class": {
          "type": "string",
          "description": "Classified source authority."
        },
        "mutation_allowed": {
          "type": "boolean",
          "description": "Whether mutation is admitted at this boundary."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of authority evidence."
        }
      },
      "required": ["source_path", "authority_class", "mutation_allowed", "evidence_digest"
      ]

    },
    "capability_bundle": {
      "type": "object",
      "description": "Named capability bundle with constituent capability identities.",
      "properties": {
        "bundle_id": {
          "type": "string",
          "description": "Stable bundle identity."
        },
        "name": {
          "type": "string",
          "description": "Buyer-facing bundle name."
        },
        "capability_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Included capability identities."
        },
        "version": {
          "type": "string",
          "description": "Bundle semantic version."
        }
      },
      "required": ["bundle_id", "name", "capability_ids", "version"
      ]

    },
    "capability_gap": {
      "type": "object",
      "description": "Detected capability gap blocking enterprise value or expansion.",
      "properties": {
        "capability_gap_id": {
          "type": "string",
          "description": "Stable identity of this capability gap observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose capability gap state was observed."
        },
        "gap_severity": {
          "type": "string",
          "description": "Observed severity of the capability gap."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["capability_gap_id", "account_id", "gap_severity", "evidence_digest", "observed_at"
      ]

    },
    "capability_gap_learning": {
      "type": "object",
      "description": "Converts observed missing capability evidence into a bounded learning signal.",
      "properties": {
        "learning_id": {
          "type": "string",
          "description": "Stable capability-gap learning identity."
        },
        "observed_capability_id": {
          "type": "string",
          "description": "Capability actually observed."
        },
        "required_capability_id": {
          "type": "string",
          "description": "Capability required by the admitted objective."
        },
        "gap_type": {
          "type": "string",
          "description": "Typed semantic, execution, evidence, or authority gap."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest proving the gap."
        }
      },
      "required": ["learning_id", "observed_capability_id", "required_capability_id", "gap_type", "evidence_digest"
      ]

    },
    "capsule_availability": {
      "type": "object",
      "description": "Records whether the exact validation capsule is available before qualification.",
      "properties": {
        "capsule_id": {
          "type": "string",
          "description": "Required capsule_id for this bounded planner contract."
        },
        "capsule_digest": {
          "type": "string",
          "description": "Required capsule_digest for deterministic planner evaluation."
        },
        "availability": {
          "type": "string",
          "description": "Required availability preserving evidence and falsifiability."
        }
      },
      "required": ["capsule_id", "capsule_digest", "availability"
      ]

    },
    "capsule_identity": {
      "type": "object",
      "description": "Binds paid workload execution to the exact immutable runtime capsule digest.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "capsule_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "identity_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "capsule_id", "identity_hash"
      ]

    },
    "case_stats": {
      "type": "object",
      "description": "Aggregate statistics computed for one process instance (case).",
      "properties": {
        "case_id": {
          "type": "string",
          "description": "Unique case identifier."
        },
        "event_count": {
          "type": "integer",
          "description": "Number of events observed for this case."
        },
        "duration_seconds": {
          "type": "number",
          "description": "Optional case duration in seconds (end minus start)."
        }
      },
      "required": ["case_id", "event_count"
      ]

    },
    "catalog_release": {
      "type": "object",
      "description": "Immutable commercial catalog release with effective time.",
      "properties": {
        "release_id": {
          "type": "string",
          "description": "Stable catalog release."
        },
        "version": {
          "type": "string",
          "description": "Catalog semantic version."
        },
        "sku_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Published SKU identities."
        },
        "effective_at": {
          "type": "string",
          "description": "Catalog activation instant."
        }
      },
      "required": ["release_id", "version", "sku_ids", "effective_at"
      ]

    },
    "causal_lineage_observation": {
      "type": "object",
      "description": "Binds a candidate cause to effect with explicit causal evidence.",
      "properties": {
        "cause_observation_id": {
          "type": "string",
          "description": "Candidate causal observation."
        },
        "effect_observation_id": {
          "type": "string",
          "description": "Observed effect identity."
        },
        "causal_basis": {
          "type": "string",
          "description": "Declared causal inference basis."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of causal evidence."
        }
      },
      "required": ["cause_observation_id", "effect_observation_id", "causal_basis", "evidence_digest"
      ]

    },
    "challenger_candidate_evaluation": {
      "type": "object",
      "description": "Evaluates a challenger on a fixed episode set and preserves a refusal outcome.",
      "properties": {
        "evaluation_id": {
          "type": "string",
          "description": "Stable challenger evaluation identity."
        },
        "challenger_policy_id": {
          "type": "string",
          "description": "Challenger policy under evaluation."
        },
        "episode_set_digest": {
          "type": "string",
          "description": "Digest of the immutable evaluation episode set."
        },
        "evaluation_seed": {
          "type": "string",
          "description": "Explicit evaluation seed."
        },
        "score": {
          "type": "number",
          "description": "Observed challenger score."
        },
        "refusal_code": {
          "type": "string",
          "description": "Typed refusal or admitted result."
        }
      },
      "required": ["evaluation_id", "challenger_policy_id", "episode_set_digest", "evaluation_seed", "score", "refusal_code"
      ]

    },
    "change_control_evidence": {
      "type": "object",
      "description": "Executable change-control evidence binding an exact subject to the approved request governing its mutation.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this change-control observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "change_request_id": {
          "type": "string",
          "description": "Identity of the change request observed at the deployment boundary."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "change_request_id", "observed_result"
      ]

    },
    "change_order_authority": {
      "type": "object",
      "description": "Requires evidence of who can authorize paid scope changes, protecting expansion revenue and delivery margin.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required change order authority input; omission is an executable typed refusal, never an inferred approval."
        },
        "authority_id": {
          "type": "string",
          "description": "Required change order authority input; omission is an executable typed refusal, never an inferred approval."
        },
        "evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "authority_id", "evidence_hash"
      ]

    },
    "changed_surface_inference": {
      "type": "object",
      "description": "Infers the exact changed surface between two repository trees.",
      "properties": {
        "prior_tree_sha": {
          "type": "string",
          "description": "Prior repository tree identity."
        },
        "current_tree_sha": {
          "type": "string",
          "description": "Current repository tree identity."
        },
        "changed_surface_digest": {
          "type": "string",
          "description": "Digest of normalized changed paths."
        },
        "inference_status": {
          "type": "string",
          "description": "Machine-readable inference standing."
        }
      },
      "required": ["prior_tree_sha", "current_tree_sha", "changed_surface_digest", "inference_status"
      ]

    },
    "channel_agreement": {
      "type": "object",
      "description": "Channel partner agreement with territory and validity.",
      "properties": {
        "agreement_id": {
          "type": "string",
          "description": "Stable channel agreement."
        },
        "partner_id": {
          "type": "string",
          "description": "Authorized channel partner."
        },
        "territory": {
          "type": "string",
          "description": "Authorized sales territory."
        },
        "valid_until": {
          "type": "string",
          "description": "Agreement expiration instant."
        }
      },
      "required": ["agreement_id", "partner_id", "territory", "valid_until"
      ]

    },
    "chargeback_rule": {
      "type": "object",
      "description": "Cost allocation rule mapping usage to a cost center.",
      "properties": {
        "rule_id": {
          "type": "string",
          "description": "Stable chargeback rule."
        },
        "cost_center": {
          "type": "string",
          "description": "Charged cost center."
        },
        "metric_name": {
          "type": "string",
          "description": "Allocated usage metric."
        },
        "rate": {
          "type": "number",
          "description": "Internal chargeback rate."
        }
      },
      "required": ["rule_id", "cost_center", "metric_name", "rate"
      ]

    },
    "cluster_quorum_state": {
      "type": "object",
      "description": "Binds an execution decision to observed cluster quorum rather than inferred availability.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "cluster_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "quorum_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "cluster_id", "quorum_hash"
      ]

    },
    "co_term_policy": {
      "type": "object",
      "description": "Co-termination policy for expansion purchases.",
      "properties": {
        "co_term_policy_id": {
          "type": "string",
          "description": "Stable identity of this co term policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "coterm_date": {
          "type": "string",
          "description": "Contract end instant to which the expansion is aligned."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["co_term_policy_id", "account_id", "coterm_date", "evidence_digest", "effective_at"
      ]

    },
    "commercial_approval": {
      "type": "object",
      "description": "Commercial approval decision with exact subject and authority.",
      "properties": {
        "approval_id": {
          "type": "string",
          "description": "Stable approval identity."
        },
        "quote_id": {
          "type": "string",
          "description": "Exact quote under approval."
        },
        "authority": {
          "type": "string",
          "description": "Principal granting commercial authority."
        },
        "status": {
          "type": "string",
          "description": "Approval lifecycle standing."
        }
      },
      "required": ["approval_id", "quote_id", "authority", "status"
      ]

    },
    "commercial_artifact_crown_evidence": {
      "type": "object",
      "description": "Executable commercial-release evidence binding the exact paid artifact to the cumulative trust evidence crown.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this commercial-artifact crown observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "artifact_digest": {
          "type": "string",
          "description": "Digest of the exact commercial artifact evaluated by the cumulative crown."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "artifact_digest", "observed_result"
      ]

    },
    "commercial_exception": {
      "type": "object",
      "description": "Represents a nonstandard commercial request as an identified exception with explicit state.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required commercial exception input; omission is an executable typed refusal, never an inferred approval."
        },
        "exception_id": {
          "type": "string",
          "description": "Required commercial exception input; omission is an executable typed refusal, never an inferred approval."
        },
        "exception_state": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "exception_id", "exception_state"
      ]

    },
    "commercial_execution_receipt": {
      "type": "object",
      "description": "Exact-subject commercial operation receipt with evidence digest.",
      "properties": {
        "receipt_id": {
          "type": "string",
          "description": "Stable commercial receipt."
        },
        "subject_id": {
          "type": "string",
          "description": "Exact commercial subject."
        },
        "operation": {
          "type": "string",
          "description": "Executed commercial operation."
        },
        "evidence_hash": {
          "type": "string",
          "description": "Digest of observed consequence evidence."
        }
      },
      "required": ["receipt_id", "subject_id", "operation", "evidence_hash"
      ]

    },
    "commercial_forecast": {
      "type": "object",
      "description": "Time-bound account forecast with amount and confidence.",
      "properties": {
        "forecast_id": {
          "type": "string",
          "description": "Stable forecast identity."
        },
        "account_id": {
          "type": "string",
          "description": "Forecast enterprise account."
        },
        "amount": {
          "type": "number",
          "description": "Forecast monetary amount."
        },
        "confidence": {
          "type": "number",
          "description": "Forecast confidence ratio."
        }
      },
      "required": ["forecast_id", "account_id", "amount", "confidence"
      ]

    },
    "commercial_outcome": {
      "type": "object",
      "description": "Verified commercial outcome attributable to the admitted customer journey.",
      "properties": {
        "commercial_outcome_id": {
          "type": "string",
          "description": "Stable identity of this commercial outcome observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose commercial outcome state was observed."
        },
        "outcome_value": {
          "type": "number",
          "description": "Verified commercial value in account currency."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["commercial_outcome_id", "account_id", "outcome_value", "evidence_digest", "observed_at"
      ]

    },
    "commercial_quote": {
      "type": "object",
      "description": "A versioned enterprise quote binding an account, currency, and lifecycle standing.",
      "properties": {
        "quote_id": {
          "type": "string",
          "description": "Stable quote identity used across approval and ordering."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account receiving the quote."
        },
        "currency": {
          "type": "string",
          "description": "ISO 4217 settlement currency."
        },
        "status": {
          "type": "string",
          "description": "Quote lifecycle standing."
        }
      },
      "required": ["quote_id", "account_id", "currency", "status"
      ]

    },
    "commercial_quote_line": {
      "type": "object",
      "description": "Enterprise quote line binding a sellable SKU, quantity, and unit price.",
      "properties": {
        "quote_id": {
          "type": "string",
          "description": "Parent quote identity."
        },
        "sku": {
          "type": "string",
          "description": "Sellable catalog SKU."
        },
        "quantity": {
          "type": "integer",
          "description": "Quoted unit count."
        },
        "unit_price": {
          "type": "number",
          "description": "Price per unit in quote currency."
        }
      },
      "required": ["quote_id", "sku", "quantity", "unit_price"
      ]

    },
    "commercial_value_realization": {
      "type": "object",
      "description": "Measured post-adoption outcome linked to its baseline.",
      "properties": {
        "realization_id": {
          "type": "string",
          "description": "Stable realization identity."
        },
        "baseline_id": {
          "type": "string",
          "description": "Compared value baseline."
        },
        "realized_value": {
          "type": "number",
          "description": "Observed post-adoption value."
        },
        "measured_at": {
          "type": "string",
          "description": "Outcome measurement instant."
        }
      },
      "required": ["realization_id", "baseline_id", "realized_value", "measured_at"
      ]

    },
    "commit_check_state_observation": {
      "type": "object",
      "description": "Projects commit check state into a machine-readable exact-subject observation.",
      "properties": {
        "commit_sha": {
          "type": "string",
          "description": "Exact checked commit."
        },
        "check_name": {
          "type": "string",
          "description": "Stable check context name."
        },
        "check_status": {
          "type": "string",
          "description": "Observed check status."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of check observation."
        }
      },
      "required": ["commit_sha", "check_name", "check_status", "observed_at"
      ]

    },
    "committed_spend": {
      "type": "object",
      "description": "Term commitment with amount, currency, and expiration.",
      "properties": {
        "commitment_id": {
          "type": "string",
          "description": "Stable commitment identity."
        },
        "amount": {
          "type": "number",
          "description": "Committed monetary amount."
        },
        "currency": {
          "type": "string",
          "description": "ISO 4217 commitment currency."
        },
        "expires_at": {
          "type": "string",
          "description": "Commitment expiration instant."
        }
      },
      "required": ["commitment_id", "amount", "currency", "expires_at"
      ]

    },
    "committed_spend_admission": {
      "type": "object",
      "description": "Qualifies a committed-spend promise as a concrete commercial decision rather than aspirational usage.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required committed spend admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "commitment_id": {
          "type": "string",
          "description": "Required committed spend admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "commitment_id", "decision"
      ]

    },
    "compatibility_contract": {
      "type": "object",
      "description": "Compatibility declaration across product, schema, and API versions.",
      "properties": {
        "contract_id": {
          "type": "string",
          "description": "Stable compatibility contract."
        },
        "product_version": {
          "type": "string",
          "description": "Product version subject."
        },
        "schema_version": {
          "type": "string",
          "description": "Supported schema version."
        },
        "api_version": {
          "type": "string",
          "description": "Supported API version."
        }
      },
      "required": ["contract_id", "product_version", "schema_version", "api_version"
      ]

    },
    "concurrency_pricing_policy": {
      "type": "object",
      "description": "Concurrent-workload packaging dimension.",
      "properties": {
        "concurrency_pricing_policy_id": {
          "type": "string",
          "description": "Stable identity of this concurrency pricing policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "included_concurrency": {
          "type": "integer",
          "description": "Concurrent workload units included in the purchased package."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["concurrency_pricing_policy_id", "account_id", "included_concurrency", "evidence_digest", "effective_at"
      ]

    },
    "configuration_export": {
      "type": "object",
      "description": "Receiptable export of deterministic enterprise configuration.",
      "properties": {
        "export_id": {
          "type": "string",
          "description": "Stable export identity."
        },
        "tenant_id": {
          "type": "string",
          "description": "Exported tenant."
        },
        "configuration_hash": {
          "type": "string",
          "description": "Canonical exported digest."
        },
        "exported_at": {
          "type": "string",
          "description": "Export completion instant."
        }
      },
      "required": ["export_id", "tenant_id", "configuration_hash", "exported_at"
      ]

    },
    "configuration_import": {
      "type": "object",
      "description": "Validated configuration import bound to an exported digest.",
      "properties": {
        "import_id": {
          "type": "string",
          "description": "Stable import identity."
        },
        "tenant_id": {
          "type": "string",
          "description": "Destination tenant."
        },
        "configuration_hash": {
          "type": "string",
          "description": "Imported canonical digest."
        },
        "status": {
          "type": "string",
          "description": "Import validation standing."
        }
      },
      "required": ["import_id", "tenant_id", "configuration_hash", "status"
      ]

    },
    "conformance_result": {
      "type": "object",
      "description": "Conformance-checking metrics computed for one trace against a model.",
      "properties": {
        "trace_id": {
          "type": "string",
          "description": "Identifier of the trace this result was computed for."
        },
        "fitness": {
          "type": "number",
          "description": "Fitness score in [0.0, 1.0]."
        },
        "precision": {
          "type": "number",
          "description": "Optional precision score in [0.0, 1.0]."
        }
      },
      "required": ["trace_id", "fitness"
      ]

    },
    "consequential_state_invalidation": {
      "type": "object",
      "description": "Invalidates affected belief state automatically after a consequential transition.",
      "properties": {
        "transition_id": {
          "type": "string",
          "description": "Consequential transition identity."
        },
        "affected_state_digest": {
          "type": "string",
          "description": "Digest of state invalidated by transition."
        },
        "invalidation_reason": {
          "type": "string",
          "description": "Typed invalidation reason."
        },
        "invalidated_at": {
          "type": "string",
          "description": "Timestamp invalidation became effective."
        }
      },
      "required": ["transition_id", "affected_state_digest", "invalidation_reason", "invalidated_at"
      ]

    },
    "consumer_equivalence_learning_guard": {
      "type": "object",
      "description": "Refuses transfer of caller-local learning to another consumer without equivalence evidence.",
      "properties": {
        "guard_id": {
          "type": "string",
          "description": "Stable consumer-equivalence guard identity."
        },
        "source_consumer_id": {
          "type": "string",
          "description": "Consumer where evidence was observed."
        },
        "target_consumer_id": {
          "type": "string",
          "description": "Consumer proposed for generalization."
        },
        "equivalence_evidence_digest": {
          "type": "string",
          "description": "Digest proving relevant consumer equivalence."
        },
        "decision": {
          "type": "string",
          "description": "TRANSFER or REFUSE decision."
        },
        "refusal_code": {
          "type": "string",
          "description": "Typed reason when equivalence is absent."
        }
      },
      "required": ["guard_id", "source_consumer_id", "target_consumer_id", "equivalence_evidence_digest", "decision", "refusal_code"
      ]

    },
    "consumer_equivalence_proof": {
      "type": "object",
      "description": "Admits central-surrogate planning only after explicit consumer-equivalence proof.",
      "properties": {
        "consumer_set_id": {
          "type": "string",
          "description": "Required consumer_set_id for this bounded planner contract."
        },
        "equivalence_proof_hash": {
          "type": "string",
          "description": "Required equivalence_proof_hash for deterministic planner evaluation."
        },
        "standing": {
          "type": "string",
          "description": "Required standing preserving evidence and falsifiability."
        }
      },
      "required": ["consumer_set_id", "equivalence_proof_hash", "standing"
      ]

    },
    "consumer_pack_pin_observation": {
      "type": "object",
      "description": "Proves a consumer used an exact immutable marketplace pack SHA.",
      "properties": {
        "consumer_repository_id": {
          "type": "string",
          "description": "Stable consumer identity."
        },
        "pack_id": {
          "type": "string",
          "description": "Consumed validation-pack identity."
        },
        "pack_sha": {
          "type": "string",
          "description": "Exact immutable pack commit."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of caller-local pin evidence."
        }
      },
      "required": ["consumer_repository_id", "pack_id", "pack_sha", "evidence_digest"
      ]

    },
    "consumption_pool": {
      "type": "object",
      "description": "Shared enterprise consumption pool with unit and balance.",
      "properties": {
        "pool_id": {
          "type": "string",
          "description": "Stable pool identity."
        },
        "account_id": {
          "type": "string",
          "description": "Owning account."
        },
        "unit": {
          "type": "string",
          "description": "Metered consumption unit."
        },
        "remaining_quantity": {
          "type": "number",
          "description": "Remaining pooled quantity."
        }
      },
      "required": ["pool_id", "account_id", "unit", "remaining_quantity"
      ]

    },
    "consumption_subscription": {
      "type": "object",
      "description": "Consumption package binding plan, account, and standing.",
      "properties": {
        "subscription_id": {
          "type": "string",
          "description": "Stable consumption subscription."
        },
        "account_id": {
          "type": "string",
          "description": "Purchasing account."
        },
        "plan_id": {
          "type": "string",
          "description": "Usage plan identity."
        },
        "status": {
          "type": "string",
          "description": "Subscription lifecycle standing."
        }
      },
      "required": ["subscription_id", "account_id", "plan_id", "status"
      ]

    },
    "container_manifest_digest_observation": {
      "type": "object",
      "description": "Resolves a mutable container tag to an immutable multi-platform manifest digest.",
      "properties": {
        "image_repository": {
          "type": "string",
          "description": "Container repository identity."
        },
        "tag": {
          "type": "string",
          "description": "Observed mutable tag."
        },
        "index_digest": {
          "type": "string",
          "description": "Resolved immutable manifest-index digest."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of registry resolution."
        }
      },
      "required": ["image_repository", "tag", "index_digest", "observed_at"
      ]

    },
    "container_platform_digest_observation": {
      "type": "object",
      "description": "Resolves a manifest index to one exact platform image digest.",
      "properties": {
        "index_digest": {
          "type": "string",
          "description": "Parent manifest-index digest."
        },
        "platform": {
          "type": "string",
          "description": "Normalized operating-system and architecture."
        },
        "platform_digest": {
          "type": "string",
          "description": "Exact platform image digest."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of platform resolution."
        }
      },
      "required": ["index_digest", "platform", "platform_digest", "observed_at"
      ]

    },
    "contracting_entity_identity": {
      "type": "object",
      "description": "Requires the counterparty that will sign the agreement, separating contractual authority from account interest.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required contracting entity identity input; omission is an executable typed refusal, never an inferred approval."
        },
        "contracting_entity_id": {
          "type": "string",
          "description": "Required contracting entity identity input; omission is an executable typed refusal, never an inferred approval."
        },
        "identity_evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "contracting_entity_id", "identity_evidence_hash"
      ]

    },
    "cost_center_allocation": {
      "type": "object",
      "description": "Chargeback allocation to an enterprise cost center.",
      "properties": {
        "cost_center_allocation_id": {
          "type": "string",
          "description": "Stable identity of this cost center allocation contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "cost_center_id": {
          "type": "string",
          "description": "Stable identity of the charged cost center."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["cost_center_allocation_id", "account_id", "cost_center_id", "evidence_digest", "effective_at"
      ]

    },
    "cost_latency_reliability_tradeoff": {
      "type": "object",
      "description": "Evaluates candidate policy utility across cost, latency, and reliability without collapsing dimensions.",
      "properties": {
        "assessment_id": {
          "type": "string",
          "description": "Stable tradeoff assessment identity."
        },
        "candidate_policy_id": {
          "type": "string",
          "description": "Candidate policy evaluated."
        },
        "cost_score": {
          "type": "number",
          "description": "Normalized execution cost."
        },
        "latency_score": {
          "type": "number",
          "description": "Normalized execution latency."
        },
        "reliability_score": {
          "type": "number",
          "description": "Observed reliability."
        },
        "utility_score": {
          "type": "number",
          "description": "Objective-bound aggregate utility."
        }
      },
      "required": ["assessment_id", "candidate_policy_id", "cost_score", "latency_score", "reliability_score", "utility_score"
      ]

    },
    "cost_of_delay_score": {
      "type": "object",
      "description": "Scores delay cost for an option without overriding explicit authority.",
      "properties": {
        "option_id": {
          "type": "string",
          "description": "Required option_id for this bounded planner contract."
        },
        "horizon": {
          "type": "string",
          "description": "Required horizon for deterministic planner evaluation."
        },
        "score": {
          "type": "string",
          "description": "Required score preserving evidence and falsifiability."
        }
      },
      "required": ["option_id", "horizon", "score"
      ]

    },
    "cost_to_serve_measurement": {
      "type": "object",
      "description": "Records attributable runtime cost-to-serve for a paid tenant and billing period.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "billing_period_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "measurement_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "billing_period_id", "measurement_hash"
      ]

    },
    "counterfactual_frontier": {
      "type": "object",
      "description": "Preserves counterfactual futures for every nondominated option.",
      "properties": {
        "option_set_id": {
          "type": "string",
          "description": "Required option_set_id for this bounded planner contract."
        },
        "world_model_hash": {
          "type": "string",
          "description": "Required world_model_hash for deterministic planner evaluation."
        },
        "frontier_hash": {
          "type": "string",
          "description": "Required frontier_hash preserving evidence and falsifiability."
        }
      },
      "required": ["option_set_id", "world_model_hash", "frontier_hash"
      ]

    },
    "counterfactual_replay": {
      "type": "object",
      "description": "Replays an episode under one declared intervention to compare the observed and counterfactual outcomes.",
      "properties": {
        "replay_id": {
          "type": "string",
          "description": "Stable counterfactual replay identity."
        },
        "source_episode_id": {
          "type": "string",
          "description": "Observed episode used as baseline."
        },
        "intervention_digest": {
          "type": "string",
          "description": "Digest of the single counterfactual intervention."
        },
        "seed": {
          "type": "string",
          "description": "Explicit deterministic replay seed."
        },
        "predicted_outcome": {
          "type": "string",
          "description": "Counterfactual outcome produced."
        },
        "comparison_digest": {
          "type": "string",
          "description": "Digest comparing observed and counterfactual traces."
        }
      },
      "required": ["replay_id", "source_episode_id", "intervention_digest", "seed", "predicted_outcome", "comparison_digest"
      ]

    },
    "crash_recovery_receipt": {
      "type": "object",
      "description": "Produces replayable evidence that a runtime crash reached a verified recovery consequence.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "crash_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "receipt_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "crash_id", "receipt_hash"
      ]

    },
    "credit_expiry_policy": {
      "type": "object",
      "description": "Deterministic expiration of prepaid commercial credits.",
      "properties": {
        "credit_expiry_policy_id": {
          "type": "string",
          "description": "Stable identity of this credit expiry policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "expires_at": {
          "type": "string",
          "description": "Exact expiration instant for the admitted credit pool."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["credit_expiry_policy_id", "account_id", "expires_at", "evidence_digest", "effective_at"
      ]

    },
    "credit_risk_admission": {
      "type": "object",
      "description": "Turns counterparty credit risk into an explicit admission decision that can refuse unsafe payment exposure.",
      "properties": {
        "account_id": {
          "type": "string",
          "description": "Required credit risk admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "risk_band": {
          "type": "string",
          "description": "Required credit risk admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["account_id", "risk_band", "decision"
      ]

    },
    "cross_sell_fit": {
      "type": "object",
      "description": "Evidence-backed fit for an adjacent enterprise capability.",
      "properties": {
        "cross_sell_fit_id": {
          "type": "string",
          "description": "Stable identity of this cross sell fit observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose cross sell fit state was observed."
        },
        "cross_sell_score": {
          "type": "number",
          "description": "Observed cross-sell fit score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["cross_sell_fit_id", "account_id", "cross_sell_score", "evidence_digest", "observed_at"
      ]

    },
    "crown_applicable_gate_coverage": {
      "type": "object",
      "description": "Requires every gate applicable to the changed subject before declaring the crown alive.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "applicable_gate_set_digest": {
          "type": "string",
          "description": "Digest of the normalized applicable gate set and results."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "applicable_gate_set_digest", "receipt_digest"
      ]

    },
    "crown_artifact_pullback_smoke": {
      "type": "object",
      "description": "Proves the published artifact can be pulled back from its distribution boundary byte-for-byte.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "pullback_digest": {
          "type": "string",
          "description": "Digest observed after artifact pullback."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "pullback_digest", "receipt_digest"
      ]

    },
    "crown_attestation_signer": {
      "type": "object",
      "description": "Requires an admitted signer identity for every propagated supply-chain attestation.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "signer_identity": {
          "type": "string",
          "description": "Verified identity that signed the attestation."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "signer_identity", "receipt_digest"
      ]

    },
    "crown_autonomic_republish": {
      "type": "object",
      "description": "Receipts autonomous downstream requalification and immutable crown republication.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "republished_crown_digest": {
          "type": "string",
          "description": "Exact digest of the republished downstream crown."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "republished_crown_digest", "receipt_digest"
      ]

    },
    "crown_capsule_toolchain": {
      "type": "object",
      "description": "Binds source capsule, validation pack, execution mode, and toolchain as one evidence identity.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "toolchain_digest": {
          "type": "string",
          "description": "Digest of the exact capsule toolchain closure."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "toolchain_digest", "receipt_digest"
      ]

    },
    "crown_cas_promotion": {
      "type": "object",
      "description": "Authorizes promotion only when the observed prior crown equals the compare-and-swap expectation.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "expected_previous_digest": {
          "type": "string",
          "description": "Expected previously promoted crown digest."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "expected_previous_digest", "receipt_digest"
      ]

    },
    "crown_check_relevance": {
      "type": "object",
      "description": "Separates irrelevant checks from exact-subject qualification without hiding required evidence.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "relevance_proof_digest": {
          "type": "string",
          "description": "Digest proving check relevance classification."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "relevance_proof_digest", "receipt_digest"
      ]

    },
    "crown_child_publish_observation": {
      "type": "object",
      "description": "Observes the exact child publication execution before its crown can propagate further.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "child_publish_run_id": {
          "type": "string",
          "description": "Exact workflow run identity that published the child crown."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "child_publish_run_id", "receipt_digest"
      ]

    },
    "crown_consumer_smoke": {
      "type": "object",
      "description": "Requires a real downstream consumer smoke consequence for the exact propagated crown.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "consumer_smoke_digest": {
          "type": "string",
          "description": "Digest of the exact-consumer smoke receipt."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "consumer_smoke_digest", "receipt_digest"
      ]

    },
    "crown_convergence_proof": {
      "type": "object",
      "description": "Produces a deterministic proof that every admitted consumer holds the expected crown.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "convergence_proof_digest": {
          "type": "string",
          "description": "Digest of the ecosystem convergence proof."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "convergence_proof_digest", "receipt_digest"
      ]

    },
    "crown_cosign_certificate": {
      "type": "object",
      "description": "Captures the keyless signing certificate identity admitted for the exact crown digest.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "certificate_identity": {
          "type": "string",
          "description": "Verified Cosign certificate identity."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "certificate_identity", "receipt_digest"
      ]

    },
    "crown_default_head_sensor": {
      "type": "object",
      "description": "Observes the exact downstream default-branch head before selecting a reconciliation candidate.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "default_head_sha": {
          "type": "string",
          "description": "Observed immutable default-branch head SHA."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "default_head_sha", "receipt_digest"
      ]

    },
    "crown_dependency_edge": {
      "type": "object",
      "description": "Declares one exact upstream-to-downstream propagation edge in the ecosystem graph.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "downstream_consumer_id": {
          "type": "string",
          "description": "Exact downstream consumer repository identity."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "downstream_consumer_id", "receipt_digest"
      ]

    },
    "crown_execution_mode": {
      "type": "object",
      "description": "Records whether qualification ran locally, in a capsule, or on a hosted runner.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "execution_mode": {
          "type": "string",
          "description": "Admitted execution-mode identifier."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "execution_mode", "receipt_digest"
      ]

    },
    "crown_fanin_convergence": {
      "type": "object",
      "description": "Requires every admitted upstream parent crown before a fan-in consumer can converge.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "fanin_set_digest": {
          "type": "string",
          "description": "Digest of the complete admitted fan-in crown set."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "fanin_set_digest", "receipt_digest"
      ]

    },
    "crown_fanout_batch": {
      "type": "object",
      "description": "Binds parallel downstream propagation to one deterministic admitted consumer set.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "fanout_set_digest": {
          "type": "string",
          "description": "Digest of the sorted fan-out consumer set."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "fanout_set_digest", "receipt_digest"
      ]

    },
    "crown_federated_phase_receipt": {
      "type": "object",
      "description": "Receipts SELECT, CONSTRUCT, DRY-RUN MANUFACTURE, QUALIFY, and RECEIPT as ordered phases.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "selected_option_digest": {
          "type": "string",
          "description": "Digest of the selected qualified propagation option."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "selected_option_digest", "receipt_digest"
      ]

    },
    "crown_freshness_window": {
      "type": "object",
      "description": "Defines the deterministic deadline after which a crown cannot be promoted without requalification.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "fresh_until": {
          "type": "string",
          "description": "UTC freshness deadline admitted by policy."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "fresh_until", "receipt_digest"
      ]

    },
    "crown_generated_source_ownership": {
      "type": "object",
      "description": "Routes drift in a generated projection back to its declared canonical semantic source.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "canonical_source_path": {
          "type": "string",
          "description": "Canonical non-generated source path authorized to repair the projection."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "canonical_source_path", "receipt_digest"
      ]

    },
    "crown_gitlink_reconciliation": {
      "type": "object",
      "description": "Reconciles a consumer gitlink to the exact admitted dependency commit.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "gitlink_commit_sha": {
          "type": "string",
          "description": "Exact submodule gitlink commit SHA."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "gitlink_commit_sha", "receipt_digest"
      ]

    },
    "crown_immutable_sha_tag": {
      "type": "object",
      "description": "Admits only artifact tags derived from an exact immutable source SHA.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "immutable_tag": {
          "type": "string",
          "description": "Immutable source-derived artifact tag."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "immutable_tag", "receipt_digest"
      ]

    },
    "crown_known_good_rollback": {
      "type": "object",
      "description": "Selects an immutable previously qualified crown for autonomous rollback.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "rollback_crown_digest": {
          "type": "string",
          "description": "Exact prior known-good crown digest."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "rollback_crown_digest", "receipt_digest"
      ]

    },
    "crown_latency_observation": {
      "type": "object",
      "description": "Records exact end-to-end propagation latency for freshness and SLO decisions.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "latency_millis": {
          "type": "string",
          "description": "Observed propagation latency in milliseconds."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "latency_millis", "receipt_digest"
      ]

    },
    "crown_lock_reconciliation": {
      "type": "object",
      "description": "Reconciles a downstream lock entry to the exact admitted upstream commit.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "lock_commit_sha": {
          "type": "string",
          "description": "Exact commit SHA encoded in the reconciled lock."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "lock_commit_sha", "receipt_digest"
      ]

    },
    "crown_manufacturer_identity": {
      "type": "object",
      "description": "Binds propagation to the exact deterministic manufacturer executable identity.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "manufacturer_digest": {
          "type": "string",
          "description": "Digest of the admitted GGen manufacturer binary."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "manufacturer_digest", "receipt_digest"
      ]

    },
    "crown_marketplace_pack_pin": {
      "type": "object",
      "description": "Requires every consumed marketplace pack to resolve to an immutable Git commit before manufacture.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "pack_commit_sha": {
          "type": "string",
          "description": "Exact immutable marketplace pack commit SHA."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "pack_commit_sha", "receipt_digest"
      ]

    },
    "crown_multiarch_platform_set": {
      "type": "object",
      "description": "Requires the crown manifest to contain the admitted architecture and operating-system set.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "platform_set_digest": {
          "type": "string",
          "description": "Digest of the normalized admitted platform set."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "platform_set_digest", "receipt_digest"
      ]

    },
    "crown_oci_manifest_binding": {
      "type": "object",
      "description": "Binds a propagated crown to its exact OCI multi-architecture index digest.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "oci_index_digest": {
          "type": "string",
          "description": "Exact OCI index digest selected for propagation."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "oci_index_digest", "receipt_digest"
      ]

    },
    "crown_package_pin_reconciliation": {
      "type": "object",
      "description": "Reconciles package resolution to an immutable version and content digest.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "package_version_digest": {
          "type": "string",
          "description": "Digest of the exact resolved package version."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "package_version_digest", "receipt_digest"
      ]

    },
    "crown_partial_checkpoint": {
      "type": "object",
      "description": "Persists exact completed propagation edges for safe recovery after partial failure.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "checkpoint_digest": {
          "type": "string",
          "description": "Digest of the completed-edge checkpoint."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "checkpoint_digest", "receipt_digest"
      ]

    },
    "crown_path_skip_refusal": {
      "type": "object",
      "description": "Refuses false ALIVE when a required qualification gate was path-skipped.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "skipped_gate_id": {
          "type": "string",
          "description": "Identity of the required gate that was skipped."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "skipped_gate_id", "receipt_digest"
      ]

    },
    "crown_planner_identity": {
      "type": "object",
      "description": "Binds autonomous propagation selection to the exact admitted planner identity.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "planner_digest": {
          "type": "string",
          "description": "Digest of the planner that selected the propagation action."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "planner_digest", "receipt_digest"
      ]

    },
    "crown_process_runtime_identity": {
      "type": "object",
      "description": "Binds process-intelligence evidence to the exact Beam4PM computation identity.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "process_runtime_digest": {
          "type": "string",
          "description": "Digest of the process-intelligence runtime subject."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "process_runtime_digest", "receipt_digest"
      ]

    },
    "crown_promotion_race": {
      "type": "object",
      "description": "Records the conflicting prior digest when a concurrent promotion wins the race.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "observed_previous_digest": {
          "type": "string",
          "description": "Observed prior crown digest at the failed CAS boundary."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "observed_previous_digest", "receipt_digest"
      ]

    },
    "crown_provenance_binding": {
      "type": "object",
      "description": "Binds build provenance to the exact source, builder, and published artifact identities.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "provenance_digest": {
          "type": "string",
          "description": "Digest of exact-subject build provenance."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "provenance_digest", "receipt_digest"
      ]

    },
    "crown_receipt_output_ownership": {
      "type": "object",
      "description": "Propagates receipt lineage together with the declared owner of every manufactured output.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "output_owner": {
          "type": "string",
          "description": "Declared owner identity for the manufactured output set."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "output_owner", "receipt_digest"
      ]

    },
    "crown_recursive_fixed_point": {
      "type": "object",
      "description": "Proves recursive ecosystem propagation reached a stable exact-crown fixed point.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "fixed_point_digest": {
          "type": "string",
          "description": "Digest of the converged ecosystem crown assignment."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "fixed_point_digest", "receipt_digest"
      ]

    },
    "crown_resume_token": {
      "type": "object",
      "description": "Binds resumed propagation to its checkpoint and current dependency graph.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "resume_token_digest": {
          "type": "string",
          "description": "Digest of the validated resume token."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "resume_token_digest", "receipt_digest"
      ]

    },
    "crown_runtime_identity": {
      "type": "object",
      "description": "Binds the propagated artifact to the exact runtime used for qualification.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "runtime_digest": {
          "type": "string",
          "description": "Digest of the admitted runtime environment."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "runtime_digest", "receipt_digest"
      ]

    },
    "crown_sbom_subject_binding": {
      "type": "object",
      "description": "Binds a retained software bill of materials to the exact published crown subject.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "sbom_digest": {
          "type": "string",
          "description": "Digest of the SBOM whose subject is the exact crown."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "sbom_digest", "receipt_digest"
      ]

    },
    "crown_second_pass_identity": {
      "type": "object",
      "description": "Proves a second lawful GGen sync is byte-identical to the first manufactured output set.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "second_pass_digest": {
          "type": "string",
          "description": "Digest of the complete second-pass output set."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "second_pass_digest", "receipt_digest"
      ]

    },
    "crown_security_scan": {
      "type": "object",
      "description": "Requires a retained security scan report bound to the exact crown artifact digest.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "scan_report_digest": {
          "type": "string",
          "description": "Digest of the exact-artifact vulnerability scan report."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "scan_report_digest", "receipt_digest"
      ]

    },
    "crown_source_capsule": {
      "type": "object",
      "description": "Binds validation to the exact ggen-ecosystem source capsule and image identity.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "capsule_digest": {
          "type": "string",
          "description": "Exact source capsule or reproducible image digest."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "capsule_digest", "receipt_digest"
      ]

    },
    "crown_stale_refusal": {
      "type": "object",
      "description": "Records crown age so stale subjects are typed and refused without actuation.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "observed_age_seconds": {
          "type": "string",
          "description": "Observed crown age in seconds at the decision boundary."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "observed_age_seconds", "receipt_digest"
      ]

    },
    "crown_supply_chain_policy": {
      "type": "object",
      "description": "Records the executable supply-chain policy decision that admitted or refused the crown.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "policy_decision_digest": {
          "type": "string",
          "description": "Digest of the exact policy input and decision."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "policy_decision_digest", "receipt_digest"
      ]

    },
    "crown_topological_order": {
      "type": "object",
      "description": "Orders crown propagation so dependencies qualify before their consumers.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "topological_rank": {
          "type": "string",
          "description": "Deterministic topological rank for this propagation node."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "topological_rank", "receipt_digest"
      ]

    },
    "crown_validation_pack": {
      "type": "object",
      "description": "Pins the executable validation pack to an immutable marketplace commit.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "validation_pack_sha": {
          "type": "string",
          "description": "Exact validation-pack Git commit SHA."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "validation_pack_sha", "receipt_digest"
      ]

    },
    "crown_workflow_run_receipt": {
      "type": "object",
      "description": "Binds a qualification receipt to the exact workflow execution and attempt.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "workflow_run_id": {
          "type": "string",
          "description": "Exact hosted workflow run and attempt identity."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "workflow_run_id", "receipt_digest"
      ]

    },
    "crown_zero_unreceipted_writes": {
      "type": "object",
      "description": "Proves every propagation write belongs to a retained authorized receipt.",
      "properties": {
        "propagation_id": {
          "type": "string",
          "description": "Unique autonomic propagation attempt identity."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact admitted Git subject SHA."
        },
        "write_set_digest": {
          "type": "string",
          "description": "Digest of the complete authorized write set."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable digest of this capability's exact-subject qualification receipt."
        }
      },
      "required": ["propagation_id", "subject_sha", "write_set_digest", "receipt_digest"
      ]

    },
    "currency_policy": {
      "type": "object",
      "description": "Allowed billing currency for an enterprise agreement.",
      "properties": {
        "currency_policy_id": {
          "type": "string",
          "description": "Stable identity of this currency policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "currency_code": {
          "type": "string",
          "description": "ISO billing currency admitted for this account."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["currency_policy_id", "account_id", "currency_code", "evidence_digest", "effective_at"
      ]

    },
    "curriculum_generation": {
      "type": "object",
      "description": "Generates an ordered, diverse learning curriculum from admitted gaps and executable falsifiers.",
      "properties": {
        "curriculum_id": {
          "type": "string",
          "description": "Stable generated curriculum identity."
        },
        "source_gap_set_digest": {
          "type": "string",
          "description": "Digest of admitted capability gaps."
        },
        "ordered_hypotheses_digest": {
          "type": "string",
          "description": "Digest of ordered hypothesis identities."
        },
        "diversity_score": {
          "type": "number",
          "description": "Semantic diversity across the curriculum."
        },
        "falsifier_coverage": {
          "type": "number",
          "description": "Share of hypotheses with executable falsifiers."
        }
      },
      "required": ["curriculum_id", "source_gap_set_digest", "ordered_hypotheses_digest", "diversity_score", "falsifier_coverage"
      ]

    },
    "customer_health": {
      "type": "object",
      "description": "Cumulative customer-health observation across usage, outcomes, and support.",
      "properties": {
        "customer_health_id": {
          "type": "string",
          "description": "Stable identity of this customer health observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose customer health state was observed."
        },
        "health_score": {
          "type": "number",
          "description": "Observed account health score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["customer_health_id", "account_id", "health_score", "evidence_digest", "observed_at"
      ]

    },
    "customer_managed_key_evidence": {
      "type": "object",
      "description": "Executable encryption evidence binding an exact subject to the customer-managed key identity actually used.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this customer-managed-key observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "key_identifier": {
          "type": "string",
          "description": "Non-secret identifier of the customer-managed key observed at encryption."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "key_identifier", "observed_result"
      ]

    },
    "customer_signal_observation": {
      "type": "object",
      "description": "Normalizes a customer signal into typed, digest-bound evidence.",
      "properties": {
        "customer_id": {
          "type": "string",
          "description": "Stable customer identity."
        },
        "signal_type": {
          "type": "string",
          "description": "Normalized customer signal type."
        },
        "signal_digest": {
          "type": "string",
          "description": "Immutable customer signal digest."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp customer signal was observed."
        }
      },
      "required": ["customer_id", "signal_type", "signal_digest", "observed_at"
      ]

    },
    "data_egress_evidence": {
      "type": "object",
      "description": "Executable egress evidence binding an exact subject to the bytes observed crossing its admitted data boundary.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this data-egress observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "egress_bytes": {
          "type": "integer",
          "description": "Observed byte count crossing the controlled egress boundary."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "egress_bytes", "observed_result"
      ]

    },
    "data_migration_scope_admission": {
      "type": "object",
      "description": "Admits a bounded data-migration scope before margin and timeline promises are accepted.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required data migration scope admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "migration_scope_id": {
          "type": "string",
          "description": "Required data migration scope admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "migration_scope_id", "decision"
      ]

    },
    "data_processing_addendum_state": {
      "type": "object",
      "description": "Requires an identified data-processing addendum state for workloads involving regulated enterprise data.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required data processing addendum state input; omission is an executable typed refusal, never an inferred approval."
        },
        "addendum_id": {
          "type": "string",
          "description": "Required data processing addendum state input; omission is an executable typed refusal, never an inferred approval."
        },
        "addendum_state": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "addendum_id", "addendum_state"
      ]

    },
    "data_readiness": {
      "type": "object",
      "description": "Measured source-data readiness for a production proof of value.",
      "properties": {
        "data_readiness_id": {
          "type": "string",
          "description": "Stable identity of this data readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose data readiness state was observed."
        },
        "data_quality_score": {
          "type": "number",
          "description": "Observed quality score of admitted customer data."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["data_readiness_id", "account_id", "data_quality_score", "evidence_digest", "observed_at"
      ]

    },
    "data_residency_policy": {
      "type": "object",
      "description": "Tenant data-location contract with allowed regions.",
      "properties": {
        "policy_id": {
          "type": "string",
          "description": "Stable residency policy."
        },
        "tenant_id": {
          "type": "string",
          "description": "Governed tenant."
        },
        "allowed_regions": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Permitted data regions."
        },
        "status": {
          "type": "string",
          "description": "Residency policy standing."
        }
      },
      "required": ["policy_id", "tenant_id", "allowed_regions", "status"
      ]

    },
    "data_volume_pricing_policy": {
      "type": "object",
      "description": "Data-volume commercial packaging dimension.",
      "properties": {
        "data_volume_pricing_policy_id": {
          "type": "string",
          "description": "Stable identity of this data volume pricing policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "unit_gb_price": {
          "type": "number",
          "description": "Admitted unit price per processed gigabyte."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["data_volume_pricing_policy_id", "account_id", "unit_gb_price", "evidence_digest", "effective_at"
      ]

    },
    "deal_desk_packet": {
      "type": "object",
      "description": "Binds cross-functional deal-desk approval evidence into one replayable packet identity.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required deal desk packet input; omission is an executable typed refusal, never an inferred approval."
        },
        "packet_id": {
          "type": "string",
          "description": "Required deal desk packet input; omission is an executable typed refusal, never an inferred approval."
        },
        "evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "packet_id", "evidence_hash"
      ]

    },
    "decision_compression_observation": {
      "type": "object",
      "description": "Compresses high-dimensional state into a bounded actionable delta with loss bound.",
      "properties": {
        "compression_id": {
          "type": "string",
          "description": "Stable compression operation identity."
        },
        "input_state_digest": {
          "type": "string",
          "description": "Digest of high-dimensional input state."
        },
        "output_delta_digest": {
          "type": "string",
          "description": "Digest of compressed delta."
        },
        "loss_bound": {
          "type": "string",
          "description": "Declared information-loss bound."
        }
      },
      "required": ["compression_id", "input_state_digest", "output_delta_digest", "loss_bound"
      ]

    },
    "decision_information_preservation": {
      "type": "object",
      "description": "Proves an adaptive projection preserves the distinctions required by a decision.",
      "properties": {
        "preservation_id": {
          "type": "string",
          "description": "Stable preservation proof identity."
        },
        "decision_id": {
          "type": "string",
          "description": "Decision whose information is protected."
        },
        "source_partition_digest": {
          "type": "string",
          "description": "Digest of source information partitions."
        },
        "projection_digest": {
          "type": "string",
          "description": "Digest of the adaptive projection."
        },
        "preserved_question_set_digest": {
          "type": "string",
          "description": "Digest of decision questions still answerable."
        },
        "loss_score": {
          "type": "number",
          "description": "Measured decision-information loss."
        }
      },
      "required": ["preservation_id", "decision_id", "source_partition_digest", "projection_digest", "preserved_question_set_digest", "loss_score"
      ]

    },
    "deletion_proof_evidence": {
      "type": "object",
      "description": "Executable deletion evidence binding an exact subject to a verifiable deletion receipt.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this deletion-proof observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "deletion_receipt_id": {
          "type": "string",
          "description": "Identity of the receipt emitted by the completed deletion operation."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "deletion_receipt_id", "observed_result"
      ]

    },
    "demo_run": {
      "type": "object",
      "description": "Receipted execution result for a reproducible enterprise demo.",
      "properties": {
        "demo_run_id": {
          "type": "string",
          "description": "Stable identity of this demo run observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose demo run state was observed."
        },
        "demo_result": {
          "type": "string",
          "description": "Observed result of the exact demo execution."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["demo_run_id", "account_id", "demo_result", "evidence_digest", "observed_at"
      ]

    },
    "demo_scenario": {
      "type": "object",
      "description": "Account-specific executable demonstration scenario for enterprise evaluation.",
      "properties": {
        "demo_scenario_id": {
          "type": "string",
          "description": "Stable identity of this demo scenario observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose demo scenario state was observed."
        },
        "scenario_name": {
          "type": "string",
          "description": "Stable name of the selected customer scenario."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["demo_scenario_id", "account_id", "scenario_name", "evidence_digest", "observed_at"
      ]

    },
    "dependency_dag": {
      "type": "object",
      "description": "Represents action dependencies as an acyclic exact-subject graph.",
      "properties": {
        "dag_id": {
          "type": "string",
          "description": "Required dag_id for this bounded planner contract."
        },
        "node_set_hash": {
          "type": "string",
          "description": "Required node_set_hash for deterministic planner evaluation."
        },
        "edge_set_hash": {
          "type": "string",
          "description": "Required edge_set_hash preserving evidence and falsifiability."
        }
      },
      "required": ["dag_id", "node_set_hash", "edge_set_hash"
      ]

    },
    "dependency_inventory_evidence": {
      "type": "object",
      "description": "Executable dependency evidence binding an exact commercial subject to its resolved dependency inventory.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this dependency inventory observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "dependency_count": {
          "type": "integer",
          "description": "Observed number of resolved direct and transitive dependencies."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "dependency_count", "observed_result"
      ]

    },
    "dependency_pin_observation": {
      "type": "object",
      "description": "Compares a declared dependency ref with its immutable resolved commit.",
      "properties": {
        "dependency_id": {
          "type": "string",
          "description": "Stable dependency identity."
        },
        "declared_ref": {
          "type": "string",
          "description": "Declared dependency reference."
        },
        "resolved_sha": {
          "type": "string",
          "description": "Resolved immutable commit."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of resolution."
        }
      },
      "required": ["dependency_id", "declared_ref", "resolved_sha", "observed_at"
      ]

    },
    "deployment_entitlement": {
      "type": "object",
      "description": "Paid right to deploy an edition into an environment.",
      "properties": {
        "entitlement_id": {
          "type": "string",
          "description": "Stable deployment entitlement."
        },
        "tenant_id": {
          "type": "string",
          "description": "Entitled tenant."
        },
        "profile_id": {
          "type": "string",
          "description": "Allowed environment profile."
        },
        "valid_until": {
          "type": "string",
          "description": "Deployment right expiration."
        }
      },
      "required": ["entitlement_id", "tenant_id", "profile_id", "valid_until"
      ]

    },
    "developer_readiness": {
      "type": "object",
      "description": "Measured developer enablement readiness for enterprise adoption.",
      "properties": {
        "developer_readiness_id": {
          "type": "string",
          "description": "Stable identity of this developer readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose developer readiness state was observed."
        },
        "developer_readiness_score": {
          "type": "number",
          "description": "Observed developer-readiness score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["developer_readiness_id", "account_id", "developer_readiness_score", "evidence_digest", "observed_at"
      ]

    },
    "dfg_edge": {
      "type": "object",
      "description": "One frequency-annotated directly-follows edge between two activities.",
      "properties": {
        "source_activity": {
          "type": "string",
          "description": "The preceding activity name."
        },
        "target_activity": {
          "type": "string",
          "description": "The following activity name."
        },
        "frequency": {
          "type": "integer",
          "description": "Observed occurrence count of this edge."
        },
        "edge_weight": {
          "type": "number",
          "description": "Structural weight of this edge for what-if simulation (B4PM-1702)."
        }
      },
      "required": ["source_activity", "target_activity", "frequency", "edge_weight"
      ]

    },
    "disaster_recovery_evidence": {
      "type": "object",
      "description": "Executable disaster-recovery evidence binding an exact subject to the instant service recovery was observed.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this disaster-recovery observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "recovered_at": {
          "type": "string",
          "description": "UTC instant at which the recovered subject passed its availability probe."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "recovered_at", "observed_result"
      ]

    },
    "discount_schedule": {
      "type": "object",
      "description": "Volume discount tier with threshold and percentage.",
      "properties": {
        "schedule_id": {
          "type": "string",
          "description": "Stable discount schedule."
        },
        "threshold": {
          "type": "number",
          "description": "Quantity activating the tier."
        },
        "discount_percent": {
          "type": "number",
          "description": "Percentage discount."
        },
        "currency": {
          "type": "string",
          "description": "Applicable settlement currency."
        }
      },
      "required": ["schedule_id", "threshold", "discount_percent", "currency"
      ]

    },
    "discovery_hypothesis": {
      "type": "object",
      "description": "Receipted enterprise discovery hypothesis linking an account to a falsifiable customer-value expectation.",
      "properties": {
        "discovery_hypothesis_id": {
          "type": "string",
          "description": "Stable identity of this discovery hypothesis observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose discovery hypothesis state was observed."
        },
        "expected_value": {
          "type": "string",
          "description": "Falsifiable value expectation stated by the customer."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["discovery_hypothesis_id", "account_id", "expected_value", "evidence_digest", "observed_at"
      ]

    },
    "dominance_witness": {
      "type": "object",
      "description": "Records the exact objective evidence proving one option dominates another.",
      "properties": {
        "dominant_option_id": {
          "type": "string",
          "description": "Required dominant_option_id for this bounded planner contract."
        },
        "dominated_option_id": {
          "type": "string",
          "description": "Required dominated_option_id for deterministic planner evaluation."
        },
        "witness_hash": {
          "type": "string",
          "description": "Required witness_hash preserving evidence and falsifiability."
        }
      },
      "required": ["dominant_option_id", "dominated_option_id", "witness_hash"
      ]

    },
    "dynamic_replan_trigger": {
      "type": "object",
      "description": "Triggers replanning when observed state invalidates a plan assumption.",
      "properties": {
        "plan_id": {
          "type": "string",
          "description": "Required plan_id for this bounded planner contract."
        },
        "event_id": {
          "type": "string",
          "description": "Required event_id for deterministic planner evaluation."
        },
        "trigger_hash": {
          "type": "string",
          "description": "Required trigger_hash preserving evidence and falsifiability."
        }
      },
      "required": ["plan_id", "event_id", "trigger_hash"
      ]

    },
    "edition_definition": {
      "type": "object",
      "description": "Enterprise edition binding capabilities and support tier.",
      "properties": {
        "edition_id": {
          "type": "string",
          "description": "Stable edition identity."
        },
        "name": {
          "type": "string",
          "description": "Buyer-facing edition name."
        },
        "bundle_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Included capability bundles."
        },
        "support_tier": {
          "type": "string",
          "description": "Default support tier."
        }
      },
      "required": ["edition_id", "name", "bundle_ids", "support_tier"
      ]

    },
    "edition_downgrade_path": {
      "type": "object",
      "description": "Controlled commercial edition downgrade path.",
      "properties": {
        "edition_downgrade_path_id": {
          "type": "string",
          "description": "Stable identity of this edition downgrade path contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "target_edition_id": {
          "type": "string",
          "description": "Edition that may be reached after capability-loss admission."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["edition_downgrade_path_id", "account_id", "target_edition_id", "evidence_digest", "effective_at"
      ]

    },
    "edition_upgrade_path": {
      "type": "object",
      "description": "Admitted commercial edition upgrade path.",
      "properties": {
        "edition_upgrade_path_id": {
          "type": "string",
          "description": "Stable identity of this edition upgrade path contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "target_edition_id": {
          "type": "string",
          "description": "Edition that may be reached without breaking contract compatibility."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["edition_upgrade_path_id", "account_id", "target_edition_id", "evidence_digest", "effective_at"
      ]

    },
    "enterprise_agreement": {
      "type": "object",
      "description": "Enterprise commercial umbrella agreement.",
      "properties": {
        "enterprise_agreement_id": {
          "type": "string",
          "description": "Stable identity of this enterprise agreement contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "agreement_version": {
          "type": "string",
          "description": "Immutable semantic version of the governing agreement."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["enterprise_agreement_id", "account_id", "agreement_version", "evidence_digest", "effective_at"
      ]

    },
    "enterprise_order": {
      "type": "object",
      "description": "Accepted enterprise order binding account, quote, and standing.",
      "properties": {
        "order_id": {
          "type": "string",
          "description": "Stable enterprise order identity."
        },
        "account_id": {
          "type": "string",
          "description": "Purchasing enterprise account."
        },
        "quote_id": {
          "type": "string",
          "description": "Accepted quote identity."
        },
        "status": {
          "type": "string",
          "description": "Order lifecycle standing."
        }
      },
      "required": ["order_id", "account_id", "quote_id", "status"
      ]

    },
    "enterprise_order_line": {
      "type": "object",
      "description": "Order line preserving SKU, quantity, and contracted unit price.",
      "properties": {
        "order_id": {
          "type": "string",
          "description": "Parent enterprise order."
        },
        "sku": {
          "type": "string",
          "description": "Ordered catalog SKU."
        },
        "quantity": {
          "type": "integer",
          "description": "Contracted unit count."
        },
        "unit_price": {
          "type": "number",
          "description": "Contracted price per unit."
        }
      },
      "required": ["order_id", "sku", "quantity", "unit_price"
      ]

    },
    "entitlement_denial_receipt": {
      "type": "object",
      "description": "Receipts a runtime denial when the paid tenant lacks the required product entitlement.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "entitlement_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "denial_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "entitlement_id", "denial_hash"
      ]

    },
    "entitlement_event": {
      "type": "object",
      "description": "One inbound marketplace entitlement/order/agreement lifecycle event, as delivered by the provider's notification topic. Append-only and at-least-once: the same event_id may be delivered any number of times, in any order relative to other events. Modeled on the Google Cloud Commerce Partner Procurement API Pub/Sub notification message, whose payload carries both an eventId and an entitlement id.",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "DEDUP KEY. The provider's own globally unique notification identifier (the eventId field of the Partner Procurement Pub/Sub message). Two deliveries carrying the same event_id are the same event and must fold to the same entitlement_state exactly once. This is provider-assigned, never locally minted -- a locally generated id would make redelivery indistinguishable from a genuine new event."
        },
        "entitlement_id": {
          "type": "string",
          "description": "The commercial identity this event applies to (Partner Procurement ENTITLEMENT_ID). Reconciliation partitions strictly by this key. Google supports multiple concurrent orders of the same product, so entitlement_id -- not account id and not product id -- is the only admissible partition key."
        },
        "event_type": {
          "type": "string",
          "description": "CLOSED SET, exactly the 13 entitlement event types published by Google Cloud Marketplace for integrated SaaS products: ENTITLEMENT_CREATION_REQUESTED | ENTITLEMENT_OFFER_ACCEPTED | ENTITLEMENT_ACTIVE | ENTITLEMENT_PLAN_CHANGE_REQUESTED | ENTITLEMENT_PLAN_CHANGED | ENTITLEMENT_PLAN_CHANGE_CANCELLED | ENTITLEMENT_PENDING_CANCELLATION | ENTITLEMENT_CANCELLATION_REVERTED | ENTITLEMENT_CANCELLED | ENTITLEMENT_CANCELLING | ENTITLEMENT_RENEWED | ENTITLEMENT_OFFER_ENDED | ENTITLEMENT_DELETED. Typed string, deliberately NOT atom: these values arrive verbatim from an external wire and binary_to_atom/2 over untrusted input is an unbounded atom-table growth path on the BEAM. An unrecognized value is a typed refusal, never a silently ignored event."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 UTC instant at which this event takes commercial effect (provider-assigned, not local receipt time). Primary component of the reconciliation watermark; ties are broken by event_id so that the fold is a total order and therefore order-independent. Local receipt time is deliberately not modeled: it would make the fold non-deterministic under replay."
        },
        "payload": {
          "type": "object",
          "description": "Optional provider-specific extra fields carried verbatim (e.g. account id, product/plan id, offer id, newPlan, newOffer, scheduled start time). Retained for receipt/audit fidelity; the reconciliation fold reads NO field from this map -- everything the fold depends on is promoted to a typed top-level field above."
        }
      },
      "required": ["event_id", "entitlement_id", "event_type", "effective_at"
      ]

    },
    "entitlement_evidence": {
      "type": "object",
      "description": "Executable entitlement evidence binding an exact subject to the paid capability grant actually evaluated.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this entitlement observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "entitlement_id": {
          "type": "string",
          "description": "Identity of the entitlement evaluated at the capability boundary."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "entitlement_id", "observed_result"
      ]

    },
    "entitlement_grant": {
      "type": "object",
      "description": "Receiptable grant of a capability to a tenant.",
      "properties": {
        "grant_id": {
          "type": "string",
          "description": "Stable entitlement grant."
        },
        "tenant_id": {
          "type": "string",
          "description": "Receiving tenant."
        },
        "capability_id": {
          "type": "string",
          "description": "Granted capability."
        },
        "valid_until": {
          "type": "string",
          "description": "Grant expiration instant."
        }
      },
      "required": ["grant_id", "tenant_id", "capability_id", "valid_until"
      ]

    },
    "entitlement_revocation": {
      "type": "object",
      "description": "Receiptable capability revocation with reason and time.",
      "properties": {
        "revocation_id": {
          "type": "string",
          "description": "Stable revocation identity."
        },
        "grant_id": {
          "type": "string",
          "description": "Revoked entitlement grant."
        },
        "reason": {
          "type": "string",
          "description": "Typed revocation reason."
        },
        "revoked_at": {
          "type": "string",
          "description": "Revocation effective instant."
        }
      },
      "required": ["revocation_id", "grant_id", "reason", "revoked_at"
      ]

    },
    "entitlement_runtime_check": {
      "type": "object",
      "description": "Records the exact paid entitlement evaluated before workload execution.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "entitlement_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "check_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "entitlement_id", "check_hash"
      ]

    },
    "entitlement_state": {
      "type": "object",
      "description": "The reconciled current state of one entitlement, derived purely by folding its entitlement_event set. The fold is idempotent (replaying an already-applied event is a no-op) and commutative in arrival order (an older event arriving after a newer one is a no-op), because an event is applied if and only if its (effective_at, event_id) pair is strictly greater than the state's (updated_at, last_applied_event_id) watermark.",
      "properties": {
        "entitlement_id": {
          "type": "string",
          "description": "The commercial identity this state describes (Partner Procurement ENTITLEMENT_ID). Unique key: at most one entitlement_state exists per entitlement_id."
        },
        "status": {
          "type": "string",
          "description": "CLOSED SET, exactly the 8 values of the Cloud Commerce Partner Procurement API's EntitlementState enum: ENTITLEMENT_STATE_UNSPECIFIED | ENTITLEMENT_ACTIVATION_REQUESTED | ENTITLEMENT_ACTIVE | ENTITLEMENT_PENDING_CANCELLATION | ENTITLEMENT_CANCELLED | ENTITLEMENT_PENDING_PLAN_CHANGE | ENTITLEMENT_PENDING_PLAN_CHANGE_APPROVAL | ENTITLEMENT_SUSPENDED. This is the provider's STATE enum, which is deliberately not the same closed set as entitlement_event.event_type: ENTITLEMENT_SUSPENDED is a reachable state with no corresponding notification event type, and ENTITLEMENT_RENEWED / ENTITLEMENT_OFFER_ENDED / ENTITLEMENT_DELETED are event types with no distinct state. The event_type -> status transition table is therefore an explicit, non-identity mapping, never a string rename."
        },
        "last_applied_event_id": {
          "type": "string",
          "description": "The event_id of the single entitlement_event that produced this state. Second (tiebreak) component of the reconciliation watermark, and the audit link from a commercial state back to the exact provider notification that caused it. Required, never undefined: an entitlement_state may only be constructed by applying a real event, so there is no lawful state without a causing event id."
        },
        "updated_at": {
          "type": "string",
          "description": "The effective_at of the last applied event -- NOT wall-clock ingestion time. First component of the reconciliation watermark. Defining it as provider effective time (a) makes the state a pure function of the event set, so the same events replayed in any order at any later date rebuild a byte-identical state, and (b) makes the strictly-greater-than admission test well-founded. A wall-clock updated_at would silently admit an out-of-order older event, because it always advances."
        }
      },
      "required": ["entitlement_id", "status", "last_applied_event_id", "updated_at"
      ]

    },
    "entropy_reduction_score": {
      "type": "object",
      "description": "Measures expected uncertainty reduction from a bounded observation action.",
      "properties": {
        "action_id": {
          "type": "string",
          "description": "Required action_id for this bounded planner contract."
        },
        "prior_entropy": {
          "type": "string",
          "description": "Required prior_entropy for deterministic planner evaluation."
        },
        "expected_posterior_entropy": {
          "type": "string",
          "description": "Required expected_posterior_entropy preserving evidence and falsifiability."
        }
      },
      "required": ["action_id", "prior_entropy", "expected_posterior_entropy"
      ]

    },
    "environment_failure_separation": {
      "type": "object",
      "description": "Separates capsule or toolchain failure from subject failure before policy learning can update.",
      "properties": {
        "assessment_id": {
          "type": "string",
          "description": "Stable separation assessment identity."
        },
        "exact_subject_sha": {
          "type": "string",
          "description": "Exact subject that was under evaluation."
        },
        "capsule_digest": {
          "type": "string",
          "description": "Exact validation capsule identity."
        },
        "failure_signal": {
          "type": "string",
          "description": "Observed environment failure signal."
        },
        "classification_basis": {
          "type": "string",
          "description": "Evidence basis for environment classification."
        }
      },
      "required": ["assessment_id", "exact_subject_sha", "capsule_digest", "failure_signal", "classification_basis"
      ]

    },
    "environment_identity": {
      "type": "object",
      "description": "Binds paid workload execution to an immutable production environment identity.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "environment_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "identity_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "environment_id", "identity_hash"
      ]

    },
    "environment_pricing_policy": {
      "type": "object",
      "description": "Environment-count commercial packaging.",
      "properties": {
        "environment_pricing_policy_id": {
          "type": "string",
          "description": "Stable identity of this environment pricing policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "environment_tier": {
          "type": "string",
          "description": "Purchased environment tier such as sandbox, nonproduction, or production."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["environment_pricing_policy_id", "account_id", "environment_tier", "evidence_digest", "effective_at"
      ]

    },
    "environment_profile": {
      "type": "object",
      "description": "Deterministic environment profile for demo, POC, or production.",
      "properties": {
        "profile_id": {
          "type": "string",
          "description": "Stable environment profile."
        },
        "environment": {
          "type": "string",
          "description": "Deployment environment class."
        },
        "region": {
          "type": "string",
          "description": "Target deployment region."
        },
        "configuration_hash": {
          "type": "string",
          "description": "Canonical configuration digest."
        }
      },
      "required": ["profile_id", "environment", "region", "configuration_hash"
      ]

    },
    "environment_signal_observation": {
      "type": "object",
      "description": "Normalizes deployment-environment evidence without human polling.",
      "properties": {
        "environment_id": {
          "type": "string",
          "description": "Stable environment identity."
        },
        "signal_type": {
          "type": "string",
          "description": "Normalized environment signal type."
        },
        "signal_digest": {
          "type": "string",
          "description": "Immutable environment signal digest."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp environment signal was observed."
        }
      },
      "required": ["environment_id", "signal_type", "signal_digest", "observed_at"
      ]

    },
    "error_budget_state": {
      "type": "object",
      "description": "Captures the exact remaining error budget that governs paid-service release policy.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "slo_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "state_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "slo_id", "state_hash"
      ]

    },
    "evaluation_seed_binding": {
      "type": "object",
      "description": "Refuses evaluation evidence whose result is not cryptographically bound to its declared seed.",
      "properties": {
        "binding_id": {
          "type": "string",
          "description": "Stable seed-binding identity."
        },
        "evaluation_id": {
          "type": "string",
          "description": "Evaluation whose seed is bound."
        },
        "seed": {
          "type": "string",
          "description": "Declared deterministic seed."
        },
        "result_digest": {
          "type": "string",
          "description": "Digest of the seeded result."
        },
        "source_digest": {
          "type": "string",
          "description": "Digest of seed provenance."
        },
        "deterministic": {
          "type": "boolean",
          "description": "Whether replay reproduced the result exactly."
        }
      },
      "required": ["binding_id", "evaluation_id", "seed", "result_digest", "source_digest", "deterministic"
      ]

    },
    "event_log": {
      "type": "object",
      "description": "A named collection of events forming one process-mining log.",
      "properties": {
        "log_id": {
          "type": "string",
          "description": "Unique log identifier."
        },
        "name": {
          "type": "string",
          "description": "Human-readable log name."
        },
        "description": {
          "type": "string",
          "description": "Optional free-text description of this log."
        }
      },
      "required": ["log_id", "name"
      ]

    },
    "event_triggered_planning": {
      "type": "object",
      "description": "Creates a bounded planning episode from an admitted world event.",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "Required event_id for this bounded planner contract."
        },
        "world_state_hash": {
          "type": "string",
          "description": "Required world_state_hash for deterministic planner evaluation."
        },
        "episode_id": {
          "type": "string",
          "description": "Required episode_id preserving evidence and falsifiability."
        }
      },
      "required": ["event_id", "world_state_hash", "episode_id"
      ]

    },
    "event_type": {
      "type": "object",
      "description": "A declared OCEL event type and its attribute schema.",
      "properties": {
        "type_name": {
          "type": "string",
          "description": "The event type name."
        },
        "attribute_names": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Optional declared attribute names for events of this type (name-only; per-attribute value types are not yet modeled)."
        }
      },
      "required": ["type_name"
      ]

    },
    "event_volume_pricing_policy": {
      "type": "object",
      "description": "Event-volume commercial packaging dimension.",
      "properties": {
        "event_volume_pricing_policy_id": {
          "type": "string",
          "description": "Stable identity of this event volume pricing policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "unit_event_price": {
          "type": "number",
          "description": "Admitted unit price per event billing unit."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["event_volume_pricing_policy_id", "account_id", "unit_event_price", "evidence_digest", "effective_at"
      ]

    },
    "evidence_freshness_evidence": {
      "type": "object",
      "description": "Executable freshness evidence binding an exact subject to the observation instant used by policy.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this evidence-freshness observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "observed_at": {
          "type": "string",
          "description": "UTC instant at which the subject evidence was observed."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "observed_at", "observed_result"
      ]

    },
    "evidence_training_sample": {
      "type": "object",
      "description": "Transforms admitted episode evidence into a provenance-bound training sample while refusing evidence-free labels.",
      "properties": {
        "sample_id": {
          "type": "string",
          "description": "Stable training sample identity."
        },
        "episode_id": {
          "type": "string",
          "description": "Source learning episode identity."
        },
        "feature_digest": {
          "type": "string",
          "description": "Digest of the decision-relevant feature projection."
        },
        "label_id": {
          "type": "string",
          "description": "Outcome or failure label identity."
        },
        "provenance_digest": {
          "type": "string",
          "description": "Immutable provenance proving how the sample was derived."
        }
      },
      "required": ["sample_id", "episode_id", "feature_digest", "label_id", "provenance_digest"
      ]

    },
    "exact_subject_binding": {
      "type": "object",
      "description": "Binds every plan to one immutable repository/ref/SHA subject.",
      "properties": {
        "subject_id": {
          "type": "string",
          "description": "Required subject_id for this bounded planner contract."
        },
        "subject_sha": {
          "type": "string",
          "description": "Required subject_sha for deterministic planner evaluation."
        },
        "binding_hash": {
          "type": "string",
          "description": "Required binding_hash preserving evidence and falsifiability."
        }
      },
      "required": ["subject_id", "subject_sha", "binding_hash"
      ]

    },
    "exception_authority": {
      "type": "object",
      "description": "Admits a commercial exception only when the exact authority and decision are present.",
      "properties": {
        "exception_id": {
          "type": "string",
          "description": "Required exception authority input; omission is an executable typed refusal, never an inferred approval."
        },
        "authority_id": {
          "type": "string",
          "description": "Required exception authority input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["exception_id", "authority_id", "decision"
      ]

    },
    "executive_business_review": {
      "type": "object",
      "description": "Observed executive business-review outcome derived from receipts.",
      "properties": {
        "executive_business_review_id": {
          "type": "string",
          "description": "Stable identity of this executive business review observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose executive business review state was observed."
        },
        "executive_outcome": {
          "type": "string",
          "description": "Decision recorded by the executive review."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["executive_business_review_id", "account_id", "executive_outcome", "evidence_digest", "observed_at"
      ]

    },
    "executive_sponsor": {
      "type": "object",
      "description": "Receipted executive sponsorship commitment for an enterprise proof of value.",
      "properties": {
        "executive_sponsor_id": {
          "type": "string",
          "description": "Stable identity of this executive sponsor observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose executive sponsor state was observed."
        },
        "sponsor_commitment": {
          "type": "string",
          "description": "Observed level of sponsor commitment."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["executive_sponsor_id", "account_id", "sponsor_commitment", "evidence_digest", "observed_at"
      ]

    },
    "expansion_opportunity": {
      "type": "object",
      "description": "Qualified account expansion opportunity backed by observed adoption evidence.",
      "properties": {
        "expansion_opportunity_id": {
          "type": "string",
          "description": "Stable identity of this expansion opportunity observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose expansion opportunity state was observed."
        },
        "expansion_value": {
          "type": "number",
          "description": "Qualified expansion value in account currency."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["expansion_opportunity_id", "account_id", "expansion_value", "evidence_digest", "observed_at"
      ]

    },
    "expansion_option": {
      "type": "object",
      "description": "Pre-negotiated expansion right for a SKU and unit ceiling.",
      "properties": {
        "option_id": {
          "type": "string",
          "description": "Stable expansion option."
        },
        "account_id": {
          "type": "string",
          "description": "Eligible enterprise account."
        },
        "sku": {
          "type": "string",
          "description": "Expandable SKU."
        },
        "max_quantity": {
          "type": "integer",
          "description": "Maximum expansion quantity."
        }
      },
      "required": ["option_id", "account_id", "sku", "max_quantity"
      ]

    },
    "expansion_receipt": {
      "type": "object",
      "description": "Final expansion receipt binding adoption evidence to realized expansion value.",
      "properties": {
        "expansion_receipt_id": {
          "type": "string",
          "description": "Stable identity of this expansion receipt observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose expansion receipt state was observed."
        },
        "receipt_value": {
          "type": "number",
          "description": "Verified expansion value named by this receipt."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["expansion_receipt_id", "account_id", "receipt_value", "evidence_digest", "observed_at"
      ]

    },
    "expansion_signal": {
      "type": "object",
      "description": "Observed usage-and-value signal indicating an expansion path.",
      "properties": {
        "expansion_signal_id": {
          "type": "string",
          "description": "Stable identity of this expansion signal observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose expansion signal state was observed."
        },
        "expansion_signal_score": {
          "type": "number",
          "description": "Measured strength of the expansion signal."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["expansion_signal_id", "account_id", "expansion_signal_score", "evidence_digest", "observed_at"
      ]

    },
    "experiment_learning_receipt": {
      "type": "object",
      "description": "Receipts an experiment result with exact subject, evidence, and bounded learning authority.",
      "properties": {
        "receipt_id": {
          "type": "string",
          "description": "Stable experiment-learning receipt identity."
        },
        "experiment_id": {
          "type": "string",
          "description": "Experiment producing the learning."
        },
        "exact_subject_sha": {
          "type": "string",
          "description": "Exact evaluated Git subject."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of experiment evidence."
        },
        "result_digest": {
          "type": "string",
          "description": "Digest of the learned result."
        },
        "authority_ceiling": {
          "type": "string",
          "description": "Boundary excluding consequential DO authority."
        }
      },
      "required": ["receipt_id", "experiment_id", "exact_subject_sha", "evidence_digest", "result_digest", "authority_ceiling"
      ]

    },
    "failed_challenger_retention": {
      "type": "object",
      "description": "Retains failed challenger evidence as reusable learning rather than deleting inconvenient results.",
      "properties": {
        "retention_id": {
          "type": "string",
          "description": "Stable failed-challenger retention identity."
        },
        "challenger_evaluation_id": {
          "type": "string",
          "description": "Failed challenger evaluation retained."
        },
        "failure_evidence_digest": {
          "type": "string",
          "description": "Digest of preserved failure evidence."
        },
        "retained_at": {
          "type": "string",
          "description": "Timestamp evidence entered durable memory."
        },
        "eligible_for_future": {
          "type": "boolean",
          "description": "Whether changed conditions may justify reevaluation."
        }
      },
      "required": ["retention_id", "challenger_evaluation_id", "failure_evidence_digest", "retained_at", "eligible_for_future"
      ]

    },
    "failure_label": {
      "type": "object",
      "description": "Classifies a preserved failure by causal scope so failed challengers remain usable learning evidence.",
      "properties": {
        "failure_id": {
          "type": "string",
          "description": "Stable failure evidence identity."
        },
        "episode_id": {
          "type": "string",
          "description": "Episode in which the failure occurred."
        },
        "failure_class": {
          "type": "string",
          "description": "Typed subject, environment, policy, or authority failure class."
        },
        "causal_scope": {
          "type": "string",
          "description": "Smallest supported causal boundary."
        },
        "preserved_evidence_digest": {
          "type": "string",
          "description": "Digest proving the failed evidence was retained."
        }
      },
      "required": ["failure_id", "episode_id", "failure_class", "causal_scope", "preserved_evidence_digest"
      ]

    },
    "federated_dogfood_learning_crown": {
      "type": "object",
      "description": "Receipts SELECT through CONSTRUCT, DRY-RUN MANUFACTURE, QUALIFY, ownership verification, and second-pass identity for one exact caller-local consumer.",
      "properties": {
        "crown_id": {
          "type": "string",
          "description": "Stable federated dogfood crown identity."
        },
        "exact_subject_sha": {
          "type": "string",
          "description": "Exact beam4pm subject qualified."
        },
        "consumer_id": {
          "type": "string",
          "description": "Caller-local consumer identity."
        },
        "pack_sha": {
          "type": "string",
          "description": "Immutable marketplace pack commit."
        },
        "capsule_digest": {
          "type": "string",
          "description": "Exact ggen-ecosystem container digest."
        },
        "manufacture_receipt_digest": {
          "type": "string",
          "description": "Digest of verified manufacture receipt."
        },
        "ownership_verification_digest": {
          "type": "string",
          "description": "Digest proving generated output ownership."
        },
        "second_pass_identity_digest": {
          "type": "string",
          "description": "Digest proving byte-identical second sync."
        },
        "workflow_sequence": {
          "type": "string",
          "description": "SELECT-CONSTRUCT-DRY_RUN_MANUFACTURE-QUALIFY-RECEIPT sequence."
        },
        "authority_ceiling": {
          "type": "string",
          "description": "Boundary guaranteeing zero unreceipted DO authority."
        }
      },
      "required": ["crown_id", "exact_subject_sha", "consumer_id", "pack_sha", "capsule_digest", "manufacture_receipt_digest", "ownership_verification_digest", "second_pass_identity_digest", "workflow_sequence", "authority_ceiling"
      ]

    },
    "forged_receipt_refusal": {
      "type": "object",
      "description": "Produces typed refusal evidence when a runtime receipt signature or subject binding is forged.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "receipt_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "refusal_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "receipt_id", "refusal_hash"
      ]

    },
    "frontier_benchmark": {
      "type": "object",
      "description": "Executable comparison contract produced from a frontier announcement.",
      "properties": {
        "benchmark_id": {
          "type": "string",
          "description": "Stable benchmark identity."
        },
        "metric": {
          "type": "string",
          "description": "Measured comparison quantity."
        },
        "acceptance_predicate": {
          "type": "string",
          "description": "Predicate that must be observed before the claim can be earned."
        },
        "falsifier": {
          "type": "string",
          "description": "Observation that defeats the benchmark claim."
        }
      },
      "required": ["benchmark_id", "metric", "acceptance_predicate", "falsifier"
      ]

    },
    "frontier_evidence": {
      "type": "object",
      "description": "Exact-subject verifier/replay evidence supporting an earned frontier-response claim.",
      "properties": {
        "subject_identity": {
          "type": "string",
          "description": "Exact implementation subject, normally repository plus commit/digest."
        },
        "verifier_identity": {
          "type": "string",
          "description": "Exact verifier identity."
        },
        "receipt_ref": {
          "type": "string",
          "description": "Execution receipt reference."
        },
        "replay_ref": {
          "type": "string",
          "description": "Replay-verification reference."
        },
        "standing": {
          "type": "string",
          "description": "Bounded standing; publication consumers must require ALIVE for an earned claim."
        }
      },
      "required": ["subject_identity", "verifier_identity", "receipt_ref", "replay_ref", "standing"
      ]

    },
    "frontier_opportunity": {
      "type": "object",
      "description": "DfCM-selected bounded response opportunity derived from an observed release.",
      "properties": {
        "source_digest": {
          "type": "string",
          "description": "Digest linking this opportunity to its observed source release."
        },
        "response_mode": {
          "type": "string",
          "description": "One of reuse, compose, extend, invent, benchmark, formalize, automate, eliminate."
        },
        "target_repository": {
          "type": "string",
          "description": "Repository coordinate selected for implementation; selection confers no DO authority."
        },
        "required_capability": {
          "type": "string",
          "description": "Capability the response must manufacture or prove."
        },
        "benchmark_id": {
          "type": "string",
          "description": "Executable benchmark identity."
        }
      },
      "required": ["source_digest", "response_mode", "target_repository", "required_capability", "benchmark_id"
      ]

    },
    "frontier_source_release": {
      "type": "object",
      "description": "Observed external frontier announcement with provenance and extracted-claim payload; observation only, never authority.",
      "properties": {
        "source_url": {
          "type": "string",
          "description": "Canonical external source URL."
        },
        "publisher": {
          "type": "string",
          "description": "Observed publisher identity."
        },
        "published_at": {
          "type": "string",
          "description": "Observed publication timestamp."
        },
        "content_digest": {
          "type": "string",
          "description": "Exact digest of the admitted source representation."
        },
        "claims": {
          "type": "object",
          "description": "Normalized observed claims; candidate semantic extraction, not execution evidence."
        }
      },
      "required": ["source_url", "publisher", "published_at", "content_digest", "claims"
      ]

    },
    "funding_approval_chain": {
      "type": "object",
      "description": "Requires a replayable funding approval chain rather than relying on a stakeholder's verbal budget claim.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required funding approval chain input; omission is an executable typed refusal, never an inferred approval."
        },
        "approval_chain_id": {
          "type": "string",
          "description": "Required funding approval chain input; omission is an executable typed refusal, never an inferred approval."
        },
        "evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "approval_chain_id", "evidence_hash"
      ]

    },
    "fx_conversion_policy": {
      "type": "object",
      "description": "Evidence-bound foreign-exchange conversion policy.",
      "properties": {
        "fx_conversion_policy_id": {
          "type": "string",
          "description": "Stable identity of this fx conversion policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "rate_source": {
          "type": "string",
          "description": "Immutable identity of the admitted FX rate source."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["fx_conversion_policy_id", "account_id", "rate_source", "evidence_digest", "effective_at"
      ]

    },
    "generated_hypothesis": {
      "type": "object",
      "description": "Generates a testable backlog hypothesis from an admitted capability gap.",
      "properties": {
        "hypothesis_id": {
          "type": "string",
          "description": "Stable generated hypothesis identity."
        },
        "source_gap_id": {
          "type": "string",
          "description": "Capability gap that generated the hypothesis."
        },
        "candidate_capability": {
          "type": "string",
          "description": "Proposed independent capability."
        },
        "expected_value": {
          "type": "number",
          "description": "Bounded expected objective value."
        },
        "falsifier_contract": {
          "type": "string",
          "description": "Executable condition that can disprove the hypothesis."
        }
      },
      "required": ["hypothesis_id", "source_gap_id", "candidate_capability", "expected_value", "falsifier_contract"
      ]

    },
    "generated_output_ownership_observation": {
      "type": "object",
      "description": "Classifies a path as generated or source-owned using its explicit provenance marker.",
      "properties": {
        "output_path": {
          "type": "string",
          "description": "Observed repository path."
        },
        "ownership_marker": {
          "type": "string",
          "description": "Observed provenance marker."
        },
        "source_input_digest": {
          "type": "string",
          "description": "Digest of canonical inputs that own the output."
        },
        "standing": {
          "type": "string",
          "description": "Machine-readable ownership standing."
        }
      },
      "required": ["output_path", "ownership_marker", "source_input_digest", "standing"
      ]

    },
    "generated_source_route": {
      "type": "object",
      "description": "Routes generated-projection defects to canonical semantic source or an independent rail.",
      "properties": {
        "projection_id": {
          "type": "string",
          "description": "Required projection_id for this bounded planner contract."
        },
        "source_coordinate": {
          "type": "string",
          "description": "Required source_coordinate for deterministic planner evaluation."
        },
        "route": {
          "type": "string",
          "description": "Required route preserving evidence and falsifiability."
        }
      },
      "required": ["projection_id", "source_coordinate", "route"
      ]

    },
    "heuristic_arc": {
      "type": "object",
      "description": "One dependency-scored candidate arc considered during heuristic-net discovery.",
      "properties": {
        "source_activity": {
          "type": "string",
          "description": "The candidate arc source activity."
        },
        "target_activity": {
          "type": "string",
          "description": "The candidate arc target activity."
        },
        "dependency_measure": {
          "type": "number",
          "description": "The computed dependency/confidence score for this candidate arc."
        }
      },
      "required": ["source_activity", "target_activity", "dependency_measure"
      ]

    },
    "historical_episode_replay": {
      "type": "object",
      "description": "Replays a historical episode against an exact ancestor and preserves any seeded divergence.",
      "properties": {
        "replay_id": {
          "type": "string",
          "description": "Stable historical replay identity."
        },
        "episode_id": {
          "type": "string",
          "description": "Historical episode being replayed."
        },
        "historical_subject_sha": {
          "type": "string",
          "description": "Exact historical subject commit."
        },
        "replay_seed": {
          "type": "string",
          "description": "Explicit deterministic evaluation seed."
        },
        "divergence_code": {
          "type": "string",
          "description": "Observed divergence or exact-match result."
        }
      },
      "required": ["replay_id", "episode_id", "historical_subject_sha", "replay_seed", "divergence_code"
      ]

    },
    "hypothesis_priority_update": {
      "type": "object",
      "description": "Updates hypothesis priority from realized evidence while retaining the prior ranking.",
      "properties": {
        "update_id": {
          "type": "string",
          "description": "Stable priority update identity."
        },
        "hypothesis_id": {
          "type": "string",
          "description": "Hypothesis being reprioritized."
        },
        "prior_priority": {
          "type": "number",
          "description": "Priority before learning."
        },
        "new_priority": {
          "type": "number",
          "description": "Evidence-updated priority."
        },
        "payoff_evidence_digest": {
          "type": "string",
          "description": "Digest supporting the update."
        }
      },
      "required": ["update_id", "hypothesis_id", "prior_priority", "new_priority", "payoff_evidence_digest"
      ]

    },
    "immutable_pack_selection": {
      "type": "object",
      "description": "Selects a marketplace pack only by exact immutable commit SHA.",
      "properties": {
        "pack_id": {
          "type": "string",
          "description": "Required pack_id for this bounded planner contract."
        },
        "pack_sha": {
          "type": "string",
          "description": "Required pack_sha for deterministic planner evaluation."
        },
        "selection_hash": {
          "type": "string",
          "description": "Required selection_hash preserving evidence and falsifiability."
        }
      },
      "required": ["pack_id", "pack_sha", "selection_hash"
      ]

    },
    "implementation_fee_admission": {
      "type": "object",
      "description": "Qualifies implementation fees separately from recurring subscription value.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required implementation fee admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "fee_id": {
          "type": "string",
          "description": "Required implementation fee admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "fee_id", "decision"
      ]

    },
    "incident_acknowledgement": {
      "type": "object",
      "description": "Records accountable acknowledgement of a production incident for a paid tenant.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "incident_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "acknowledgement_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "incident_id", "acknowledgement_hash"
      ]

    },
    "incident_detection_event": {
      "type": "object",
      "description": "Records the detected production incident and exact affected paid service.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "incident_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "detection_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "incident_id", "detection_hash"
      ]

    },
    "incident_response_evidence": {
      "type": "object",
      "description": "Executable incident evidence binding an exact subject to the response case opened by a real detection.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this incident-response observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "incident_id": {
          "type": "string",
          "description": "Identity of the incident response case observed by the control."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "incident_id", "observed_result"
      ]

    },
    "indemnity_scope_admission": {
      "type": "object",
      "description": "Admits an identified indemnity scope so exceptional legal exposure cannot hide inside a qualified deal.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required indemnity scope admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "indemnity_scope_id": {
          "type": "string",
          "description": "Required indemnity scope admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "indemnity_scope_id", "decision"
      ]

    },
    "information_partition_observation": {
      "type": "object",
      "description": "Partitions a state vector by a decision-relevant information key.",
      "properties": {
        "partition_id": {
          "type": "string",
          "description": "Stable information partition identity."
        },
        "state_vector_id": {
          "type": "string",
          "description": "Source state-vector identity."
        },
        "partition_key": {
          "type": "string",
          "description": "Decision-relevant partition key."
        },
        "information_digest": {
          "type": "string",
          "description": "Digest of partition contents."
        }
      },
      "required": ["partition_id", "state_vector_id", "partition_key", "information_digest"
      ]

    },
    "insurance_requirement": {
      "type": "object",
      "description": "Admits contractual insurance coverage only with an exact coverage identity and supporting evidence.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required insurance requirement input; omission is an executable typed refusal, never an inferred approval."
        },
        "coverage_id": {
          "type": "string",
          "description": "Required insurance requirement input; omission is an executable typed refusal, never an inferred approval."
        },
        "evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "coverage_id", "evidence_hash"
      ]

    },
    "integration_readiness": {
      "type": "object",
      "description": "Executable readiness contract for required enterprise integrations.",
      "properties": {
        "integration_readiness_id": {
          "type": "string",
          "description": "Stable identity of this integration readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose integration readiness state was observed."
        },
        "integration_score": {
          "type": "number",
          "description": "Observed integration-readiness score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["integration_readiness_id", "account_id", "integration_score", "evidence_digest", "observed_at"
      ]

    },
    "integration_scope_admission": {
      "type": "object",
      "description": "Admits a bounded enterprise integration scope before solution fit becomes a commercial commitment.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required integration scope admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "integration_scope_id": {
          "type": "string",
          "description": "Required integration scope admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "integration_scope_id", "decision"
      ]

    },
    "invoice_entity_identity": {
      "type": "object",
      "description": "Requires the entity responsible for invoice acceptance, preventing qualified demand from becoming uncollectable revenue.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required invoice entity identity input; omission is an executable typed refusal, never an inferred approval."
        },
        "invoice_entity_id": {
          "type": "string",
          "description": "Required invoice entity identity input; omission is an executable typed refusal, never an inferred approval."
        },
        "identity_evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "invoice_entity_id", "identity_evidence_hash"
      ]

    },
    "invoice_line_item": {
      "type": "object",
      "description": "Auditable commercial invoice line.",
      "properties": {
        "invoice_line_item_id": {
          "type": "string",
          "description": "Stable identity of this invoice line item contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "line_amount": {
          "type": "number",
          "description": "Observed amount for this exact invoice line."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["invoice_line_item_id", "account_id", "line_amount", "evidence_digest", "effective_at"
      ]

    },
    "invoice_schedule": {
      "type": "object",
      "description": "Deterministic invoicing cadence for a billing account.",
      "properties": {
        "schedule_id": {
          "type": "string",
          "description": "Stable schedule identity."
        },
        "billing_account_id": {
          "type": "string",
          "description": "Bill-to account subject."
        },
        "cadence": {
          "type": "string",
          "description": "Invoice cadence."
        },
        "next_invoice_at": {
          "type": "string",
          "description": "Next scheduled invoice instant."
        }
      },
      "required": ["schedule_id", "billing_account_id", "cadence", "next_invoice_at"
      ]

    },
    "irreversibility_budget": {
      "type": "object",
      "description": "Limits irreversible commitments within one bounded planning episode.",
      "properties": {
        "episode_id": {
          "type": "string",
          "description": "Required episode_id for this bounded planner contract."
        },
        "budget": {
          "type": "string",
          "description": "Required budget for deterministic planner evaluation."
        },
        "consumed": {
          "type": "string",
          "description": "Required consumed preserving evidence and falsifiability."
        }
      },
      "required": ["episode_id", "budget", "consumed"
      ]

    },
    "k8s_object_ref": {
      "type": "object",
      "description": "A reference to one Kubernetes object observed in the runtime topology.",
      "properties": {
        "kind": {
          "type": "string",
          "description": "The Kubernetes object kind (e.g. Pod, Deployment)."
        },
        "name": {
          "type": "string",
          "description": "The object name."
        },
        "namespace": {
          "type": "string",
          "description": "Optional object namespace (absent for cluster-scoped kinds such as Node, PersistentVolume, ClusterRole, or Namespace itself)."
        }
      },
      "required": ["kind", "name"
      ]

    },
    "late_arriving_usage": {
      "type": "object",
      "description": "Admission of usage received after its billing window.",
      "properties": {
        "late_arriving_usage_id": {
          "type": "string",
          "description": "Stable identity of this late arriving usage contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "occurred_at": {
          "type": "string",
          "description": "Original occurrence instant for the delayed usage."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["late_arriving_usage_id", "account_id", "occurred_at", "evidence_digest", "effective_at"
      ]

    },
    "latency_budget_observation": {
      "type": "object",
      "description": "Records an observed runtime latency against the admitted paid-service latency budget.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "workload_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "observation_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "workload_id", "observation_hash"
      ]

    },
    "leakage_finding": {
      "type": "object",
      "description": "One conformance-deviation finding with exact case identity, measured fitness/precision and optional amount at risk.",
      "properties": {
        "case_id": {
          "type": "string",
          "description": "Exact process-case identity that deviated from the admitted reference model."
        },
        "fitness": {
          "type": "number",
          "description": "Observed conformance fitness for this case."
        },
        "precision": {
          "type": "number",
          "description": "Observed conformance precision for this case against the admitted model."
        },
        "amount_at_risk": {
          "type": "number",
          "description": "Optional admitted amount associated with the deviating case; absent evidence remains absent rather than being coerced to zero."
        }
      },
      "required": ["case_id", "fitness", "precision"
      ]

    },
    "learning_episode": {
      "type": "object",
      "description": "Compiles one exact-subject execution episode into bounded learning evidence without granting actuation authority.",
      "properties": {
        "episode_id": {
          "type": "string",
          "description": "Stable learning episode identity."
        },
        "exact_subject_sha": {
          "type": "string",
          "description": "Exact Git subject observed by the episode."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest binding the episode to immutable evidence."
        },
        "outcome": {
          "type": "string",
          "description": "Observed success, refusal, or failure outcome."
        },
        "authority_ceiling": {
          "type": "string",
          "description": "Maximum authority learning may influence; DO authority remains excluded."
        }
      },
      "required": ["episode_id", "exact_subject_sha", "evidence_digest", "outcome", "authority_ceiling"
      ]

    },
    "least_authority_evidence": {
      "type": "object",
      "description": "Executable least-authority evidence binding an exact subject to the permissions actually granted during execution.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this least-authority permission observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "granted_permissions": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Canonical set of permissions observed at the execution boundary."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "granted_permissions", "observed_result"
      ]

    },
    "legal_blocker": {
      "type": "object",
      "description": "Encodes an unresolved legal blocker as a typed refusal with an actionable identity.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required legal blocker input; omission is an executable typed refusal, never an inferred approval."
        },
        "blocker_id": {
          "type": "string",
          "description": "Required legal blocker input; omission is an executable typed refusal, never an inferred approval."
        },
        "refusal_code": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "blocker_id", "refusal_code"
      ]

    },
    "liability_cap_admission": {
      "type": "object",
      "description": "Admits a specific liability-cap position before commercial approval.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required liability cap admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "liability_cap_id": {
          "type": "string",
          "description": "Required liability cap admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "liability_cap_id", "decision"
      ]

    },
    "license_evidence": {
      "type": "object",
      "description": "Executable licensing evidence binding an exact subject to its machine-verified SPDX expression.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this license-expression observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "license_expression": {
          "type": "string",
          "description": "SPDX license expression observed for the commercial subject."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "license_expression", "observed_result"
      ]

    },
    "log_trace": {
      "type": "object",
      "description": "One case-centric trace: an ordered activity sequence for a single case.",
      "properties": {
        "case_id": {
          "type": "string",
          "description": "Identifier of the case this trace belongs to."
        },
        "activity_sequence": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Ordered list of activity names observed for this case."
        }
      },
      "required": ["case_id", "activity_sequence"
      ]

    },
    "machine_actionable_delta": {
      "type": "object",
      "description": "Emits the compressed state change and recommended next autonomic action.",
      "properties": {
        "subject_id": {
          "type": "string",
          "description": "Exact changed subject identity."
        },
        "prior_state_digest": {
          "type": "string",
          "description": "Prior admitted state digest."
        },
        "delta_digest": {
          "type": "string",
          "description": "Digest of machine-actionable delta."
        },
        "recommended_action": {
          "type": "string",
          "description": "Bounded next action for the control plane."
        }
      },
      "required": ["subject_id", "prior_state_digest", "delta_digest", "recommended_action"
      ]

    },
    "manufacture_receipt_presence_observation": {
      "type": "object",
      "description": "Observes whether real manufacture emitted a receipt for the exact subject.",
      "properties": {
        "subject_sha": {
          "type": "string",
          "description": "Exact manufactured subject."
        },
        "receipt_id": {
          "type": "string",
          "description": "Stable manufacture receipt identity."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Immutable receipt content digest."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp receipt was observed."
        }
      },
      "required": ["subject_sha", "receipt_id", "receipt_digest", "observed_at"
      ]

    },
    "manufacture_receipt_validity_observation": {
      "type": "object",
      "description": "Records independent verification of receipt subject, digest, and verifier identity.",
      "properties": {
        "receipt_digest": {
          "type": "string",
          "description": "Immutable receipt digest."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact subject claimed by receipt."
        },
        "verification_status": {
          "type": "string",
          "description": "Machine-readable verification result."
        },
        "verifier_identity": {
          "type": "string",
          "description": "Exact verifier implementation identity."
        }
      },
      "required": ["receipt_digest", "subject_sha", "verification_status", "verifier_identity"
      ]

    },
    "master_service_agreement_binding": {
      "type": "object",
      "description": "Exact MSA binding for an order.",
      "properties": {
        "master_service_agreement_binding_id": {
          "type": "string",
          "description": "Stable identity of this master service agreement binding contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "msa_digest": {
          "type": "string",
          "description": "Content digest of the governing master service agreement."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["master_service_agreement_binding_id", "account_id", "msa_digest", "evidence_digest", "effective_at"
      ]

    },
    "master_service_agreement_state": {
      "type": "object",
      "description": "Tracks the exact master service agreement and its executable admission state rather than treating legal review as a boolean.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required master service agreement state input; omission is an executable typed refusal, never an inferred approval."
        },
        "agreement_id": {
          "type": "string",
          "description": "Required master service agreement state input; omission is an executable typed refusal, never an inferred approval."
        },
        "agreement_state": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "agreement_id", "agreement_state"
      ]

    },
    "mcts_plan_candidate": {
      "type": "object",
      "description": "Represents a seeded Monte Carlo tree-search plan and its rollout evidence.",
      "properties": {
        "plan_id": {
          "type": "string",
          "description": "Required plan_id for this bounded planner contract."
        },
        "seed": {
          "type": "string",
          "description": "Required seed for deterministic planner evaluation."
        },
        "rollout_hash": {
          "type": "string",
          "description": "Required rollout_hash preserving evidence and falsifiability."
        }
      },
      "required": ["plan_id", "seed", "rollout_hash"
      ]

    },
    "meta_router": {
      "type": "object",
      "description": "Selects a planner from a portfolio using observed problem characteristics.",
      "properties": {
        "portfolio_id": {
          "type": "string",
          "description": "Required portfolio_id for this bounded planner contract."
        },
        "observation_hash": {
          "type": "string",
          "description": "Required observation_hash for deterministic planner evaluation."
        },
        "selected_planner_id": {
          "type": "string",
          "description": "Required selected_planner_id preserving evidence and falsifiability."
        }
      },
      "required": ["portfolio_id", "observation_hash", "selected_planner_id"
      ]

    },
    "meter_definition": {
      "type": "object",
      "description": "Immutable billable-meter definition.",
      "properties": {
        "meter_definition_id": {
          "type": "string",
          "description": "Stable identity of this meter definition contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "meter_name": {
          "type": "string",
          "description": "Stable buyer-visible identity of the commercial meter."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["meter_definition_id", "account_id", "meter_name", "evidence_digest", "effective_at"
      ]

    },
    "meter_dimension": {
      "type": "object",
      "description": "Typed dimension attached to a commercial meter.",
      "properties": {
        "meter_dimension_id": {
          "type": "string",
          "description": "Stable identity of this meter dimension contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "dimension_name": {
          "type": "string",
          "description": "Stable dimension identity used for pricing and reporting."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["meter_dimension_id", "account_id", "dimension_name", "evidence_digest", "effective_at"
      ]

    },
    "meter_rollup": {
      "type": "object",
      "description": "Deterministic rollup policy for metered observations.",
      "properties": {
        "meter_rollup_id": {
          "type": "string",
          "description": "Stable identity of this meter rollup contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "rollup_function": {
          "type": "string",
          "description": "Admitted aggregation function identity."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["meter_rollup_id", "account_id", "rollup_function", "evidence_digest", "effective_at"
      ]

    },
    "metered_usage_sample": {
      "type": "object",
      "description": "Records a bounded production usage sample eligible for tenant-level metering.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "usage_sample_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "sample_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "usage_sample_id", "sample_hash"
      ]

    },
    "migration_contract": {
      "type": "object",
      "description": "Versioned migration path with source, target, and rollback identity.",
      "properties": {
        "migration_id": {
          "type": "string",
          "description": "Stable migration identity."
        },
        "from_version": {
          "type": "string",
          "description": "Source product version."
        },
        "to_version": {
          "type": "string",
          "description": "Target product version."
        },
        "rollback_plan": {
          "type": "string",
          "description": "Rollback plan identity."
        }
      },
      "required": ["migration_id", "from_version", "to_version", "rollback_plan"
      ]

    },
    "migration_readiness": {
      "type": "object",
      "description": "Measured migration readiness and bounded effort for enterprise adoption.",
      "properties": {
        "migration_readiness_id": {
          "type": "string",
          "description": "Stable identity of this migration readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose migration readiness state was observed."
        },
        "migration_effort_days": {
          "type": "integer",
          "description": "Estimated engineering days for the admitted migration."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["migration_readiness_id", "account_id", "migration_effort_days", "evidence_digest", "observed_at"
      ]

    },
    "minimum_commitment_schedule": {
      "type": "object",
      "description": "Time-phased minimum-spend commitment.",
      "properties": {
        "minimum_commitment_schedule_id": {
          "type": "string",
          "description": "Stable identity of this minimum commitment schedule contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "committed_amount": {
          "type": "number",
          "description": "Commercially admitted minimum amount for the schedule window."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["minimum_commitment_schedule_id", "account_id", "committed_amount", "evidence_digest", "effective_at"
      ]

    },
    "minimum_term_admission": {
      "type": "object",
      "description": "Admits the minimum paid term as a decision input needed for durable contract value.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required minimum term admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "minimum_term": {
          "type": "string",
          "description": "Required minimum term admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "minimum_term", "decision"
      ]

    },
    "multiarch_evidence": {
      "type": "object",
      "description": "Executable architecture evidence binding an exact subject to the platform set actually present in its OCI index.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this multi-architecture observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "platforms": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Canonical set of platforms observed in the admitted OCI index."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "platforms", "observed_result"
      ]

    },
    "mutable_identity_refusal_evidence": {
      "type": "object",
      "description": "Executable refusal evidence recording the mutable artifact reference rejected by procurement policy.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this mutable-identity refusal observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "mutable_reference": {
          "type": "string",
          "description": "Mutable reference observed and refused before artifact admission."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "mutable_reference", "observed_result"
      ]

    },
    "mutual_information_score": {
      "type": "object",
      "description": "Ranks observations by expected mutual information with decision-relevant state.",
      "properties": {
        "observation_id": {
          "type": "string",
          "description": "Required observation_id for this bounded planner contract."
        },
        "target_state_id": {
          "type": "string",
          "description": "Required target_state_id for deterministic planner evaluation."
        },
        "score": {
          "type": "string",
          "description": "Required score preserving evidence and falsifiability."
        }
      },
      "required": ["observation_id", "target_state_id", "score"
      ]

    },
    "negative_fixture_generation": {
      "type": "object",
      "description": "Generates a falsifying fixture from a preserved failure and binds the expected refusal.",
      "properties": {
        "fixture_id": {
          "type": "string",
          "description": "Stable generated fixture identity."
        },
        "source_failure_id": {
          "type": "string",
          "description": "Preserved failure used as source."
        },
        "invariant_id": {
          "type": "string",
          "description": "Invariant challenged by the fixture."
        },
        "generated_input_digest": {
          "type": "string",
          "description": "Digest of deterministic negative input."
        },
        "expected_refusal_code": {
          "type": "string",
          "description": "Exact refusal required for success."
        }
      },
      "required": ["fixture_id", "source_failure_id", "invariant_id", "generated_input_digest", "expected_refusal_code"
      ]

    },
    "next_lawful_actuation": {
      "type": "object",
      "description": "Selects the next bounded action from scored admissible options without human micro-scheduling.",
      "properties": {
        "episode_id": {
          "type": "string",
          "description": "Required episode_id for this bounded planner contract."
        },
        "selected_action_id": {
          "type": "string",
          "description": "Required selected_action_id for deterministic planner evaluation."
        },
        "selection_receipt_hash": {
          "type": "string",
          "description": "Required selection_receipt_hash preserving evidence and falsifiability."
        }
      },
      "required": ["episode_id", "selected_action_id", "selection_receipt_hash"
      ]

    },
    "no_authority_learning_guard": {
      "type": "object",
      "description": "Refuses any learned update that would expand consequential authority.",
      "properties": {
        "guard_id": {
          "type": "string",
          "description": "Stable no-authority guard identity."
        },
        "candidate_update_id": {
          "type": "string",
          "description": "Learned update under review."
        },
        "current_authority": {
          "type": "string",
          "description": "Existing admitted authority."
        },
        "requested_authority": {
          "type": "string",
          "description": "Authority implied by the update."
        },
        "decision": {
          "type": "string",
          "description": "ADMIT or REFUSE guard decision."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Evidence supporting the authority comparison."
        }
      },
      "required": ["guard_id", "candidate_update_id", "current_authority", "requested_authority", "decision", "evidence_digest"
      ]

    },
    "node_failover_event": {
      "type": "object",
      "description": "Records the exact node failure and observed failover consequence for a paid service.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "node_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "failover_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "node_id", "failover_hash"
      ]

    },
    "nonproduction_discount_policy": {
      "type": "object",
      "description": "Explicit discount for nonproduction capacity.",
      "properties": {
        "nonproduction_discount_policy_id": {
          "type": "string",
          "description": "Stable identity of this nonproduction discount policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "discount_percent": {
          "type": "number",
          "description": "Approved nonproduction discount percentage."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["nonproduction_discount_policy_id", "account_id", "discount_percent", "evidence_digest", "effective_at"
      ]

    },
    "normalized_event_observation": {
      "type": "object",
      "description": "Normalizes a source event into stable identity, type, and event time.",
      "properties": {
        "source_system": {
          "type": "string",
          "description": "Originating system identity."
        },
        "event_id": {
          "type": "string",
          "description": "Stable source event identity."
        },
        "event_type": {
          "type": "string",
          "description": "Normalized event type."
        },
        "event_time": {
          "type": "string",
          "description": "Normalized event timestamp."
        }
      },
      "required": ["source_system", "event_id", "event_type", "event_time"
      ]

    },
    "novelty_reward": {
      "type": "object",
      "description": "Rewards only measured distance from prior admitted hypotheses to resist repetitive backlog generation.",
      "properties": {
        "reward_id": {
          "type": "string",
          "description": "Stable novelty reward identity."
        },
        "hypothesis_id": {
          "type": "string",
          "description": "Candidate hypothesis identity."
        },
        "feature_vector_digest": {
          "type": "string",
          "description": "Digest of semantic features."
        },
        "nearest_prior_distance": {
          "type": "number",
          "description": "Distance to nearest admitted prior hypothesis."
        },
        "reward": {
          "type": "number",
          "description": "Bounded novelty reward."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Evidence supporting the distance calculation."
        }
      },
      "required": ["reward_id", "hypothesis_id", "feature_vector_digest", "nearest_prior_distance", "reward", "evidence_digest"
      ]

    },
    "novelty_score": {
      "type": "object",
      "description": "Rewards options that expand the lawful reachable capability frontier.",
      "properties": {
        "option_id": {
          "type": "string",
          "description": "Required option_id for this bounded planner contract."
        },
        "reference_set_hash": {
          "type": "string",
          "description": "Required reference_set_hash for deterministic planner evaluation."
        },
        "score": {
          "type": "string",
          "description": "Required score preserving evidence and falsifiability."
        }
      },
      "required": ["option_id", "reference_set_hash", "score"
      ]

    },
    "object_attribute_change": {
      "type": "object",
      "description": "One recorded change to a time-indexed object attribute.",
      "properties": {
        "object_id": {
          "type": "string",
          "description": "Identifier of the object whose attribute changed."
        },
        "attribute_name": {
          "type": "string",
          "description": "The changed attribute name."
        },
        "old_value": {
          "type": "string",
          "description": "Optional previous value (absent if this is the first recorded value)."
        },
        "new_value": {
          "type": "string",
          "description": "The new value after the change."
        },
        "changed_at": {
          "type": "string",
          "description": "ISO8601 timestamp the change took effect."
        }
      },
      "required": ["object_id", "attribute_name", "new_value", "changed_at"
      ]

    },
    "object_type": {
      "type": "object",
      "description": "A declared OCEL object type and its attribute schema.",
      "properties": {
        "type_name": {
          "type": "string",
          "description": "The object type name."
        },
        "attribute_names": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Optional declared attribute names for objects of this type (name-only; per-attribute value types are not yet modeled)."
        }
      },
      "required": ["type_name"
      ]

    },
    "object_volume_pricing_policy": {
      "type": "object",
      "description": "Object-volume commercial packaging dimension.",
      "properties": {
        "object_volume_pricing_policy_id": {
          "type": "string",
          "description": "Stable identity of this object volume pricing policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "unit_object_price": {
          "type": "number",
          "description": "Admitted unit price per object billing unit."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["object_volume_pricing_policy_id", "account_id", "unit_object_price", "evidence_digest", "effective_at"
      ]

    },
    "objection": {
      "type": "object",
      "description": "Typed enterprise objection linked to an exact account and evidence.",
      "properties": {
        "objection_id": {
          "type": "string",
          "description": "Stable identity of this objection observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose objection state was observed."
        },
        "objection_type": {
          "type": "string",
          "description": "Category of the observed buying objection."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["objection_id", "account_id", "objection_type", "evidence_digest", "observed_at"
      ]

    },
    "objection_resolution": {
      "type": "object",
      "description": "Receipted resolution of an enterprise buying objection.",
      "properties": {
        "objection_resolution_id": {
          "type": "string",
          "description": "Stable identity of this objection resolution observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose objection resolution state was observed."
        },
        "resolution_status": {
          "type": "string",
          "description": "Observed resolution status."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["objection_resolution_id", "account_id", "resolution_status", "evidence_digest", "observed_at"
      ]

    },
    "observation_deduplication_decision": {
      "type": "object",
      "description": "Records an executable accept-or-duplicate decision for an observation digest.",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "Observed event identity."
        },
        "event_digest": {
          "type": "string",
          "description": "Canonical event digest."
        },
        "dedup_key": {
          "type": "string",
          "description": "Deterministic deduplication key."
        },
        "decision": {
          "type": "string",
          "description": "Machine-readable deduplication decision."
        }
      },
      "required": ["event_id", "event_digest", "dedup_key", "decision"
      ]

    },
    "observation_entropy_estimate": {
      "type": "object",
      "description": "Records entropy of a state vector using an explicit estimation method.",
      "properties": {
        "state_vector_id": {
          "type": "string",
          "description": "Measured state-vector identity."
        },
        "entropy_method": {
          "type": "string",
          "description": "Named entropy estimation method."
        },
        "entropy_value": {
          "type": "number",
          "description": "Observed entropy estimate."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of entropy estimation."
        }
      },
      "required": ["state_vector_id", "entropy_method", "entropy_value", "observed_at"
      ]

    },
    "observation_freshness_assessment": {
      "type": "object",
      "description": "Evaluates an observation against an explicit freshness deadline.",
      "properties": {
        "observation_id": {
          "type": "string",
          "description": "Stable observation identity."
        },
        "observed_at": {
          "type": "string",
          "description": "Time observation was produced."
        },
        "freshness_deadline": {
          "type": "string",
          "description": "Latest acceptable observation time."
        },
        "freshness_status": {
          "type": "string",
          "description": "Machine-readable freshness standing."
        }
      },
      "required": ["observation_id", "observed_at", "freshness_deadline", "freshness_status"
      ]

    },
    "observation_projection_update": {
      "type": "object",
      "description": "Updates future observation shape from an admitted gap while preserving an explicit authority ceiling.",
      "properties": {
        "update_id": {
          "type": "string",
          "description": "Stable observation update identity."
        },
        "projection_id": {
          "type": "string",
          "description": "Projection being adapted."
        },
        "prior_digest": {
          "type": "string",
          "description": "Digest of the prior projection."
        },
        "new_digest": {
          "type": "string",
          "description": "Digest of the candidate projection."
        },
        "triggering_gap_id": {
          "type": "string",
          "description": "Admitted capability gap causing the update."
        },
        "authority_ceiling": {
          "type": "string",
          "description": "Boundary excluding consequential actuation authority."
        }
      },
      "required": ["update_id", "projection_id", "prior_digest", "new_digest", "triggering_gap_id", "authority_ceiling"
      ]

    },
    "observation_staleness_invalidation": {
      "type": "object",
      "description": "Invalidates stale state and declares whether replacement evidence is required.",
      "properties": {
        "observation_id": {
          "type": "string",
          "description": "Stale observation identity."
        },
        "invalidated_at": {
          "type": "string",
          "description": "Time invalidation became effective."
        },
        "staleness_reason": {
          "type": "string",
          "description": "Typed reason for invalidation."
        },
        "replacement_required": {
          "type": "boolean",
          "description": "Whether new evidence must be acquired."
        }
      },
      "required": ["observation_id", "invalidated_at", "staleness_reason", "replacement_required"
      ]

    },
    "oc_declare_constraint": {
      "type": "object",
      "description": "One declarative object-centric behavioral constraint between two activities.",
      "properties": {
        "constraint_id": {
          "type": "string",
          "description": "Unique constraint identifier."
        },
        "source_activity": {
          "type": "string",
          "description": "The constraint source activity."
        },
        "target_activity": {
          "type": "string",
          "description": "The constraint target activity."
        },
        "constraint_type": {
          "type": "string",
          "description": "One of: response | precedence | succession | non_coexistence (standard DECLARE constraint template names)."
        }
      },
      "required": ["constraint_id", "source_activity", "target_activity", "constraint_type"
      ]

    },
    "ocel_attribute": {
      "type": "object",
      "description": "One named, timestamped attribute value on an OCEL event or object.",
      "properties": {
        "attribute_name": {
          "type": "string",
          "description": "The attribute name."
        },
        "attribute_value": {
          "type": "string",
          "description": "The attribute value, serialized as a string."
        },
        "recorded_at": {
          "type": "string",
          "description": "Optional ISO8601 timestamp this value was recorded/became effective."
        }
      },
      "required": ["attribute_name", "attribute_value"
      ]

    },
    "ocel_event": {
      "type": "object",
      "description": "An OCEL 2.0 event: one recorded occurrence in an object-centric log.",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "Unique event identifier."
        },
        "event_type": {
          "type": "string",
          "description": "The event's type/activity name."
        },
        "event_time": {
          "type": "string",
          "description": "ISO8601 timestamp the event occurred."
        },
        "attributes": {
          "type": "object",
          "description": "Arbitrary named event attributes."
        }
      },
      "required": ["event_id", "event_type", "event_time"
      ]

    },
    "ocel_object": {
      "type": "object",
      "description": "An OCEL 2.0 object: one entity participating in the process.",
      "properties": {
        "object_id": {
          "type": "string",
          "description": "Unique object identifier."
        },
        "object_type": {
          "type": "string",
          "description": "The object's type."
        },
        "attributes": {
          "type": "object",
          "description": "Arbitrary named current attribute values (a flat snapshot, not the time-indexed history -- see bpm:ocel_attribute and bpm:object_attribute_change for per-timestamp values)."
        }
      },
      "required": ["object_id", "object_type"
      ]

    },
    "ocel_relationship": {
      "type": "object",
      "description": "A qualified relationship linking an event or object to an object.",
      "properties": {
        "qualifier": {
          "type": "string",
          "description": "The relationship qualifier/role name."
        },
        "object_id": {
          "type": "string",
          "description": "The related object's identifier."
        }
      },
      "required": ["qualifier", "object_id"
      ]

    },
    "oci_manifest_evidence": {
      "type": "object",
      "description": "Executable OCI evidence binding an exact subject to its verified manifest-list identity.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this OCI-manifest observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "manifest_digest": {
          "type": "string",
          "description": "Digest of the OCI manifest or index verified for the subject."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "manifest_digest", "observed_result"
      ]

    },
    "offline_bundle_evidence": {
      "type": "object",
      "description": "Executable private-deployment evidence binding an exact subject to a verified offline installation bundle.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this offline-bundle observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "bundle_digest": {
          "type": "string",
          "description": "Digest of the offline bundle admitted by the verifier."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "bundle_digest", "observed_result"
      ]

    },
    "operator_readiness": {
      "type": "object",
      "description": "Measured operations-team readiness for governed production use.",
      "properties": {
        "operator_readiness_id": {
          "type": "string",
          "description": "Stable identity of this operator readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose operator readiness state was observed."
        },
        "operator_readiness_score": {
          "type": "number",
          "description": "Observed operator-readiness score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["operator_readiness_id", "account_id", "operator_readiness_score", "evidence_digest", "observed_at"
      ]

    },
    "opportunity_currency_contract": {
      "type": "object",
      "description": "Admits opportunity value only with an explicit deal currency and immutable foreign-exchange basis, making forecast value comparable and replayable.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required opportunity currency contract input; omission is an executable typed refusal, never an inferred approval."
        },
        "currency_code": {
          "type": "string",
          "description": "Required opportunity currency contract input; omission is an executable typed refusal, never an inferred approval."
        },
        "fx_basis_id": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "currency_code", "fx_basis_id"
      ]

    },
    "opportunity_value_range": {
      "type": "object",
      "description": "Represents commercial value as an evidenced range rather than false point precision, enabling downside-aware qualification.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required opportunity value range input; omission is an executable typed refusal, never an inferred approval."
        },
        "minimum_value": {
          "type": "string",
          "description": "Required opportunity value range input; omission is an executable typed refusal, never an inferred approval."
        },
        "maximum_value": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "minimum_value", "maximum_value"
      ]

    },
    "optimization_plan_candidate": {
      "type": "object",
      "description": "Represents an optimization-derived plan with objective and solver receipt.",
      "properties": {
        "plan_id": {
          "type": "string",
          "description": "Required plan_id for this bounded planner contract."
        },
        "objective_id": {
          "type": "string",
          "description": "Required objective_id for deterministic planner evaluation."
        },
        "solver_receipt_hash": {
          "type": "string",
          "description": "Required solver_receipt_hash preserving evidence and falsifiability."
        }
      },
      "required": ["plan_id", "objective_id", "solver_receipt_hash"
      ]

    },
    "option_generation": {
      "type": "object",
      "description": "Generates a reversible DfCM option set without premature selection.",
      "properties": {
        "state_id": {
          "type": "string",
          "description": "Required state_id for this bounded planner contract."
        },
        "generator_id": {
          "type": "string",
          "description": "Required generator_id for deterministic planner evaluation."
        },
        "option_set_hash": {
          "type": "string",
          "description": "Required option_set_hash preserving evidence and falsifiability."
        }
      },
      "required": ["state_id", "generator_id", "option_set_hash"
      ]

    },
    "order_form_admission": {
      "type": "object",
      "description": "Admits the exact order form that expresses the buyer's priced scope and authorized terms.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required order form admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "order_form_id": {
          "type": "string",
          "description": "Required order form admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "order_form_id", "decision"
      ]

    },
    "order_form_version": {
      "type": "object",
      "description": "Immutable commercial order-form version.",
      "properties": {
        "order_form_version_id": {
          "type": "string",
          "description": "Stable identity of this order form version contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "order_form_digest": {
          "type": "string",
          "description": "Content digest of the accepted order form."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["order_form_version_id", "account_id", "order_form_digest", "evidence_digest", "effective_at"
      ]

    },
    "orthogonality_reward": {
      "type": "object",
      "description": "Scores independent semantic coverage against the full comparison set.",
      "properties": {
        "reward_id": {
          "type": "string",
          "description": "Stable orthogonality reward identity."
        },
        "candidate_id": {
          "type": "string",
          "description": "Candidate capability identity."
        },
        "comparison_set_digest": {
          "type": "string",
          "description": "Digest of capabilities compared."
        },
        "minimum_semantic_distance": {
          "type": "number",
          "description": "Minimum distance from any prior capability."
        },
        "reward": {
          "type": "number",
          "description": "Bounded orthogonality reward."
        },
        "independence_evidence_digest": {
          "type": "string",
          "description": "Digest proving distinct execution semantics."
        }
      },
      "required": ["reward_id", "candidate_id", "comparison_set_digest", "minimum_semantic_distance", "reward", "independence_evidence_digest"
      ]

    },
    "orthogonality_score": {
      "type": "object",
      "description": "Measures semantic independence between candidate work items.",
      "properties": {
        "left_option_id": {
          "type": "string",
          "description": "Required left_option_id for this bounded planner contract."
        },
        "right_option_id": {
          "type": "string",
          "description": "Required right_option_id for deterministic planner evaluation."
        },
        "score": {
          "type": "string",
          "description": "Required score preserving evidence and falsifiability."
        }
      },
      "required": ["left_option_id", "right_option_id", "score"
      ]

    },
    "outcome_label": {
      "type": "object",
      "description": "Assigns an evidence-backed outcome class without allowing self-reported success to confirm itself.",
      "properties": {
        "label_id": {
          "type": "string",
          "description": "Stable outcome label identity."
        },
        "episode_id": {
          "type": "string",
          "description": "Exact episode being labeled."
        },
        "outcome_class": {
          "type": "string",
          "description": "Observed outcome class."
        },
        "confidence_basis": {
          "type": "string",
          "description": "Independent basis for label confidence."
        },
        "independent_evidence_digest": {
          "type": "string",
          "description": "Digest of evidence outside the candidate policy claim."
        }
      },
      "required": ["label_id", "episode_id", "outcome_class", "confidence_basis", "independent_evidence_digest"
      ]

    },
    "output_ownership_gate": {
      "type": "object",
      "description": "Refuses manufacture promotion until every output path has admitted ownership.",
      "properties": {
        "subject_id": {
          "type": "string",
          "description": "Required subject_id for this bounded planner contract."
        },
        "ownership_manifest_hash": {
          "type": "string",
          "description": "Required ownership_manifest_hash for deterministic planner evaluation."
        },
        "standing": {
          "type": "string",
          "description": "Required standing preserving evidence and falsifiability."
        }
      },
      "required": ["subject_id", "ownership_manifest_hash", "standing"
      ]

    },
    "overage_invoice": {
      "type": "object",
      "description": "Invoice consequence for measured overage.",
      "properties": {
        "overage_invoice_id": {
          "type": "string",
          "description": "Stable identity of this overage invoice contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "overage_amount": {
          "type": "number",
          "description": "Observed overage amount after admitted aggregation."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["overage_invoice_id", "account_id", "overage_amount", "evidence_digest", "effective_at"
      ]

    },
    "overage_policy": {
      "type": "object",
      "description": "Explicit overage treatment for a quota boundary.",
      "properties": {
        "policy_id": {
          "type": "string",
          "description": "Stable policy identity."
        },
        "quota_id": {
          "type": "string",
          "description": "Governed quota."
        },
        "unit_price": {
          "type": "number",
          "description": "Price per overage unit."
        },
        "behavior": {
          "type": "string",
          "description": "Overage enforcement behavior."
        }
      },
      "required": ["policy_id", "quota_id", "unit_price", "behavior"
      ]

    },
    "package_release_observation": {
      "type": "object",
      "description": "Observes a package version together with its immutable distribution digest.",
      "properties": {
        "package_id": {
          "type": "string",
          "description": "Stable package identity."
        },
        "version": {
          "type": "string",
          "description": "Observed published version."
        },
        "immutable_digest": {
          "type": "string",
          "description": "Immutable package content digest."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of package observation."
        }
      },
      "required": ["package_id", "version", "immutable_digest", "observed_at"
      ]

    },
    "paid_workload_outcome_receipt": {
      "type": "object",
      "description": "Produces the terminal receipt proving a paid workload delivered its admitted business outcome.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "workload_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "outcome_receipt_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "workload_id", "outcome_receipt_hash"
      ]

    },
    "pareto_filter": {
      "type": "object",
      "description": "Removes strictly dominated options while preserving incomparable alternatives.",
      "properties": {
        "option_set_id": {
          "type": "string",
          "description": "Required option_set_id for this bounded planner contract."
        },
        "objective_set_hash": {
          "type": "string",
          "description": "Required objective_set_hash for deterministic planner evaluation."
        },
        "pareto_set_hash": {
          "type": "string",
          "description": "Required pareto_set_hash preserving evidence and falsifiability."
        }
      },
      "required": ["option_set_id", "objective_set_hash", "pareto_set_hash"
      ]

    },
    "path_schema": {
      "type": "object",
      "description": "One reusable, scored connection pattern between two OCEL types.",
      "properties": {
        "schema_id": {
          "type": "string",
          "description": "Unique path schema identifier."
        },
        "source_type": {
          "type": "string",
          "description": "The source event/object type name."
        },
        "target_type": {
          "type": "string",
          "description": "The target event/object type name."
        },
        "support": {
          "type": "integer",
          "description": "Observed instance count supporting this schema."
        }
      },
      "required": ["schema_id", "source_type", "target_type", "support"
      ]

    },
    "path_schema_query": {
      "type": "object",
      "description": "A discovery query bounding source/target types and maximum schema length.",
      "properties": {
        "source_type": {
          "type": "string",
          "description": "The query source type name."
        },
        "target_type": {
          "type": "string",
          "description": "The query target type name."
        },
        "max_length": {
          "type": "integer",
          "description": "Maximum number of hops to search."
        }
      },
      "required": ["source_type", "target_type", "max_length"
      ]

    },
    "payment_terms": {
      "type": "object",
      "description": "Contracted payment window and late-policy identity.",
      "properties": {
        "terms_id": {
          "type": "string",
          "description": "Stable payment terms identity."
        },
        "net_days": {
          "type": "integer",
          "description": "Days from invoice to due date."
        },
        "late_policy": {
          "type": "string",
          "description": "Late-payment policy identity."
        },
        "status": {
          "type": "string",
          "description": "Terms lifecycle standing."
        }
      },
      "required": ["terms_id", "net_days", "late_policy", "status"
      ]

    },
    "payment_terms_admission": {
      "type": "object",
      "description": "Admits payment terms only when authorized evidence supports the cash-conversion assumption.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required payment terms admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "payment_terms": {
          "type": "string",
          "description": "Required payment terms admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "authority_evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "payment_terms", "authority_evidence_hash"
      ]

    },
    "performance_slo_evidence": {
      "type": "object",
      "description": "Executable performance evidence binding an exact subject to the percentile latency actually measured.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this performance-SLO observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "latency_p95_ms": {
          "type": "integer",
          "description": "Observed p95 end-to-end latency in milliseconds for the admitted subject."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "latency_p95_ms", "observed_result"
      ]

    },
    "petri_arc": {
      "type": "object",
      "description": "A weighted arc connecting a place and a transition (either direction).",
      "properties": {
        "source_id": {
          "type": "string",
          "description": "Identifier of the arc's source node."
        },
        "target_id": {
          "type": "string",
          "description": "Identifier of the arc's target node."
        },
        "weight": {
          "type": "integer",
          "description": "Arc weight (tokens consumed/produced per firing)."
        }
      },
      "required": ["source_id", "target_id", "weight"
      ]

    },
    "petri_place": {
      "type": "object",
      "description": "A place in a Petri net, holding a non-negative token count.",
      "properties": {
        "place_id": {
          "type": "string",
          "description": "Unique place identifier."
        },
        "tokens": {
          "type": "integer",
          "description": "Current token count (marking) at this place."
        }
      },
      "required": ["place_id", "tokens"
      ]

    },
    "petri_transition": {
      "type": "object",
      "description": "A transition in a Petri net.",
      "properties": {
        "transition_id": {
          "type": "string",
          "description": "Unique transition identifier."
        },
        "label": {
          "type": "string",
          "description": "Optional human-readable/activity label (silent transition if absent)."
        }
      },
      "required": ["transition_id"
      ]

    },
    "plan_lineage": {
      "type": "object",
      "description": "Tracks derivation, repair, and supersession across plan generations.",
      "properties": {
        "plan_id": {
          "type": "string",
          "description": "Required plan_id for this bounded planner contract."
        },
        "parent_plan_id": {
          "type": "string",
          "description": "Required parent_plan_id for deterministic planner evaluation."
        },
        "lineage_hash": {
          "type": "string",
          "description": "Required lineage_hash preserving evidence and falsifiability."
        }
      },
      "required": ["plan_id", "parent_plan_id", "lineage_hash"
      ]

    },
    "plan_memory": {
      "type": "object",
      "description": "Stores reusable plan evidence without converting historical success into current authority.",
      "properties": {
        "plan_id": {
          "type": "string",
          "description": "Required plan_id for this bounded planner contract."
        },
        "evidence_hash": {
          "type": "string",
          "description": "Required evidence_hash for deterministic planner evaluation."
        },
        "memory_hash": {
          "type": "string",
          "description": "Required memory_hash preserving evidence and falsifiability."
        }
      },
      "required": ["plan_id", "evidence_hash", "memory_hash"
      ]

    },
    "planner_capability_profile": {
      "type": "object",
      "description": "Declares the problem features and guarantees supported by one planner.",
      "properties": {
        "planner_id": {
          "type": "string",
          "description": "Required planner_id for this bounded planner contract."
        },
        "capability_set": {
          "type": "string",
          "description": "Required capability_set for deterministic planner evaluation."
        },
        "profile_hash": {
          "type": "string",
          "description": "Required profile_hash preserving evidence and falsifiability."
        }
      },
      "required": ["planner_id", "capability_set", "profile_hash"
      ]

    },
    "planner_identity": {
      "type": "object",
      "description": "Separates planner identity from policy, role, agent, and authority identities.",
      "properties": {
        "planner_id": {
          "type": "string",
          "description": "Required planner_id for this bounded planner contract."
        },
        "planner_kind": {
          "type": "string",
          "description": "Required planner_kind for deterministic planner evaluation."
        },
        "identity_hash": {
          "type": "string",
          "description": "Required identity_hash preserving evidence and falsifiability."
        }
      },
      "required": ["planner_id", "planner_kind", "identity_hash"
      ]

    },
    "planner_payoff_observation": {
      "type": "object",
      "description": "Records objective-specific planner payoff from independent episode evidence.",
      "properties": {
        "observation_id": {
          "type": "string",
          "description": "Stable planner payoff observation identity."
        },
        "planner_id": {
          "type": "string",
          "description": "Planner whose realized payoff is measured."
        },
        "objective_id": {
          "type": "string",
          "description": "Objective used for evaluation."
        },
        "payoff": {
          "type": "number",
          "description": "Observed numeric payoff."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Independent evidence supporting the payoff."
        },
        "episode_id": {
          "type": "string",
          "description": "Episode that realized the payoff."
        }
      },
      "required": ["observation_id", "planner_id", "objective_id", "payoff", "evidence_digest", "episode_id"
      ]

    },
    "planner_policy_comparison": {
      "type": "object",
      "description": "Compares incumbent and challenger policies under one planner and one admitted objective.",
      "properties": {
        "comparison_id": {
          "type": "string",
          "description": "Stable comparison identity."
        },
        "planner_id": {
          "type": "string",
          "description": "Planner held constant for comparison."
        },
        "incumbent_policy_id": {
          "type": "string",
          "description": "Incumbent policy identity."
        },
        "challenger_policy_id": {
          "type": "string",
          "description": "Challenger policy identity."
        },
        "payoff_delta": {
          "type": "number",
          "description": "Challenger minus incumbent payoff."
        },
        "winner_evidence_digest": {
          "type": "string",
          "description": "Digest proving the selected winner."
        }
      },
      "required": ["comparison_id", "planner_id", "incumbent_policy_id", "challenger_policy_id", "payoff_delta", "winner_evidence_digest"
      ]

    },
    "planner_portfolio": {
      "type": "object",
      "description": "Preserves a diverse set of planners for one bounded planning episode.",
      "properties": {
        "portfolio_id": {
          "type": "string",
          "description": "Required portfolio_id for this bounded planner contract."
        },
        "planner_ids": {
          "type": "string",
          "description": "Required planner_ids for deterministic planner evaluation."
        },
        "diversity_hash": {
          "type": "string",
          "description": "Required diversity_hash preserving evidence and falsifiability."
        }
      },
      "required": ["portfolio_id", "planner_ids", "diversity_hash"
      ]

    },
    "planner_routing_update": {
      "type": "object",
      "description": "Routes future planning to a better-supported planner with a declared rollback policy.",
      "properties": {
        "update_id": {
          "type": "string",
          "description": "Stable planner-routing update identity."
        },
        "route_id": {
          "type": "string",
          "description": "Planning route being adapted."
        },
        "prior_planner_id": {
          "type": "string",
          "description": "Previously selected planner."
        },
        "selected_planner_id": {
          "type": "string",
          "description": "Evidence-selected future planner."
        },
        "payoff_basis_digest": {
          "type": "string",
          "description": "Digest of comparative payoff evidence."
        },
        "rollback_policy_id": {
          "type": "string",
          "description": "Policy restoring the prior route on regression."
        }
      },
      "required": ["update_id", "route_id", "prior_planner_id", "selected_planner_id", "payoff_basis_digest", "rollback_policy_id"
      ]

    },
    "planning_action": {
      "type": "object",
      "description": "One PDDL-style planning action with its preconditions and effects.",
      "properties": {
        "action_name": {
          "type": "string",
          "description": "The action name."
        },
        "preconditions": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Optional list of precondition fact names."
        },
        "effects": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Optional list of effect fact names this action produces."
        }
      },
      "required": ["action_name"
      ]

    },
    "planning_state": {
      "type": "object",
      "description": "One planning-search state as a bounded set of true facts.",
      "properties": {
        "state_id": {
          "type": "string",
          "description": "Unique state identifier."
        },
        "facts": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of fact names true in this state."
        }
      },
      "required": ["state_id", "facts"
      ]

    },
    "poc_exit_criteria": {
      "type": "object",
      "description": "Measured POC exit-criteria result that gates production readiness.",
      "properties": {
        "poc_exit_criteria_id": {
          "type": "string",
          "description": "Stable identity of this poc exit criteria observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose poc exit criteria state was observed."
        },
        "criteria_pass_rate": {
          "type": "number",
          "description": "Fraction of customer-approved exit criteria satisfied."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["poc_exit_criteria_id", "account_id", "criteria_pass_rate", "evidence_digest", "observed_at"
      ]

    },
    "poc_risk": {
      "type": "object",
      "description": "Evidence-backed POC delivery risk visible to the solution architect.",
      "properties": {
        "poc_risk_id": {
          "type": "string",
          "description": "Stable identity of this poc risk observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose poc risk state was observed."
        },
        "risk_score": {
          "type": "number",
          "description": "Observed delivery-risk score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["poc_risk_id", "account_id", "risk_score", "evidence_digest", "observed_at"
      ]

    },
    "poc_scope": {
      "type": "object",
      "description": "Bounded proof-of-concept scope with an explicit number of customer use cases.",
      "properties": {
        "poc_scope_id": {
          "type": "string",
          "description": "Stable identity of this poc scope observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose poc scope state was observed."
        },
        "use_case_count": {
          "type": "integer",
          "description": "Count of admitted use cases in the POC."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["poc_scope_id", "account_id", "use_case_count", "evidence_digest", "observed_at"
      ]

    },
    "poc_timeline": {
      "type": "object",
      "description": "Measured proof-of-concept timeline tied to verified customer value.",
      "properties": {
        "poc_timeline_id": {
          "type": "string",
          "description": "Stable identity of this poc timeline observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose poc timeline state was observed."
        },
        "days_to_value": {
          "type": "integer",
          "description": "Calendar days from admitted POC start to verified value."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["poc_timeline_id", "account_id", "days_to_value", "evidence_digest", "observed_at"
      ]

    },
    "policy_binding": {
      "type": "object",
      "description": "Binds one planner to parameters, objective, observations, and action projection.",
      "properties": {
        "policy_id": {
          "type": "string",
          "description": "Required policy_id for this bounded planner contract."
        },
        "planner_id": {
          "type": "string",
          "description": "Required planner_id for deterministic planner evaluation."
        },
        "policy_hash": {
          "type": "string",
          "description": "Required policy_hash preserving evidence and falsifiability."
        }
      },
      "required": ["policy_id", "planner_id", "policy_hash"
      ]

    },
    "policy_decision": {
      "type": "object",
      "description": "One admission/authority policy decision recorded for an attempted action.",
      "properties": {
        "decision_id": {
          "type": "string",
          "description": "Unique decision identifier."
        },
        "verdict": {
          "type": "string",
          "description": "One of: admitted | refused | blocked."
        },
        "reason": {
          "type": "string",
          "description": "Optional human-readable reason for this verdict."
        }
      },
      "required": ["decision_id", "verdict"
      ]

    },
    "policy_payoff_observation": {
      "type": "object",
      "description": "Records bounded policy payoff separately from planner identity and parameters.",
      "properties": {
        "observation_id": {
          "type": "string",
          "description": "Stable policy payoff observation identity."
        },
        "policy_id": {
          "type": "string",
          "description": "Exact policy identity."
        },
        "parameter_digest": {
          "type": "string",
          "description": "Digest of bounded policy parameters."
        },
        "objective_id": {
          "type": "string",
          "description": "Objective used to score the policy."
        },
        "payoff": {
          "type": "number",
          "description": "Observed numeric policy payoff."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Independent evidence supporting the payoff."
        }
      },
      "required": ["observation_id", "policy_id", "parameter_digest", "objective_id", "payoff", "evidence_digest"
      ]

    },
    "powl_choice_graph_edge": {
      "type": "object",
      "description": "One directed edge of a ChoiceGraphNode.edges set over ChoiceGraphEndpoint (Start | Child(usize) | End); may be cyclic (a self-loop Child(i)->Child(i) is a POWL 1.0-style loop over a single child, generalized).",
      "properties": {
        "from_kind": {
          "type": "string",
          "description": "The edge source endpoint kind: start | child | end (ChoiceGraphEndpoint variant tag; start has no incoming edges, end has no outgoing edges per Def. 3.6)."
        },
        "from_child_index": {
          "type": "integer",
          "description": "Index into the parent ChoiceGraphNode's children when from_kind is child; absent otherwise."
        },
        "to_kind": {
          "type": "string",
          "description": "The edge target endpoint kind: start | child | end."
        },
        "to_child_index": {
          "type": "integer",
          "description": "Index into the parent ChoiceGraphNode's children when to_kind is child; absent otherwise."
        }
      },
      "required": ["from_kind", "to_kind"
      ]

    },
    "powl_freq": {
      "type": "object",
      "description": "A POWL node's multiplicity/frequency tag (Freq): how many times it may occur.",
      "properties": {
        "min_freq": {
          "type": "integer",
          "description": "Minimum occurrence count; 0 means the node is skippable."
        },
        "max_freq": {
          "type": "integer",
          "description": "Maximum occurrence count; absent means unbounded."
        }
      },
      "required": ["min_freq"
      ]

    },
    "powl_leaf": {
      "type": "object",
      "description": "A POWL leaf activity (PowlLeaf): a silent or non-silent activity carrying its own frequency tag, flattened for wire transport.",
      "properties": {
        "activity_label": {
          "type": "string",
          "description": "The activity name; absent when is_tau is true (a silent leaf)."
        },
        "is_tau": {
          "type": "boolean",
          "description": "True iff this leaf is a silent (tau) activity, i.e. Leaf::activity_label was LeafLabel::Tau rather than LeafLabel::Activity."
        },
        "min_freq": {
          "type": "integer",
          "description": "This leaf's Freq.min_freq, flattened onto the leaf record."
        },
        "max_freq": {
          "type": "integer",
          "description": "This leaf's Freq.max_freq, flattened onto the leaf record; absent means unbounded."
        }
      },
      "required": ["is_tau", "min_freq"
      ]

    },
    "powl_partial_order_edge": {
      "type": "object",
      "description": "One strict order edge of a PartialOrderNode.order set: from_index must happen before to_index among the parent node's children.",
      "properties": {
        "from_index": {
          "type": "integer",
          "description": "Index into the parent PartialOrderNode's children that must happen first."
        },
        "to_index": {
          "type": "integer",
          "description": "Index into the parent PartialOrderNode's children that must happen after from_index."
        }
      },
      "required": ["from_index", "to_index"
      ]

    },
    "powl_projection": {
      "type": "object",
      "description": "Binds a plan candidate to an exact POWL process-plan projection.",
      "properties": {
        "plan_id": {
          "type": "string",
          "description": "Required plan_id for this bounded planner contract."
        },
        "powl_hash": {
          "type": "string",
          "description": "Required powl_hash for deterministic planner evaluation."
        },
        "projection_receipt_hash": {
          "type": "string",
          "description": "Required projection_receipt_hash preserving evidence and falsifiability."
        }
      },
      "required": ["plan_id", "powl_hash", "projection_receipt_hash"
      ]

    },
    "ppddl_projection": {
      "type": "object",
      "description": "Binds a plan candidate to an exact PPDDL problem/domain projection.",
      "properties": {
        "plan_id": {
          "type": "string",
          "description": "Required plan_id for this bounded planner contract."
        },
        "domain_hash": {
          "type": "string",
          "description": "Required domain_hash for deterministic planner evaluation."
        },
        "problem_hash": {
          "type": "string",
          "description": "Required problem_hash preserving evidence and falsifiability."
        }
      },
      "required": ["plan_id", "domain_hash", "problem_hash"
      ]

    },
    "premium_connector_pricing": {
      "type": "object",
      "description": "Paid integration-connector add-on.",
      "properties": {
        "premium_connector_pricing_id": {
          "type": "string",
          "description": "Stable identity of this premium connector pricing contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "connector_id": {
          "type": "string",
          "description": "Stable identity of the separately priced connector."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["premium_connector_pricing_id", "account_id", "connector_id", "evidence_digest", "effective_at"
      ]

    },
    "prepaid_credit_balance": {
      "type": "object",
      "description": "Receipted prepaid-credit balance.",
      "properties": {
        "prepaid_credit_balance_id": {
          "type": "string",
          "description": "Stable identity of this prepaid credit balance contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "remaining_credit": {
          "type": "number",
          "description": "Observed remaining prepaid credit after admitted usage."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["prepaid_credit_balance_id", "account_id", "remaining_credit", "evidence_digest", "effective_at"
      ]

    },
    "price_book_version": {
      "type": "object",
      "description": "Immutable enterprise price-book release.",
      "properties": {
        "price_book_version_id": {
          "type": "string",
          "description": "Stable identity of this price book version contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "version": {
          "type": "string",
          "description": "Buyer-visible semantic version of the admitted price book."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["price_book_version_id", "account_id", "version", "evidence_digest", "effective_at"
      ]

    },
    "pricing_basis_contract": {
      "type": "object",
      "description": "Binds the opportunity to an exact pricing basis and evidence identity before quote construction.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required pricing basis contract input; omission is an executable typed refusal, never an inferred approval."
        },
        "pricing_basis_id": {
          "type": "string",
          "description": "Required pricing basis contract input; omission is an executable typed refusal, never an inferred approval."
        },
        "evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "pricing_basis_id", "evidence_hash"
      ]

    },
    "privacy_classification_evidence": {
      "type": "object",
      "description": "Executable privacy evidence binding an exact subject to the classification enforced for its handled data.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this privacy-classification observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "privacy_class": {
          "type": "string",
          "description": "Privacy classification observed at the enforcement boundary."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "privacy_class", "observed_result"
      ]

    },
    "private_offer": {
      "type": "object",
      "description": "Negotiated private offer with customer, price, and expiration.",
      "properties": {
        "offer_id": {
          "type": "string",
          "description": "Stable private offer identity."
        },
        "account_id": {
          "type": "string",
          "description": "Target enterprise account."
        },
        "total_price": {
          "type": "number",
          "description": "Negotiated total price."
        },
        "expires_at": {
          "type": "string",
          "description": "Offer expiration instant."
        }
      },
      "required": ["offer_id", "account_id", "total_price", "expires_at"
      ]

    },
    "private_registry_evidence": {
      "type": "object",
      "description": "Executable private-registry evidence binding an exact subject to the digest pulled from the authorized registry.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this private-registry observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "registry_digest": {
          "type": "string",
          "description": "Digest observed after resolving the subject from the private registry."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "registry_digest", "observed_result"
      ]

    },
    "process_variant": {
      "type": "object",
      "description": "One distinct activity-sequence variant observed in a log, with its frequency.",
      "properties": {
        "variant_id": {
          "type": "string",
          "description": "Unique variant identifier."
        },
        "activity_sequence": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Ordered list of activity names making up this variant."
        },
        "frequency": {
          "type": "integer",
          "description": "Number of traces observed with exactly this activity sequence."
        }
      },
      "required": ["variant_id", "activity_sequence", "frequency"
      ]

    },
    "process_volume_pricing_policy": {
      "type": "object",
      "description": "Process-model commercial packaging dimension.",
      "properties": {
        "process_volume_pricing_policy_id": {
          "type": "string",
          "description": "Stable identity of this process volume pricing policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "unit_process_price": {
          "type": "number",
          "description": "Admitted unit price per managed process."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["process_volume_pricing_policy_id", "account_id", "unit_process_price", "evidence_digest", "effective_at"
      ]

    },
    "procurement_acceptance_evidence": {
      "type": "object",
      "description": "Executable procurement evidence binding the exact commercial subject to the final decision derived from admitted controls.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this procurement-acceptance observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "acceptance_decision": {
          "type": "string",
          "description": "Observed procurement decision derived from the exact subject evidence set."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "acceptance_decision", "observed_result"
      ]

    },
    "procurement_blocker": {
      "type": "object",
      "description": "Encodes an unresolved procurement blocker as a typed refusal instead of allowing false booking readiness.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required procurement blocker input; omission is an executable typed refusal, never an inferred approval."
        },
        "blocker_id": {
          "type": "string",
          "description": "Required procurement blocker input; omission is an executable typed refusal, never an inferred approval."
        },
        "refusal_code": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "blocker_id", "refusal_code"
      ]

    },
    "procurement_channel_selection": {
      "type": "object",
      "description": "Admits exactly one purchasing channel with evidence, preventing direct, marketplace, reseller, and distributor routes from conflicting.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required procurement channel selection input; omission is an executable typed refusal, never an inferred approval."
        },
        "channel_id": {
          "type": "string",
          "description": "Required procurement channel selection input; omission is an executable typed refusal, never an inferred approval."
        },
        "selection_evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "channel_id", "selection_evidence_hash"
      ]

    },
    "procurement_readiness": {
      "type": "object",
      "description": "Observable procurement journey state for an enterprise commercial transition.",
      "properties": {
        "procurement_readiness_id": {
          "type": "string",
          "description": "Stable identity of this procurement readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose procurement readiness state was observed."
        },
        "procurement_stage": {
          "type": "string",
          "description": "Current admitted procurement stage."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["procurement_readiness_id", "account_id", "procurement_stage", "evidence_digest", "observed_at"
      ]

    },
    "production_readiness": {
      "type": "object",
      "description": "Cumulative evidence-backed production readiness for the enterprise account.",
      "properties": {
        "production_readiness_id": {
          "type": "string",
          "description": "Stable identity of this production readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose production readiness state was observed."
        },
        "production_readiness_score": {
          "type": "number",
          "description": "Observed production-readiness score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["production_readiness_id", "account_id", "production_readiness_score", "evidence_digest", "observed_at"
      ]

    },
    "promotion_decision": {
      "type": "object",
      "description": "Promotes or refuses a challenger by applying a pre-existing threshold to independent evidence.",
      "properties": {
        "decision_id": {
          "type": "string",
          "description": "Stable promotion decision identity."
        },
        "candidate_policy_id": {
          "type": "string",
          "description": "Candidate policy considered."
        },
        "threshold_id": {
          "type": "string",
          "description": "Immutable threshold applied."
        },
        "evidence_set_digest": {
          "type": "string",
          "description": "Digest of independent evaluation evidence."
        },
        "decision": {
          "type": "string",
          "description": "PROMOTE or REFUSE result."
        },
        "refusal_code": {
          "type": "string",
          "description": "Typed reason when promotion is refused."
        }
      },
      "required": ["decision_id", "candidate_policy_id", "threshold_id", "evidence_set_digest", "decision", "refusal_code"
      ]

    },
    "promotion_threshold": {
      "type": "object",
      "description": "Defines immutable minimum evidence and payoff conditions for challenger promotion.",
      "properties": {
        "threshold_id": {
          "type": "string",
          "description": "Stable promotion threshold identity."
        },
        "objective_id": {
          "type": "string",
          "description": "Objective governed by the threshold."
        },
        "minimum_payoff": {
          "type": "number",
          "description": "Minimum challenger payoff."
        },
        "maximum_regression": {
          "type": "number",
          "description": "Maximum admissible regression."
        },
        "minimum_evidence_count": {
          "type": "integer",
          "description": "Minimum independent evidence count."
        },
        "authority_binding": {
          "type": "string",
          "description": "Authority that owns threshold changes."
        }
      },
      "required": ["threshold_id", "objective_id", "minimum_payoff", "maximum_regression", "minimum_evidence_count", "authority_binding"
      ]

    },
    "proof_of_value_budget": {
      "type": "object",
      "description": "Qualifies whether a proof-of-value has an actual budget and decision rather than free-pilot ambiguity.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required proof of value budget input; omission is an executable typed refusal, never an inferred approval."
        },
        "budget_id": {
          "type": "string",
          "description": "Required proof of value budget input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "budget_id", "decision"
      ]

    },
    "proof_of_value_exit_gate": {
      "type": "object",
      "description": "Defines the exact commercial exit gate that converts a proof-of-value into a paid deployment or refusal.",
      "properties": {
        "pov_id": {
          "type": "string",
          "description": "Required proof of value exit gate input; omission is an executable typed refusal, never an inferred approval."
        },
        "exit_gate_id": {
          "type": "string",
          "description": "Required proof of value exit gate input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["pov_id", "exit_gate_id", "decision"
      ]

    },
    "proof_of_value_package": {
      "type": "object",
      "description": "Paid proof-of-value package with measurable exit criterion.",
      "properties": {
        "proof_of_value_package_id": {
          "type": "string",
          "description": "Stable identity of this proof of value package contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "success_metric": {
          "type": "string",
          "description": "Canonical metric that determines proof-of-value acceptance."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["proof_of_value_package_id", "account_id", "success_metric", "evidence_digest", "effective_at"
      ]

    },
    "propagation_score": {
      "type": "object",
      "description": "Scores downstream consequences across the dependency and capability graph.",
      "properties": {
        "option_id": {
          "type": "string",
          "description": "Required option_id for this bounded planner contract."
        },
        "graph_hash": {
          "type": "string",
          "description": "Required graph_hash for deterministic planner evaluation."
        },
        "score": {
          "type": "string",
          "description": "Required score preserving evidence and falsifiability."
        }
      },
      "required": ["option_id", "graph_hash", "score"
      ]

    },
    "proration_policy": {
      "type": "object",
      "description": "Deterministic mid-term proration policy.",
      "properties": {
        "proration_policy_id": {
          "type": "string",
          "description": "Stable identity of this proration policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "proration_method": {
          "type": "string",
          "description": "Admitted algorithm identity for partial-period charges."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["proration_policy_id", "account_id", "proration_method", "evidence_digest", "effective_at"
      ]

    },
    "provenance_binding_evidence": {
      "type": "object",
      "description": "Executable procurement evidence binding one commercial artifact to its exact repository commit and observed provenance verification result.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this provenance observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "builder_identity": {
          "type": "string",
          "description": "Immutable identity of the builder that emitted the provenance."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verification consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "builder_identity", "observed_result"
      ]

    },
    "provenance_binding_observation": {
      "type": "object",
      "description": "Binds observation, source capsule, and evidence digest into one provenance fact.",
      "properties": {
        "observation_id": {
          "type": "string",
          "description": "Stable observation identity."
        },
        "source_capsule_digest": {
          "type": "string",
          "description": "Exact source capsule digest."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Immutable supporting evidence digest."
        },
        "binding_status": {
          "type": "string",
          "description": "Machine-readable provenance standing."
        }
      },
      "required": ["observation_id", "source_capsule_digest", "evidence_digest", "binding_status"
      ]

    },
    "psro_population": {
      "type": "object",
      "description": "Maintains a population of policies and response oracles for meta-routing.",
      "properties": {
        "population_id": {
          "type": "string",
          "description": "Required population_id for this bounded planner contract."
        },
        "policy_ids": {
          "type": "string",
          "description": "Required policy_ids for deterministic planner evaluation."
        },
        "population_hash": {
          "type": "string",
          "description": "Required population_hash preserving evidence and falsifiability."
        }
      },
      "required": ["population_id", "policy_ids", "population_hash"
      ]

    },
    "purchase_order_binding": {
      "type": "object",
      "description": "Customer purchase-order evidence bound to an enterprise order.",
      "properties": {
        "binding_id": {
          "type": "string",
          "description": "Stable binding identity."
        },
        "order_id": {
          "type": "string",
          "description": "Enterprise order subject."
        },
        "purchase_order_number": {
          "type": "string",
          "description": "Customer purchase-order number."
        },
        "status": {
          "type": "string",
          "description": "Binding validation standing."
        }
      },
      "required": ["binding_id", "order_id", "purchase_order_number", "status"
      ]

    },
    "purchase_order_requirement": {
      "type": "object",
      "description": "Makes the buyer's purchase-order requirement explicit and evidenced before booking readiness.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required purchase order requirement input; omission is an executable typed refusal, never an inferred approval."
        },
        "requirement_id": {
          "type": "string",
          "description": "Required purchase order requirement input; omission is an executable typed refusal, never an inferred approval."
        },
        "evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "requirement_id", "evidence_hash"
      ]

    },
    "purchasing_entity_identity": {
      "type": "object",
      "description": "Requires the legal entity that will issue purchasing authority before commercial progression.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required purchasing entity identity input; omission is an executable typed refusal, never an inferred approval."
        },
        "purchasing_entity_id": {
          "type": "string",
          "description": "Required purchasing entity identity input; omission is an executable typed refusal, never an inferred approval."
        },
        "identity_evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "purchasing_entity_id", "identity_evidence_hash"
      ]

    },
    "queue_snapshot": {
      "type": "object",
      "description": "One point-in-time observation of a queue depth.",
      "properties": {
        "queue_name": {
          "type": "string",
          "description": "Name of the observed queue."
        },
        "depth": {
          "type": "integer",
          "description": "Observed queue depth at this point in time."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 timestamp of this observation."
        }
      },
      "required": ["queue_name", "depth", "observed_at"
      ]

    },
    "quota_burst_allowance": {
      "type": "object",
      "description": "Purchased temporary quota burst allowance.",
      "properties": {
        "quota_burst_allowance_id": {
          "type": "string",
          "description": "Stable identity of this quota burst allowance contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "burst_units": {
          "type": "number",
          "description": "Additional units admitted above the contracted quota."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["quota_burst_allowance_id", "account_id", "burst_units", "evidence_digest", "effective_at"
      ]

    },
    "quota_override": {
      "type": "object",
      "description": "Explicitly authorized quota override.",
      "properties": {
        "quota_override_id": {
          "type": "string",
          "description": "Stable identity of this quota override contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "override_units": {
          "type": "number",
          "description": "Authorized replacement quota for the bounded window."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["quota_override_id", "account_id", "override_units", "evidence_digest", "effective_at"
      ]

    },
    "quota_policy": {
      "type": "object",
      "description": "Enforceable quota limit over a named measurement window.",
      "properties": {
        "quota_id": {
          "type": "string",
          "description": "Stable quota identity."
        },
        "metric_name": {
          "type": "string",
          "description": "Governed metric."
        },
        "limit": {
          "type": "number",
          "description": "Maximum allowed quantity."
        },
        "window": {
          "type": "string",
          "description": "Quota measurement window."
        }
      },
      "required": ["quota_id", "metric_name", "limit", "window"
      ]

    },
    "ramp_commitment": {
      "type": "object",
      "description": "Phased commercial commitment with effective window.",
      "properties": {
        "ramp_id": {
          "type": "string",
          "description": "Stable ramp identity."
        },
        "phase": {
          "type": "integer",
          "description": "Ordered ramp phase."
        },
        "committed_amount": {
          "type": "number",
          "description": "Phase commitment amount."
        },
        "effective_at": {
          "type": "string",
          "description": "Phase activation instant."
        }
      },
      "required": ["ramp_id", "phase", "committed_amount", "effective_at"
      ]

    },
    "rate_card_entry": {
      "type": "object",
      "description": "Sellable unit price bound to an immutable price book.",
      "properties": {
        "rate_card_entry_id": {
          "type": "string",
          "description": "Stable identity of this rate card entry contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "unit_price": {
          "type": "number",
          "description": "Admitted unit price; currency is supplied by the linked currency policy."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["rate_card_entry_id", "account_id", "unit_price", "evidence_digest", "effective_at"
      ]

    },
    "rate_distortion_budget": {
      "type": "object",
      "description": "Bounds observation compression by maximum decision-relevant information distortion.",
      "properties": {
        "budget_id": {
          "type": "string",
          "description": "Stable rate-distortion budget identity."
        },
        "decision_id": {
          "type": "string",
          "description": "Decision protected by the budget."
        },
        "source_information_bits": {
          "type": "number",
          "description": "Information present before compression."
        },
        "retained_information_bits": {
          "type": "number",
          "description": "Information retained after projection."
        },
        "maximum_distortion": {
          "type": "number",
          "description": "Maximum admitted decision distortion."
        }
      },
      "required": ["budget_id", "decision_id", "source_information_bits", "retained_information_bits", "maximum_distortion"
      ]

    },
    "reachability_analysis": {
      "type": "object",
      "description": "Determines whether an admitted goal remains reachable from current state.",
      "properties": {
        "state_id": {
          "type": "string",
          "description": "Required state_id for this bounded planner contract."
        },
        "goal_id": {
          "type": "string",
          "description": "Required goal_id for deterministic planner evaluation."
        },
        "reachability_proof_hash": {
          "type": "string",
          "description": "Required reachability_proof_hash preserving evidence and falsifiability."
        }
      },
      "required": ["state_id", "goal_id", "reachability_proof_hash"
      ]

    },
    "receipt_learning_compilation": {
      "type": "object",
      "description": "Compiles only verified receipt-chain members into a deterministic learning corpus.",
      "properties": {
        "compilation_id": {
          "type": "string",
          "description": "Stable receipt compilation identity."
        },
        "receipt_chain_head": {
          "type": "string",
          "description": "Digest of the admitted receipt chain head."
        },
        "admitted_receipt_count": {
          "type": "integer",
          "description": "Number of verified receipts admitted."
        },
        "rejected_receipt_count": {
          "type": "integer",
          "description": "Number of receipts refused by validation."
        },
        "learning_digest": {
          "type": "string",
          "description": "Digest of the compiled learning corpus."
        }
      },
      "required": ["compilation_id", "receipt_chain_head", "admitted_receipt_count", "rejected_receipt_count", "learning_digest"
      ]

    },
    "receipt_replay_evidence": {
      "type": "object",
      "description": "Executable replay evidence binding an exact subject to the deterministic result reproduced from its receipt.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this receipt-replay observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "replay_result_hash": {
          "type": "string",
          "description": "Hash of the consequence reproduced by receipt replay."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "replay_result_hash", "observed_result"
      ]

    },
    "receipt_replay_request": {
      "type": "object",
      "description": "Admits an exact receipt replay request for independent consumer verification.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "receipt_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "replay_request_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "receipt_id", "replay_request_hash"
      ]

    },
    "receipt_required_gate": {
      "type": "object",
      "description": "Prevents any actuation candidate from becoming selectable without a verifiable receipt plan.",
      "properties": {
        "action_id": {
          "type": "string",
          "description": "Required action_id for this bounded planner contract."
        },
        "receipt_contract_id": {
          "type": "string",
          "description": "Required receipt_contract_id for deterministic planner evaluation."
        },
        "standing": {
          "type": "string",
          "description": "Required standing preserving evidence and falsifiability."
        }
      },
      "required": ["action_id", "receipt_contract_id", "standing"
      ]

    },
    "receipt_signature": {
      "type": "object",
      "description": "Admits a cryptographic receipt signature bound to an exact paid runtime consequence.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "receipt_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "signature_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "receipt_id", "signature_hash"
      ]

    },
    "receipt_subject_binding": {
      "type": "object",
      "description": "Binds a runtime receipt to the exact workload subject it claims to evidence.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "workload_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "binding_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "workload_id", "binding_hash"
      ]

    },
    "receipt_verification": {
      "type": "object",
      "description": "Records independent verification of a production receipt before value is recognized.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "receipt_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "verification_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "receipt_id", "verification_hash"
      ]

    },
    "recovery_plan": {
      "type": "object",
      "description": "Executable account recovery plan with a measured recovery interval.",
      "properties": {
        "recovery_plan_id": {
          "type": "string",
          "description": "Stable identity of this recovery plan observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose recovery plan state was observed."
        },
        "recovery_time_hours": {
          "type": "number",
          "description": "Verified hours from failure to recovered customer consequence."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["recovery_plan_id", "account_id", "recovery_time_hours", "evidence_digest", "observed_at"
      ]

    },
    "recovery_point_receipt": {
      "type": "object",
      "description": "Receipts the observed recovery point and protected data boundary after failure.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "incident_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "recovery_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "incident_id", "recovery_hash"
      ]

    },
    "recovery_time_receipt": {
      "type": "object",
      "description": "Receipts the observed recovery-time consequence against the paid-service objective.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "incident_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "recovery_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "incident_id", "recovery_hash"
      ]

    },
    "refund_policy": {
      "type": "object",
      "description": "Deterministic refund eligibility policy.",
      "properties": {
        "refund_policy_id": {
          "type": "string",
          "description": "Stable identity of this refund policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "refund_method": {
          "type": "string",
          "description": "Admitted method used to calculate a refund."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["refund_policy_id", "account_id", "refund_method", "evidence_digest", "effective_at"
      ]

    },
    "refusal_boundary_observation": {
      "type": "object",
      "description": "Emits REFUSED with exact authority boundary and evidence.",
      "properties": {
        "subject_id": {
          "type": "string",
          "description": "Exact refused subject identity."
        },
        "refusal_code": {
          "type": "string",
          "description": "Typed refusal reason."
        },
        "authority_boundary": {
          "type": "string",
          "description": "Boundary that denied the action."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest supporting refusal."
        }
      },
      "required": ["subject_id", "refusal_code", "authority_boundary", "evidence_digest"
      ]

    },
    "refusal_threshold": {
      "type": "object",
      "description": "Defines an immutable evidence boundary that forces policy refusal when crossed.",
      "properties": {
        "threshold_id": {
          "type": "string",
          "description": "Stable refusal threshold identity."
        },
        "refusal_class": {
          "type": "string",
          "description": "Failure or risk class governed."
        },
        "limit": {
          "type": "number",
          "description": "Numeric refusal boundary."
        },
        "metric_id": {
          "type": "string",
          "description": "Metric compared to the boundary."
        },
        "authority_binding": {
          "type": "string",
          "description": "Authority that alone may revise the threshold."
        }
      },
      "required": ["threshold_id", "refusal_class", "limit", "metric_id", "authority_binding"
      ]

    },
    "region_pricing_policy": {
      "type": "object",
      "description": "Deployment-region commercial adjustment.",
      "properties": {
        "region_pricing_policy_id": {
          "type": "string",
          "description": "Stable identity of this region pricing policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "region_code": {
          "type": "string",
          "description": "Exact cloud or sovereign region governed by this price policy."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["region_pricing_policy_id", "account_id", "region_code", "evidence_digest", "effective_at"
      ]

    },
    "regression_detector": {
      "type": "object",
      "description": "Detects candidate regression against a fixed baseline and metric before promotion.",
      "properties": {
        "detection_id": {
          "type": "string",
          "description": "Stable regression detection identity."
        },
        "baseline_policy_id": {
          "type": "string",
          "description": "Admitted baseline policy."
        },
        "candidate_policy_id": {
          "type": "string",
          "description": "Candidate policy under test."
        },
        "metric_id": {
          "type": "string",
          "description": "Metric defining regression."
        },
        "regression_delta": {
          "type": "number",
          "description": "Measured candidate regression."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Independent comparison evidence."
        }
      },
      "required": ["detection_id", "baseline_policy_id", "candidate_policy_id", "metric_id", "regression_delta", "evidence_digest"
      ]

    },
    "regression_refusal": {
      "type": "object",
      "description": "Refuses promotion when observed regression exceeds the immutable acceptance threshold.",
      "properties": {
        "refusal_id": {
          "type": "string",
          "description": "Stable regression refusal identity."
        },
        "candidate_policy_id": {
          "type": "string",
          "description": "Candidate refused promotion."
        },
        "regression_detection_id": {
          "type": "string",
          "description": "Supporting regression detection."
        },
        "acceptance_threshold": {
          "type": "number",
          "description": "Maximum admitted regression."
        },
        "observed_regression": {
          "type": "number",
          "description": "Measured regression."
        },
        "authority_ceiling": {
          "type": "string",
          "description": "Boundary preventing self-override of refusal."
        }
      },
      "required": ["refusal_id", "candidate_policy_id", "regression_detection_id", "acceptance_threshold", "observed_regression", "authority_ceiling"
      ]

    },
    "remediation_sla_evidence": {
      "type": "object",
      "description": "Executable remediation evidence binding an exact subject and finding to its enforced due instant.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this remediation-SLA observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "remediation_due_at": {
          "type": "string",
          "description": "UTC deadline enforced for remediation of the admitted finding."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "remediation_due_at", "observed_result"
      ]

    },
    "renewal_evidence": {
      "type": "object",
      "description": "Receipted value evidence supporting a multi-year renewal decision.",
      "properties": {
        "renewal_evidence_id": {
          "type": "string",
          "description": "Stable identity of this renewal evidence observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose renewal evidence state was observed."
        },
        "renewal_value": {
          "type": "number",
          "description": "Verified value supporting renewal."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["renewal_evidence_id", "account_id", "renewal_value", "evidence_digest", "observed_at"
      ]

    },
    "renewal_health": {
      "type": "object",
      "description": "Measured renewal health derived from adoption and realized-value evidence.",
      "properties": {
        "renewal_health_id": {
          "type": "string",
          "description": "Stable identity of this renewal health observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose renewal health state was observed."
        },
        "renewal_health_score": {
          "type": "number",
          "description": "Observed renewal-health score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["renewal_health_id", "account_id", "renewal_health_score", "evidence_digest", "observed_at"
      ]

    },
    "renewal_option": {
      "type": "object",
      "description": "Contractual renewal option with term and notice deadline.",
      "properties": {
        "option_id": {
          "type": "string",
          "description": "Stable renewal option."
        },
        "subscription_id": {
          "type": "string",
          "description": "Renewable subscription."
        },
        "term_months": {
          "type": "integer",
          "description": "Renewal term months."
        },
        "notice_by": {
          "type": "string",
          "description": "Renewal notice deadline."
        }
      },
      "required": ["option_id", "subscription_id", "term_months", "notice_by"
      ]

    },
    "renewal_risk": {
      "type": "object",
      "description": "Evidence-backed renewal risk requiring customer-success action.",
      "properties": {
        "renewal_risk_id": {
          "type": "string",
          "description": "Stable identity of this renewal risk observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose renewal risk state was observed."
        },
        "renewal_risk_score": {
          "type": "number",
          "description": "Observed renewal-risk score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["renewal_risk_id", "account_id", "renewal_risk_score", "evidence_digest", "observed_at"
      ]

    },
    "renewal_term_admission": {
      "type": "object",
      "description": "Makes renewal duration and its decision explicit for lifetime-value qualification.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required renewal term admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "renewal_term": {
          "type": "string",
          "description": "Required renewal term admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "renewal_term", "decision"
      ]

    },
    "repair_effectiveness_measurement": {
      "type": "object",
      "description": "Measures whether a repair reduced the target failure over a fixed evaluation window.",
      "properties": {
        "measurement_id": {
          "type": "string",
          "description": "Stable repair measurement identity."
        },
        "repair_id": {
          "type": "string",
          "description": "Repair being evaluated."
        },
        "baseline_failure_rate": {
          "type": "number",
          "description": "Pre-repair failure rate."
        },
        "post_repair_failure_rate": {
          "type": "number",
          "description": "Post-repair failure rate."
        },
        "evaluation_window_digest": {
          "type": "string",
          "description": "Digest of the compared episode windows."
        }
      },
      "required": ["measurement_id", "repair_id", "baseline_failure_rate", "post_repair_failure_rate", "evaluation_window_digest"
      ]

    },
    "replay_environment_identity": {
      "type": "object",
      "description": "Binds receipt replay to an exact environment identity so mismatched replay is refused.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "environment_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "identity_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "environment_id", "identity_hash"
      ]

    },
    "repository_ancestry_observation": {
      "type": "object",
      "description": "Records mechanically observed ancestry between repository commits so containment never requires human reconstruction.",
      "properties": {
        "ancestor_sha": {
          "type": "string",
          "description": "Candidate ancestor commit."
        },
        "descendant_sha": {
          "type": "string",
          "description": "Candidate descendant commit."
        },
        "relation": {
          "type": "string",
          "description": "Observed ancestry relation."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the mechanical ancestry evidence."
        }
      },
      "required": ["ancestor_sha", "descendant_sha", "relation", "evidence_digest"
      ]

    },
    "repository_default_branch_observation": {
      "type": "object",
      "description": "Observes a repository default branch and its exact head so autonomic state can invalidate on branch drift.",
      "properties": {
        "repository_id": {
          "type": "string",
          "description": "Stable repository identity observed by the sensor."
        },
        "default_branch": {
          "type": "string",
          "description": "Observed default branch name."
        },
        "head_sha": {
          "type": "string",
          "description": "Exact commit at the observed default-branch head."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp bounding the freshness of this observation."
        }
      },
      "required": ["repository_id", "default_branch", "head_sha", "observed_at"
      ]

    },
    "repository_exact_head_observation": {
      "type": "object",
      "description": "Binds a repository observation to an exact branch head and prior head for autonomous change detection.",
      "properties": {
        "repository_id": {
          "type": "string",
          "description": "Stable repository identity."
        },
        "branch_name": {
          "type": "string",
          "description": "Observed branch name."
        },
        "head_sha": {
          "type": "string",
          "description": "Exact observed head commit."
        },
        "previous_head_sha": {
          "type": "string",
          "description": "Previously admitted head used to detect change."
        }
      },
      "required": ["repository_id", "branch_name", "head_sha", "previous_head_sha"
      ]

    },
    "repository_worktree_state_observation": {
      "type": "object",
      "description": "Captures caller-local repository worktree state and dirty-surface cardinality before manufacture.",
      "properties": {
        "repository_id": {
          "type": "string",
          "description": "Stable repository identity."
        },
        "worktree_hash": {
          "type": "string",
          "description": "Digest of tracked caller-local worktree state."
        },
        "dirty_path_count": {
          "type": "integer",
          "description": "Number of changed tracked paths."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of worktree observation."
        }
      },
      "required": ["repository_id", "worktree_hash", "dirty_path_count", "observed_at"
      ]

    },
    "reproducible_build_evidence": {
      "type": "object",
      "description": "Executable reproducibility evidence binding an exact subject to the independently reproduced build digest.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this reproducible-build observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "build_digest": {
          "type": "string",
          "description": "Digest observed from the independent reproduction build."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "build_digest", "observed_result"
      ]

    },
    "reseller_authorization": {
      "type": "object",
      "description": "SKU-scoped reseller authorization with standing.",
      "properties": {
        "authorization_id": {
          "type": "string",
          "description": "Stable authorization identity."
        },
        "reseller_id": {
          "type": "string",
          "description": "Authorized reseller."
        },
        "sku": {
          "type": "string",
          "description": "Authorized sellable SKU."
        },
        "status": {
          "type": "string",
          "description": "Authorization standing."
        }
      },
      "required": ["authorization_id", "reseller_id", "sku", "status"
      ]

    },
    "reserve_work_promotion": {
      "type": "object",
      "description": "Automatically promotes the highest-value lawful reserve when primary work blocks.",
      "properties": {
        "blocked_work_id": {
          "type": "string",
          "description": "Required blocked_work_id for this bounded planner contract."
        },
        "reserve_set_hash": {
          "type": "string",
          "description": "Required reserve_set_hash for deterministic planner evaluation."
        },
        "promoted_work_id": {
          "type": "string",
          "description": "Required promoted_work_id preserving evidence and falsifiability."
        }
      },
      "required": ["blocked_work_id", "reserve_set_hash", "promoted_work_id"
      ]

    },
    "residency_evidence": {
      "type": "object",
      "description": "Executable residency evidence binding an exact subject to the region where its controlled data operation occurred.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this data-residency observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "region": {
          "type": "string",
          "description": "Observed region for the controlled data operation."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "region", "observed_result"
      ]

    },
    "resource_allocation": {
      "type": "object",
      "description": "One recorded assignment of a resource to an activity occurrence.",
      "properties": {
        "resource_id": {
          "type": "string",
          "description": "Identifier of the assigned resource."
        },
        "activity": {
          "type": "string",
          "description": "The activity the resource was assigned to."
        },
        "event_id": {
          "type": "string",
          "description": "Identifier of the specific event occurrence."
        }
      },
      "required": ["resource_id", "activity", "event_id"
      ]

    },
    "resource_capacity_plan": {
      "type": "object",
      "description": "Allocates finite compute, time, and concurrency capacity across lawful options.",
      "properties": {
        "episode_id": {
          "type": "string",
          "description": "Required episode_id for this bounded planner contract."
        },
        "resource_pool_hash": {
          "type": "string",
          "description": "Required resource_pool_hash for deterministic planner evaluation."
        },
        "allocation_hash": {
          "type": "string",
          "description": "Required allocation_hash preserving evidence and falsifiability."
        }
      },
      "required": ["episode_id", "resource_pool_hash", "allocation_hash"
      ]

    },
    "retention_policy_evidence": {
      "type": "object",
      "description": "Executable retention evidence binding an exact subject to the duration enforced by the storage lifecycle.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this retention-policy observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "retention_days": {
          "type": "integer",
          "description": "Enforced number of days before lifecycle expiry."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "retention_days", "observed_result"
      ]

    },
    "retention_pricing_policy": {
      "type": "object",
      "description": "Retention-duration commercial packaging.",
      "properties": {
        "retention_pricing_policy_id": {
          "type": "string",
          "description": "Stable identity of this retention pricing policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "retention_days": {
          "type": "integer",
          "description": "Purchased evidence and event-data retention period in days."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["retention_pricing_policy_id", "account_id", "retention_days", "evidence_digest", "effective_at"
      ]

    },
    "revenue_attribution": {
      "type": "object",
      "description": "Receipted revenue attribution bound to exact enterprise evidence.",
      "properties": {
        "revenue_attribution_id": {
          "type": "string",
          "description": "Stable identity of this revenue attribution observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose revenue attribution state was observed."
        },
        "attributed_revenue": {
          "type": "number",
          "description": "Revenue attributed to the verified value path."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["revenue_attribution_id", "account_id", "attributed_revenue", "evidence_digest", "observed_at"
      ]

    },
    "revenue_contract_admission": {
      "type": "object",
      "description": "Produces the terminal immutable admission receipt binding a qualified opportunity to an executable paid revenue contract.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required revenue contract admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "contract_id": {
          "type": "string",
          "description": "Required revenue contract admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "admission_receipt_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "contract_id", "admission_receipt_hash"
      ]

    },
    "revenue_schedule_assumption": {
      "type": "object",
      "description": "Binds forecast revenue timing to a named schedule and evidence identity so bookings and realization assumptions can be independently challenged.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required revenue schedule assumption input; omission is an executable typed refusal, never an inferred approval."
        },
        "schedule_id": {
          "type": "string",
          "description": "Required revenue schedule assumption input; omission is an executable typed refusal, never an inferred approval."
        },
        "assumption_evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "schedule_id", "assumption_evidence_hash"
      ]

    },
    "reversibility_weight": {
      "type": "object",
      "description": "Weights reversible actions above irreversible ones until evidence justifies commitment.",
      "properties": {
        "action_id": {
          "type": "string",
          "description": "Required action_id for this bounded planner contract."
        },
        "rollback_id": {
          "type": "string",
          "description": "Required rollback_id for deterministic planner evaluation."
        },
        "weight": {
          "type": "string",
          "description": "Required weight preserving evidence and falsifiability."
        }
      },
      "required": ["action_id", "rollback_id", "weight"
      ]

    },
    "rework_cost": {
      "type": "object",
      "description": "One observed process case with quantified retry/rework loop count and weighted cost evidence.",
      "properties": {
        "case_id": {
          "type": "string",
          "description": "Exact process-case identity carrying the observed rework."
        },
        "loop_count": {
          "type": "integer",
          "description": "Observed count of retry/rework loop markers for this case."
        },
        "weighted_cost": {
          "type": "number",
          "description": "Observed case amount weighted by the admitted rework-loop rule; units are inherited from the admitted source evidence."
        }
      },
      "required": ["case_id", "loop_count", "weighted_cost"
      ]

    },
    "rfp_response_evidence": {
      "type": "object",
      "description": "Executable procurement evidence binding an exact subject to a deterministic RFP answer set.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this RFP-response observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "answer_set_hash": {
          "type": "string",
          "description": "Digest of the answer set derived from verified subject facts."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "answer_set_hash", "observed_result"
      ]

    },
    "role_compatibility": {
      "type": "object",
      "description": "Evaluates whether a policy is lawful for an assigned role in the current world.",
      "properties": {
        "role_id": {
          "type": "string",
          "description": "Required role_id for this bounded planner contract."
        },
        "policy_id": {
          "type": "string",
          "description": "Required policy_id for deterministic planner evaluation."
        },
        "compatibility": {
          "type": "string",
          "description": "Required compatibility preserving evidence and falsifiability."
        }
      },
      "required": ["role_id", "policy_id", "compatibility"
      ]

    },
    "rollback_checkpoint": {
      "type": "object",
      "description": "Binds rollback authority to an immutable runtime checkpoint and verified state digest.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "checkpoint_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "state_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "checkpoint_id", "state_hash"
      ]

    },
    "rollback_decision": {
      "type": "object",
      "description": "Receipted rollback decision and recovered account consequence.",
      "properties": {
        "rollback_decision_id": {
          "type": "string",
          "description": "Stable identity of this rollback decision observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose rollback decision state was observed."
        },
        "rollback_result": {
          "type": "string",
          "description": "Observed outcome of the rollback action."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["rollback_decision_id", "account_id", "rollback_result", "evidence_digest", "observed_at"
      ]

    },
    "rollback_evidence": {
      "type": "object",
      "description": "Executable rollback evidence binding an exact subject to the artifact identity restored by a real rollback transition.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this rollback observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "rollback_digest": {
          "type": "string",
          "description": "Digest of the prior artifact restored by the rollback operation."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "rollback_digest", "observed_result"
      ]

    },
    "rollback_outcome_learning": {
      "type": "object",
      "description": "Learns whether rollback restored admitted state without treating rollback completion as success by itself.",
      "properties": {
        "learning_id": {
          "type": "string",
          "description": "Stable rollback learning identity."
        },
        "rollback_id": {
          "type": "string",
          "description": "Rollback transition evaluated."
        },
        "pre_rollback_state_digest": {
          "type": "string",
          "description": "State digest before rollback."
        },
        "post_rollback_state_digest": {
          "type": "string",
          "description": "State digest after rollback."
        },
        "recovery_score": {
          "type": "number",
          "description": "Measured degree of state restoration."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Independent recovery evidence."
        }
      },
      "required": ["learning_id", "rollback_id", "pre_rollback_state_digest", "post_rollback_state_digest", "recovery_score", "evidence_digest"
      ]

    },
    "rolling_upgrade_plan": {
      "type": "object",
      "description": "Admits an identified rolling upgrade plan with deterministic production evidence.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "upgrade_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "plan_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "upgrade_id", "plan_hash"
      ]

    },
    "root_cause_pattern": {
      "type": "object",
      "description": "Stores a reusable root-cause pattern only after independent reproduction.",
      "properties": {
        "pattern_id": {
          "type": "string",
          "description": "Stable root-cause pattern identity."
        },
        "failure_class": {
          "type": "string",
          "description": "Failure class explained by the pattern."
        },
        "causal_graph_digest": {
          "type": "string",
          "description": "Digest of the causal graph."
        },
        "confirmed_reproducer_digest": {
          "type": "string",
          "description": "Independent reproducer evidence."
        },
        "reusable_scope": {
          "type": "string",
          "description": "Bounded scope where reuse is admissible."
        }
      },
      "required": ["pattern_id", "failure_class", "causal_graph_digest", "confirmed_reproducer_digest", "reusable_scope"
      ]

    },
    "root_cause_reuse_decision": {
      "type": "object",
      "description": "Admits or refuses reuse of a historical root cause using measured similarity evidence.",
      "properties": {
        "decision_id": {
          "type": "string",
          "description": "Stable reuse decision identity."
        },
        "pattern_id": {
          "type": "string",
          "description": "Historical root-cause pattern considered."
        },
        "current_failure_id": {
          "type": "string",
          "description": "Current failure being diagnosed."
        },
        "similarity_score": {
          "type": "number",
          "description": "Measured causal similarity."
        },
        "applicability_evidence_digest": {
          "type": "string",
          "description": "Evidence proving or refusing applicability."
        }
      },
      "required": ["decision_id", "pattern_id", "current_failure_id", "similarity_score", "applicability_evidence_digest"
      ]

    },
    "runtime_health_observation": {
      "type": "object",
      "description": "Captures runtime health state with exact supporting evidence.",
      "properties": {
        "runtime_id": {
          "type": "string",
          "description": "Stable runtime identity."
        },
        "health_state": {
          "type": "string",
          "description": "Observed runtime health classification."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of runtime health evidence."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp health was observed."
        }
      },
      "required": ["runtime_id", "health_state", "evidence_digest", "observed_at"
      ]

    },
    "runtime_policy_decision": {
      "type": "object",
      "description": "Records an explicit runtime policy decision before paid workload actuation.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "decision_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "policy_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "decision_id", "policy_hash"
      ]

    },
    "sanctions_screening_result": {
      "type": "object",
      "description": "Requires a named sanctions screening result before accepting the commercial counterparty.",
      "properties": {
        "account_id": {
          "type": "string",
          "description": "Required sanctions screening result input; omission is an executable typed refusal, never an inferred approval."
        },
        "screening_id": {
          "type": "string",
          "description": "Required sanctions screening result input; omission is an executable typed refusal, never an inferred approval."
        },
        "screening_result": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["account_id", "screening_id", "screening_result"
      ]

    },
    "sandbox_entitlement": {
      "type": "object",
      "description": "Nonproduction sandbox entitlement with a hard ceiling.",
      "properties": {
        "sandbox_entitlement_id": {
          "type": "string",
          "description": "Stable identity of this sandbox entitlement contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "sandbox_limit": {
          "type": "integer",
          "description": "Maximum purchased sandbox environments."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["sandbox_entitlement_id", "account_id", "sandbox_limit", "evidence_digest", "effective_at"
      ]

    },
    "saturation_detection": {
      "type": "object",
      "description": "Detects diminishing learning returns from measured gain, novelty, and coverage evidence.",
      "properties": {
        "detection_id": {
          "type": "string",
          "description": "Stable saturation detection identity."
        },
        "curriculum_id": {
          "type": "string",
          "description": "Curriculum being measured."
        },
        "recent_gain": {
          "type": "number",
          "description": "Recent objective improvement."
        },
        "gain_slope": {
          "type": "number",
          "description": "Trend in marginal learning gain."
        },
        "novelty_floor": {
          "type": "number",
          "description": "Minimum admitted novelty."
        },
        "coverage_digest": {
          "type": "string",
          "description": "Digest of evaluated capability coverage."
        }
      },
      "required": ["detection_id", "curriculum_id", "recent_gain", "gain_slope", "novelty_floor", "coverage_digest"
      ]

    },
    "sbom_inventory_evidence": {
      "type": "object",
      "description": "Executable SBOM evidence binding an exact commercial subject to the observed component inventory cardinality.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this SBOM inventory observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "component_count": {
          "type": "integer",
          "description": "Observed number of components in the parsed SBOM inventory."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "component_count", "observed_result"
      ]

    },
    "seat_pricing_policy": {
      "type": "object",
      "description": "Named-seat enterprise packaging dimension.",
      "properties": {
        "seat_pricing_policy_id": {
          "type": "string",
          "description": "Stable identity of this seat pricing policy contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "seat_price": {
          "type": "number",
          "description": "Admitted unit price for one named enterprise seat."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["seat_pricing_policy_id", "account_id", "seat_price", "evidence_digest", "effective_at"
      ]

    },
    "second_pass_byte_identity_observation": {
      "type": "object",
      "description": "Proves the second manufacture pass produced byte-identical owned outputs.",
      "properties": {
        "first_tree_digest": {
          "type": "string",
          "description": "Digest after first manufacture pass."
        },
        "second_tree_digest": {
          "type": "string",
          "description": "Digest after second manufacture pass."
        },
        "byte_identity": {
          "type": "boolean",
          "description": "Whether the output trees are byte-identical."
        },
        "receipt_digest": {
          "type": "string",
          "description": "Receipt binding the replay result."
        }
      },
      "required": ["first_tree_digest", "second_tree_digest", "byte_identity", "receipt_digest"
      ]

    },
    "second_run_identity_objective": {
      "type": "object",
      "description": "Makes byte-identical second manufacture a first-class planning objective.",
      "properties": {
        "subject_id": {
          "type": "string",
          "description": "Required subject_id for this bounded planner contract."
        },
        "first_tree_hash": {
          "type": "string",
          "description": "Required first_tree_hash for deterministic planner evaluation."
        },
        "second_tree_hash": {
          "type": "string",
          "description": "Required second_tree_hash preserving evidence and falsifiability."
        }
      },
      "required": ["subject_id", "first_tree_hash", "second_tree_hash"
      ]

    },
    "secret_boundary_evidence": {
      "type": "object",
      "description": "Executable secrets-boundary evidence proving the admitted subject used an authorized credential source.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this secret-boundary observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "secret_source": {
          "type": "string",
          "description": "Observed source of credentials used by the execution."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "secret_source", "observed_result"
      ]

    },
    "security_addendum_state": {
      "type": "object",
      "description": "Requires an identified security addendum state before promising a paid production deployment.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required security addendum state input; omission is an executable typed refusal, never an inferred approval."
        },
        "addendum_id": {
          "type": "string",
          "description": "Required security addendum state input; omission is an executable typed refusal, never an inferred approval."
        },
        "addendum_state": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "addendum_id", "addendum_state"
      ]

    },
    "security_blocker": {
      "type": "object",
      "description": "Encodes an unresolved security blocker as a typed refusal before production commitment.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required security blocker input; omission is an executable typed refusal, never an inferred approval."
        },
        "blocker_id": {
          "type": "string",
          "description": "Required security blocker input; omission is an executable typed refusal, never an inferred approval."
        },
        "refusal_code": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "blocker_id", "refusal_code"
      ]

    },
    "security_readiness": {
      "type": "object",
      "description": "Evidence-backed security-control readiness for enterprise adoption.",
      "properties": {
        "security_readiness_id": {
          "type": "string",
          "description": "Stable identity of this security readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose security readiness state was observed."
        },
        "control_coverage": {
          "type": "number",
          "description": "Fraction of required controls with accepted evidence."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["security_readiness_id", "account_id", "control_coverage", "evidence_digest", "observed_at"
      ]

    },
    "seeded_evaluation": {
      "type": "object",
      "description": "Produces a reproducible policy evaluation bound to an explicit seed and dataset.",
      "properties": {
        "evaluation_id": {
          "type": "string",
          "description": "Stable seeded evaluation identity."
        },
        "seed": {
          "type": "string",
          "description": "Explicit deterministic seed."
        },
        "dataset_digest": {
          "type": "string",
          "description": "Immutable evaluation dataset digest."
        },
        "policy_id": {
          "type": "string",
          "description": "Policy evaluated."
        },
        "score": {
          "type": "number",
          "description": "Observed evaluation score."
        },
        "replay_digest": {
          "type": "string",
          "description": "Digest of deterministic replay output."
        }
      },
      "required": ["evaluation_id", "seed", "dataset_digest", "policy_id", "score", "replay_digest"
      ]

    },
    "semantic_drift_observation": {
      "type": "object",
      "description": "Classifies semantic change between prior and current canonical digests.",
      "properties": {
        "subject_id": {
          "type": "string",
          "description": "Observed semantic subject."
        },
        "prior_semantic_digest": {
          "type": "string",
          "description": "Prior canonical semantic digest."
        },
        "current_semantic_digest": {
          "type": "string",
          "description": "Current canonical semantic digest."
        },
        "drift_class": {
          "type": "string",
          "description": "Machine-readable semantic drift class."
        }
      },
      "required": ["subject_id", "prior_semantic_digest", "current_semantic_digest", "drift_class"
      ]

    },
    "service_credit": {
      "type": "object",
      "description": "Receiptable credit issued for an SLO breach.",
      "properties": {
        "credit_id": {
          "type": "string",
          "description": "Stable service credit."
        },
        "slo_id": {
          "type": "string",
          "description": "Breached SLO."
        },
        "amount": {
          "type": "number",
          "description": "Credit monetary amount."
        },
        "currency": {
          "type": "string",
          "description": "ISO 4217 credit currency."
        }
      },
      "required": ["credit_id", "slo_id", "amount", "currency"
      ]

    },
    "service_credit_admission": {
      "type": "object",
      "description": "Qualifies the exact service-credit obligation that prices runtime reliability risk.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required service credit admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "service_credit_id": {
          "type": "string",
          "description": "Required service credit admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "service_credit_id", "decision"
      ]

    },
    "service_credit_ledger": {
      "type": "object",
      "description": "Auditable customer service-credit balance.",
      "properties": {
        "service_credit_ledger_id": {
          "type": "string",
          "description": "Stable identity of this service credit ledger contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "credit_amount": {
          "type": "number",
          "description": "Observed service-credit amount after an admitted SLO consequence."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["service_credit_ledger_id", "account_id", "credit_amount", "evidence_digest", "effective_at"
      ]

    },
    "service_health_snapshot": {
      "type": "object",
      "description": "Captures machine-readable paid-service health for support and escalation decisions.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "service_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "snapshot_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "service_id", "snapshot_hash"
      ]

    },
    "service_level_objective": {
      "type": "object",
      "description": "Measurable service target bound to a support contract.",
      "properties": {
        "slo_id": {
          "type": "string",
          "description": "Stable SLO identity."
        },
        "contract_id": {
          "type": "string",
          "description": "Governing support contract."
        },
        "target_percent": {
          "type": "number",
          "description": "Contracted target percentage."
        },
        "measurement_window": {
          "type": "string",
          "description": "SLO measurement window."
        }
      },
      "required": ["slo_id", "contract_id", "target_percent", "measurement_window"
      ]

    },
    "service_slo_contract": {
      "type": "object",
      "description": "Admits the exact service-level objective contract governing a paid workload.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "slo_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "contract_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "slo_id", "contract_hash"
      ]

    },
    "service_span": {
      "type": "object",
      "description": "One OpenTelemetry-style tracing span observed for a service call.",
      "properties": {
        "span_id": {
          "type": "string",
          "description": "Unique span identifier."
        },
        "service_name": {
          "type": "string",
          "description": "Name of the service that produced this span."
        },
        "duration_ms": {
          "type": "integer",
          "description": "Span duration in milliseconds."
        },
        "parent_span_id": {
          "type": "string",
          "description": "Optional identifier of the parent span (the OpenTelemetry parent/child link; absent on a trace's root span). This is NOT the OpenTelemetry `links` field."
        },
        "trace_id": {
          "type": "string",
          "description": "Identifier of the trace this span belongs to (the partition key the temporal-adjacency arm groups by; parent/child links never cross it)."
        },
        "start_time": {
          "type": "string",
          "description": "ISO8601 timestamp the span started (the only ordering the temporal-adjacency arm consults; the parent/child arm never reads it)."
        }
      },
      "required": ["span_id", "service_name", "duration_ms", "trace_id", "start_time"
      ]

    },
    "shadow_challenger_execution": {
      "type": "object",
      "description": "Runs a challenger in observation-only shadow mode with no consequential DO authority.",
      "properties": {
        "execution_id": {
          "type": "string",
          "description": "Stable shadow execution identity."
        },
        "incumbent_policy_id": {
          "type": "string",
          "description": "Live incumbent policy identity."
        },
        "challenger_policy_id": {
          "type": "string",
          "description": "Shadow challenger policy identity."
        },
        "shared_observation_digest": {
          "type": "string",
          "description": "Digest of identical inputs supplied to both policies."
        },
        "shadow_receipt_digest": {
          "type": "string",
          "description": "Receipt proving challenger execution remained shadow-only."
        },
        "authority_mode": {
          "type": "string",
          "description": "Authority mode, required to remain non-consequential."
        }
      },
      "required": ["execution_id", "incumbent_policy_id", "challenger_policy_id", "shared_observation_digest", "shadow_receipt_digest", "authority_mode"
      ]

    },
    "showback_allocation": {
      "type": "object",
      "description": "Non-billing usage allocation for enterprise transparency.",
      "properties": {
        "allocation_id": {
          "type": "string",
          "description": "Stable showback allocation."
        },
        "project_id": {
          "type": "string",
          "description": "Attributed tenant project."
        },
        "metric_name": {
          "type": "string",
          "description": "Reported usage metric."
        },
        "quantity": {
          "type": "number",
          "description": "Attributed usage quantity."
        }
      },
      "required": ["allocation_id", "project_id", "metric_name", "quantity"
      ]

    },
    "signature_evidence": {
      "type": "object",
      "description": "Executable signature evidence binding an exact subject to the workload identity accepted by cryptographic verification.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this artifact-signature observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "signature_identity": {
          "type": "string",
          "description": "Workload identity observed in the verified artifact signature."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "signature_identity", "observed_result"
      ]

    },
    "sku_definition": {
      "type": "object",
      "description": "Catalog SKU binding edition, billing model, and lifecycle.",
      "properties": {
        "sku": {
          "type": "string",
          "description": "Stable sellable SKU."
        },
        "edition_id": {
          "type": "string",
          "description": "Product edition identity."
        },
        "billing_model": {
          "type": "string",
          "description": "Commercial billing model."
        },
        "status": {
          "type": "string",
          "description": "SKU lifecycle standing."
        }
      },
      "required": ["sku", "edition_id", "billing_model", "status"
      ]

    },
    "sla_offer_admission": {
      "type": "object",
      "description": "Admits an exact service-level offer before it becomes a paid customer commitment.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required sla offer admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "sla_offer_id": {
          "type": "string",
          "description": "Required sla offer admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "sla_offer_id", "decision"
      ]

    },
    "sojourn_time": {
      "type": "object",
      "description": "The total dwell/duration time an object was associated with one event/activity (object-centric sojourn time -- a duration metric, distinct from bpm:sync_time's wait-for-another-object metric).",
      "properties": {
        "object_id": {
          "type": "string",
          "description": "Identifier of the object this measurement is for."
        },
        "event_type": {
          "type": "string",
          "description": "The event type/activity this sojourn measurement is at."
        },
        "seconds": {
          "type": "number",
          "description": "Sojourn duration in seconds."
        }
      },
      "required": ["object_id", "event_type", "seconds"
      ]

    },
    "solution_fit": {
      "type": "object",
      "description": "Measured solution-fit observation for the account's admitted requirements.",
      "properties": {
        "solution_fit_id": {
          "type": "string",
          "description": "Stable identity of this solution fit observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose solution fit state was observed."
        },
        "fit_score": {
          "type": "number",
          "description": "Fraction of admitted requirements satisfied."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["solution_fit_id", "account_id", "fit_score", "evidence_digest", "observed_at"
      ]

    },
    "span_edge": {
      "type": "object",
      "description": "One frequency-annotated service-to-service edge derived from tracing spans, tagged with the evidence class that supports it: parent_child_link (observed -- a parent/child span link exists) or temporal_adjacency (inferred -- consecutive start_time order within one trace, no link consulted).",
      "properties": {
        "source_service": {
          "type": "string",
          "description": "service_name of the source span (the parent for parent_child_link; the earlier-starting span for temporal_adjacency)."
        },
        "target_service": {
          "type": "string",
          "description": "service_name of the target span (the child for parent_child_link; the next-starting span for temporal_adjacency)."
        },
        "frequency": {
          "type": "integer",
          "description": "For parent_child_link: the number of parent -> child links with this (source_service, target_service). For temporal_adjacency: the number of consecutive start_time pairs with it, summed over traces. Comparable only when both arms are computed over the same span set, which causal_dfg_from_spans/1 guarantees."
        },
        "evidence": {
          "type": "string",
          "description": "Evidence class: parent_child_link (observed) or temporal_adjacency (inferred). No other value is emitted."
        }
      },
      "required": ["source_service", "target_service", "frequency", "evidence"
      ]

    },
    "spend_drawdown": {
      "type": "object",
      "description": "Observed consumption against committed spend.",
      "properties": {
        "spend_drawdown_id": {
          "type": "string",
          "description": "Stable identity of this spend drawdown contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "consumed_amount": {
          "type": "number",
          "description": "Amount actually drawn from the committed-spend balance."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["spend_drawdown_id", "account_id", "consumed_amount", "evidence_digest", "effective_at"
      ]

    },
    "stakeholder_map": {
      "type": "object",
      "description": "Executable stakeholder-coverage contract for a qualified enterprise evaluation.",
      "properties": {
        "stakeholder_map_id": {
          "type": "string",
          "description": "Stable identity of this stakeholder map observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose stakeholder map state was observed."
        },
        "stakeholder_count": {
          "type": "integer",
          "description": "Number of distinct admitted stakeholder roles."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["stakeholder_map_id", "account_id", "stakeholder_count", "evidence_digest", "observed_at"
      ]

    },
    "stale_plan_refusal": {
      "type": "object",
      "description": "Refuses execution when subject, pack, policy, or world identity has drifted.",
      "properties": {
        "plan_id": {
          "type": "string",
          "description": "Required plan_id for this bounded planner contract."
        },
        "admitted_preimage_hash": {
          "type": "string",
          "description": "Required admitted_preimage_hash for deterministic planner evaluation."
        },
        "observed_preimage_hash": {
          "type": "string",
          "description": "Required observed_preimage_hash preserving evidence and falsifiability."
        }
      },
      "required": ["plan_id", "admitted_preimage_hash", "observed_preimage_hash"
      ]

    },
    "stale_receipt_refusal": {
      "type": "object",
      "description": "Produces typed refusal evidence when a runtime receipt is outside its admitted freshness window.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "receipt_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "refusal_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "receipt_id", "refusal_hash"
      ]

    },
    "stale_subject_refusal_evidence": {
      "type": "object",
      "description": "Executable refusal evidence binding a rejected observation to the stale subject identity that caused denial.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this stale-subject refusal observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "stale_sha": {
          "type": "string",
          "description": "Stale subject SHA observed and refused by exact-head policy."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "stale_sha", "observed_result"
      ]

    },
    "standing_state_observation": {
      "type": "object",
      "description": "Emits scoped ALIVE, PARTIAL_ALIVE, BLOCKED, UNKNOWN, or BUILD_BROKEN state.",
      "properties": {
        "subject_id": {
          "type": "string",
          "description": "Exact standing subject identity."
        },
        "standing": {
          "type": "string",
          "description": "Machine-readable standing vocabulary value."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest supporting the standing."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp standing was observed."
        }
      },
      "required": ["subject_id", "standing", "evidence_digest", "observed_at"
      ]

    },
    "stopping_criterion": {
      "type": "object",
      "description": "Stops further experiments only when an immutable evidence window satisfies a declared criterion.",
      "properties": {
        "criterion_id": {
          "type": "string",
          "description": "Stable stopping criterion identity."
        },
        "objective_id": {
          "type": "string",
          "description": "Objective governed by stopping."
        },
        "minimum_gain": {
          "type": "number",
          "description": "Minimum gain required to continue."
        },
        "maximum_episodes": {
          "type": "integer",
          "description": "Maximum bounded experiment count."
        },
        "evidence_window_digest": {
          "type": "string",
          "description": "Digest of evidence used to stop."
        },
        "authority_binding": {
          "type": "string",
          "description": "Authority that owns criterion changes."
        }
      },
      "required": ["criterion_id", "objective_id", "minimum_gain", "maximum_episodes", "evidence_window_digest", "authority_binding"
      ]

    },
    "subject_failure_separation": {
      "type": "object",
      "description": "Attributes a failure to the exact subject only after the validation capsule is independently admitted.",
      "properties": {
        "assessment_id": {
          "type": "string",
          "description": "Stable subject-failure assessment identity."
        },
        "exact_subject_sha": {
          "type": "string",
          "description": "Exact subject that reproduced the failure."
        },
        "capsule_standing": {
          "type": "string",
          "description": "Independent standing of the validation capsule."
        },
        "subject_failure_code": {
          "type": "string",
          "description": "Typed failure attributed to the subject."
        },
        "independent_reproducer_digest": {
          "type": "string",
          "description": "Digest of an independent reproducer."
        }
      },
      "required": ["assessment_id", "exact_subject_sha", "capsule_standing", "subject_failure_code", "independent_reproducer_digest"
      ]

    },
    "submodule_lock_observation": {
      "type": "object",
      "description": "Compares an exact gitlink with its declared dependency lock to expose submodule drift automatically.",
      "properties": {
        "submodule_path": {
          "type": "string",
          "description": "Caller-local submodule path."
        },
        "gitlink_sha": {
          "type": "string",
          "description": "Exact gitlink commit."
        },
        "lock_sha": {
          "type": "string",
          "description": "Declared immutable lock commit."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of comparison."
        }
      },
      "required": ["submodule_path", "gitlink_sha", "lock_sha", "observed_at"
      ]

    },
    "submodule_registration_observation": {
      "type": "object",
      "description": "Observes whether every gitlink has repository registration metadata before autonomous checkout.",
      "properties": {
        "submodule_path": {
          "type": "string",
          "description": "Observed gitlink path."
        },
        "registration_state": {
          "type": "string",
          "description": "Registration standing for the gitlink."
        },
        "repository_url": {
          "type": "string",
          "description": "Resolved immutable repository source."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of registration evidence."
        }
      },
      "required": ["submodule_path", "registration_state", "repository_url", "evidence_digest"
      ]

    },
    "success_plan": {
      "type": "object",
      "description": "Executable customer-success plan target with receipted account identity.",
      "properties": {
        "success_plan_id": {
          "type": "string",
          "description": "Stable identity of this success plan observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose success plan state was observed."
        },
        "success_target": {
          "type": "string",
          "description": "Next verified customer-success target."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["success_plan_id", "account_id", "success_target", "evidence_digest", "observed_at"
      ]

    },
    "supervisor_restart_policy": {
      "type": "object",
      "description": "Binds an OTP supervisor to an explicit restart policy governing paid workload resilience.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "supervisor_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "policy_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "supervisor_id", "policy_hash"
      ]

    },
    "support_contract": {
      "type": "object",
      "description": "Purchased support tier with response target and term.",
      "properties": {
        "contract_id": {
          "type": "string",
          "description": "Stable support contract."
        },
        "account_id": {
          "type": "string",
          "description": "Covered enterprise account."
        },
        "tier": {
          "type": "string",
          "description": "Purchased support tier."
        },
        "valid_until": {
          "type": "string",
          "description": "Support term expiration."
        }
      },
      "required": ["contract_id", "account_id", "tier", "valid_until"
      ]

    },
    "support_diagnostic_bundle": {
      "type": "object",
      "description": "Binds machine-readable support diagnostics to the exact tenant and incident subject.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "incident_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "bundle_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "incident_id", "bundle_hash"
      ]

    },
    "support_escalation_evidence": {
      "type": "object",
      "description": "Executable support evidence binding an exact subject to the accountable escalation owner.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this support-escalation observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "escalation_owner": {
          "type": "string",
          "description": "Observed accountable owner of the admitted escalation."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "escalation_owner", "observed_result"
      ]

    },
    "support_readiness": {
      "type": "object",
      "description": "Measured support-team readiness for enterprise production.",
      "properties": {
        "support_readiness_id": {
          "type": "string",
          "description": "Stable identity of this support readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose support readiness state was observed."
        },
        "support_readiness_score": {
          "type": "number",
          "description": "Observed support-readiness score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["support_readiness_id", "account_id", "support_readiness_score", "evidence_digest", "observed_at"
      ]

    },
    "support_sla_evidence": {
      "type": "object",
      "description": "Executable support evidence binding an exact subject to the response latency actually observed.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this support-SLA observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "response_minutes": {
          "type": "integer",
          "description": "Observed minutes from support intake to acknowledged response."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "response_minutes", "observed_result"
      ]

    },
    "support_tier_admission": {
      "type": "object",
      "description": "Admits the paid support tier that determines service obligation and cost-to-serve.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required support tier admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "support_tier_id": {
          "type": "string",
          "description": "Required support tier admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "support_tier_id", "decision"
      ]

    },
    "support_window_evidence": {
      "type": "object",
      "description": "Executable support-window evidence binding an exact subject to the channel available during the observed coverage window.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this support-window observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "support_channel": {
          "type": "string",
          "description": "Observed support channel satisfying the admitted coverage window."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "support_channel", "observed_result"
      ]

    },
    "sync_time": {
      "type": "object",
      "description": "The time one object waited to synchronize with another object at a shared event.",
      "properties": {
        "object_id": {
          "type": "string",
          "description": "Identifier of the object this measurement is for."
        },
        "delaying_object_id": {
          "type": "string",
          "description": "Optional identifier of the object that caused the delay."
        },
        "seconds": {
          "type": "number",
          "description": "Synchronization wait duration in seconds."
        }
      },
      "required": ["object_id", "seconds"
      ]

    },
    "target_metric": {
      "type": "object",
      "description": "Customer-approved target metric for a qualified proof of value.",
      "properties": {
        "target_metric_id": {
          "type": "string",
          "description": "Stable identity of this target metric observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose target metric state was observed."
        },
        "target_value": {
          "type": "number",
          "description": "Target value required for acceptance."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["target_metric_id", "account_id", "target_value", "evidence_digest", "observed_at"
      ]

    },
    "tax_jurisdiction_evidence": {
      "type": "object",
      "description": "Binds the contracting entity to an evidenced tax jurisdiction before price and invoice admission.",
      "properties": {
        "contracting_entity_id": {
          "type": "string",
          "description": "Required tax jurisdiction evidence input; omission is an executable typed refusal, never an inferred approval."
        },
        "tax_jurisdiction": {
          "type": "string",
          "description": "Required tax jurisdiction evidence input; omission is an executable typed refusal, never an inferred approval."
        },
        "evidence_hash": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["contracting_entity_id", "tax_jurisdiction", "evidence_hash"
      ]

    },
    "tax_jurisdiction_rule": {
      "type": "object",
      "description": "Tax jurisdiction routing for a contracting entity.",
      "properties": {
        "tax_jurisdiction_rule_id": {
          "type": "string",
          "description": "Stable identity of this tax jurisdiction rule contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "jurisdiction_code": {
          "type": "string",
          "description": "Admitted tax jurisdiction code for invoice treatment."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["tax_jurisdiction_rule_id", "account_id", "jurisdiction_code", "evidence_digest", "effective_at"
      ]

    },
    "technical_blocker": {
      "type": "object",
      "description": "Encodes an unresolved architecture or integration blocker as a typed refusal before commercial acceptance.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required technical blocker input; omission is an executable typed refusal, never an inferred approval."
        },
        "blocker_id": {
          "type": "string",
          "description": "Required technical blocker input; omission is an executable typed refusal, never an inferred approval."
        },
        "refusal_code": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "blocker_id", "refusal_code"
      ]

    },
    "temporal_order_observation": {
      "type": "object",
      "description": "Encodes observed ordering between two observations and its evidence basis.",
      "properties": {
        "earlier_observation_id": {
          "type": "string",
          "description": "Earlier observation identity."
        },
        "later_observation_id": {
          "type": "string",
          "description": "Later observation identity."
        },
        "ordering_basis": {
          "type": "string",
          "description": "Clock or sequence basis for ordering."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of temporal evidence."
        }
      },
      "required": ["earlier_observation_id", "later_observation_id", "ordering_basis", "evidence_digest"
      ]

    },
    "tenant_account": {
      "type": "object",
      "description": "Commercial tenant bound to account, region, and edition.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Stable tenant identity."
        },
        "account_id": {
          "type": "string",
          "description": "Owning enterprise account."
        },
        "home_region": {
          "type": "string",
          "description": "Tenant home region."
        },
        "edition_id": {
          "type": "string",
          "description": "Provisioned product edition."
        }
      },
      "required": ["tenant_id", "account_id", "home_region", "edition_id"
      ]

    },
    "tenant_data_partition": {
      "type": "object",
      "description": "Binds tenant data to an explicit storage partition whose isolation consequence is independently verifiable.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "partition_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "isolation_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "partition_id", "isolation_hash"
      ]

    },
    "tenant_key_scope": {
      "type": "object",
      "description": "Binds tenant cryptographic material to one runtime key scope and attested key identity.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "key_scope_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "key_attestation_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "key_scope_id", "key_attestation_hash"
      ]

    },
    "tenant_project": {
      "type": "object",
      "description": "Billable tenant project with cost-center attribution.",
      "properties": {
        "project_id": {
          "type": "string",
          "description": "Stable tenant project."
        },
        "tenant_id": {
          "type": "string",
          "description": "Owning tenant."
        },
        "cost_center": {
          "type": "string",
          "description": "Enterprise cost-center code."
        },
        "status": {
          "type": "string",
          "description": "Project lifecycle standing."
        }
      },
      "required": ["project_id", "tenant_id", "cost_center", "status"
      ]

    },
    "tenant_resource_quota": {
      "type": "object",
      "description": "Binds a paid tenant to an enforceable CPU and memory quota with observed utilization evidence.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "quota_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "utilization_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "quota_id", "utilization_hash"
      ]

    },
    "tenant_runtime_boundary": {
      "type": "object",
      "description": "Binds a paid tenant to one explicit runtime isolation boundary and replayable evidence identity.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity whose workload is isolated."
        },
        "boundary_id": {
          "type": "string",
          "description": "Immutable runtime isolation boundary identity."
        },
        "evidence_hash": {
          "type": "string",
          "description": "Digest binding the observed tenant isolation consequence."
        }
      },
      "required": ["tenant_id", "boundary_id", "evidence_hash"
      ]

    },
    "term_subscription": {
      "type": "object",
      "description": "Fixed-term package with explicit start and end.",
      "properties": {
        "subscription_id": {
          "type": "string",
          "description": "Stable term subscription."
        },
        "sku": {
          "type": "string",
          "description": "Subscribed SKU."
        },
        "starts_at": {
          "type": "string",
          "description": "Term start instant."
        },
        "ends_at": {
          "type": "string",
          "description": "Term end instant."
        }
      },
      "required": ["subscription_id", "sku", "starts_at", "ends_at"
      ]

    },
    "termination_right_admission": {
      "type": "object",
      "description": "Qualifies termination rights that materially change collectible contract value and delivery exposure.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required termination right admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "termination_right_id": {
          "type": "string",
          "description": "Required termination right admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "termination_right_id", "decision"
      ]

    },
    "time_to_value": {
      "type": "object",
      "description": "Measured elapsed time to the customer's first verified business value.",
      "properties": {
        "time_to_value_id": {
          "type": "string",
          "description": "Stable identity of this time to value observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose time to value state was observed."
        },
        "verified_days": {
          "type": "integer",
          "description": "Days from admission to first verified value."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["time_to_value_id", "account_id", "verified_days", "evidence_digest", "observed_at"
      ]

    },
    "toolchain_identity": {
      "type": "object",
      "description": "Binds a production consequence to the exact BEAM and manufacturing toolchain identity.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "toolchain_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "identity_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "toolchain_id", "identity_hash"
      ]

    },
    "toolchain_identity_observation": {
      "type": "object",
      "description": "Observes exact tool name, version, and executable bytes used for validation.",
      "properties": {
        "tool_name": {
          "type": "string",
          "description": "Observed tool identity."
        },
        "tool_version": {
          "type": "string",
          "description": "Observed tool version."
        },
        "executable_digest": {
          "type": "string",
          "description": "Digest of executed tool bytes."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of toolchain observation."
        }
      },
      "required": ["tool_name", "tool_version", "executable_digest", "observed_at"
      ]

    },
    "training_readiness": {
      "type": "object",
      "description": "Observed workforce training readiness for production adoption.",
      "properties": {
        "training_readiness_id": {
          "type": "string",
          "description": "Stable identity of this training readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose training readiness state was observed."
        },
        "training_completion_rate": {
          "type": "number",
          "description": "Fraction of assigned learners completing qualification."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["training_readiness_id", "account_id", "training_completion_rate", "evidence_digest", "observed_at"
      ]

    },
    "training_scope_admission": {
      "type": "object",
      "description": "Qualifies training scope as an explicit paid delivery obligation.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required training scope admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "training_scope_id": {
          "type": "string",
          "description": "Required training scope admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "training_scope_id", "decision"
      ]

    },
    "trajectory_window": {
      "type": "object",
      "description": "Bounds a software-manufacturing trajectory by exact endpoints so learning cannot silently mix histories.",
      "properties": {
        "window_id": {
          "type": "string",
          "description": "Stable trajectory window identity."
        },
        "repository_id": {
          "type": "string",
          "description": "Caller-local repository identity."
        },
        "first_subject_sha": {
          "type": "string",
          "description": "Exact first subject in the window."
        },
        "last_subject_sha": {
          "type": "string",
          "description": "Exact final subject in the window."
        },
        "episode_count": {
          "type": "integer",
          "description": "Number of admitted episodes in the window."
        }
      },
      "required": ["window_id", "repository_id", "first_subject_sha", "last_subject_sha", "episode_count"
      ]

    },
    "trial_entitlement": {
      "type": "object",
      "description": "Time-bounded enterprise trial entitlement.",
      "properties": {
        "trial_entitlement_id": {
          "type": "string",
          "description": "Stable identity of this trial entitlement contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "trial_expires_at": {
          "type": "string",
          "description": "Exact instant at which trial capability must be refused."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["trial_entitlement_id", "account_id", "trial_expires_at", "evidence_digest", "effective_at"
      ]

    },
    "true_up_policy": {
      "type": "object",
      "description": "End-of-period reconciliation policy for committed consumption.",
      "properties": {
        "policy_id": {
          "type": "string",
          "description": "Stable true-up identity."
        },
        "commitment_id": {
          "type": "string",
          "description": "Commitment under reconciliation."
        },
        "cadence": {
          "type": "string",
          "description": "True-up cadence."
        },
        "shortfall_behavior": {
          "type": "string",
          "description": "Shortfall treatment."
        }
      },
      "required": ["policy_id", "commitment_id", "cadence", "shortfall_behavior"
      ]

    },
    "type_edge": {
      "type": "object",
      "description": "A directed, qualified edge in the OCEL type graph between two types.",
      "properties": {
        "source_type": {
          "type": "string",
          "description": "The edge source type name."
        },
        "target_type": {
          "type": "string",
          "description": "The edge target type name."
        },
        "qualifier": {
          "type": "string",
          "description": "The relationship qualifier/role name for this edge."
        },
        "direction": {
          "type": "string",
          "description": "One of: e2o | o2o."
        }
      },
      "required": ["source_type", "target_type", "qualifier", "direction"
      ]

    },
    "uncertainty_aware_selection": {
      "type": "object",
      "description": "Selects only when confidence and downside bounds satisfy explicit values.",
      "properties": {
        "option_set_id": {
          "type": "string",
          "description": "Required option_set_id for this bounded planner contract."
        },
        "uncertainty_model_hash": {
          "type": "string",
          "description": "Required uncertainty_model_hash for deterministic planner evaluation."
        },
        "selected_option_id": {
          "type": "string",
          "description": "Required selected_option_id preserving evidence and falsifiability."
        }
      },
      "required": ["option_set_id", "uncertainty_model_hash", "selected_option_id"
      ]

    },
    "uncertainty_observation": {
      "type": "object",
      "description": "Represents typed UNKNOWN uncertainty instead of manufacturing false certainty.",
      "properties": {
        "observation_id": {
          "type": "string",
          "description": "Observation with uncertainty."
        },
        "uncertainty_kind": {
          "type": "string",
          "description": "Typed uncertainty classification."
        },
        "confidence_basis": {
          "type": "string",
          "description": "Basis for any confidence estimate."
        },
        "standing": {
          "type": "string",
          "description": "Machine-readable UNKNOWN-compatible standing."
        }
      },
      "required": ["observation_id", "uncertainty_kind", "confidence_basis", "standing"
      ]

    },
    "unit_economics_snapshot": {
      "type": "object",
      "description": "Evidence-bound gross-margin snapshot.",
      "properties": {
        "unit_economics_snapshot_id": {
          "type": "string",
          "description": "Stable identity of this unit economics snapshot contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "gross_margin": {
          "type": "number",
          "description": "Observed gross margin derived from admitted revenue and cost evidence."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["unit_economics_snapshot_id", "account_id", "gross_margin", "evidence_digest", "effective_at"
      ]

    },
    "unsupported_capability_evidence": {
      "type": "object",
      "description": "Executable boundary evidence identifying a requested enterprise capability that the exact subject truthfully refuses.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this unsupported-capability observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "capability_name": {
          "type": "string",
          "description": "Capability name observed at the unsupported routing boundary."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "capability_name", "observed_result"
      ]

    },
    "upgrade_evidence": {
      "type": "object",
      "description": "Executable upgrade evidence binding an exact subject to the source version accepted by the migration path.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this upgrade-assurance observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "from_version": {
          "type": "string",
          "description": "Version from which the observed upgrade execution began."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "from_version", "observed_result"
      ]

    },
    "upsell_readiness": {
      "type": "object",
      "description": "Measured readiness to transition the account to greater product capacity.",
      "properties": {
        "upsell_readiness_id": {
          "type": "string",
          "description": "Stable identity of this upsell readiness observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose upsell readiness state was observed."
        },
        "upsell_score": {
          "type": "number",
          "description": "Observed upsell-readiness score."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["upsell_readiness_id", "account_id", "upsell_score", "evidence_digest", "observed_at"
      ]

    },
    "usage_aggregation_window": {
      "type": "object",
      "description": "Deterministic usage aggregation window.",
      "properties": {
        "usage_aggregation_window_id": {
          "type": "string",
          "description": "Stable identity of this usage aggregation window contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "window_seconds": {
          "type": "integer",
          "description": "Exact billing aggregation window in seconds."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["usage_aggregation_window_id", "account_id", "window_seconds", "evidence_digest", "effective_at"
      ]

    },
    "usage_correction": {
      "type": "object",
      "description": "Receipted correction to previously reported usage.",
      "properties": {
        "usage_correction_id": {
          "type": "string",
          "description": "Stable identity of this usage correction contract."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account governed by this commercial contract."
        },
        "corrected_quantity": {
          "type": "number",
          "description": "Replacement usage quantity bound to correction evidence."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence admitting this commercial consequence."
        },
        "effective_at": {
          "type": "string",
          "description": "ISO8601 instant at which this contract becomes effective."
        }
      },
      "required": ["usage_correction_id", "account_id", "corrected_quantity", "evidence_digest", "effective_at"
      ]

    },
    "usage_event": {
      "type": "object",
      "description": "One metered usage/consumption occurrence to be billed against an entitlement. event_id is the idempotency key that is ALSO the operationId reported to the provider's usage API, so local dedup and provider-side dedup are keyed identically and can never disagree.",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "DEDUP KEY. Stable, deterministic, locally minted identifier for this metered occurrence -- and the exact value sent as operationId on the Service Control services.report call, which is the provider's own documented deduplication mechanism. It MUST be derived from the occurrence itself, never from a random UUID or a wall clock: a re-minted id on retry would defeat both local and provider-side dedup and double bill. Counting the same event_id twice in a billing_reconciliation.total_quantity is the exact MP6 failure."
        },
        "entitlement_id": {
          "type": "string",
          "description": "The entitlement this usage is billed against (Partner Procurement ENTITLEMENT_ID); resolves to the provider's consumerId at report time. Usage whose entitlement_id has no reconciled entitlement_state is a typed refusal, never silently billed and never silently dropped."
        },
        "quantity": {
          "type": "number",
          "description": "The measured amount for this single occurrence, in the unit implied by metric_name. Non-negative: a correction is a separate, explicitly-typed adjustment, never a negative usage_event, because negative quantities make the dedup-set sum order-sensitive at the boundaries and make a double-counted event cancellable rather than detectable."
        },
        "metric_name": {
          "type": "string",
          "description": "The fully qualified metered dimension this occurrence counts against. Reconciliation partitions by (entitlement_id, metric_name): quantities in different metrics are never summed together."
        },
        "occurred_at": {
          "type": "string",
          "description": "ISO8601 UTC instant the usage actually occurred (not when it was observed, queued, or reported)."
        }
      },
      "required": ["event_id", "entitlement_id", "quantity", "metric_name", "occurred_at"
      ]

    },
    "usage_plan": {
      "type": "object",
      "description": "Meter-to-price plan binding dimension, unit, and billing mode.",
      "properties": {
        "plan_id": {
          "type": "string",
          "description": "Stable usage plan identity."
        },
        "metric_name": {
          "type": "string",
          "description": "Metered dimension."
        },
        "unit": {
          "type": "string",
          "description": "Commercial measurement unit."
        },
        "billing_mode": {
          "type": "string",
          "description": "Billing calculation mode."
        }
      },
      "required": ["plan_id", "metric_name", "unit", "billing_mode"
      ]

    },
    "usage_reconciliation_receipt": {
      "type": "object",
      "description": "Receipts deduplicated usage reconciliation before an invoiceable quantity is recognized.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "billing_period_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "reconciliation_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "billing_period_id", "reconciliation_hash"
      ]

    },
    "usage_signal": {
      "type": "object",
      "description": "Measured product-usage signal for an admitted enterprise account.",
      "properties": {
        "usage_signal_id": {
          "type": "string",
          "description": "Stable identity of this usage signal observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose usage signal state was observed."
        },
        "active_user_count": {
          "type": "integer",
          "description": "Distinct active users in the observation window."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["usage_signal_id", "account_id", "active_user_count", "evidence_digest", "observed_at"
      ]

    },
    "validation_capsule_drift_observation": {
      "type": "object",
      "description": "Compares expected and observed capsule digests and emits typed drift.",
      "properties": {
        "expected_digest": {
          "type": "string",
          "description": "Admitted capsule digest."
        },
        "observed_digest": {
          "type": "string",
          "description": "Currently resolved capsule digest."
        },
        "drift_status": {
          "type": "string",
          "description": "Machine-readable capsule drift result."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of digest comparison."
        }
      },
      "required": ["expected_digest", "observed_digest", "drift_status", "observed_at"
      ]

    },
    "validation_capsule_identity_observation": {
      "type": "object",
      "description": "Binds source capsule, validation pack, execution mode, toolchain, and immutable image.",
      "properties": {
        "capsule_repository": {
          "type": "string",
          "description": "Capsule source repository."
        },
        "capsule_sha": {
          "type": "string",
          "description": "Exact capsule source commit."
        },
        "execution_mode": {
          "type": "string",
          "description": "Observed capsule execution mode."
        },
        "image_digest": {
          "type": "string",
          "description": "Immutable platform image digest."
        }
      },
      "required": ["capsule_repository", "capsule_sha", "execution_mode", "image_digest"
      ]

    },
    "value_baseline": {
      "type": "object",
      "description": "Measured pre-adoption business baseline for ROI comparison.",
      "properties": {
        "baseline_id": {
          "type": "string",
          "description": "Stable baseline identity."
        },
        "account_id": {
          "type": "string",
          "description": "Measured enterprise account."
        },
        "metric_name": {
          "type": "string",
          "description": "Business value metric."
        },
        "baseline_value": {
          "type": "number",
          "description": "Pre-adoption measured value."
        }
      },
      "required": ["baseline_id", "account_id", "metric_name", "baseline_value"
      ]

    },
    "value_driver": {
      "type": "object",
      "description": "Account-specific value driver tied to an observed annualized business outcome.",
      "properties": {
        "value_driver_id": {
          "type": "string",
          "description": "Stable identity of this value driver observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose value driver state was observed."
        },
        "annual_value": {
          "type": "number",
          "description": "Observed annualized value in account currency."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["value_driver_id", "account_id", "annual_value", "evidence_digest", "observed_at"
      ]

    },
    "value_of_information_estimate": {
      "type": "object",
      "description": "Estimates expected information gain and acquisition cost for a pending decision.",
      "properties": {
        "decision_id": {
          "type": "string",
          "description": "Decision requiring additional information."
        },
        "evidence_candidate_id": {
          "type": "string",
          "description": "Candidate evidence acquisition."
        },
        "expected_information_gain": {
          "type": "number",
          "description": "Expected reduction in uncertainty."
        },
        "cost_basis": {
          "type": "string",
          "description": "Evidence acquisition cost basis."
        }
      },
      "required": ["decision_id", "evidence_candidate_id", "expected_information_gain", "cost_basis"
      ]

    },
    "value_of_information_score": {
      "type": "object",
      "description": "Scores the expected value of acquiring missing information before selection.",
      "properties": {
        "option_id": {
          "type": "string",
          "description": "Required option_id for this bounded planner contract."
        },
        "observation_id": {
          "type": "string",
          "description": "Required observation_id for deterministic planner evaluation."
        },
        "score": {
          "type": "string",
          "description": "Required score preserving evidence and falsifiability."
        }
      },
      "required": ["option_id", "observation_id", "score"
      ]

    },
    "value_realization": {
      "type": "object",
      "description": "Receipted realized customer value derived from observed consequences.",
      "properties": {
        "value_realization_id": {
          "type": "string",
          "description": "Stable identity of this value realization observation."
        },
        "account_id": {
          "type": "string",
          "description": "Enterprise account whose value realization state was observed."
        },
        "realized_value": {
          "type": "number",
          "description": "Verified realized value in account currency."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting this customer-value observation."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the enterprise consequence was observed."
        }
      },
      "required": ["value_realization_id", "account_id", "realized_value", "evidence_digest", "observed_at"
      ]

    },
    "value_realization_feedback": {
      "type": "object",
      "description": "Feeds independently measured realized value back into future policy ranking.",
      "properties": {
        "feedback_id": {
          "type": "string",
          "description": "Stable value feedback identity."
        },
        "objective_id": {
          "type": "string",
          "description": "Value objective being measured."
        },
        "baseline_value": {
          "type": "number",
          "description": "Value before the policy episode."
        },
        "realized_value": {
          "type": "number",
          "description": "Observed value after the episode."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Independent outcome evidence."
        },
        "policy_id": {
          "type": "string",
          "description": "Policy associated with the outcome."
        }
      },
      "required": ["feedback_id", "objective_id", "baseline_value", "realized_value", "evidence_digest", "policy_id"
      ]

    },
    "value_receipt": {
      "type": "object",
      "description": "Customer-controlled before/after value receipt binding a measured operational or economic outcome to exact evidence.",
      "properties": {
        "value_receipt_id": {
          "type": "string",
          "description": "Stable identity of this before/after value receipt."
        },
        "account_id": {
          "type": "string",
          "description": "Customer or enterprise account whose measured value is receipted."
        },
        "metric_name": {
          "type": "string",
          "description": "Canonical metric identity used for both baseline and observed values."
        },
        "baseline_value": {
          "type": "number",
          "description": "Admitted baseline value before the measured change or observation window."
        },
        "observed_value": {
          "type": "number",
          "description": "Observed value after the measured change or observation window."
        },
        "evidence_digest": {
          "type": "string",
          "description": "Digest of the exact evidence supporting the baseline and observed consequence."
        },
        "observed_at": {
          "type": "string",
          "description": "ISO8601 instant the receipted value consequence was observed."
        }
      },
      "required": ["value_receipt_id", "account_id", "metric_name", "baseline_value", "observed_value", "evidence_digest", "observed_at"
      ]

    },
    "value_telemetry_sample": {
      "type": "object",
      "description": "Records a production telemetry sample tied to the paid business-value measurement contract.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "outcome_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "telemetry_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "outcome_id", "telemetry_hash"
      ]

    },
    "vendor_registration_state": {
      "type": "object",
      "description": "Makes buyer vendor-registration completion an explicit revenue gate instead of an invisible procurement delay.",
      "properties": {
        "account_id": {
          "type": "string",
          "description": "Required vendor registration state input; omission is an executable typed refusal, never an inferred approval."
        },
        "registration_id": {
          "type": "string",
          "description": "Required vendor registration state input; omission is an executable typed refusal, never an inferred approval."
        },
        "registration_state": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["account_id", "registration_id", "registration_state"
      ]

    },
    "vendor_risk_evidence": {
      "type": "object",
      "description": "Executable vendor-risk evidence binding an exact subject to the risk score emitted from verified controls.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this vendor-risk observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "risk_score": {
          "type": "number",
          "description": "Observed normalized vendor-risk score derived from executable evidence."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "risk_score", "observed_result"
      ]

    },
    "verification_depth_update": {
      "type": "object",
      "description": "Raises or lowers verification depth from observed risk without weakening the immutable minimum court.",
      "properties": {
        "update_id": {
          "type": "string",
          "description": "Stable verification-depth update identity."
        },
        "verifier_id": {
          "type": "string",
          "description": "Verifier whose depth is adapted."
        },
        "prior_depth": {
          "type": "integer",
          "description": "Prior verification depth."
        },
        "new_depth": {
          "type": "integer",
          "description": "Risk-adjusted verification depth."
        },
        "risk_signal_digest": {
          "type": "string",
          "description": "Digest of observed risk evidence."
        },
        "minimum_depth": {
          "type": "integer",
          "description": "Immutable minimum acceptance depth."
        }
      },
      "required": ["update_id", "verifier_id", "prior_depth", "new_depth", "risk_signal_digest", "minimum_depth"
      ]

    },
    "version_lifecycle_evidence": {
      "type": "object",
      "description": "Executable lifecycle evidence binding an exact subject version to its enforced support horizon.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this version-lifecycle observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "supported_until": {
          "type": "string",
          "description": "UTC instant through which the subject version is admitted for support."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "supported_until", "observed_result"
      ]

    },
    "volume_tier_admission": {
      "type": "object",
      "description": "Admits a volume discount tier only through an explicit decision tied to the opportunity.",
      "properties": {
        "opportunity_id": {
          "type": "string",
          "description": "Required volume tier admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "volume_tier_id": {
          "type": "string",
          "description": "Required volume tier admission input; omission is an executable typed refusal, never an inferred approval."
        },
        "decision": {
          "type": "string",
          "description": "Immutable decision or evidence identity used to verify and replay this bounded commercial admission."
        }
      },
      "required": ["opportunity_id", "volume_tier_id", "decision"
      ]

    },
    "vulnerability_scan_evidence": {
      "type": "object",
      "description": "Executable vulnerability evidence binding an exact subject to the findings actually emitted by a scanner.",
      "properties": {
        "evidence_id": {
          "type": "string",
          "description": "Content-addressed identity of this vulnerability-scan observation."
        },
        "subject_sha": {
          "type": "string",
          "description": "Exact immutable beam4pm commit verified by this observation."
        },
        "vulnerability_count": {
          "type": "integer",
          "description": "Observed count of admitted vulnerability findings."
        },
        "observed_result": {
          "type": "string",
          "description": "Observed verifier consequence: verified or refused."
        }
      },
      "required": ["evidence_id", "subject_sha", "vulnerability_count", "observed_result"
      ]

    },
    "wip_limit_gate": {
      "type": "object",
      "description": "Refuses selections that would exceed the admitted work-in-process ceiling.",
      "properties": {
        "episode_id": {
          "type": "string",
          "description": "Required episode_id for this bounded planner contract."
        },
        "wip_limit": {
          "type": "string",
          "description": "Required wip_limit for deterministic planner evaluation."
        },
        "standing": {
          "type": "string",
          "description": "Required standing preserving evidence and falsifiability."
        }
      },
      "required": ["episode_id", "wip_limit", "standing"
      ]

    },
    "workflow_definition_digest_observation": {
      "type": "object",
      "description": "Binds a workflow definition to exact bytes and repository subject before trusting its runs.",
      "properties": {
        "workflow_path": {
          "type": "string",
          "description": "Workflow source path."
        },
        "definition_sha256": {
          "type": "string",
          "description": "Digest of exact workflow bytes."
        },
        "source_sha": {
          "type": "string",
          "description": "Repository commit containing the workflow."
        },
        "observed_at": {
          "type": "string",
          "description": "Timestamp of workflow observation."
        }
      },
      "required": ["workflow_path", "definition_sha256", "source_sha", "observed_at"
      ]

    },
    "workflow_job_state_observation": {
      "type": "object",
      "description": "Observes a workflow job, runner identity, and conclusion inside an exact run.",
      "properties": {
        "job_id": {
          "type": "string",
          "description": "Stable workflow-job identity."
        },
        "run_id": {
          "type": "string",
          "description": "Owning workflow-run identity."
        },
        "runner_identity": {
          "type": "string",
          "description": "Observed runner or capsule identity."
        },
        "conclusion": {
          "type": "string",
          "description": "Observed terminal job conclusion."
        }
      },
      "required": ["job_id", "run_id", "runner_identity", "conclusion"
      ]

    },
    "workflow_run_state_observation": {
      "type": "object",
      "description": "Observes a workflow run against its exact head and terminal conclusion.",
      "properties": {
        "run_id": {
          "type": "string",
          "description": "Stable workflow-run identity."
        },
        "workflow_id": {
          "type": "string",
          "description": "Stable workflow-definition identity."
        },
        "head_sha": {
          "type": "string",
          "description": "Exact subject commit executed."
        },
        "conclusion": {
          "type": "string",
          "description": "Observed terminal run conclusion."
        }
      },
      "required": ["run_id", "workflow_id", "head_sha", "conclusion"
      ]

    },
    "workload_backpressure_signal": {
      "type": "object",
      "description": "Records runtime backpressure as measured evidence before additional paid work is accepted.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "signal_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "measurement_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "signal_id", "measurement_hash"
      ]

    },
    "workload_cancellation_receipt": {
      "type": "object",
      "description": "Records deterministic cancellation consequence for a paid workload execution.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "execution_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "receipt_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "execution_id", "receipt_hash"
      ]

    },
    "workload_execution_identity": {
      "type": "object",
      "description": "Assigns every paid workload execution an immutable subject identity before runtime consequence.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "execution_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "subject_digest": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "execution_id", "subject_digest"
      ]

    },
    "workload_idempotency_key": {
      "type": "object",
      "description": "Binds a paid execution to an idempotency key so retried delivery cannot be billed or actuated twice.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "execution_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "idempotency_key": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "execution_id", "idempotency_key"
      ]

    },
    "workload_queue_depth": {
      "type": "object",
      "description": "Records paid workload queue depth as an exact capacity and backpressure observation.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "queue_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "measurement_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "queue_id", "measurement_hash"
      ]

    },
    "workload_retry_policy": {
      "type": "object",
      "description": "Admits a bounded retry policy with explicit identity and replayable configuration evidence.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "policy_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "policy_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "policy_id", "policy_hash"
      ]

    },
    "workload_timeout_budget": {
      "type": "object",
      "description": "Admits an execution timeout budget that prevents unbounded paid runtime consumption.",
      "properties": {
        "tenant_id": {
          "type": "string",
          "description": "Exact paid tenant identity for this bounded runtime observation."
        },
        "budget_id": {
          "type": "string",
          "description": "Exact runtime subject identity required for independent verification and replay."
        },
        "budget_hash": {
          "type": "string",
          "description": "Immutable evidence identity binding the observed production consequence."
        }
      },
      "required": ["tenant_id", "budget_id", "budget_hash"
      ]

    }
  }
}
