%% GENERATED by ggen from the admitted bpm:RecordType graph. Do not edit.

-module(beam4pm_types_tests).

-include_lib("eunit/include/eunit.hrl").

-export([
    alignment_move_ok_test/0,
    alignment_move_missing_field_test/0,
    dfg_edge_ok_test/0,
    dfg_edge_missing_field_test/0,
    ocel_event_ok_test/0,
    ocel_event_missing_field_test/0,
    ocel_object_ok_test/0,
    ocel_object_missing_field_test/0,
    ocel_relationship_ok_test/0,
    ocel_relationship_missing_field_test/0,
    petri_arc_ok_test/0,
    petri_arc_missing_field_test/0,
    petri_place_ok_test/0,
    petri_place_missing_field_test/0,
    petri_transition_ok_test/0,
    petri_transition_missing_field_test/0
]).

alignment_move_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_types:new_alignment_move(#{
        move_type => some_atom,
        cost => 1
    })).

alignment_move_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_types:new_alignment_move(#{})).

dfg_edge_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_types:new_dfg_edge(#{
        source_activity => <<"x">>,
        target_activity => <<"x">>,
        frequency => 1
    })).

dfg_edge_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_types:new_dfg_edge(#{})).

ocel_event_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_types:new_ocel_event(#{
        event_id => <<"x">>,
        event_type => <<"x">>,
        event_time => <<"2026-01-01T00:00:00Z">>,
        attributes => #{}
    })).

ocel_event_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_types:new_ocel_event(#{})).

ocel_object_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_types:new_ocel_object(#{
        object_id => <<"x">>,
        object_type => <<"x">>,
        attributes => #{}
    })).

ocel_object_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_types:new_ocel_object(#{})).

ocel_relationship_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_types:new_ocel_relationship(#{
        qualifier => <<"x">>,
        object_id => <<"x">>
    })).

ocel_relationship_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_types:new_ocel_relationship(#{})).

petri_arc_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_types:new_petri_arc(#{
        source_id => <<"x">>,
        target_id => <<"x">>,
        weight => 1
    })).

petri_arc_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_types:new_petri_arc(#{})).

petri_place_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_types:new_petri_place(#{
        place_id => <<"x">>,
        tokens => 1
    })).

petri_place_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_types:new_petri_place(#{})).

petri_transition_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_types:new_petri_transition(#{
        transition_id => <<"x">>,
        label => <<"x">>
    })).

petri_transition_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_types:new_petri_transition(#{})).

