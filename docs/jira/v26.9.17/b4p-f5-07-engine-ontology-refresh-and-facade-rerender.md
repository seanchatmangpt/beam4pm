---
id: b4p-f5-07-engine-ontology-refresh-and-facade-rerender
type: oslc_cm:ChangeRequest
requirement: earl:TestRequirement
dcterms:title: "beam4pm: bpm:EngineOp ontology refresh + ggen re-render of the three ferroplan facades for the reconciled ABI surface"
standing: BLOCKED
branch: chore/engineop-ferroplan-refresh
worktree: (beam4pm main checkout)
created: 2026-09-17T21:40:00Z
source: lib/beam4pm_ferroplan.ex header ("GENERATED by ggen from the admitted bpm:Engine graph (ferroplan). Do not edit.") + ferroplan crates/ferroplan-wasm/src/wasi_abi.rs op surface
depends: b4p-f5-05
---

Read `_CONTEXT.md` first. The engine's surface is ontology: the Elixir,
Erlang, and Gleam facades are ggen renders of `bpm:Engine` / `bpm:EngineOp`
/ `bpm:EngineArg` facts. A hand-edit to any facade is a source-authority
violation; an ABI change without an ontology change is silent drift.

## Scope

1. Diff the **admitted op set** (ontology `bpm:EngineOp` facts for the
   ferroplan engine) against the **reconciled engine's** WASM ABI surface
   (`crates/ferroplan-wasm/src/wasi_abi.rs` ops + their request/response
   shapes, including `PlannerLimits` fields and `FP_*` error codes as
   admitted in the ontology's error-envelope convention
   `bpm:ErrorCollapse_single_key_inspect`).
2. Admit the delta as ontology facts (new ops, changed arg shapes, changed
   default budgets such as the `max_wall_ms` watchdog semantics — note the
   BEAM caller's never-block-past-budget property is load-bearing for a
   wasmex-hosted engine and must be reflected in admitted arg defaults).
3. Re-render the three facades via ggen (no hand edits); verify the renders
   compile and the module headers still carry their GENERATED marker.
4. Extend the Elixir/Erlang/Gleam parity tests to any newly admitted op —
   parity across all three facades, on real fixtures, with the
   error-in-envelope convention asserted (`{:ok, %{"error" => ...}}` with
   the stage code, never a bare `{:error, _}`).

## Gates

- Ontology diff table committed (op | admitted? | engine? | disposition).
- `mix ggen <sync/check per repo convention>` clean; facades re-rendered
  with zero hand-authored diffs outside generated regions.
- `mix test test/beam4pm_ferroplan_facades_test.exs` exit 0 including new
  parity cases.

## History
| ts | standing | branch+SHA | gates+exits | remaining |
|---|---|---|---|---|
| 2026-09-17T21:40:00Z | BLOCKED | — | — | all |
| 2026-09-18T16:55:00Z | PARTIAL_ALIVE | beam4pm branch `chore/engineop-ferroplan-prep @ 5c1fa0c` | Scope 1 done + scope 2 drafted. OP-DIFF.md: 33 ops name-identical admitted<->engine — 0 new, 0 removed (the anticipated hddl_solve/htn_plan/fond_policy were already admitted at ace23e5 — falsifier survived as evidence). Real delta: 1 new FP code (FP_HDDL_ROOT_MISMATCH @6cacbda wasi_abi.rs:527-530), 2 renames (->FP_TIMEOUT/FP_WORKER_PANICKED, now canonical via the e90928d reconciliation), PlannerLimits.max_wall_ms watchdog semantics (WASI adapter forces max_wall_ms=0 for hddl_solve — wasi1 has no threads — so the BEAM caller's 120s heavy timeout is the never-block-past-budget bound; fond_policy honors per-round; htn_plan never consults); session_think caps (FP_LIMIT_SEARCH 1..1e6, FP_LIMIT_MEMORY 1..2048MiB) admitted as doc gaps. ONTOLOGY-DELTA-proposal.ttl drafted (4 revised bpm:opDoc literals, exact admitted idiom, PROPOSAL — admission via ggen sync only). d00dcf8 wasm-crate diff EMPTY — one reconciled ABI surface. REMAINING: apply delta via ontology admission + ggen re-render + 3-way parity tests (items 3-4), now unblocked by the bump branch | admission + render post-bump-merge |
