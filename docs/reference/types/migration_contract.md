# migration_contract

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Versioned migration path with source, target, and rollback identity.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `migration_id` | `string` | yes | Stable migration identity. |
| `from_version` | `string` | yes | Source product version. |
| `to_version` | `string` | yes | Target product version. |
| `rollback_plan` | `string` | yes | Rollback plan identity. |

## Constructors

Each of beam4pm's four language projections exposes a `new_migration_contract/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_migration_contract/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_migration_contract/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_migration_contract` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `migration_contract` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
