# meta_router

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Selects a planner from a portfolio using observed problem characteristics.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `portfolio_id` | `string` | yes | Required portfolio_id for this bounded planner contract. |
| `observation_hash` | `string` | yes | Required observation_hash for deterministic planner evaluation. |
| `selected_planner_id` | `string` | yes | Required selected_planner_id preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_meta_router/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_meta_router/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_meta_router/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_meta_router` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `meta_router` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
