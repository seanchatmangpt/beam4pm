# planning_blackboard_conflict

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Captures mutually incompatible blackboard claims without silent last-writer wins.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `conflict_id` | `string` | yes | Blackboard conflict identity. |
| `claim_set_digest` | `string` | yes | Digest of incompatible claims. |
| `conflict_type` | `string` | yes | Typed conflict classification. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planning_blackboard_conflict/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planning_blackboard_conflict/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planning_blackboard_conflict/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planning_blackboard_conflict` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planning_blackboard_conflict` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
