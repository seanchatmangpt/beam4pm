# canary_decision

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Receipted enterprise canary decision based on an observed rollout consequence.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `canary_decision_id` | `string` | yes | Stable identity of this canary decision observation. |
| `account_id` | `string` | yes | Enterprise account whose canary decision state was observed. |
| `canary_result` | `string` | yes | Observed outcome of the canary qualification. |
| `evidence_digest` | `string` | yes | Digest of the exact evidence supporting this customer-value observation. |
| `observed_at` | `datetime` | yes | ISO8601 instant the enterprise consequence was observed. |

## Constructors

Each of beam4pm's four language projections exposes a `new_canary_decision/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_canary_decision/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_canary_decision/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_canary_decision` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `canary_decision` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
