# goal_set_binding

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Binds explicit valued goals to one admitted planning problem.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `problem_id` | `string` | yes | Planning problem receiving the goals. |
| `goal_set_digest` | `string` | yes | Digest of the ordered goal set. |
| `value_profile` | `string` | yes | Explicit value profile used for selection. |

## Constructors

Each of beam4pm's four language projections exposes a `new_goal_set_binding/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_goal_set_binding/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_goal_set_binding/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_goal_set_binding` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `goal_set_binding` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
