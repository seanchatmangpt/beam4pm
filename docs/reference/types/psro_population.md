# psro_population

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Maintains a population of policies and response oracles for meta-routing.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `population_id` | `string` | yes | Required population_id for this bounded planner contract. |
| `policy_ids` | `string` | yes | Required policy_ids for deterministic planner evaluation. |
| `population_hash` | `string` | yes | Required population_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_psro_population/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_psro_population/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_psro_population/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_psro_population` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `psro_population` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
