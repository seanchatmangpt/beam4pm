# changed_surface_inference

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Infers the exact changed surface between two repository trees.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `prior_tree_sha` | `string` | yes | Prior repository tree identity. |
| `current_tree_sha` | `string` | yes | Current repository tree identity. |
| `changed_surface_digest` | `string` | yes | Digest of normalized changed paths. |
| `inference_status` | `string` | yes | Machine-readable inference standing. |

## Constructors

Each of beam4pm's four language projections exposes a `new_changed_surface_inference/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_changed_surface_inference/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_changed_surface_inference/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_changed_surface_inference` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `changed_surface_inference` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
