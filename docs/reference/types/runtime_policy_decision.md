# runtime_policy_decision

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Records an explicit runtime policy decision before paid workload actuation.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `tenant_id` | `string` | yes | Exact paid tenant identity for this bounded runtime observation. |
| `decision_id` | `string` | yes | Exact runtime subject identity required for independent verification and replay. |
| `policy_hash` | `string` | yes | Immutable evidence identity binding the observed production consequence. |

## Constructors

Each of beam4pm's four language projections exposes a `new_runtime_policy_decision/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_runtime_policy_decision/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_runtime_policy_decision/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_runtime_policy_decision` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `runtime_policy_decision` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
