# planner_capability_profile

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Declares the problem features and guarantees supported by one planner.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `planner_id` | `string` | yes | Required planner_id for this bounded planner contract. |
| `capability_set` | `string` | yes | Required capability_set for deterministic planner evaluation. |
| `profile_hash` | `string` | yes | Required profile_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planner_capability_profile/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planner_capability_profile/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planner_capability_profile/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planner_capability_profile` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planner_capability_profile` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
