# chargeback_rule

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Cost allocation rule mapping usage to a cost center.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `rule_id` | `string` | yes | Stable chargeback rule. |
| `cost_center` | `string` | yes | Charged cost center. |
| `metric_name` | `string` | yes | Allocated usage metric. |
| `rate` | `float` | yes | Internal chargeback rate. |

## Constructors

Each of beam4pm's four language projections exposes a `new_chargeback_rule/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_chargeback_rule/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_chargeback_rule/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_chargeback_rule` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `chargeback_rule` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
