# ocel_object

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

An OCEL 2.0 object: one entity participating in the process.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `object_id` | `string` | yes | Unique object identifier. |
| `object_type` | `string` | yes | The object's type. |
| `attributes` | `map` | no | Arbitrary named current attribute values (a flat snapshot, not the time-indexed history -- see bpm:ocel_attribute and bpm:object_attribute_change for per-timestamp values). |

## Constructors

Each of beam4pm's four language projections exposes a `new_ocel_object/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_ocel_object/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_ocel_object/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_ocel_object` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `ocel_object` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
