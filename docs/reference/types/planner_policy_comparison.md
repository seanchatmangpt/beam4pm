# planner_policy_comparison

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Compares incumbent and challenger policies under one planner and one admitted objective.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `comparison_id` | `string` | yes | Stable comparison identity. |
| `planner_id` | `string` | yes | Planner held constant for comparison. |
| `incumbent_policy_id` | `string` | yes | Incumbent policy identity. |
| `challenger_policy_id` | `string` | yes | Challenger policy identity. |
| `payoff_delta` | `float` | yes | Challenger minus incumbent payoff. |
| `winner_evidence_digest` | `string` | yes | Digest proving the selected winner. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planner_policy_comparison/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planner_policy_comparison/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planner_policy_comparison/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planner_policy_comparison` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planner_policy_comparison` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
