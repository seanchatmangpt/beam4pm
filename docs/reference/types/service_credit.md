# service_credit

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Receiptable credit issued for an SLO breach.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `credit_id` | `string` | yes | Stable service credit. |
| `slo_id` | `string` | yes | Breached SLO. |
| `amount` | `float` | yes | Credit monetary amount. |
| `currency` | `string` | yes | ISO 4217 credit currency. |

## Constructors

Each of beam4pm's four language projections exposes a `new_service_credit/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_service_credit/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_service_credit/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_service_credit` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `service_credit` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
