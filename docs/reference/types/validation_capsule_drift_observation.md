# validation_capsule_drift_observation

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Compares expected and observed capsule digests and emits typed drift.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `expected_digest` | `string` | yes | Admitted capsule digest. |
| `observed_digest` | `string` | yes | Currently resolved capsule digest. |
| `drift_status` | `string` | yes | Machine-readable capsule drift result. |
| `observed_at` | `datetime` | yes | Timestamp of digest comparison. |

## Constructors

Each of beam4pm's four language projections exposes a `new_validation_capsule_drift_observation/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_validation_capsule_drift_observation/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_validation_capsule_drift_observation/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_validation_capsule_drift_observation` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `validation_capsule_drift_observation` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
