# committed_spend

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Term commitment with amount, currency, and expiration.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `commitment_id` | `string` | yes | Stable commitment identity. |
| `amount` | `float` | yes | Committed monetary amount. |
| `currency` | `string` | yes | ISO 4217 commitment currency. |
| `expires_at` | `datetime` | yes | Commitment expiration instant. |

## Constructors

Each of beam4pm's four language projections exposes a `new_committed_spend/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_committed_spend/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_committed_spend/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_committed_spend` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `committed_spend` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
