# wip_limit_gate

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Refuses selections that would exceed the admitted work-in-process ceiling.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `episode_id` | `string` | yes | Required episode_id for this bounded planner contract. |
| `wip_limit` | `string` | yes | Required wip_limit for deterministic planner evaluation. |
| `standing` | `string` | yes | Required standing preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_wip_limit_gate/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_wip_limit_gate/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_wip_limit_gate/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_wip_limit_gate` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `wip_limit_gate` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
