# counterfactual_frontier

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Preserves counterfactual futures for every nondominated option.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `option_set_id` | `string` | yes | Required option_set_id for this bounded planner contract. |
| `world_model_hash` | `string` | yes | Required world_model_hash for deterministic planner evaluation. |
| `frontier_hash` | `string` | yes | Required frontier_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_counterfactual_frontier/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_counterfactual_frontier/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_counterfactual_frontier/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_counterfactual_frontier` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `counterfactual_frontier` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
