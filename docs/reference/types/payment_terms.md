# payment_terms

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Contracted payment window and late-policy identity.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `terms_id` | `string` | yes | Stable payment terms identity. |
| `net_days` | `integer` | yes | Days from invoice to due date. |
| `late_policy` | `string` | yes | Late-payment policy identity. |
| `status` | `atom` | yes | Terms lifecycle standing. |

## Constructors

Each of beam4pm's four language projections exposes a `new_payment_terms/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_payment_terms/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_payment_terms/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_payment_terms` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `payment_terms` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
