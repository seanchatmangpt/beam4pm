# deviation_repair_option

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Generates a reversible repair option for one conformance deviation.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `option_id` | `string` | yes | Deviation repair option identity. |
| `deviation_id` | `string` | yes | Deviation addressed by the option. |
| `repair_plan_digest` | `string` | yes | Digest of the bounded repair plan. |

## Constructors

Each of beam4pm's four language projections exposes a `new_deviation_repair_option/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_deviation_repair_option/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_deviation_repair_option/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_deviation_repair_option` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `deviation_repair_option` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
