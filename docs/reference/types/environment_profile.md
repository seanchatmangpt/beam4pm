# environment_profile

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Deterministic environment profile for demo, POC, or production.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `profile_id` | `string` | yes | Stable environment profile. |
| `environment` | `atom` | yes | Deployment environment class. |
| `region` | `string` | yes | Target deployment region. |
| `configuration_hash` | `string` | yes | Canonical configuration digest. |

## Constructors

Each of beam4pm's four language projections exposes a `new_environment_profile/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_environment_profile/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_environment_profile/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_environment_profile` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `environment_profile` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
