# oc_declare_constraint

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One declarative object-centric behavioral constraint between two activities.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `constraint_id` | `string` | yes | Unique constraint identifier. |
| `source_activity` | `string` | yes | The constraint source activity. |
| `target_activity` | `string` | yes | The constraint target activity. |
| `constraint_type` | `atom` | yes | One of: response | precedence | succession | non_coexistence (standard DECLARE constraint template names). |

## Constructors

Each of beam4pm's four language projections exposes a `new_oc_declare_constraint/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_oc_declare_constraint/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_oc_declare_constraint/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_oc_declare_constraint` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `oc_declare_constraint` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
