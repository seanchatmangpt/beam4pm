# plan_lineage

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Tracks derivation, repair, and supersession across plan generations.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `plan_id` | `string` | yes | Required plan_id for this bounded planner contract. |
| `parent_plan_id` | `string` | yes | Required parent_plan_id for deterministic planner evaluation. |
| `lineage_hash` | `string` | yes | Required lineage_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_plan_lineage/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_plan_lineage/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_plan_lineage/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_plan_lineage` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `plan_lineage` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
