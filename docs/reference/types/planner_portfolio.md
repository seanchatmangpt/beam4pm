# planner_portfolio

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Preserves a diverse set of planners for one bounded planning episode.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `portfolio_id` | `string` | yes | Required portfolio_id for this bounded planner contract. |
| `planner_ids` | `string` | yes | Required planner_ids for deterministic planner evaluation. |
| `diversity_hash` | `string` | yes | Required diversity_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planner_portfolio/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planner_portfolio/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planner_portfolio/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planner_portfolio` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planner_portfolio` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
