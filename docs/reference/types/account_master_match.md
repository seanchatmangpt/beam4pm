# account_master_match

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Admits a source account only when it is bound to one canonical Fortune-5 account by immutable matching evidence; prevents pipeline value from being booked against an ambiguous customer identity.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `source_account_id` | `string` | yes | Required commercial-admission input for account master match. Missing input is a typed refusal. |
| `canonical_account_id` | `string` | yes | Required commercial-admission input for account master match. Missing input is a typed refusal. |
| `match_evidence_hash` | `string` | yes | Immutable evidence identity that makes this admission independently replayable. |

## Constructors

Each of beam4pm's four language projections exposes a `new_account_master_match/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_account_master_match/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_account_master_match/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_account_master_match` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `account_master_match` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
