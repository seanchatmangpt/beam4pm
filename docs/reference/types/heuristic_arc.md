# heuristic_arc

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One dependency-scored candidate arc considered during heuristic-net discovery.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `source_activity` | `string` | yes | The candidate arc source activity. |
| `target_activity` | `string` | yes | The candidate arc target activity. |
| `dependency_measure` | `float` | yes | The computed dependency/confidence score for this candidate arc. |

## Constructors

Each of beam4pm's four language projections exposes a `new_heuristic_arc/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_heuristic_arc/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_heuristic_arc/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_heuristic_arc` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `heuristic_arc` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
