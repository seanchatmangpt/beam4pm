# planning_action

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One PDDL-style planning action with its preconditions and effects.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `action_name` | `string` | yes | The action name. |
| `preconditions` | `list_string` | no | Optional list of precondition fact names. |
| `effects` | `list_string` | no | Optional list of effect fact names this action produces. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planning_action/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planning_action/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planning_action/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planning_action` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planning_action` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
