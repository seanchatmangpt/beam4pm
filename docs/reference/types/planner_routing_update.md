# planner_routing_update

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Routes future planning to a better-supported planner with a declared rollback policy.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `update_id` | `string` | yes | Stable planner-routing update identity. |
| `route_id` | `string` | yes | Planning route being adapted. |
| `prior_planner_id` | `string` | yes | Previously selected planner. |
| `selected_planner_id` | `string` | yes | Evidence-selected future planner. |
| `payoff_basis_digest` | `string` | yes | Digest of comparative payoff evidence. |
| `rollback_policy_id` | `string` | yes | Policy restoring the prior route on regression. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planner_routing_update/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planner_routing_update/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planner_routing_update/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planner_routing_update` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planner_routing_update` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
