# event_type

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

A declared OCEL event type and its attribute schema.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `type_name` | `string` | yes | The event type name. |
| `attribute_names` | `list_string` | no | Optional declared attribute names for events of this type (name-only; per-attribute value types are not yet modeled). |

## Constructors

Each of beam4pm's four language projections exposes a `new_event_type/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_event_type/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_event_type/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_event_type` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `event_type` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
