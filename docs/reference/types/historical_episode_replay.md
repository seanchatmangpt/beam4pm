# historical_episode_replay

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Replays a historical episode against an exact ancestor and preserves any seeded divergence.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `replay_id` | `string` | yes | Stable historical replay identity. |
| `episode_id` | `string` | yes | Historical episode being replayed. |
| `historical_subject_sha` | `string` | yes | Exact historical subject commit. |
| `replay_seed` | `string` | yes | Explicit deterministic evaluation seed. |
| `divergence_code` | `string` | yes | Observed divergence or exact-match result. |

## Constructors

Each of beam4pm's four language projections exposes a `new_historical_episode_replay/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_historical_episode_replay/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_historical_episode_replay/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_historical_episode_replay` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `historical_episode_replay` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
