# cost_latency_reliability_tradeoff

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Evaluates candidate policy utility across cost, latency, and reliability without collapsing dimensions.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `assessment_id` | `string` | yes | Stable tradeoff assessment identity. |
| `candidate_policy_id` | `string` | yes | Candidate policy evaluated. |
| `cost_score` | `float` | yes | Normalized execution cost. |
| `latency_score` | `float` | yes | Normalized execution latency. |
| `reliability_score` | `float` | yes | Observed reliability. |
| `utility_score` | `float` | yes | Objective-bound aggregate utility. |

## Constructors

Each of beam4pm's four language projections exposes a `new_cost_latency_reliability_tradeoff/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_cost_latency_reliability_tradeoff/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_cost_latency_reliability_tradeoff/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_cost_latency_reliability_tradeoff` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `cost_latency_reliability_tradeoff` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
