# event_triggered_planning

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Creates a bounded planning episode from an admitted world event.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `event_id` | `string` | yes | Required event_id for this bounded planner contract. |
| `world_state_hash` | `string` | yes | Required world_state_hash for deterministic planner evaluation. |
| `episode_id` | `string` | yes | Required episode_id preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_event_triggered_planning/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_event_triggered_planning/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_event_triggered_planning/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_event_triggered_planning` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `event_triggered_planning` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
