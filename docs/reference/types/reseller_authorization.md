# reseller_authorization

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

SKU-scoped reseller authorization with standing.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `authorization_id` | `string` | yes | Stable authorization identity. |
| `reseller_id` | `string` | yes | Authorized reseller. |
| `sku` | `string` | yes | Authorized sellable SKU. |
| `status` | `atom` | yes | Authorization standing. |

## Constructors

Each of beam4pm's four language projections exposes a `new_reseller_authorization/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_reseller_authorization/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_reseller_authorization/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_reseller_authorization` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `reseller_authorization` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
