# planning_state

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One planning-search state as a bounded set of true facts.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `state_id` | `string` | yes | Unique state identifier. |
| `facts` | `list_string` | yes | List of fact names true in this state. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planning_state/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planning_state/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planning_state/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planning_state` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planning_state` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
