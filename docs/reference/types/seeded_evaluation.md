# seeded_evaluation

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Produces a reproducible policy evaluation bound to an explicit seed and dataset.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `evaluation_id` | `string` | yes | Stable seeded evaluation identity. |
| `seed` | `string` | yes | Explicit deterministic seed. |
| `dataset_digest` | `string` | yes | Immutable evaluation dataset digest. |
| `policy_id` | `string` | yes | Policy evaluated. |
| `score` | `float` | yes | Observed evaluation score. |
| `replay_digest` | `string` | yes | Digest of deterministic replay output. |

## Constructors

Each of beam4pm's four language projections exposes a `new_seeded_evaluation/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_seeded_evaluation/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_seeded_evaluation/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_seeded_evaluation` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `seeded_evaluation` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
