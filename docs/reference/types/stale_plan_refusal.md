# stale_plan_refusal

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Refuses execution when subject, pack, policy, or world identity has drifted.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `plan_id` | `string` | yes | Required plan_id for this bounded planner contract. |
| `admitted_preimage_hash` | `string` | yes | Required admitted_preimage_hash for deterministic planner evaluation. |
| `observed_preimage_hash` | `string` | yes | Required observed_preimage_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_stale_plan_refusal/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_stale_plan_refusal/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_stale_plan_refusal/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_stale_plan_refusal` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `stale_plan_refusal` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
