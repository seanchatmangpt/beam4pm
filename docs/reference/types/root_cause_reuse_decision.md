# root_cause_reuse_decision

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Admits or refuses reuse of a historical root cause using measured similarity evidence.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `decision_id` | `string` | yes | Stable reuse decision identity. |
| `pattern_id` | `string` | yes | Historical root-cause pattern considered. |
| `current_failure_id` | `string` | yes | Current failure being diagnosed. |
| `similarity_score` | `float` | yes | Measured causal similarity. |
| `applicability_evidence_digest` | `string` | yes | Evidence proving or refusing applicability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_root_cause_reuse_decision/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_root_cause_reuse_decision/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_root_cause_reuse_decision/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_root_cause_reuse_decision` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `root_cause_reuse_decision` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
