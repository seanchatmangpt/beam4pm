# deployment_entitlement

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Paid right to deploy an edition into an environment.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `entitlement_id` | `string` | yes | Stable deployment entitlement. |
| `tenant_id` | `string` | yes | Entitled tenant. |
| `profile_id` | `string` | yes | Allowed environment profile. |
| `valid_until` | `datetime` | yes | Deployment right expiration. |

## Constructors

Each of beam4pm's four language projections exposes a `new_deployment_entitlement/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_deployment_entitlement/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_deployment_entitlement/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_deployment_entitlement` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `deployment_entitlement` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
