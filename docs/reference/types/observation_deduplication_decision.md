# observation_deduplication_decision

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Records an executable accept-or-duplicate decision for an observation digest.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `event_id` | `string` | yes | Observed event identity. |
| `event_digest` | `string` | yes | Canonical event digest. |
| `dedup_key` | `string` | yes | Deterministic deduplication key. |
| `decision` | `string` | yes | Machine-readable deduplication decision. |

## Constructors

Each of beam4pm's four language projections exposes a `new_observation_deduplication_decision/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_observation_deduplication_decision/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_observation_deduplication_decision/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_observation_deduplication_decision` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `observation_deduplication_decision` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
