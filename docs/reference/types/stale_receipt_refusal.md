# stale_receipt_refusal

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Produces typed refusal evidence when a runtime receipt is outside its admitted freshness window.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `tenant_id` | `string` | yes | Exact paid tenant identity for this bounded runtime observation. |
| `receipt_id` | `string` | yes | Exact runtime subject identity required for independent verification and replay. |
| `refusal_hash` | `string` | yes | Immutable evidence identity binding the observed production consequence. |

## Constructors

Each of beam4pm's four language projections exposes a `new_stale_receipt_refusal/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_stale_receipt_refusal/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_stale_receipt_refusal/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_stale_receipt_refusal` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `stale_receipt_refusal` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
