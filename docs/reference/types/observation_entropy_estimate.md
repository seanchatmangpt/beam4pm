# observation_entropy_estimate

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Records entropy of a state vector using an explicit estimation method.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `state_vector_id` | `string` | yes | Measured state-vector identity. |
| `entropy_method` | `string` | yes | Named entropy estimation method. |
| `entropy_value` | `float` | yes | Observed entropy estimate. |
| `observed_at` | `datetime` | yes | Timestamp of entropy estimation. |

## Constructors

Each of beam4pm's four language projections exposes a `new_observation_entropy_estimate/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_observation_entropy_estimate/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_observation_entropy_estimate/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_observation_entropy_estimate` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `observation_entropy_estimate` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
