# verification_depth_update

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Raises or lowers verification depth from observed risk without weakening the immutable minimum court.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `update_id` | `string` | yes | Stable verification-depth update identity. |
| `verifier_id` | `string` | yes | Verifier whose depth is adapted. |
| `prior_depth` | `integer` | yes | Prior verification depth. |
| `new_depth` | `integer` | yes | Risk-adjusted verification depth. |
| `risk_signal_digest` | `string` | yes | Digest of observed risk evidence. |
| `minimum_depth` | `integer` | yes | Immutable minimum acceptance depth. |

## Constructors

Each of beam4pm's four language projections exposes a `new_verification_depth_update/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_verification_depth_update/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_verification_depth_update/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_verification_depth_update` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `verification_depth_update` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
