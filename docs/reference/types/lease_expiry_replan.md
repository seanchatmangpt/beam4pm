# lease_expiry_replan

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Invalidates stale leased plans and returns work to the lawful option frontier.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `replan_id` | `string` | yes | Lease-expiry replan identity. |
| `expired_lease_id` | `string` | yes | Lease whose authority expired. |
| `returned_frontier_digest` | `string` | yes | Digest of restored work options. |

## Constructors

Each of beam4pm's four language projections exposes a `new_lease_expiry_replan/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_lease_expiry_replan/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_lease_expiry_replan/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_lease_expiry_replan` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `lease_expiry_replan` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
