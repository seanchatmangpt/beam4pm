# path_schema_query

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

A discovery query bounding source/target types and maximum schema length.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `source_type` | `string` | yes | The query source type name. |
| `target_type` | `string` | yes | The query target type name. |
| `max_length` | `integer` | yes | Maximum number of hops to search. |

## Constructors

Each of beam4pm's four language projections exposes a `new_path_schema_query/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_path_schema_query/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_path_schema_query/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_path_schema_query` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `path_schema_query` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
