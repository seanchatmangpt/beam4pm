# regression_detector

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Detects candidate regression against a fixed baseline and metric before promotion.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `detection_id` | `string` | yes | Stable regression detection identity. |
| `baseline_policy_id` | `string` | yes | Admitted baseline policy. |
| `candidate_policy_id` | `string` | yes | Candidate policy under test. |
| `metric_id` | `string` | yes | Metric defining regression. |
| `regression_delta` | `float` | yes | Measured candidate regression. |
| `evidence_digest` | `string` | yes | Independent comparison evidence. |

## Constructors

Each of beam4pm's four language projections exposes a `new_regression_detector/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_regression_detector/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_regression_detector/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_regression_detector` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `regression_detector` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
