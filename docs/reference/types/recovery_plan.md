# recovery_plan

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Executable account recovery plan with a measured recovery interval.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `recovery_plan_id` | `string` | yes | Stable identity of this recovery plan observation. |
| `account_id` | `string` | yes | Enterprise account whose recovery plan state was observed. |
| `recovery_time_hours` | `float` | yes | Verified hours from failure to recovered customer consequence. |
| `evidence_digest` | `string` | yes | Digest of the exact evidence supporting this customer-value observation. |
| `observed_at` | `datetime` | yes | ISO8601 instant the enterprise consequence was observed. |

## Constructors

Each of beam4pm's four language projections exposes a `new_recovery_plan/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_recovery_plan/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_recovery_plan/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_recovery_plan` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `recovery_plan` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
