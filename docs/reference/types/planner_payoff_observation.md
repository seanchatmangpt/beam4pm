# planner_payoff_observation

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Records objective-specific planner payoff from independent episode evidence.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `observation_id` | `string` | yes | Stable planner payoff observation identity. |
| `planner_id` | `string` | yes | Planner whose realized payoff is measured. |
| `objective_id` | `string` | yes | Objective used for evaluation. |
| `payoff` | `float` | yes | Observed numeric payoff. |
| `evidence_digest` | `string` | yes | Independent evidence supporting the payoff. |
| `episode_id` | `string` | yes | Episode that realized the payoff. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planner_payoff_observation/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planner_payoff_observation/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planner_payoff_observation/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planner_payoff_observation` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planner_payoff_observation` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
