# process_variant

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One distinct activity-sequence variant observed in a log, with its frequency.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `variant_id` | `string` | yes | Unique variant identifier. |
| `activity_sequence` | `list_string` | yes | Ordered list of activity names making up this variant. |
| `frequency` | `integer` | yes | Number of traces observed with exactly this activity sequence. |

## Constructors

Each of beam4pm's four language projections exposes a `new_process_variant/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_process_variant/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_process_variant/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_process_variant` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `process_variant` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
