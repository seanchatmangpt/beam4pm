# curriculum_generation

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Generates an ordered, diverse learning curriculum from admitted gaps and executable falsifiers.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `curriculum_id` | `string` | yes | Stable generated curriculum identity. |
| `source_gap_set_digest` | `string` | yes | Digest of admitted capability gaps. |
| `ordered_hypotheses_digest` | `string` | yes | Digest of ordered hypothesis identities. |
| `diversity_score` | `float` | yes | Semantic diversity across the curriculum. |
| `falsifier_coverage` | `float` | yes | Share of hypotheses with executable falsifiers. |

## Constructors

Each of beam4pm's four language projections exposes a `new_curriculum_generation/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_curriculum_generation/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_curriculum_generation/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_curriculum_generation` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `curriculum_generation` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
