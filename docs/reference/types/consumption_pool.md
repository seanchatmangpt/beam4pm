# consumption_pool

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Shared enterprise consumption pool with unit and balance.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `pool_id` | `string` | yes | Stable pool identity. |
| `account_id` | `string` | yes | Owning account. |
| `unit` | `string` | yes | Metered consumption unit. |
| `remaining_quantity` | `float` | yes | Remaining pooled quantity. |

## Constructors

Each of beam4pm's four language projections exposes a `new_consumption_pool/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_consumption_pool/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_consumption_pool/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_consumption_pool` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `consumption_pool` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
