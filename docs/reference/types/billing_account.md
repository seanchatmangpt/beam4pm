# billing_account

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Bill-to account with currency and invoice delivery profile.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `billing_account_id` | `string` | yes | Stable bill-to account identity. |
| `account_id` | `string` | yes | Commercial account owner. |
| `currency` | `string` | yes | ISO 4217 billing currency. |
| `invoice_profile` | `string` | yes | Invoice delivery profile identity. |

## Constructors

Each of beam4pm's four language projections exposes a `new_billing_account/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_billing_account/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_billing_account/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_billing_account` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `billing_account` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
