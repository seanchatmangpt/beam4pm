# action_eligibility_decision

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Calculates whether an action is lawful from constraints, authority, and receipts.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `decision_id` | `string` | yes | Eligibility decision identity. |
| `action_id` | `string` | yes | Candidate action evaluated. |
| `standing` | `string` | yes | Typed eligible or refused standing. |

## Constructors

Each of beam4pm's four language projections exposes a `new_action_eligibility_decision/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_action_eligibility_decision/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_action_eligibility_decision/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_action_eligibility_decision` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `action_eligibility_decision` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
