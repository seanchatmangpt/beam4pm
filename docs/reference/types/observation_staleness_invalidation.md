# observation_staleness_invalidation

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Invalidates stale state and declares whether replacement evidence is required.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `observation_id` | `string` | yes | Stale observation identity. |
| `invalidated_at` | `datetime` | yes | Time invalidation became effective. |
| `staleness_reason` | `string` | yes | Typed reason for invalidation. |
| `replacement_required` | `boolean` | yes | Whether new evidence must be acquired. |

## Constructors

Each of beam4pm's four language projections exposes a `new_observation_staleness_invalidation/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_observation_staleness_invalidation/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_observation_staleness_invalidation/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_observation_staleness_invalidation` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `observation_staleness_invalidation` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
