# authority_ceiling

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Caps every candidate action at the exact authority grant available to its subject.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `action_id` | `string` | yes | Required action_id for this bounded planner contract. |
| `grant_id` | `string` | yes | Required grant_id for deterministic planner evaluation. |
| `ceiling` | `string` | yes | Required ceiling preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_authority_ceiling/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_authority_ceiling/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_authority_ceiling/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_authority_ceiling` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `authority_ceiling` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
