# pareto_filter

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Removes strictly dominated options while preserving incomparable alternatives.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `option_set_id` | `string` | yes | Required option_set_id for this bounded planner contract. |
| `objective_set_hash` | `string` | yes | Required objective_set_hash for deterministic planner evaluation. |
| `pareto_set_hash` | `string` | yes | Required pareto_set_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_pareto_filter/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_pareto_filter/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_pareto_filter/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_pareto_filter` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `pareto_filter` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
