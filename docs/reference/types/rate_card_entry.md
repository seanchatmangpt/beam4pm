# rate_card_entry

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Sellable unit price bound to an immutable price book.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `rate_card_entry_id` | `string` | yes | Stable identity of this rate card entry contract. |
| `account_id` | `string` | yes | Enterprise account governed by this commercial contract. |
| `unit_price` | `float` | yes | Admitted unit price; currency is supplied by the linked currency policy. |
| `evidence_digest` | `string` | yes | Digest of the exact evidence admitting this commercial consequence. |
| `effective_at` | `datetime` | yes | ISO8601 instant at which this contract becomes effective. |

## Constructors

Each of beam4pm's four language projections exposes a `new_rate_card_entry/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_rate_card_entry/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_rate_card_entry/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_rate_card_entry` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `rate_card_entry` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
