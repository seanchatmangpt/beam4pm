# powl_projection

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Binds a plan candidate to an exact POWL process-plan projection.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `plan_id` | `string` | yes | Required plan_id for this bounded planner contract. |
| `powl_hash` | `string` | yes | Required powl_hash for deterministic planner evaluation. |
| `projection_receipt_hash` | `string` | yes | Required projection_receipt_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_powl_projection/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_powl_projection/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_powl_projection/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_powl_projection` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `powl_projection` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
