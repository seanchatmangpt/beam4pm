# resource_allocation

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One recorded assignment of a resource to an activity occurrence.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `resource_id` | `string` | yes | Identifier of the assigned resource. |
| `activity` | `string` | yes | The activity the resource was assigned to. |
| `event_id` | `string` | yes | Identifier of the specific event occurrence. |

## Constructors

Each of beam4pm's four language projections exposes a `new_resource_allocation/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_resource_allocation/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_resource_allocation/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_resource_allocation` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `resource_allocation` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
