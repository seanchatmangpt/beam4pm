# propagation_score

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Scores downstream consequences across the dependency and capability graph.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `option_id` | `string` | yes | Required option_id for this bounded planner contract. |
| `graph_hash` | `string` | yes | Required graph_hash for deterministic planner evaluation. |
| `score` | `string` | yes | Required score preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_propagation_score/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_propagation_score/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_propagation_score/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_propagation_score` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `propagation_score` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
