# petri_arc

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

A weighted arc connecting a place and a transition (either direction).

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `source_id` | `string` | yes | Identifier of the arc's source node. |
| `target_id` | `string` | yes | Identifier of the arc's target node. |
| `weight` | `integer` | yes | Arc weight (tokens consumed/produced per firing). |

## Constructors

Each of beam4pm's four language projections exposes a `new_petri_arc/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_petri_arc/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_petri_arc/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_petri_arc` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `petri_arc` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
