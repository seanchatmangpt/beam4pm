# crown_fanin_convergence

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Requires every admitted upstream parent crown before a fan-in consumer can converge.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `propagation_id` | `string` | yes | Unique autonomic propagation attempt identity. |
| `subject_sha` | `string` | yes | Exact admitted Git subject SHA. |
| `fanin_set_digest` | `string` | yes | Digest of the complete admitted fan-in crown set. |
| `receipt_digest` | `string` | yes | Immutable digest of this capability's exact-subject qualification receipt. |

## Constructors

Each of beam4pm's four language projections exposes a `new_crown_fanin_convergence/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_crown_fanin_convergence/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_crown_fanin_convergence/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_crown_fanin_convergence` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `crown_fanin_convergence` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
