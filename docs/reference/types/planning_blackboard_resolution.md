# planning_blackboard_resolution

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Records evidence-based resolution while preserving losing lawful claims.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `resolution_id` | `string` | yes | Conflict resolution identity. |
| `conflict_id` | `string` | yes | Conflict being resolved. |
| `preserved_option_digest` | `string` | yes | Digest of preserved unselected claims. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planning_blackboard_resolution/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planning_blackboard_resolution/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planning_blackboard_resolution/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planning_blackboard_resolution` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planning_blackboard_resolution` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
