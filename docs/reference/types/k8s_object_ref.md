# k8s_object_ref

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

A reference to one Kubernetes object observed in the runtime topology.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `kind` | `string` | yes | The Kubernetes object kind (e.g. Pod, Deployment). |
| `name` | `string` | yes | The object name. |
| `namespace` | `string` | no | Optional object namespace (absent for cluster-scoped kinds such as Node, PersistentVolume, ClusterRole, or Namespace itself). |

## Constructors

Each of beam4pm's four language projections exposes a `new_k8s_object_ref/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_k8s_object_ref/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_k8s_object_ref/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_k8s_object_ref` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `k8s_object_ref` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
