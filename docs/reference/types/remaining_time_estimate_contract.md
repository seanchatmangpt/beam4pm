# remaining_time_estimate_contract

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Binds a calibrated completion-time estimate to an exact planning case.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `estimate_id` | `string` | yes | Remaining-time estimate identity. |
| `case_state_digest` | `string` | yes | Digest of current case state. |
| `interval_digest` | `string` | yes | Digest of calibrated time interval. |

## Constructors

Each of beam4pm's four language projections exposes a `new_remaining_time_estimate_contract/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_remaining_time_estimate_contract/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_remaining_time_estimate_contract/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_remaining_time_estimate_contract` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `remaining_time_estimate_contract` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
