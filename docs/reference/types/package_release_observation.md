# package_release_observation

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Observes a package version together with its immutable distribution digest.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `package_id` | `string` | yes | Stable package identity. |
| `version` | `string` | yes | Observed published version. |
| `immutable_digest` | `string` | yes | Immutable package content digest. |
| `observed_at` | `datetime` | yes | Timestamp of package observation. |

## Constructors

Each of beam4pm's four language projections exposes a `new_package_release_observation/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_package_release_observation/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_package_release_observation/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_package_release_observation` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `package_release_observation` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
