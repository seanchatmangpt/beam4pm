# crown_resume_token

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Binds resumed propagation to its checkpoint and current dependency graph.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `propagation_id` | `string` | yes | Unique autonomic propagation attempt identity. |
| `subject_sha` | `string` | yes | Exact admitted Git subject SHA. |
| `resume_token_digest` | `string` | yes | Digest of the validated resume token. |
| `receipt_digest` | `string` | yes | Immutable digest of this capability's exact-subject qualification receipt. |

## Constructors

Each of beam4pm's four language projections exposes a `new_crown_resume_token/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_crown_resume_token/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_crown_resume_token/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_crown_resume_token` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `crown_resume_token` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
