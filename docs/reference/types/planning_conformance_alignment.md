# planning_conformance_alignment

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Aligns an observed planning trace to its admitted policy and process model.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `alignment_id` | `string` | yes | Conformance alignment identity. |
| `trace_digest` | `string` | yes | Digest of observed planning events. |
| `model_digest` | `string` | yes | Digest of the admitted model. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planning_conformance_alignment/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planning_conformance_alignment/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planning_conformance_alignment/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planning_conformance_alignment` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planning_conformance_alignment` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
