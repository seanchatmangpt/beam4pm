# alignment_move

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One step of a conformance-checking alignment between log and model.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `move_type` | `atom` | yes | One of: sync | model | log | silent. |
| `cost` | `integer` | yes | Non-negative cost assigned to this move. |

## Constructors

Each of beam4pm's four language projections exposes a `new_alignment_move/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_alignment_move/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_alignment_move/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_alignment_move` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `alignment_move` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
