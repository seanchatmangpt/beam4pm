# ramp_commitment

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Phased commercial commitment with effective window.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `ramp_id` | `string` | yes | Stable ramp identity. |
| `phase` | `integer` | yes | Ordered ramp phase. |
| `committed_amount` | `float` | yes | Phase commitment amount. |
| `effective_at` | `datetime` | yes | Phase activation instant. |

## Constructors

Each of beam4pm's four language projections exposes a `new_ramp_commitment/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_ramp_commitment/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_ramp_commitment/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_ramp_commitment` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `ramp_commitment` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
