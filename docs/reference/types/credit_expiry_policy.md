# credit_expiry_policy

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Deterministic expiration of prepaid commercial credits.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `credit_expiry_policy_id` | `string` | yes | Stable identity of this credit expiry policy contract. |
| `account_id` | `string` | yes | Enterprise account governed by this commercial contract. |
| `expires_at` | `datetime` | yes | Exact expiration instant for the admitted credit pool. |
| `evidence_digest` | `string` | yes | Digest of the exact evidence admitting this commercial consequence. |
| `effective_at` | `datetime` | yes | ISO8601 instant at which this contract becomes effective. |

## Constructors

Each of beam4pm's four language projections exposes a `new_credit_expiry_policy/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_credit_expiry_policy/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_credit_expiry_policy/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_credit_expiry_policy` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `credit_expiry_policy` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
