# sojourn_time

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

The total dwell/duration time an object was associated with one event/activity (object-centric sojourn time -- a duration metric, distinct from bpm:sync_time's wait-for-another-object metric).

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `object_id` | `string` | yes | Identifier of the object this measurement is for. |
| `event_type` | `string` | yes | The event type/activity this sojourn measurement is at. |
| `seconds` | `float` | yes | Sojourn duration in seconds. |

## Constructors

Each of beam4pm's four language projections exposes a `new_sojourn_time/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_sojourn_time/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_sojourn_time/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_sojourn_time` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `sojourn_time` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
