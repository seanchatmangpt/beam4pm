# reversibility_weight

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Weights reversible actions above irreversible ones until evidence justifies commitment.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `action_id` | `string` | yes | Required action_id for this bounded planner contract. |
| `rollback_id` | `string` | yes | Required rollback_id for deterministic planner evaluation. |
| `weight` | `string` | yes | Required weight preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_reversibility_weight/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_reversibility_weight/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_reversibility_weight/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_reversibility_weight` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `reversibility_weight` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
