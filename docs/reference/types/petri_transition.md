# petri_transition

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

A transition in a Petri net.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `transition_id` | `string` | yes | Unique transition identifier. |
| `label` | `string` | no | Optional human-readable/activity label (silent transition if absent). |

## Constructors

Each of beam4pm's four language projections exposes a `new_petri_transition/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_petri_transition/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_petri_transition/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_petri_transition` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `petri_transition` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
