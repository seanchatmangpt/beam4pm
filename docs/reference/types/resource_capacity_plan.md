# resource_capacity_plan

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Allocates finite compute, time, and concurrency capacity across lawful options.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `episode_id` | `string` | yes | Required episode_id for this bounded planner contract. |
| `resource_pool_hash` | `string` | yes | Required resource_pool_hash for deterministic planner evaluation. |
| `allocation_hash` | `string` | yes | Required allocation_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_resource_capacity_plan/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_resource_capacity_plan/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_resource_capacity_plan/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_resource_capacity_plan` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `resource_capacity_plan` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
