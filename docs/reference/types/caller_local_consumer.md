# caller_local_consumer

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Preserves each caller-local consumer as an independently planned exact subject.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `consumer_id` | `string` | yes | Required consumer_id for this bounded planner contract. |
| `subject_sha` | `string` | yes | Required subject_sha for deterministic planner evaluation. |
| `consumer_hash` | `string` | yes | Required consumer_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_caller_local_consumer/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_caller_local_consumer/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_caller_local_consumer/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_caller_local_consumer` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `caller_local_consumer` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
