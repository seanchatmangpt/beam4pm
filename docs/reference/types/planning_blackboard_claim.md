# planning_blackboard_claim

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Claims bounded planning work with an explicit owner and expiry.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `claim_id` | `string` | yes | Blackboard claim identity. |
| `work_item_id` | `string` | yes | Bounded work item claimed. |
| `owner_subject_sha` | `string` | yes | Exact caller-local owner subject. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planning_blackboard_claim/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planning_blackboard_claim/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planning_blackboard_claim/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planning_blackboard_claim` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planning_blackboard_claim` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
