# receipt_learning_compilation

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Compiles only verified receipt-chain members into a deterministic learning corpus.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `compilation_id` | `string` | yes | Stable receipt compilation identity. |
| `receipt_chain_head` | `string` | yes | Digest of the admitted receipt chain head. |
| `admitted_receipt_count` | `integer` | yes | Number of verified receipts admitted. |
| `rejected_receipt_count` | `integer` | yes | Number of receipts refused by validation. |
| `learning_digest` | `string` | yes | Digest of the compiled learning corpus. |

## Constructors

Each of beam4pm's four language projections exposes a `new_receipt_learning_compilation/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_receipt_learning_compilation/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_receipt_learning_compilation/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_receipt_learning_compilation` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `receipt_learning_compilation` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
