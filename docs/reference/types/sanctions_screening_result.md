# sanctions_screening_result

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Requires a named sanctions screening result before accepting the commercial counterparty.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `account_id` | `string` | yes | Required sanctions screening result input; omission is an executable typed refusal, never an inferred approval. |
| `screening_id` | `string` | yes | Required sanctions screening result input; omission is an executable typed refusal, never an inferred approval. |
| `screening_result` | `string` | yes | Immutable decision or evidence identity used to verify and replay this bounded commercial admission. |

## Constructors

Each of beam4pm's four language projections exposes a `new_sanctions_screening_result/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_sanctions_screening_result/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_sanctions_screening_result/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_sanctions_screening_result` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `sanctions_screening_result` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
