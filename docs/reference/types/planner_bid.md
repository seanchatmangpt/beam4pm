# planner_bid

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Bids for bounded work using declared cost, confidence, and capsule identity.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `bid_id` | `string` | yes | Planner bid identity. |
| `work_item_id` | `string` | yes | Bounded work item being bid. |
| `score_digest` | `string` | yes | Digest of cost, confidence, and evidence scores. |

## Constructors

Each of beam4pm's four language projections exposes a `new_planner_bid/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_planner_bid/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_planner_bid/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_planner_bid` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `planner_bid` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
