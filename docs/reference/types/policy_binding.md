# policy_binding

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Binds one planner to parameters, objective, observations, and action projection.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `policy_id` | `string` | yes | Required policy_id for this bounded planner contract. |
| `planner_id` | `string` | yes | Required planner_id for deterministic planner evaluation. |
| `policy_hash` | `string` | yes | Required policy_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_policy_binding/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_policy_binding/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_policy_binding/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_policy_binding` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `policy_binding` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
