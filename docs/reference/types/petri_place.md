# petri_place

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

A place in a Petri net, holding a non-negative token count.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `place_id` | `string` | yes | Unique place identifier. |
| `tokens` | `integer` | yes | Current token count (marking) at this place. |

## Constructors

Each of beam4pm's four language projections exposes a `new_petri_place/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_petri_place/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_petri_place/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_petri_place` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `petri_place` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
