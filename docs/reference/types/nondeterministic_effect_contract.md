# nondeterministic_effect_contract

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Makes nondeterministic effects explicit for FOND planning and replay.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `action_id` | `string` | yes | Action whose effects are nondeterministic. |
| `effect_set_digest` | `string` | yes | Digest of the alternative effect sets. |
| `selection_semantics` | `string` | yes | Environment selection semantics. |

## Constructors

Each of beam4pm's four language projections exposes a `new_nondeterministic_effect_contract/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_nondeterministic_effect_contract/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_nondeterministic_effect_contract/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_nondeterministic_effect_contract` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `nondeterministic_effect_contract` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
