# capsule_availability

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Records whether the exact validation capsule is available before qualification.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `capsule_id` | `string` | yes | Required capsule_id for this bounded planner contract. |
| `capsule_digest` | `string` | yes | Required capsule_digest for deterministic planner evaluation. |
| `availability` | `string` | yes | Required availability preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_capsule_availability/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_capsule_availability/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_capsule_availability/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_capsule_availability` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `capsule_availability` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
