# root_cause_pattern

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Stores a reusable root-cause pattern only after independent reproduction.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `pattern_id` | `string` | yes | Stable root-cause pattern identity. |
| `failure_class` | `string` | yes | Failure class explained by the pattern. |
| `causal_graph_digest` | `string` | yes | Digest of the causal graph. |
| `confirmed_reproducer_digest` | `string` | yes | Independent reproducer evidence. |
| `reusable_scope` | `string` | yes | Bounded scope where reuse is admissible. |

## Constructors

Each of beam4pm's four language projections exposes a `new_root_cause_pattern/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_root_cause_pattern/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_root_cause_pattern/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_root_cause_pattern` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `root_cause_pattern` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
