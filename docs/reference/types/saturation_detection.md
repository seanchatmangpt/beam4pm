# saturation_detection

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Detects diminishing learning returns from measured gain, novelty, and coverage evidence.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `detection_id` | `string` | yes | Stable saturation detection identity. |
| `curriculum_id` | `string` | yes | Curriculum being measured. |
| `recent_gain` | `float` | yes | Recent objective improvement. |
| `gain_slope` | `float` | yes | Trend in marginal learning gain. |
| `novelty_floor` | `float` | yes | Minimum admitted novelty. |
| `coverage_digest` | `string` | yes | Digest of evaluated capability coverage. |

## Constructors

Each of beam4pm's four language projections exposes a `new_saturation_detection/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_saturation_detection/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_saturation_detection/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_saturation_detection` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `saturation_detection` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
