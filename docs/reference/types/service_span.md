# service_span

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One OpenTelemetry-style tracing span observed for a service call.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `span_id` | `string` | yes | Unique span identifier. |
| `service_name` | `string` | yes | Name of the service that produced this span. |
| `duration_ms` | `integer` | yes | Span duration in milliseconds. |
| `parent_span_id` | `string` | no | Optional identifier of the parent span. |

## Constructors

Each of beam4pm's four language projections exposes a `new_service_span/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_service_span/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_service_span/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_service_span` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `service_span` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
