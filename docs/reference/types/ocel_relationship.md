# ocel_relationship

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

A qualified relationship linking an event or object to an object.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `qualifier` | `string` | yes | The relationship qualifier/role name. |
| `object_id` | `string` | yes | The related object's identifier. |

## Constructors

Each of beam4pm's four language projections exposes a `new_ocel_relationship/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_ocel_relationship/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_ocel_relationship/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_ocel_relationship` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `ocel_relationship` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
