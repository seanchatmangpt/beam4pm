# entitlement_grant

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Receiptable grant of a capability to a tenant.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `grant_id` | `string` | yes | Stable entitlement grant. |
| `tenant_id` | `string` | yes | Receiving tenant. |
| `capability_id` | `string` | yes | Granted capability. |
| `valid_until` | `datetime` | yes | Grant expiration instant. |

## Constructors

Each of beam4pm's four language projections exposes a `new_entitlement_grant/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_entitlement_grant/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_entitlement_grant/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_entitlement_grant` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `entitlement_grant` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
