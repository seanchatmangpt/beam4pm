# toolchain_identity_observation

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Observes exact tool name, version, and executable bytes used for validation.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `tool_name` | `string` | yes | Observed tool identity. |
| `tool_version` | `string` | yes | Observed tool version. |
| `executable_digest` | `string` | yes | Digest of executed tool bytes. |
| `observed_at` | `datetime` | yes | Timestamp of toolchain observation. |

## Constructors

Each of beam4pm's four language projections exposes a `new_toolchain_identity_observation/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_toolchain_identity_observation/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_toolchain_identity_observation/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_toolchain_identity_observation` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `toolchain_identity_observation` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
