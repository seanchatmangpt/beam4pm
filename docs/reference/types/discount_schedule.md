# discount_schedule

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Volume discount tier with threshold and percentage.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `schedule_id` | `string` | yes | Stable discount schedule. |
| `threshold` | `float` | yes | Quantity activating the tier. |
| `discount_percent` | `float` | yes | Percentage discount. |
| `currency` | `string` | yes | Applicable settlement currency. |

## Constructors

Each of beam4pm's four language projections exposes a `new_discount_schedule/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_discount_schedule/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_discount_schedule/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_discount_schedule` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `discount_schedule` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
