# queue_snapshot

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One point-in-time observation of a queue depth.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `queue_name` | `string` | yes | Name of the observed queue. |
| `depth` | `integer` | yes | Observed queue depth at this point in time. |
| `observed_at` | `datetime` | yes | ISO8601 timestamp of this observation. |

## Constructors

Each of beam4pm's four language projections exposes a `new_queue_snapshot/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_queue_snapshot/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_queue_snapshot/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_queue_snapshot` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `queue_snapshot` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
