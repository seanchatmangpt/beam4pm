# second_run_identity_objective

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Makes byte-identical second manufacture a first-class planning objective.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `subject_id` | `string` | yes | Required subject_id for this bounded planner contract. |
| `first_tree_hash` | `string` | yes | Required first_tree_hash for deterministic planner evaluation. |
| `second_tree_hash` | `string` | yes | Required second_tree_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_second_run_identity_objective/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_second_run_identity_objective/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_second_run_identity_objective/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_second_run_identity_objective` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `second_run_identity_objective` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
