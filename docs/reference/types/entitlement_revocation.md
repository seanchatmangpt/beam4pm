# entitlement_revocation

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Receiptable capability revocation with reason and time.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `revocation_id` | `string` | yes | Stable revocation identity. |
| `grant_id` | `string` | yes | Revoked entitlement grant. |
| `reason` | `string` | yes | Typed revocation reason. |
| `revoked_at` | `datetime` | yes | Revocation effective instant. |

## Constructors

Each of beam4pm's four language projections exposes a `new_entitlement_revocation/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_entitlement_revocation/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_entitlement_revocation/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_entitlement_revocation` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `entitlement_revocation` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
