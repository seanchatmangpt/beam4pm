# observation_partition

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Partitions observationally equivalent world states for contingent planning.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `partition_id` | `string` | yes | Observation partition identity. |
| `state_set_digest` | `string` | yes | Digest of member world states. |
| `observation_key` | `string` | yes | Observation defining equivalence. |

## Constructors

Each of beam4pm's four language projections exposes a `new_observation_partition/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_observation_partition/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_observation_partition/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_observation_partition` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `observation_partition` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
