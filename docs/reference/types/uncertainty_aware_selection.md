# uncertainty_aware_selection

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Selects only when confidence and downside bounds satisfy explicit values.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `option_set_id` | `string` | yes | Required option_set_id for this bounded planner contract. |
| `uncertainty_model_hash` | `string` | yes | Required uncertainty_model_hash for deterministic planner evaluation. |
| `selected_option_id` | `string` | yes | Required selected_option_id preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_uncertainty_aware_selection/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_uncertainty_aware_selection/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_uncertainty_aware_selection/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_uncertainty_aware_selection` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `uncertainty_aware_selection` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
