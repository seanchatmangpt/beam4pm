# optimization_plan_candidate

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Represents an optimization-derived plan with objective and solver receipt.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `plan_id` | `string` | yes | Required plan_id for this bounded planner contract. |
| `objective_id` | `string` | yes | Required objective_id for deterministic planner evaluation. |
| `solver_receipt_hash` | `string` | yes | Required solver_receipt_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_optimization_plan_candidate/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_optimization_plan_candidate/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_optimization_plan_candidate/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_optimization_plan_candidate` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `optimization_plan_candidate` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
