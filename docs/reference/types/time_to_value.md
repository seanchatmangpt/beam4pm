# time_to_value

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Measured elapsed time to the customer's first verified business value.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `time_to_value_id` | `string` | yes | Stable identity of this time to value observation. |
| `account_id` | `string` | yes | Enterprise account whose time to value state was observed. |
| `verified_days` | `integer` | yes | Days from admission to first verified value. |
| `evidence_digest` | `string` | yes | Digest of the exact evidence supporting this customer-value observation. |
| `observed_at` | `datetime` | yes | ISO8601 instant the enterprise consequence was observed. |

## Constructors

Each of beam4pm's four language projections exposes a `new_time_to_value/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_time_to_value/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_time_to_value/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_time_to_value` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `time_to_value` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
