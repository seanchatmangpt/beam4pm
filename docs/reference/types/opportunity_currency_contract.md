# opportunity_currency_contract

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Admits opportunity value only with an explicit deal currency and immutable foreign-exchange basis, making forecast value comparable and replayable.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `opportunity_id` | `string` | yes | Required opportunity currency contract input; omission is an executable typed refusal, never an inferred approval. |
| `currency_code` | `string` | yes | Required opportunity currency contract input; omission is an executable typed refusal, never an inferred approval. |
| `fx_basis_id` | `string` | yes | Immutable decision or evidence identity used to verify and replay this bounded commercial admission. |

## Constructors

Each of beam4pm's four language projections exposes a `new_opportunity_currency_contract/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_opportunity_currency_contract/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_opportunity_currency_contract/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_opportunity_currency_contract` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `opportunity_currency_contract` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
