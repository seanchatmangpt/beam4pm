# invoice_schedule

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Deterministic invoicing cadence for a billing account.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `schedule_id` | `string` | yes | Stable schedule identity. |
| `billing_account_id` | `string` | yes | Bill-to account subject. |
| `cadence` | `atom` | yes | Invoice cadence. |
| `next_invoice_at` | `datetime` | yes | Next scheduled invoice instant. |

## Constructors

Each of beam4pm's four language projections exposes a `new_invoice_schedule/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_invoice_schedule/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_invoice_schedule/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_invoice_schedule` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `invoice_schedule` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
