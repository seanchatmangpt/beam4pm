# plan_memory

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Stores reusable plan evidence without converting historical success into current authority.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `plan_id` | `string` | yes | Required plan_id for this bounded planner contract. |
| `evidence_hash` | `string` | yes | Required evidence_hash for deterministic planner evaluation. |
| `memory_hash` | `string` | yes | Required memory_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_plan_memory/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_plan_memory/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_plan_memory/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_plan_memory` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `plan_memory` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
