# account_parent_scope

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Qualifies an account only when its buying scope is bound to the correct global parent, preventing subsidiary demand from being double-counted as independent enterprise pipeline.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `account_id` | `string` | yes | Required account parent scope input; omission is an executable typed refusal, never an inferred approval. |
| `parent_account_id` | `string` | yes | Required account parent scope input; omission is an executable typed refusal, never an inferred approval. |
| `scope_evidence_hash` | `string` | yes | Immutable decision or evidence identity used to verify and replay this bounded commercial admission. |

## Constructors

Each of beam4pm's four language projections exposes a `new_account_parent_scope/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_account_parent_scope/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_account_parent_scope/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_account_parent_scope` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `account_parent_scope` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
