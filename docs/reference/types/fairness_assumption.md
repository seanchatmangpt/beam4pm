# fairness_assumption

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Binds an explicit fairness assumption to a cyclic policy instead of hiding it.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `assumption_id` | `string` | yes | Stable fairness assumption identity. |
| `policy_digest` | `string` | yes | Policy graph governed by the assumption. |
| `evidence_digest` | `string` | yes | Digest of evidence supporting fairness. |

## Constructors

Each of beam4pm's four language projections exposes a `new_fairness_assumption/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_fairness_assumption/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_fairness_assumption/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_fairness_assumption` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `fairness_assumption` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
