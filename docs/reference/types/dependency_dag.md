# dependency_dag

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Represents action dependencies as an acyclic exact-subject graph.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `dag_id` | `string` | yes | Required dag_id for this bounded planner contract. |
| `node_set_hash` | `string` | yes | Required node_set_hash for deterministic planner evaluation. |
| `edge_set_hash` | `string` | yes | Required edge_set_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_dependency_dag/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_dependency_dag/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_dependency_dag/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_dependency_dag` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `dependency_dag` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
