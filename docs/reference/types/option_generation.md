# option_generation

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Generates a reversible DfCM option set without premature selection.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `state_id` | `string` | yes | Required state_id for this bounded planner contract. |
| `generator_id` | `string` | yes | Required generator_id for deterministic planner evaluation. |
| `option_set_hash` | `string` | yes | Required option_set_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_option_generation/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_option_generation/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_option_generation/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_option_generation` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `option_generation` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
