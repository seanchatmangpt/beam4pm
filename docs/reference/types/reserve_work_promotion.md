# reserve_work_promotion

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Automatically promotes the highest-value lawful reserve when primary work blocks.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `blocked_work_id` | `string` | yes | Required blocked_work_id for this bounded planner contract. |
| `reserve_set_hash` | `string` | yes | Required reserve_set_hash for deterministic planner evaluation. |
| `promoted_work_id` | `string` | yes | Required promoted_work_id preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_reserve_work_promotion/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_reserve_work_promotion/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_reserve_work_promotion/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_reserve_work_promotion` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `reserve_work_promotion` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
