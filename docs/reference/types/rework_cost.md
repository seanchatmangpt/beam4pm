# rework_cost

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One observed process case with quantified retry/rework loop count and weighted cost evidence.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `case_id` | `string` | yes | Exact process-case identity carrying the observed rework. |
| `loop_count` | `integer` | yes | Observed count of retry/rework loop markers for this case. |
| `weighted_cost` | `float` | yes | Observed case amount weighted by the admitted rework-loop rule; units are inherited from the admitted source evidence. |

## Constructors

Each of beam4pm's four language projections exposes a `new_rework_cost/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_rework_cost/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_rework_cost/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_rework_cost` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `rework_cost` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
