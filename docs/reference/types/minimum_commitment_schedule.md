# minimum_commitment_schedule

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Time-phased minimum-spend commitment.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `minimum_commitment_schedule_id` | `string` | yes | Stable identity of this minimum commitment schedule contract. |
| `account_id` | `string` | yes | Enterprise account governed by this commercial contract. |
| `committed_amount` | `float` | yes | Commercially admitted minimum amount for the schedule window. |
| `evidence_digest` | `string` | yes | Digest of the exact evidence admitting this commercial consequence. |
| `effective_at` | `datetime` | yes | ISO8601 instant at which this contract becomes effective. |

## Constructors

Each of beam4pm's four language projections exposes a `new_minimum_commitment_schedule/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_minimum_commitment_schedule/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_minimum_commitment_schedule/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_minimum_commitment_schedule` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `minimum_commitment_schedule` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
