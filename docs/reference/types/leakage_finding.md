# leakage_finding

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One conformance-deviation finding with exact case identity, measured fitness/precision and optional amount at risk.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `case_id` | `string` | yes | Exact process-case identity that deviated from the admitted reference model. |
| `fitness` | `float` | yes | Observed conformance fitness for this case. |
| `precision` | `float` | yes | Observed conformance precision for this case against the admitted model. |
| `amount_at_risk` | `float` | no | Optional admitted amount associated with the deviating case; absent evidence remains absent rather than being coerced to zero. |

## Constructors

Each of beam4pm's four language projections exposes a `new_leakage_finding/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_leakage_finding/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_leakage_finding/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_leakage_finding` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `leakage_finding` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
