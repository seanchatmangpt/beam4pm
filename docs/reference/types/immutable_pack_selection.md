# immutable_pack_selection

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

Selects a marketplace pack only by exact immutable commit SHA.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `pack_id` | `string` | yes | Required pack_id for this bounded planner contract. |
| `pack_sha` | `string` | yes | Required pack_sha for deterministic planner evaluation. |
| `selection_hash` | `string` | yes | Required selection_hash preserving evidence and falsifiability. |

## Constructors

Each of beam4pm's four language projections exposes a `new_immutable_pack_selection/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_immutable_pack_selection/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_immutable_pack_selection/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_immutable_pack_selection` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `immutable_pack_selection` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
