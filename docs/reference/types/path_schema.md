# path_schema

*Part of the [beam4pm types reference](../beam4pm_types_reference.md). GENERATED by ggen_igniter -- do not edit.*

One reusable, scored connection pattern between two OCEL types.

## Fields

| Field | Type | Required | Doc |
|---|---|---|---|
| `schema_id` | `string` | yes | Unique path schema identifier. |
| `source_type` | `string` | yes | The source event/object type name. |
| `target_type` | `string` | yes | The target event/object type name. |
| `support` | `integer` | yes | Observed instance count supporting this schema. |

## Constructors

Each of beam4pm's four language projections exposes a `new_path_schema/1` (or
language-equivalent) validating constructor over this record's admitted field set:

- Erlang: `beam4pm_types:new_path_schema/1` (`src/beam4pm_types.erl`)
- Elixir: `BeamPM.Types.new_path_schema/1` (`lib/beam4pm_types.ex`)
- Gleam: `beam4pm/types.new_path_schema` (`gleam/src/beam4pm/types.gleam`)
- JSON Schema: `path_schema` definition (`schema/beam4pm_types.schema.json`)

Every constructor returns `{ok, Record}` on success or `{error, {missing_field, Field}}`
for the first missing required field, in field-order.
