%%% GENERATED by ggen (beam4pm-wasm-engine-pack) from the WASM engine ABI
%%% graph. Do not edit.
%%%
%%% Erlang-side reflection over the four BEAM-hosted WASM engines and their
%%% wire ops -- the engine-op counterpart of the record-type manifest.
%%% Reflection only: this module dispatches nothing and grants no authority.
%%% Consequence 'do' means mutation INSIDE the engine's wasm linear memory
%%% (handle mint, handle free, in-engine session/graph/OCEL edit), never
%%% host actuation.

-module(beam4pm_engine_manifest).

-export([engines/0, ops/0, ops_for/1, engine/1, op/2,
         read_ops/0, do_ops/0, timeout_ms/2, stats/0]).

-type engine() :: #{name := binary(), abi_prefix := binary(),
                    facade_module := binary(), erlang_module := binary(),
                    wasm_rel_path := binary(),
                    cheap_timeout_ms := pos_integer(),
                    heavy_timeout_ms := pos_integer()}.

-type op() :: #{engine := binary(), op := binary(),
                consequence := read | do, timeout_class := cheap | heavy,
                request_fields := [binary()]}.

-spec engines() -> [engine()].
engines() ->
    [
     #{name => <<"ferroplan">>,
       abi_prefix => <<"fp_">>,
       facade_module => <<"BeamPM.Ferroplan">>,
       erlang_module => <<"beam4pm_ferroplan">>,
       wasm_rel_path => <<"native/ferroplan/target/wasm32-wasip1/release/ferroplan_wasm.wasm">>,
       cheap_timeout_ms => 30000,
       heavy_timeout_ms => 120000},
     #{name => <<"petgraph">>,
       abi_prefix => <<"pg_">>,
       facade_module => <<"BeamPM.Petgraph">>,
       erlang_module => <<"beam4pm_petgraph">>,
       wasm_rel_path => <<"native/petgraph-wasm/target/wasm32-wasip1/release/petgraph_wasm.wasm">>,
       cheap_timeout_ms => 30000,
       heavy_timeout_ms => 30000},
     #{name => <<"rust4pm">>,
       abi_prefix => <<"r4pm_">>,
       facade_module => <<"BeamPM.Rust4PM">>,
       erlang_module => <<"beam4pm_rust4pm">>,
       wasm_rel_path => <<"native/rust4pm-wasm/target/wasm32-wasip1/release/rust4pm_wasm.wasm">>,
       cheap_timeout_ms => 30000,
       heavy_timeout_ms => 120000},
     #{name => <<"tract">>,
       abi_prefix => <<"tr_">>,
       facade_module => <<"BeamPM.Tract">>,
       erlang_module => <<"beam4pm_tract">>,
       wasm_rel_path => <<"native/tract-wasm/target/wasm32-wasip1/release/tract_wasm.wasm">>,
       cheap_timeout_ms => 30000,
       heavy_timeout_ms => 60000}
    ].

-spec ops() -> [op()].
ops() ->
    [
     #{engine => <<"ferroplan">>,
       op => <<"explain">>,
       consequence => read,
       timeout_class => heavy,
       request_fields => [<<"domain">>, <<"problem">>, <<"plan">>]},
     #{engine => <<"ferroplan">>,
       op => <<"plan">>,
       consequence => read,
       timeout_class => heavy,
       request_fields => [<<"domain">>, <<"problem">>]},
     #{engine => <<"ferroplan">>,
       op => <<"plan_production">>,
       consequence => read,
       timeout_class => heavy,
       request_fields => [<<"domain">>, <<"problem">>]},
     #{engine => <<"ferroplan">>,
       op => <<"readiness">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => []},
     #{engine => <<"ferroplan">>,
       op => <<"session_advance">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_apply_start">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"name">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_drop_plan">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_elapse">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"dt">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_fact">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"name">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_fluent">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"name">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_fork">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_free">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_goal_met">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_has_plan">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_mind_bytes">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_new">>,
       consequence => do,
       timeout_class => heavy,
       request_fields => [<<"domain">>, <<"problem">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_observe">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"sight">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_plan_valid">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"plan">>, <<"from">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_restrict_contains">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"filter">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_restrict_prefix_claims">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"prefix">>, <<"claimed">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_set_fact">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"name">>, <<"value">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_set_fluent">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"name">>, <<"value">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_set_goal">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"goal">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_set_timed_fact">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"dt">>, <<"name">>, <<"value">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_step">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_suffix">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_think">>,
       consequence => do,
       timeout_class => heavy,
       request_fields => [<<"handle">>, <<"evals">>, <<"mem_mb">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_valid">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"ferroplan">>,
       op => <<"session_world_bytes">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"ferroplan">>,
       op => <<"version">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => []},
     #{engine => <<"petgraph">>,
       op => <<"add_edge">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"from">>, <<"to">>, <<"weight">>]},
     #{engine => <<"petgraph">>,
       op => <<"add_node">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"name">>]},
     #{engine => <<"petgraph">>,
       op => <<"edge_count">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"petgraph">>,
       op => <<"free_graph">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"petgraph">>,
       op => <<"graph_new">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => []},
     #{engine => <<"petgraph">>,
       op => <<"is_cyclic">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"petgraph">>,
       op => <<"node_count">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"petgraph">>,
       op => <<"scc">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"petgraph">>,
       op => <<"shortest_path">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"from">>, <<"to">>]},
     #{engine => <<"petgraph">>,
       op => <<"toposort">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"activities_to_alphabet">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"activity_position">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"activity">>]},
     #{engine => <<"rust4pm">>,
       op => <<"align_trace">>,
       consequence => read,
       timeout_class => heavy,
       request_fields => [<<"net_handle">>, <<"trace">>]},
     #{engine => <<"rust4pm">>,
       op => <<"align_variants">>,
       consequence => read,
       timeout_class => heavy,
       request_fields => [<<"log_handle">>, <<"net_handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"compute_fitness">>,
       consequence => read,
       timeout_class => heavy,
       request_fields => [<<"log_handle">>, <<"net_handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"discover_alphappp">>,
       consequence => do,
       timeout_class => heavy,
       request_fields => [<<"handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"discover_dfg">>,
       consequence => read,
       timeout_class => heavy,
       request_fields => [<<"handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"free_log">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"free_net">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"free_ocel">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"ocel_handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"import_ocel_json">>,
       consequence => do,
       timeout_class => heavy,
       request_fields => [<<"content">>]},
     #{engine => <<"rust4pm">>,
       op => <<"import_ocel_xml">>,
       consequence => do,
       timeout_class => heavy,
       request_fields => [<<"content_b64">>]},
     #{engine => <<"rust4pm">>,
       op => <<"import_pnml">>,
       consequence => do,
       timeout_class => heavy,
       request_fields => [<<"content">>]},
     #{engine => <<"rust4pm">>,
       op => <<"import_xes">>,
       consequence => do,
       timeout_class => heavy,
       request_fields => [<<"content">>]},
     #{engine => <<"rust4pm">>,
       op => <<"import_xes_gz">>,
       consequence => do,
       timeout_class => heavy,
       request_fields => [<<"content_b64">>]},
     #{engine => <<"rust4pm">>,
       op => <<"log_stats">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"ocel_add_event">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"ocel_handle">>, <<"id">>, <<"type">>, <<"time">>]},
     #{engine => <<"rust4pm">>,
       op => <<"ocel_add_event_type">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"ocel_handle">>, <<"name">>, <<"attributes">>]},
     #{engine => <<"rust4pm">>,
       op => <<"ocel_add_object">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"ocel_handle">>, <<"id">>, <<"type">>, <<"o2o">>]},
     #{engine => <<"rust4pm">>,
       op => <<"ocel_add_object_type">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"ocel_handle">>, <<"name">>, <<"attributes">>]},
     #{engine => <<"rust4pm">>,
       op => <<"ocel_dfg_of_object_type">>,
       consequence => read,
       timeout_class => heavy,
       request_fields => [<<"ocel_handle">>, <<"object_type">>]},
     #{engine => <<"rust4pm">>,
       op => <<"ocel_new">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => []},
     #{engine => <<"rust4pm">>,
       op => <<"ocel_stats">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"ocel_handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"ocel_to_json">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"ocel_handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"ocel_to_xml">>,
       consequence => read,
       timeout_class => heavy,
       request_fields => [<<"ocel_handle">>]},
     #{engine => <<"rust4pm">>,
       op => <<"ocel_variants_of_object_type">>,
       consequence => read,
       timeout_class => heavy,
       request_fields => [<<"ocel_handle">>, <<"object_type">>]},
     #{engine => <<"rust4pm">>,
       op => <<"top_n_variants">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>, <<"n">>]},
     #{engine => <<"rust4pm">>,
       op => <<"xes_to_ocel">>,
       consequence => do,
       timeout_class => heavy,
       request_fields => [<<"handle">>, <<"case_object_type">>, <<"qualifier">>]},
     #{engine => <<"tract">>,
       op => <<"free_model">>,
       consequence => do,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"tract">>,
       op => <<"load_model">>,
       consequence => do,
       timeout_class => heavy,
       request_fields => [<<"model_b64">>]},
     #{engine => <<"tract">>,
       op => <<"model_info">>,
       consequence => read,
       timeout_class => cheap,
       request_fields => [<<"handle">>]},
     #{engine => <<"tract">>,
       op => <<"run">>,
       consequence => read,
       timeout_class => heavy,
       request_fields => [<<"handle">>, <<"inputs">>]}
    ].

-spec ops_for(binary()) -> [op()].
ops_for(Engine) when is_binary(Engine) ->
    [O || O = #{engine := E} <- ops(), E =:= Engine].

-spec engine(binary()) -> engine() | undefined.
engine(Name) when is_binary(Name) ->
    case [E || E = #{name := N} <- engines(), N =:= Name] of
        [E | _] -> E;
        [] -> undefined
    end.

-spec op(binary(), binary()) -> op() | undefined.
op(Engine, Name) when is_binary(Engine), is_binary(Name) ->
    case [O || O = #{engine := E, op := N} <- ops(), E =:= Engine, N =:= Name] of
        [O | _] -> O;
        [] -> undefined
    end.

-spec read_ops() -> [op()].
read_ops() -> [O || O = #{consequence := C} <- ops(), C =:= read].

-spec do_ops() -> [op()].
do_ops() -> [O || O = #{consequence := C} <- ops(), C =:= do].

-spec timeout_ms(binary(), binary()) -> pos_integer() | undefined.
timeout_ms(EngineName, OpName) ->
    case {engine(EngineName), op(EngineName, OpName)} of
        {undefined, _} -> undefined;
        {_, undefined} -> undefined;
        { #{cheap_timeout_ms := C, heavy_timeout_ms := H}, #{timeout_class := Class} } ->
            case Class of
                heavy -> H;
                cheap -> C
            end
    end.

-spec stats() -> #{engines := non_neg_integer(), ops := non_neg_integer(),
                   read := non_neg_integer(), do := non_neg_integer()}.
stats() ->
    #{engines => length(engines()),
      ops => length(ops()),
      read => length(read_ops()),
      do => length(do_ops())}.
