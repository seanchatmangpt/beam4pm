# GENERATED by ggen_igniter from the admitted bpm:RecordType graph. Do not edit.

defmodule BeamPM.Ash.Resources.AccountDiscovery do
  @moduledoc "Receipted enterprise account-discovery outcome that binds qualification evidence to a measurable discovery score."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :account_discovery_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :discovery_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.AccountMasterMatch do
  @moduledoc "Admits a source account only when it is bound to one canonical Fortune-5 account by immutable matching evidence; prevents pipeline value from being booked against an ambiguous customer identity."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :source_account_id, :string, public?: true, allow_nil?: false
    attribute :canonical_account_id, :string, public?: true, allow_nil?: false
    attribute :match_evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.AccountParentScope do
  @moduledoc "Qualifies an account only when its buying scope is bound to the correct global parent, preventing subsidiary demand from being double-counted as independent enterprise pipeline."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :parent_account_id, :string, public?: true, allow_nil?: false
    attribute :scope_evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ActivationEvent do
  @moduledoc "First verified activation event for an enterprise user cohort."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :activation_event_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :activation_type, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.AdoptionMilestone do
  @moduledoc "Receipted customer adoption milestone tied to observable use."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :adoption_milestone_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :milestone_name, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.AlignmentMove do
  @moduledoc "One step of a conformance-checking alignment between log and model."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :move_type, :atom, public?: true, allow_nil?: false
    attribute :cost, :integer, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ArchitectureReadiness do
  @moduledoc "Measured architecture readiness for the customer's production environment."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :architecture_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :architecture_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.BaselineMetric do
  @moduledoc "Pre-POC customer baseline used to falsify or confirm realized value."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :baseline_metric_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :baseline_value, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.BeneficialOwnerEvidence do
  @moduledoc "Binds beneficial-owner identity to immutable evidence for counterparties that require enhanced diligence."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :owner_id, :string, public?: true, allow_nil?: false
    attribute :evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.BillingReconciliation do
  @moduledoc "The reconciled billable total for one (entitlement_id, metric_name) pair over one half-open period [period_start, period_end). INVARIANT: total_quantity is exactly the sum of usage_event.quantity over the distinct event_ids listed in applied_event_ids, summed in that list's ascending order; applied_event_ids is sorted and duplicate-free. Consequently the same usage_event.event_id can never contribute twice, no matter how many times or in what order it appears in the input stream."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :entitlement_id, :string, public?: true, allow_nil?: false
    attribute :metric_name, :string, public?: true, allow_nil?: false
    attribute :total_quantity, :float, public?: true, allow_nil?: false
    attribute :applied_event_ids, {:array, :string}, public?: true, allow_nil?: false
    attribute :period_start, :utc_datetime, public?: true, allow_nil?: false
    attribute :period_end, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.BookingReadiness do
  @moduledoc "Produces an explicit booking-readiness decision after commercial, legal, security, and procurement evidence converge."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :readiness_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.BudgetPeriodAlignment do
  @moduledoc "Qualifies whether the buying timeline lands inside an approved budget period, exposing unfunded timing risk."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :budget_period, :string, public?: true, allow_nil?: false
    attribute :alignment_result, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.BuyingCommittee do
  @moduledoc "Measured buying-committee coverage for an enterprise decision path."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :buying_committee_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :committee_coverage, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.CanaryDecision do
  @moduledoc "Receipted enterprise canary decision based on an observed rollout consequence."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :canary_decision_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :canary_result, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.CapabilityGap do
  @moduledoc "Detected capability gap blocking enterprise value or expansion."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :capability_gap_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :gap_severity, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.CaseStats do
  @moduledoc "Aggregate statistics computed for one process instance (case)."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :case_id, :string, public?: true, allow_nil?: false
    attribute :event_count, :integer, public?: true, allow_nil?: false
    attribute :duration_seconds, :float, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ChangeOrderAuthority do
  @moduledoc "Requires evidence of who can authorize paid scope changes, protecting expansion revenue and delivery margin."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :authority_id, :string, public?: true, allow_nil?: false
    attribute :evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.CommercialException do
  @moduledoc "Represents a nonstandard commercial request as an identified exception with explicit state."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :exception_id, :string, public?: true, allow_nil?: false
    attribute :exception_state, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.CommercialOutcome do
  @moduledoc "Verified commercial outcome attributable to the admitted customer journey."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :commercial_outcome_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :outcome_value, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.CommittedSpendAdmission do
  @moduledoc "Qualifies a committed-spend promise as a concrete commercial decision rather than aspirational usage."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :commitment_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ConformanceResult do
  @moduledoc "Conformance-checking metrics computed for one trace against a model."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :trace_id, :string, public?: true, allow_nil?: false
    attribute :fitness, :float, public?: true, allow_nil?: false
    attribute :precision, :float, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ContractingEntityIdentity do
  @moduledoc "Requires the counterparty that will sign the agreement, separating contractual authority from account interest."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :contracting_entity_id, :string, public?: true, allow_nil?: false
    attribute :identity_evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.CreditRiskAdmission do
  @moduledoc "Turns counterparty credit risk into an explicit admission decision that can refuse unsafe payment exposure."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :risk_band, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.CrossSellFit do
  @moduledoc "Evidence-backed fit for an adjacent enterprise capability."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :cross_sell_fit_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :cross_sell_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.CustomerHealth do
  @moduledoc "Cumulative customer-health observation across usage, outcomes, and support."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :customer_health_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :health_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.DataMigrationScopeAdmission do
  @moduledoc "Admits a bounded data-migration scope before margin and timeline promises are accepted."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :migration_scope_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.DataProcessingAddendumState do
  @moduledoc "Requires an identified data-processing addendum state for workloads involving regulated enterprise data."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :addendum_id, :string, public?: true, allow_nil?: false
    attribute :addendum_state, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.DataReadiness do
  @moduledoc "Measured source-data readiness for a production proof of value."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :data_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :data_quality_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.DealDeskPacket do
  @moduledoc "Binds cross-functional deal-desk approval evidence into one replayable packet identity."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :packet_id, :string, public?: true, allow_nil?: false
    attribute :evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.DemoRun do
  @moduledoc "Receipted execution result for a reproducible enterprise demo."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :demo_run_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :demo_result, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.DemoScenario do
  @moduledoc "Account-specific executable demonstration scenario for enterprise evaluation."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :demo_scenario_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :scenario_name, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.DeveloperReadiness do
  @moduledoc "Measured developer enablement readiness for enterprise adoption."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :developer_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :developer_readiness_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.DfgEdge do
  @moduledoc "One frequency-annotated directly-follows edge between two activities."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :source_activity, :string, public?: true, allow_nil?: false
    attribute :target_activity, :string, public?: true, allow_nil?: false
    attribute :frequency, :integer, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.DiscoveryHypothesis do
  @moduledoc "Receipted enterprise discovery hypothesis linking an account to a falsifiable customer-value expectation."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :discovery_hypothesis_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :expected_value, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.EntitlementEvent do
  @moduledoc "One inbound marketplace entitlement/order/agreement lifecycle event, as delivered by the provider's notification topic. Append-only and at-least-once: the same event_id may be delivered any number of times, in any order relative to other events. Modeled on the Google Cloud Commerce Partner Procurement API Pub/Sub notification message, whose payload carries both an eventId and an entitlement id."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :event_id, :string, public?: true, allow_nil?: false
    attribute :entitlement_id, :string, public?: true, allow_nil?: false
    attribute :event_type, :string, public?: true, allow_nil?: false
    attribute :effective_at, :utc_datetime, public?: true, allow_nil?: false
    attribute :payload, :map, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.EntitlementState do
  @moduledoc "The reconciled current state of one entitlement, derived purely by folding its entitlement_event set. The fold is idempotent (replaying an already-applied event is a no-op) and commutative in arrival order (an older event arriving after a newer one is a no-op), because an event is applied if and only if its (effective_at, event_id) pair is strictly greater than the state's (updated_at, last_applied_event_id) watermark."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :entitlement_id, :string, public?: true, allow_nil?: false
    attribute :status, :string, public?: true, allow_nil?: false
    attribute :last_applied_event_id, :string, public?: true, allow_nil?: false
    attribute :updated_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.EventLog do
  @moduledoc "A named collection of events forming one process-mining log."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :log_id, :string, public?: true, allow_nil?: false
    attribute :name, :string, public?: true, allow_nil?: false
    attribute :description, :string, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.EventType do
  @moduledoc "A declared OCEL event type and its attribute schema."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :type_name, :string, public?: true, allow_nil?: false
    attribute :attribute_names, {:array, :string}, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ExceptionAuthority do
  @moduledoc "Admits a commercial exception only when the exact authority and decision are present."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :exception_id, :string, public?: true, allow_nil?: false
    attribute :authority_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ExecutiveBusinessReview do
  @moduledoc "Observed executive business-review outcome derived from receipts."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :executive_business_review_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :executive_outcome, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ExecutiveSponsor do
  @moduledoc "Receipted executive sponsorship commitment for an enterprise proof of value."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :executive_sponsor_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :sponsor_commitment, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ExpansionOpportunity do
  @moduledoc "Qualified account expansion opportunity backed by observed adoption evidence."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :expansion_opportunity_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :expansion_value, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ExpansionReceipt do
  @moduledoc "Final expansion receipt binding adoption evidence to realized expansion value."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :expansion_receipt_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :receipt_value, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ExpansionSignal do
  @moduledoc "Observed usage-and-value signal indicating an expansion path."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :expansion_signal_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :expansion_signal_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.FundingApprovalChain do
  @moduledoc "Requires a replayable funding approval chain rather than relying on a stakeholder's verbal budget claim."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :approval_chain_id, :string, public?: true, allow_nil?: false
    attribute :evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.HeuristicArc do
  @moduledoc "One dependency-scored candidate arc considered during heuristic-net discovery."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :source_activity, :string, public?: true, allow_nil?: false
    attribute :target_activity, :string, public?: true, allow_nil?: false
    attribute :dependency_measure, :float, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ImplementationFeeAdmission do
  @moduledoc "Qualifies implementation fees separately from recurring subscription value."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :fee_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.IndemnityScopeAdmission do
  @moduledoc "Admits an identified indemnity scope so exceptional legal exposure cannot hide inside a qualified deal."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :indemnity_scope_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.InsuranceRequirement do
  @moduledoc "Admits contractual insurance coverage only with an exact coverage identity and supporting evidence."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :coverage_id, :string, public?: true, allow_nil?: false
    attribute :evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.IntegrationReadiness do
  @moduledoc "Executable readiness contract for required enterprise integrations."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :integration_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :integration_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.IntegrationScopeAdmission do
  @moduledoc "Admits a bounded enterprise integration scope before solution fit becomes a commercial commitment."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :integration_scope_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.InvoiceEntityIdentity do
  @moduledoc "Requires the entity responsible for invoice acceptance, preventing qualified demand from becoming uncollectable revenue."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :invoice_entity_id, :string, public?: true, allow_nil?: false
    attribute :identity_evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.K8sObjectRef do
  @moduledoc "A reference to one Kubernetes object observed in the runtime topology."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :kind, :string, public?: true, allow_nil?: false
    attribute :name, :string, public?: true, allow_nil?: false
    attribute :namespace, :string, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.LegalBlocker do
  @moduledoc "Encodes an unresolved legal blocker as a typed refusal with an actionable identity."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :blocker_id, :string, public?: true, allow_nil?: false
    attribute :refusal_code, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.LiabilityCapAdmission do
  @moduledoc "Admits a specific liability-cap position before commercial approval."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :liability_cap_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.LogTrace do
  @moduledoc "One case-centric trace: an ordered activity sequence for a single case."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :case_id, :string, public?: true, allow_nil?: false
    attribute :activity_sequence, {:array, :string}, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.MasterServiceAgreementState do
  @moduledoc "Tracks the exact master service agreement and its executable admission state rather than treating legal review as a boolean."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :agreement_id, :string, public?: true, allow_nil?: false
    attribute :agreement_state, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.MigrationReadiness do
  @moduledoc "Measured migration readiness and bounded effort for enterprise adoption."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :migration_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :migration_effort_days, :integer, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.MinimumTermAdmission do
  @moduledoc "Admits the minimum paid term as a decision input needed for durable contract value."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :minimum_term, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ObjectAttributeChange do
  @moduledoc "One recorded change to a time-indexed object attribute."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :object_id, :string, public?: true, allow_nil?: false
    attribute :attribute_name, :string, public?: true, allow_nil?: false
    attribute :old_value, :string, public?: true
    attribute :new_value, :string, public?: true, allow_nil?: false
    attribute :changed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ObjectType do
  @moduledoc "A declared OCEL object type and its attribute schema."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :type_name, :string, public?: true, allow_nil?: false
    attribute :attribute_names, {:array, :string}, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.Objection do
  @moduledoc "Typed enterprise objection linked to an exact account and evidence."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :objection_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :objection_type, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ObjectionResolution do
  @moduledoc "Receipted resolution of an enterprise buying objection."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :objection_resolution_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :resolution_status, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.OcDeclareConstraint do
  @moduledoc "One declarative object-centric behavioral constraint between two activities."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :constraint_id, :string, public?: true, allow_nil?: false
    attribute :source_activity, :string, public?: true, allow_nil?: false
    attribute :target_activity, :string, public?: true, allow_nil?: false
    attribute :constraint_type, :atom, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.OcelAttribute do
  @moduledoc "One named, timestamped attribute value on an OCEL event or object."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :attribute_name, :string, public?: true, allow_nil?: false
    attribute :attribute_value, :string, public?: true, allow_nil?: false
    attribute :recorded_at, :utc_datetime, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.OcelEvent do
  @moduledoc "An OCEL 2.0 event: one recorded occurrence in an object-centric log."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :event_id, :string, public?: true, allow_nil?: false
    attribute :event_type, :string, public?: true, allow_nil?: false
    attribute :event_time, :utc_datetime, public?: true, allow_nil?: false
    attribute :attributes, :map, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.OcelObject do
  @moduledoc "An OCEL 2.0 object: one entity participating in the process."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :object_id, :string, public?: true, allow_nil?: false
    attribute :object_type, :string, public?: true, allow_nil?: false
    attribute :attributes, :map, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.OcelRelationship do
  @moduledoc "A qualified relationship linking an event or object to an object."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :qualifier, :string, public?: true, allow_nil?: false
    attribute :object_id, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.OperatorReadiness do
  @moduledoc "Measured operations-team readiness for governed production use."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :operator_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :operator_readiness_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.OpportunityCurrencyContract do
  @moduledoc "Admits opportunity value only with an explicit deal currency and immutable foreign-exchange basis, making forecast value comparable and replayable."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :currency_code, :string, public?: true, allow_nil?: false
    attribute :fx_basis_id, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.OpportunityValueRange do
  @moduledoc "Represents commercial value as an evidenced range rather than false point precision, enabling downside-aware qualification."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :minimum_value, :string, public?: true, allow_nil?: false
    attribute :maximum_value, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.OrderFormAdmission do
  @moduledoc "Admits the exact order form that expresses the buyer's priced scope and authorized terms."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :order_form_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PathSchema do
  @moduledoc "One reusable, scored connection pattern between two OCEL types."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :schema_id, :string, public?: true, allow_nil?: false
    attribute :source_type, :string, public?: true, allow_nil?: false
    attribute :target_type, :string, public?: true, allow_nil?: false
    attribute :support, :integer, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PathSchemaQuery do
  @moduledoc "A discovery query bounding source/target types and maximum schema length."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :source_type, :string, public?: true, allow_nil?: false
    attribute :target_type, :string, public?: true, allow_nil?: false
    attribute :max_length, :integer, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PaymentTermsAdmission do
  @moduledoc "Admits payment terms only when authorized evidence supports the cash-conversion assumption."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :payment_terms, :string, public?: true, allow_nil?: false
    attribute :authority_evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PetriArc do
  @moduledoc "A weighted arc connecting a place and a transition (either direction)."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :source_id, :string, public?: true, allow_nil?: false
    attribute :target_id, :string, public?: true, allow_nil?: false
    attribute :weight, :integer, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PetriPlace do
  @moduledoc "A place in a Petri net, holding a non-negative token count."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :place_id, :string, public?: true, allow_nil?: false
    attribute :tokens, :integer, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PetriTransition do
  @moduledoc "A transition in a Petri net."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :transition_id, :string, public?: true, allow_nil?: false
    attribute :label, :string, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PlanningAction do
  @moduledoc "One PDDL-style planning action with its preconditions and effects."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :action_name, :string, public?: true, allow_nil?: false
    attribute :preconditions, {:array, :string}, public?: true
    attribute :effects, {:array, :string}, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PlanningState do
  @moduledoc "One planning-search state as a bounded set of true facts."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :state_id, :string, public?: true, allow_nil?: false
    attribute :facts, {:array, :string}, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PocExitCriteria do
  @moduledoc "Measured POC exit-criteria result that gates production readiness."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :poc_exit_criteria_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :criteria_pass_rate, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PocRisk do
  @moduledoc "Evidence-backed POC delivery risk visible to the solution architect."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :poc_risk_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :risk_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PocScope do
  @moduledoc "Bounded proof-of-concept scope with an explicit number of customer use cases."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :poc_scope_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :use_case_count, :integer, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PocTimeline do
  @moduledoc "Measured proof-of-concept timeline tied to verified customer value."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :poc_timeline_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :days_to_value, :integer, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PolicyDecision do
  @moduledoc "One admission/authority policy decision recorded for an attempted action."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :decision_id, :string, public?: true, allow_nil?: false
    attribute :verdict, :atom, public?: true, allow_nil?: false
    attribute :reason, :string, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PricingBasisContract do
  @moduledoc "Binds the opportunity to an exact pricing basis and evidence identity before quote construction."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :pricing_basis_id, :string, public?: true, allow_nil?: false
    attribute :evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ProcessVariant do
  @moduledoc "One distinct activity-sequence variant observed in a log, with its frequency."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :variant_id, :string, public?: true, allow_nil?: false
    attribute :activity_sequence, {:array, :string}, public?: true, allow_nil?: false
    attribute :frequency, :integer, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ProcurementBlocker do
  @moduledoc "Encodes an unresolved procurement blocker as a typed refusal instead of allowing false booking readiness."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :blocker_id, :string, public?: true, allow_nil?: false
    attribute :refusal_code, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ProcurementChannelSelection do
  @moduledoc "Admits exactly one purchasing channel with evidence, preventing direct, marketplace, reseller, and distributor routes from conflicting."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :channel_id, :string, public?: true, allow_nil?: false
    attribute :selection_evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ProcurementReadiness do
  @moduledoc "Observable procurement journey state for an enterprise commercial transition."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :procurement_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :procurement_stage, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ProductionReadiness do
  @moduledoc "Cumulative evidence-backed production readiness for the enterprise account."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :production_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :production_readiness_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ProofOfValueBudget do
  @moduledoc "Qualifies whether a proof-of-value has an actual budget and decision rather than free-pilot ambiguity."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :budget_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ProofOfValueExitGate do
  @moduledoc "Defines the exact commercial exit gate that converts a proof-of-value into a paid deployment or refusal."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :pov_id, :string, public?: true, allow_nil?: false
    attribute :exit_gate_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PurchaseOrderRequirement do
  @moduledoc "Makes the buyer's purchase-order requirement explicit and evidenced before booking readiness."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :requirement_id, :string, public?: true, allow_nil?: false
    attribute :evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.PurchasingEntityIdentity do
  @moduledoc "Requires the legal entity that will issue purchasing authority before commercial progression."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :purchasing_entity_id, :string, public?: true, allow_nil?: false
    attribute :identity_evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.QueueSnapshot do
  @moduledoc "One point-in-time observation of a queue depth."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :queue_name, :string, public?: true, allow_nil?: false
    attribute :depth, :integer, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.RecoveryPlan do
  @moduledoc "Executable account recovery plan with a measured recovery interval."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :recovery_plan_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :recovery_time_hours, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.RenewalEvidence do
  @moduledoc "Receipted value evidence supporting a multi-year renewal decision."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :renewal_evidence_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :renewal_value, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.RenewalHealth do
  @moduledoc "Measured renewal health derived from adoption and realized-value evidence."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :renewal_health_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :renewal_health_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.RenewalRisk do
  @moduledoc "Evidence-backed renewal risk requiring customer-success action."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :renewal_risk_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :renewal_risk_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.RenewalTermAdmission do
  @moduledoc "Makes renewal duration and its decision explicit for lifetime-value qualification."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :renewal_term, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ResourceAllocation do
  @moduledoc "One recorded assignment of a resource to an activity occurrence."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :resource_id, :string, public?: true, allow_nil?: false
    attribute :activity, :string, public?: true, allow_nil?: false
    attribute :event_id, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.RevenueAttribution do
  @moduledoc "Receipted revenue attribution bound to exact enterprise evidence."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :revenue_attribution_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :attributed_revenue, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.RevenueContractAdmission do
  @moduledoc "Produces the terminal immutable admission receipt binding a qualified opportunity to an executable paid revenue contract."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :contract_id, :string, public?: true, allow_nil?: false
    attribute :admission_receipt_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.RevenueScheduleAssumption do
  @moduledoc "Binds forecast revenue timing to a named schedule and evidence identity so bookings and realization assumptions can be independently challenged."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :schedule_id, :string, public?: true, allow_nil?: false
    attribute :assumption_evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.RollbackDecision do
  @moduledoc "Receipted rollback decision and recovered account consequence."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :rollback_decision_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :rollback_result, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.SanctionsScreeningResult do
  @moduledoc "Requires a named sanctions screening result before accepting the commercial counterparty."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :screening_id, :string, public?: true, allow_nil?: false
    attribute :screening_result, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.SecurityAddendumState do
  @moduledoc "Requires an identified security addendum state before promising a paid production deployment."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :addendum_id, :string, public?: true, allow_nil?: false
    attribute :addendum_state, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.SecurityBlocker do
  @moduledoc "Encodes an unresolved security blocker as a typed refusal before production commitment."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :blocker_id, :string, public?: true, allow_nil?: false
    attribute :refusal_code, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.SecurityReadiness do
  @moduledoc "Evidence-backed security-control readiness for enterprise adoption."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :security_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :control_coverage, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ServiceCreditAdmission do
  @moduledoc "Qualifies the exact service-credit obligation that prices runtime reliability risk."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :service_credit_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ServiceSpan do
  @moduledoc "One OpenTelemetry-style tracing span observed for a service call."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :span_id, :string, public?: true, allow_nil?: false
    attribute :service_name, :string, public?: true, allow_nil?: false
    attribute :duration_ms, :integer, public?: true, allow_nil?: false
    attribute :parent_span_id, :string, public?: true
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.SlaOfferAdmission do
  @moduledoc "Admits an exact service-level offer before it becomes a paid customer commitment."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :sla_offer_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.SojournTime do
  @moduledoc "The total dwell/duration time an object was associated with one event/activity (object-centric sojourn time -- a duration metric, distinct from bpm:sync_time's wait-for-another-object metric)."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :object_id, :string, public?: true, allow_nil?: false
    attribute :event_type, :string, public?: true, allow_nil?: false
    attribute :seconds, :float, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.SolutionFit do
  @moduledoc "Measured solution-fit observation for the account's admitted requirements."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :solution_fit_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :fit_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.StakeholderMap do
  @moduledoc "Executable stakeholder-coverage contract for a qualified enterprise evaluation."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :stakeholder_map_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :stakeholder_count, :integer, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.SuccessPlan do
  @moduledoc "Executable customer-success plan target with receipted account identity."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :success_plan_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :success_target, :string, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.SupportReadiness do
  @moduledoc "Measured support-team readiness for enterprise production."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :support_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :support_readiness_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.SupportTierAdmission do
  @moduledoc "Admits the paid support tier that determines service obligation and cost-to-serve."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :support_tier_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.SyncTime do
  @moduledoc "The time one object waited to synchronize with another object at a shared event."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :object_id, :string, public?: true, allow_nil?: false
    attribute :delaying_object_id, :string, public?: true
    attribute :seconds, :float, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.TargetMetric do
  @moduledoc "Customer-approved target metric for a qualified proof of value."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :target_metric_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :target_value, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.TaxJurisdictionEvidence do
  @moduledoc "Binds the contracting entity to an evidenced tax jurisdiction before price and invoice admission."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :contracting_entity_id, :string, public?: true, allow_nil?: false
    attribute :tax_jurisdiction, :string, public?: true, allow_nil?: false
    attribute :evidence_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.TechnicalBlocker do
  @moduledoc "Encodes an unresolved architecture or integration blocker as a typed refusal before commercial acceptance."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :blocker_id, :string, public?: true, allow_nil?: false
    attribute :refusal_code, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.TerminationRightAdmission do
  @moduledoc "Qualifies termination rights that materially change collectible contract value and delivery exposure."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :termination_right_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.TimeToValue do
  @moduledoc "Measured elapsed time to the customer's first verified business value."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :time_to_value_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :verified_days, :integer, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.TrainingReadiness do
  @moduledoc "Observed workforce training readiness for production adoption."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :training_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :training_completion_rate, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.TrainingScopeAdmission do
  @moduledoc "Qualifies training scope as an explicit paid delivery obligation."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :training_scope_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.TypeEdge do
  @moduledoc "A directed, qualified edge in the OCEL type graph between two types."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :source_type, :string, public?: true, allow_nil?: false
    attribute :target_type, :string, public?: true, allow_nil?: false
    attribute :qualifier, :string, public?: true, allow_nil?: false
    attribute :direction, :atom, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.UpsellReadiness do
  @moduledoc "Measured readiness to transition the account to greater product capacity."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :upsell_readiness_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :upsell_score, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.UsageEvent do
  @moduledoc "One metered usage/consumption occurrence to be billed against an entitlement. event_id is the idempotency key that is ALSO the operationId reported to the provider's usage API, so local dedup and provider-side dedup are keyed identically and can never disagree."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :event_id, :string, public?: true, allow_nil?: false
    attribute :entitlement_id, :string, public?: true, allow_nil?: false
    attribute :quantity, :float, public?: true, allow_nil?: false
    attribute :metric_name, :string, public?: true, allow_nil?: false
    attribute :occurred_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.UsageSignal do
  @moduledoc "Measured product-usage signal for an admitted enterprise account."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :usage_signal_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :active_user_count, :integer, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ValueDriver do
  @moduledoc "Account-specific value driver tied to an observed annualized business outcome."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :value_driver_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :annual_value, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.ValueRealization do
  @moduledoc "Receipted realized customer value derived from observed consequences."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :value_realization_id, :string, public?: true, allow_nil?: false
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :realized_value, :float, public?: true, allow_nil?: false
    attribute :evidence_digest, :string, public?: true, allow_nil?: false
    attribute :observed_at, :utc_datetime, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.VendorRegistrationState do
  @moduledoc "Makes buyer vendor-registration completion an explicit revenue gate instead of an invisible procurement delay."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :account_id, :string, public?: true, allow_nil?: false
    attribute :registration_id, :string, public?: true, allow_nil?: false
    attribute :registration_state, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Resources.VolumeTierAdmission do
  @moduledoc "Admits a volume discount tier only through an explicit decision tied to the opportunity."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :opportunity_id, :string, public?: true, allow_nil?: false
    attribute :volume_tier_id, :string, public?: true, allow_nil?: false
    attribute :decision, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

defmodule BeamPM.Ash.Domain do
  @moduledoc "GENERATED Ash domain over all admitted bpm:RecordType resources."
  use Ash.Domain, validate_config_inclusion?: false

  resources do
    resource BeamPM.Ash.Resources.AccountDiscovery
    resource BeamPM.Ash.Resources.AccountMasterMatch
    resource BeamPM.Ash.Resources.AccountParentScope
    resource BeamPM.Ash.Resources.ActivationEvent
    resource BeamPM.Ash.Resources.AdoptionMilestone
    resource BeamPM.Ash.Resources.AlignmentMove
    resource BeamPM.Ash.Resources.ArchitectureReadiness
    resource BeamPM.Ash.Resources.BaselineMetric
    resource BeamPM.Ash.Resources.BeneficialOwnerEvidence
    resource BeamPM.Ash.Resources.BillingReconciliation
    resource BeamPM.Ash.Resources.BookingReadiness
    resource BeamPM.Ash.Resources.BudgetPeriodAlignment
    resource BeamPM.Ash.Resources.BuyingCommittee
    resource BeamPM.Ash.Resources.CanaryDecision
    resource BeamPM.Ash.Resources.CapabilityGap
    resource BeamPM.Ash.Resources.CaseStats
    resource BeamPM.Ash.Resources.ChangeOrderAuthority
    resource BeamPM.Ash.Resources.CommercialException
    resource BeamPM.Ash.Resources.CommercialOutcome
    resource BeamPM.Ash.Resources.CommittedSpendAdmission
    resource BeamPM.Ash.Resources.ConformanceResult
    resource BeamPM.Ash.Resources.ContractingEntityIdentity
    resource BeamPM.Ash.Resources.CreditRiskAdmission
    resource BeamPM.Ash.Resources.CrossSellFit
    resource BeamPM.Ash.Resources.CustomerHealth
    resource BeamPM.Ash.Resources.DataMigrationScopeAdmission
    resource BeamPM.Ash.Resources.DataProcessingAddendumState
    resource BeamPM.Ash.Resources.DataReadiness
    resource BeamPM.Ash.Resources.DealDeskPacket
    resource BeamPM.Ash.Resources.DemoRun
    resource BeamPM.Ash.Resources.DemoScenario
    resource BeamPM.Ash.Resources.DeveloperReadiness
    resource BeamPM.Ash.Resources.DfgEdge
    resource BeamPM.Ash.Resources.DiscoveryHypothesis
    resource BeamPM.Ash.Resources.EntitlementEvent
    resource BeamPM.Ash.Resources.EntitlementState
    resource BeamPM.Ash.Resources.EventLog
    resource BeamPM.Ash.Resources.EventType
    resource BeamPM.Ash.Resources.ExceptionAuthority
    resource BeamPM.Ash.Resources.ExecutiveBusinessReview
    resource BeamPM.Ash.Resources.ExecutiveSponsor
    resource BeamPM.Ash.Resources.ExpansionOpportunity
    resource BeamPM.Ash.Resources.ExpansionReceipt
    resource BeamPM.Ash.Resources.ExpansionSignal
    resource BeamPM.Ash.Resources.FundingApprovalChain
    resource BeamPM.Ash.Resources.HeuristicArc
    resource BeamPM.Ash.Resources.ImplementationFeeAdmission
    resource BeamPM.Ash.Resources.IndemnityScopeAdmission
    resource BeamPM.Ash.Resources.InsuranceRequirement
    resource BeamPM.Ash.Resources.IntegrationReadiness
    resource BeamPM.Ash.Resources.IntegrationScopeAdmission
    resource BeamPM.Ash.Resources.InvoiceEntityIdentity
    resource BeamPM.Ash.Resources.K8sObjectRef
    resource BeamPM.Ash.Resources.LegalBlocker
    resource BeamPM.Ash.Resources.LiabilityCapAdmission
    resource BeamPM.Ash.Resources.LogTrace
    resource BeamPM.Ash.Resources.MasterServiceAgreementState
    resource BeamPM.Ash.Resources.MigrationReadiness
    resource BeamPM.Ash.Resources.MinimumTermAdmission
    resource BeamPM.Ash.Resources.ObjectAttributeChange
    resource BeamPM.Ash.Resources.ObjectType
    resource BeamPM.Ash.Resources.Objection
    resource BeamPM.Ash.Resources.ObjectionResolution
    resource BeamPM.Ash.Resources.OcDeclareConstraint
    resource BeamPM.Ash.Resources.OcelAttribute
    resource BeamPM.Ash.Resources.OcelEvent
    resource BeamPM.Ash.Resources.OcelObject
    resource BeamPM.Ash.Resources.OcelRelationship
    resource BeamPM.Ash.Resources.OperatorReadiness
    resource BeamPM.Ash.Resources.OpportunityCurrencyContract
    resource BeamPM.Ash.Resources.OpportunityValueRange
    resource BeamPM.Ash.Resources.OrderFormAdmission
    resource BeamPM.Ash.Resources.PathSchema
    resource BeamPM.Ash.Resources.PathSchemaQuery
    resource BeamPM.Ash.Resources.PaymentTermsAdmission
    resource BeamPM.Ash.Resources.PetriArc
    resource BeamPM.Ash.Resources.PetriPlace
    resource BeamPM.Ash.Resources.PetriTransition
    resource BeamPM.Ash.Resources.PlanningAction
    resource BeamPM.Ash.Resources.PlanningState
    resource BeamPM.Ash.Resources.PocExitCriteria
    resource BeamPM.Ash.Resources.PocRisk
    resource BeamPM.Ash.Resources.PocScope
    resource BeamPM.Ash.Resources.PocTimeline
    resource BeamPM.Ash.Resources.PolicyDecision
    resource BeamPM.Ash.Resources.PricingBasisContract
    resource BeamPM.Ash.Resources.ProcessVariant
    resource BeamPM.Ash.Resources.ProcurementBlocker
    resource BeamPM.Ash.Resources.ProcurementChannelSelection
    resource BeamPM.Ash.Resources.ProcurementReadiness
    resource BeamPM.Ash.Resources.ProductionReadiness
    resource BeamPM.Ash.Resources.ProofOfValueBudget
    resource BeamPM.Ash.Resources.ProofOfValueExitGate
    resource BeamPM.Ash.Resources.PurchaseOrderRequirement
    resource BeamPM.Ash.Resources.PurchasingEntityIdentity
    resource BeamPM.Ash.Resources.QueueSnapshot
    resource BeamPM.Ash.Resources.RecoveryPlan
    resource BeamPM.Ash.Resources.RenewalEvidence
    resource BeamPM.Ash.Resources.RenewalHealth
    resource BeamPM.Ash.Resources.RenewalRisk
    resource BeamPM.Ash.Resources.RenewalTermAdmission
    resource BeamPM.Ash.Resources.ResourceAllocation
    resource BeamPM.Ash.Resources.RevenueAttribution
    resource BeamPM.Ash.Resources.RevenueContractAdmission
    resource BeamPM.Ash.Resources.RevenueScheduleAssumption
    resource BeamPM.Ash.Resources.RollbackDecision
    resource BeamPM.Ash.Resources.SanctionsScreeningResult
    resource BeamPM.Ash.Resources.SecurityAddendumState
    resource BeamPM.Ash.Resources.SecurityBlocker
    resource BeamPM.Ash.Resources.SecurityReadiness
    resource BeamPM.Ash.Resources.ServiceCreditAdmission
    resource BeamPM.Ash.Resources.ServiceSpan
    resource BeamPM.Ash.Resources.SlaOfferAdmission
    resource BeamPM.Ash.Resources.SojournTime
    resource BeamPM.Ash.Resources.SolutionFit
    resource BeamPM.Ash.Resources.StakeholderMap
    resource BeamPM.Ash.Resources.SuccessPlan
    resource BeamPM.Ash.Resources.SupportReadiness
    resource BeamPM.Ash.Resources.SupportTierAdmission
    resource BeamPM.Ash.Resources.SyncTime
    resource BeamPM.Ash.Resources.TargetMetric
    resource BeamPM.Ash.Resources.TaxJurisdictionEvidence
    resource BeamPM.Ash.Resources.TechnicalBlocker
    resource BeamPM.Ash.Resources.TerminationRightAdmission
    resource BeamPM.Ash.Resources.TimeToValue
    resource BeamPM.Ash.Resources.TrainingReadiness
    resource BeamPM.Ash.Resources.TrainingScopeAdmission
    resource BeamPM.Ash.Resources.TypeEdge
    resource BeamPM.Ash.Resources.UpsellReadiness
    resource BeamPM.Ash.Resources.UsageEvent
    resource BeamPM.Ash.Resources.UsageSignal
    resource BeamPM.Ash.Resources.ValueDriver
    resource BeamPM.Ash.Resources.ValueRealization
    resource BeamPM.Ash.Resources.VendorRegistrationState
    resource BeamPM.Ash.Resources.VolumeTierAdmission
  end
end


defmodule BeamPM.Autonomy.Kernel do
  @moduledoc """
  GENERATED post-LLM autonomy kernel.

  The kernel preserves a DfCM candidate frontier, keeps SELECT separate from
  authority, manufactures non-actuating intents, permits DO only through BRCE,
  binds actual consequences into receipts, verifies replay, and emits bounded
  learning signals. Model/LLM output can propose candidates; it never receives
  actuation authority from this module.
  """

  @type candidate :: %{
          required(:id) => String.t(),
          required(:utility) => number(),
          required(:risk) => number(),
          required(:reversibility) => number(),
          required(:evidence) => number(),
          optional(atom()) => term()
        }

  @spec frontier([candidate()], term()) :: map()
  def frontier(candidates, observation) when is_list(candidates) and candidates != [] do
    ranked =
      candidates
      |> Enum.map(&Map.put(&1, :score, score(&1)))
      |> Enum.sort_by(fn c -> {-c.score, c.id} end)

    %{
      kind: :dfcm_frontier,
      standing: :alive,
      observation_hash: digest(observation),
      candidates: ranked,
      selected: nil,
      authority: nil
    }
  end

  @spec simulate(map(), String.t(), term()) :: {:ok, map()} | {:refused, atom()}
  def simulate(%{kind: :dfcm_frontier, candidates: candidates}, candidate_id, predicted) do
    if Enum.any?(candidates, &(&1.id == candidate_id)) do
      {:ok,
       %{
         kind: :counterfactual,
         candidate_id: candidate_id,
         prediction: predicted,
         prediction_hash: digest(predicted),
         executed?: false
       }}
    else
      {:refused, :unknown_candidate}
    end
  end

  @spec select(map(), map()) :: {:ok, map()} | {:refused, atom()}
  def select(%{kind: :dfcm_frontier, candidates: candidates} = frontier, policy)
      when is_map(policy) do
    max_risk = Map.get(policy, :max_risk, 1.0)
    min_evidence = Map.get(policy, :min_evidence, 0.0)
    min_reversibility = Map.get(policy, :min_reversibility, 0.0)

    case Enum.find(candidates, fn c ->
           c.risk <= max_risk and c.evidence >= min_evidence and
             c.reversibility >= min_reversibility
         end) do
      nil ->
        {:refused, :no_admissible_candidate}

      candidate ->
        {:ok,
         %{
           kind: :selection,
           frontier_hash: digest(frontier),
           candidate: candidate,
           policy_hash: digest(policy),
           authorized?: false
         }}
    end
  end

  @spec intent(map(), String.t()) :: {:ok, map()} | {:refused, atom()}
  def intent(%{kind: :selection, candidate: candidate, authorized?: false} = selection, hook_id)
      when is_binary(hook_id) do
    {:ok,
     %{
       kind: :actuation_intent,
       hook_id: hook_id,
       candidate_id: candidate.id,
       selection_hash: digest(selection),
       authorized?: false,
       executed?: false
     }}
  end

  def intent(_, _), do: {:refused, :invalid_selection}

  @spec grant(map(), map()) :: {:ok, map()} | {:refused, atom()}
  def grant(%{kind: :actuation_intent} = intent, authority) when is_map(authority) do
    required_intent_hash = digest(intent)

    cond do
      Map.get(authority, :intent_hash) != required_intent_hash ->
        {:refused, :intent_identity_mismatch}

      Map.get(authority, :candidate_id) != intent.candidate_id ->
        {:refused, :scope_mismatch}

      Map.get(authority, :do, false) != true ->
        {:refused, :do_not_authorized}

      true ->
        {:ok,
         %{
           kind: :authority_grant,
           candidate_id: intent.candidate_id,
           intent_hash: required_intent_hash,
           authority_id: Map.fetch!(authority, :authority_id),
           do: true
         }}
    end
  end

  @spec brce_do(map(), map(), tuple()) :: {:ok, map()} | {:refused, atom()}
  def brce_do(
        %{kind: :actuation_intent} = intent,
        %{kind: :authority_grant, do: true} = grant,
        {:ets_put, table, key, value}
      ) do
    cond do
      grant.intent_hash != digest(intent) ->
        {:refused, :grant_identity_mismatch}

      grant.candidate_id != intent.candidate_id ->
        {:refused, :grant_scope_mismatch}

      true ->
        true = :ets.insert(table, {key, value})
        consequence = {:ets_value, key, :ets.lookup(table, key)}

        {:ok,
         %{
           kind: :execution_receipt,
           standing: :alive,
           candidate_id: intent.candidate_id,
           intent_hash: digest(intent),
           grant_hash: digest(grant),
           consequence: consequence,
           consequence_hash: digest(consequence),
           executed?: true
         }}
    end
  end

  def brce_do(_, _, _), do: {:refused, :brce_boundary_violation}

  @spec replay(map(), term()) :: {:ok, map()} | {:refused, atom()}
  def replay(%{kind: :execution_receipt, executed?: true} = receipt, observed_consequence) do
    if digest(observed_consequence) == receipt.consequence_hash do
      {:ok,
       %{
         kind: :replay_result,
         verified?: true,
         receipt_hash: digest(receipt),
         consequence_hash: receipt.consequence_hash
       }}
    else
      {:refused, :consequence_mismatch}
    end
  end

  def replay(_, _), do: {:refused, :not_an_execution_receipt}

  @spec learn(map(), map()) :: {:ok, map()} | {:refused, atom()}
  def learn(%{kind: :replay_result, verified?: true} = replay, frontier) do
    {:ok,
     %{
       kind: :learning_signal,
       verified?: true,
       replay_hash: digest(replay),
       frontier_hash: digest(frontier),
       policy_update: %{evidence_delta: 1, authority_delta: 0}
     }}
  end

  def learn(_, _), do: {:refused, :unverified_replay}

  @spec digest(term()) :: String.t()
  def digest(term) do
    :crypto.hash(:sha256, :erlang.term_to_binary(term))
    |> Base.encode16(case: :lower)
  end

  defp score(candidate) do
    candidate.utility * 100 + candidate.reversibility * 20 + candidate.evidence * 10 -
      candidate.risk * 50
  end
end
