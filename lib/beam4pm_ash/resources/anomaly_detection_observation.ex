# GENERATED by ggen_igniter from the admitted bpm:RecordType graph. Do not edit.

defmodule BeamPM.Ash.Resources.AnomalyDetectionObservation do
  @moduledoc "Scores an observed subject against an exact baseline digest."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :subject_id, :string, public?: true, allow_nil?: false
    attribute :baseline_digest, :string, public?: true, allow_nil?: false
    attribute :observation_digest, :string, public?: true, allow_nil?: false
    attribute :anomaly_score, :float, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

