# GENERATED by ggen_igniter from the admitted bpm:RecordType graph. Do not edit.

defmodule BeamPM.Ash.Resources.ResourceCapacityPlan do
  @moduledoc "Allocates finite compute, time, and concurrency capacity across lawful options."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :episode_id, :string, public?: true, allow_nil?: false
    attribute :resource_pool_hash, :string, public?: true, allow_nil?: false
    attribute :allocation_hash, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

