# GENERATED by ggen_igniter from the admitted bpm:RecordType graph. Do not edit.

defmodule BeamPM.Ash.Resources.PlannerBid do
  @moduledoc "Bids for bounded work using declared cost, confidence, and capsule identity."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :bid_id, :string, public?: true, allow_nil?: false
    attribute :work_item_id, :string, public?: true, allow_nil?: false
    attribute :score_digest, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

