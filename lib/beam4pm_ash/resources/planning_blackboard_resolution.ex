# GENERATED by ggen_igniter from the admitted bpm:RecordType graph. Do not edit.

defmodule BeamPM.Ash.Resources.PlanningBlackboardResolution do
  @moduledoc "Records evidence-based resolution while preserving losing lawful claims."
  use Ash.Resource,
    domain: BeamPM.Ash.Domain,
    data_layer: Ash.DataLayer.Ets,
    validate_domain_inclusion?: false

  attributes do
    uuid_primary_key :id
    attribute :resolution_id, :string, public?: true, allow_nil?: false
    attribute :conflict_id, :string, public?: true, allow_nil?: false
    attribute :preserved_option_digest, :string, public?: true, allow_nil?: false
  end

  actions do
    default_accept :*
    defaults [:create, :read]
  end
end

