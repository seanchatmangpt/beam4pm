# GENERATED by ggen from the admitted mcp:MessageType graph. Do not edit.

defmodule BeamPM.MCP.Contracts.McpResource do
  @moduledoc """
  A resource definition advertised by an MCP server.
  """

  defstruct [:uri, :name, :title, :description, :mime_type, :size]

  @type t :: %__MODULE__{
    uri: String.t() | nil,
    name: String.t() | nil,
    title: String.t() | nil,
    description: String.t() | nil,
    mime_type: String.t() | nil,
    size: integer() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :uri) -> {:error, {:missing_field, :uri}}
      not Map.has_key?(attrs, :name) -> {:error, {:missing_field, :name}}
      true ->
        {:ok, %__MODULE__{
          uri: Map.get(attrs, :uri),
          name: Map.get(attrs, :name),
          title: Map.get(attrs, :title),
          description: Map.get(attrs, :description),
          mime_type: Map.get(attrs, :mime_type),
          size: Map.get(attrs, :size)
        }}
    end
  end
end

defmodule BeamPM.MCP.Contracts.McpResourceReadRequest do
  @moduledoc """
  A resources/read request payload.
  """

  defstruct [:uri]

  @type t :: %__MODULE__{
    uri: String.t() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :uri) -> {:error, {:missing_field, :uri}}
      true ->
        {:ok, %__MODULE__{
          uri: Map.get(attrs, :uri)
        }}
    end
  end
end

defmodule BeamPM.MCP.Contracts.McpResourceReadResult do
  @moduledoc """
  A resources/read result payload.
  """

  defstruct [:contents]

  @type t :: %__MODULE__{
    contents: map() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :contents) -> {:error, {:missing_field, :contents}}
      true ->
        {:ok, %__MODULE__{
          contents: Map.get(attrs, :contents)
        }}
    end
  end
end

defmodule BeamPM.MCP.Contracts.McpTool do
  @moduledoc """
  A tool definition advertised by an MCP server.
  """

  defstruct [:name, :title, :description, :input_schema, :output_schema]

  @type t :: %__MODULE__{
    name: String.t() | nil,
    title: String.t() | nil,
    description: String.t() | nil,
    input_schema: map() | nil,
    output_schema: map() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :name) -> {:error, {:missing_field, :name}}
      not Map.has_key?(attrs, :input_schema) -> {:error, {:missing_field, :input_schema}}
      true ->
        {:ok, %__MODULE__{
          name: Map.get(attrs, :name),
          title: Map.get(attrs, :title),
          description: Map.get(attrs, :description),
          input_schema: Map.get(attrs, :input_schema),
          output_schema: Map.get(attrs, :output_schema)
        }}
    end
  end
end

defmodule BeamPM.MCP.Contracts.McpToolCallRequest do
  @moduledoc """
  A tools/call request payload.
  """

  defstruct [:name, :arguments]

  @type t :: %__MODULE__{
    name: String.t() | nil,
    arguments: map() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :name) -> {:error, {:missing_field, :name}}
      true ->
        {:ok, %__MODULE__{
          name: Map.get(attrs, :name),
          arguments: Map.get(attrs, :arguments)
        }}
    end
  end
end

defmodule BeamPM.MCP.Contracts.McpToolCallResult do
  @moduledoc """
  A tools/call result payload.
  """

  defstruct [:content, :is_error, :structured_content]

  @type t :: %__MODULE__{
    content: map() | nil,
    is_error: boolean() | nil,
    structured_content: map() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :content) -> {:error, {:missing_field, :content}}
      true ->
        {:ok, %__MODULE__{
          content: Map.get(attrs, :content),
          is_error: Map.get(attrs, :is_error),
          structured_content: Map.get(attrs, :structured_content)
        }}
    end
  end
end

defmodule BeamPM.MCP.Contracts.RpcErrorResponse do
  @moduledoc """
  A JSON-RPC 2.0 error response message.
  """

  defstruct [:jsonrpc, :id, :error]

  @type t :: %__MODULE__{
    jsonrpc: String.t() | nil,
    id: String.t() | nil,
    error: map() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :jsonrpc) -> {:error, {:missing_field, :jsonrpc}}
      not Map.has_key?(attrs, :id) -> {:error, {:missing_field, :id}}
      not Map.has_key?(attrs, :error) -> {:error, {:missing_field, :error}}
      true ->
        {:ok, %__MODULE__{
          jsonrpc: Map.get(attrs, :jsonrpc),
          id: Map.get(attrs, :id),
          error: Map.get(attrs, :error)
        }}
    end
  end
end

defmodule BeamPM.MCP.Contracts.RpcNotification do
  @moduledoc """
  A JSON-RPC 2.0 notification message (no id).
  """

  defstruct [:jsonrpc, :method, :params]

  @type t :: %__MODULE__{
    jsonrpc: String.t() | nil,
    method: String.t() | nil,
    params: map() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :jsonrpc) -> {:error, {:missing_field, :jsonrpc}}
      not Map.has_key?(attrs, :method) -> {:error, {:missing_field, :method}}
      true ->
        {:ok, %__MODULE__{
          jsonrpc: Map.get(attrs, :jsonrpc),
          method: Map.get(attrs, :method),
          params: Map.get(attrs, :params)
        }}
    end
  end
end

defmodule BeamPM.MCP.Contracts.RpcRequest do
  @moduledoc """
  A JSON-RPC 2.0 request message.
  """

  defstruct [:jsonrpc, :id, :method, :params]

  @type t :: %__MODULE__{
    jsonrpc: String.t() | nil,
    id: String.t() | nil,
    method: String.t() | nil,
    params: map() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :jsonrpc) -> {:error, {:missing_field, :jsonrpc}}
      not Map.has_key?(attrs, :id) -> {:error, {:missing_field, :id}}
      not Map.has_key?(attrs, :method) -> {:error, {:missing_field, :method}}
      true ->
        {:ok, %__MODULE__{
          jsonrpc: Map.get(attrs, :jsonrpc),
          id: Map.get(attrs, :id),
          method: Map.get(attrs, :method),
          params: Map.get(attrs, :params)
        }}
    end
  end
end

defmodule BeamPM.MCP.Contracts.RpcResultResponse do
  @moduledoc """
  A JSON-RPC 2.0 success response message.
  """

  defstruct [:jsonrpc, :id, :result]

  @type t :: %__MODULE__{
    jsonrpc: String.t() | nil,
    id: String.t() | nil,
    result: map() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :jsonrpc) -> {:error, {:missing_field, :jsonrpc}}
      not Map.has_key?(attrs, :id) -> {:error, {:missing_field, :id}}
      not Map.has_key?(attrs, :result) -> {:error, {:missing_field, :result}}
      true ->
        {:ok, %__MODULE__{
          jsonrpc: Map.get(attrs, :jsonrpc),
          id: Map.get(attrs, :id),
          result: Map.get(attrs, :result)
        }}
    end
  end
end

