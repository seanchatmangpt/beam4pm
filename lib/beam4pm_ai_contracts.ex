# GENERATED by ggen from the admitted aic:ContractType graph. Do not edit.

defmodule BeamPM.AI.Contracts.AdmissionDecision do
  @moduledoc "An independently adjudicated verdict on a subject (e.g. a tool_intent), carrying a typed reason and the policy identity it was decided under."

  defstruct [:decision_id, :subject_id, :principal_id, :verdict, :typed_reason, :policy_id, :policy_version, :decided_at]

  @type t :: %__MODULE__{
    decision_id: String.t() | nil,
    subject_id: String.t() | nil,
    principal_id: String.t() | nil,
    verdict: String.t() | nil,
    typed_reason: String.t() | nil,
    policy_id: String.t() | nil,
    policy_version: String.t() | nil,
    decided_at: String.t() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :decision_id) -> {:error, {:missing_field, :decision_id}}
      not Map.has_key?(attrs, :subject_id) -> {:error, {:missing_field, :subject_id}}
      not Map.has_key?(attrs, :verdict) -> {:error, {:missing_field, :verdict}}
      not Map.has_key?(attrs, :typed_reason) -> {:error, {:missing_field, :typed_reason}}
      true ->
        {:ok, %__MODULE__{
          decision_id: Map.get(attrs, :decision_id),
          subject_id: Map.get(attrs, :subject_id),
          principal_id: Map.get(attrs, :principal_id),
          verdict: Map.get(attrs, :verdict),
          typed_reason: Map.get(attrs, :typed_reason),
          policy_id: Map.get(attrs, :policy_id),
          policy_version: Map.get(attrs, :policy_version),
          decided_at: Map.get(attrs, :decided_at)
        }}
    end
  end
end

defmodule BeamPM.AI.Contracts.CandidateClaim do
  @moduledoc "A candidate factual claim produced by a research task, linked to the evidence that supports or contradicts it."

  defstruct [:claim_id, :task_id, :statement, :support_evidence_ids, :contradict_evidence_ids, :confidence, :semantic_status]

  @type t :: %__MODULE__{
    claim_id: String.t() | nil,
    task_id: String.t() | nil,
    statement: String.t() | nil,
    support_evidence_ids: [String.t()] | nil,
    contradict_evidence_ids: [String.t()] | nil,
    confidence: float() | nil,
    semantic_status: String.t() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :claim_id) -> {:error, {:missing_field, :claim_id}}
      not Map.has_key?(attrs, :task_id) -> {:error, {:missing_field, :task_id}}
      not Map.has_key?(attrs, :statement) -> {:error, {:missing_field, :statement}}
      not Map.has_key?(attrs, :semantic_status) -> {:error, {:missing_field, :semantic_status}}
      true ->
        {:ok, %__MODULE__{
          claim_id: Map.get(attrs, :claim_id),
          task_id: Map.get(attrs, :task_id),
          statement: Map.get(attrs, :statement),
          support_evidence_ids: Map.get(attrs, :support_evidence_ids),
          contradict_evidence_ids: Map.get(attrs, :contradict_evidence_ids),
          confidence: Map.get(attrs, :confidence),
          semantic_status: Map.get(attrs, :semantic_status)
        }}
    end
  end
end

defmodule BeamPM.AI.Contracts.EvidenceEnvelope do
  @moduledoc "A content-addressed unit of ingested evidence, carrying provenance, access-control, and redaction metadata for downstream research and grounding."

  defstruct [:evidence_id, :tenant_id, :source_uri, :source_kind, :content_hash, :mime_type, :modality, :observed_at, :retrieved_at, :acl_labels, :canonical_entity_ids, :redaction_class, :schema_version]

  @type t :: %__MODULE__{
    evidence_id: String.t() | nil,
    tenant_id: String.t() | nil,
    source_uri: String.t() | nil,
    source_kind: String.t() | nil,
    content_hash: String.t() | nil,
    mime_type: String.t() | nil,
    modality: String.t() | nil,
    observed_at: String.t() | nil,
    retrieved_at: String.t() | nil,
    acl_labels: [String.t()] | nil,
    canonical_entity_ids: [String.t()] | nil,
    redaction_class: String.t() | nil,
    schema_version: String.t() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :evidence_id) -> {:error, {:missing_field, :evidence_id}}
      not Map.has_key?(attrs, :source_uri) -> {:error, {:missing_field, :source_uri}}
      not Map.has_key?(attrs, :content_hash) -> {:error, {:missing_field, :content_hash}}
      not Map.has_key?(attrs, :schema_version) -> {:error, {:missing_field, :schema_version}}
      true ->
        {:ok, %__MODULE__{
          evidence_id: Map.get(attrs, :evidence_id),
          tenant_id: Map.get(attrs, :tenant_id),
          source_uri: Map.get(attrs, :source_uri),
          source_kind: Map.get(attrs, :source_kind),
          content_hash: Map.get(attrs, :content_hash),
          mime_type: Map.get(attrs, :mime_type),
          modality: Map.get(attrs, :modality),
          observed_at: Map.get(attrs, :observed_at),
          retrieved_at: Map.get(attrs, :retrieved_at),
          acl_labels: Map.get(attrs, :acl_labels),
          canonical_entity_ids: Map.get(attrs, :canonical_entity_ids),
          redaction_class: Map.get(attrs, :redaction_class),
          schema_version: Map.get(attrs, :schema_version)
        }}
    end
  end
end

defmodule BeamPM.AI.Contracts.EvidenceHit do
  @moduledoc "One scored retrieval result linking a query to an evidence_envelope."

  defstruct [:hit_id, :query_id, :evidence_id, :score, :retrieval_method, :rank]

  @type t :: %__MODULE__{
    hit_id: String.t() | nil,
    query_id: String.t() | nil,
    evidence_id: String.t() | nil,
    score: float() | nil,
    retrieval_method: String.t() | nil,
    rank: integer() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :hit_id) -> {:error, {:missing_field, :hit_id}}
      not Map.has_key?(attrs, :evidence_id) -> {:error, {:missing_field, :evidence_id}}
      not Map.has_key?(attrs, :score) -> {:error, {:missing_field, :score}}
      not Map.has_key?(attrs, :rank) -> {:error, {:missing_field, :rank}}
      true ->
        {:ok, %__MODULE__{
          hit_id: Map.get(attrs, :hit_id),
          query_id: Map.get(attrs, :query_id),
          evidence_id: Map.get(attrs, :evidence_id),
          score: Map.get(attrs, :score),
          retrieval_method: Map.get(attrs, :retrieval_method),
          rank: Map.get(attrs, :rank)
        }}
    end
  end
end

defmodule BeamPM.AI.Contracts.ModelCallReceipt do
  @moduledoc "Deterministic evidence binding a single model-gateway call's request and response identities, for replay and audit."

  defstruct [:call_id, :provider_profile, :model_id_or_snapshot, :request_hash, :response_hash, :prompt_template_id, :prompt_template_version, :latency_ms, :trace_id]

  @type t :: %__MODULE__{
    call_id: String.t() | nil,
    provider_profile: String.t() | nil,
    model_id_or_snapshot: String.t() | nil,
    request_hash: String.t() | nil,
    response_hash: String.t() | nil,
    prompt_template_id: String.t() | nil,
    prompt_template_version: String.t() | nil,
    latency_ms: integer() | nil,
    trace_id: String.t() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :call_id) -> {:error, {:missing_field, :call_id}}
      not Map.has_key?(attrs, :provider_profile) -> {:error, {:missing_field, :provider_profile}}
      not Map.has_key?(attrs, :request_hash) -> {:error, {:missing_field, :request_hash}}
      not Map.has_key?(attrs, :response_hash) -> {:error, {:missing_field, :response_hash}}
      true ->
        {:ok, %__MODULE__{
          call_id: Map.get(attrs, :call_id),
          provider_profile: Map.get(attrs, :provider_profile),
          model_id_or_snapshot: Map.get(attrs, :model_id_or_snapshot),
          request_hash: Map.get(attrs, :request_hash),
          response_hash: Map.get(attrs, :response_hash),
          prompt_template_id: Map.get(attrs, :prompt_template_id),
          prompt_template_version: Map.get(attrs, :prompt_template_version),
          latency_ms: Map.get(attrs, :latency_ms),
          trace_id: Map.get(attrs, :trace_id)
        }}
    end
  end
end

defmodule BeamPM.AI.Contracts.ResearchTask do
  @moduledoc "A bounded research request issued by a principal, scoping freshness, privacy, and resource limits for the work it authorizes."

  defstruct [:task_id, :principal_id, :query, :freshness_requirement, :privacy_class, :max_tokens, :max_tool_calls, :output_contract, :policy_version, :correlation_id]

  @type t :: %__MODULE__{
    task_id: String.t() | nil,
    principal_id: String.t() | nil,
    query: String.t() | nil,
    freshness_requirement: String.t() | nil,
    privacy_class: String.t() | nil,
    max_tokens: integer() | nil,
    max_tool_calls: integer() | nil,
    output_contract: String.t() | nil,
    policy_version: String.t() | nil,
    correlation_id: String.t() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :task_id) -> {:error, {:missing_field, :task_id}}
      not Map.has_key?(attrs, :principal_id) -> {:error, {:missing_field, :principal_id}}
      not Map.has_key?(attrs, :query) -> {:error, {:missing_field, :query}}
      not Map.has_key?(attrs, :policy_version) -> {:error, {:missing_field, :policy_version}}
      true ->
        {:ok, %__MODULE__{
          task_id: Map.get(attrs, :task_id),
          principal_id: Map.get(attrs, :principal_id),
          query: Map.get(attrs, :query),
          freshness_requirement: Map.get(attrs, :freshness_requirement),
          privacy_class: Map.get(attrs, :privacy_class),
          max_tokens: Map.get(attrs, :max_tokens),
          max_tool_calls: Map.get(attrs, :max_tool_calls),
          output_contract: Map.get(attrs, :output_contract),
          policy_version: Map.get(attrs, :policy_version),
          correlation_id: Map.get(attrs, :correlation_id)
        }}
    end
  end
end

defmodule BeamPM.AI.Contracts.ToolIntent do
  @moduledoc "A proposed tool invocation awaiting admission. Carries no execution authority of any kind -- authority and admission are the exclusive responsibility of the concurrent beam4pm-post-llm-runtime-pack's rt: vocabulary; this type is inert data describing what a caller would like to do."

  defstruct [:intent_id, :task_id, :tool_id, :tool_version, :canonical_args_hash, :side_effect_class, :required_capability, :dry_run, :idempotency_key, :status]

  @type t :: %__MODULE__{
    intent_id: String.t() | nil,
    task_id: String.t() | nil,
    tool_id: String.t() | nil,
    tool_version: String.t() | nil,
    canonical_args_hash: String.t() | nil,
    side_effect_class: String.t() | nil,
    required_capability: String.t() | nil,
    dry_run: boolean() | nil,
    idempotency_key: String.t() | nil,
    status: String.t() | nil
  }

  @spec new(map()) :: {:ok, t()} | {:error, {:missing_field, atom()}}
  def new(attrs) when is_map(attrs) do
    cond do
      not Map.has_key?(attrs, :intent_id) -> {:error, {:missing_field, :intent_id}}
      not Map.has_key?(attrs, :task_id) -> {:error, {:missing_field, :task_id}}
      not Map.has_key?(attrs, :tool_id) -> {:error, {:missing_field, :tool_id}}
      not Map.has_key?(attrs, :canonical_args_hash) -> {:error, {:missing_field, :canonical_args_hash}}
      not Map.has_key?(attrs, :side_effect_class) -> {:error, {:missing_field, :side_effect_class}}
      not Map.has_key?(attrs, :status) -> {:error, {:missing_field, :status}}
      true ->
        {:ok, %__MODULE__{
          intent_id: Map.get(attrs, :intent_id),
          task_id: Map.get(attrs, :task_id),
          tool_id: Map.get(attrs, :tool_id),
          tool_version: Map.get(attrs, :tool_version),
          canonical_args_hash: Map.get(attrs, :canonical_args_hash),
          side_effect_class: Map.get(attrs, :side_effect_class),
          required_capability: Map.get(attrs, :required_capability),
          dry_run: Map.get(attrs, :dry_run),
          idempotency_key: Map.get(attrs, :idempotency_key),
          status: Map.get(attrs, :status)
        }}
    end
  end
end

