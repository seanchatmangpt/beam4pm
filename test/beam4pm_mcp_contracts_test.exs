# GENERATED by ggen from the admitted mcp:MessageType graph. Do not edit.
defmodule BeamPM.MCP.Contracts.GeneratedTest do
  use ExUnit.Case, async: true

  test "mcp_resource new/1 succeeds when all fields are present" do
    attrs = %{
      uri: "x",
      name: "x",
      title: "x",
      description: "x",
      mime_type: "x",
      size: 1
    }

    assert {:ok, _} = BeamPM.MCP.Contracts.McpResource.new(attrs)
  end

  test "mcp_resource new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.MCP.Contracts.McpResource.new(%{})
  end


  test "mcp_resource_read_request new/1 succeeds when all fields are present" do
    attrs = %{
      uri: "x"
    }

    assert {:ok, _} = BeamPM.MCP.Contracts.McpResourceReadRequest.new(attrs)
  end

  test "mcp_resource_read_request new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.MCP.Contracts.McpResourceReadRequest.new(%{})
  end


  test "mcp_resource_read_result new/1 succeeds when all fields are present" do
    attrs = %{
      contents: %{}
    }

    assert {:ok, _} = BeamPM.MCP.Contracts.McpResourceReadResult.new(attrs)
  end

  test "mcp_resource_read_result new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.MCP.Contracts.McpResourceReadResult.new(%{})
  end


  test "mcp_tool new/1 succeeds when all fields are present" do
    attrs = %{
      name: "x",
      title: "x",
      description: "x",
      input_schema: %{},
      output_schema: %{}
    }

    assert {:ok, _} = BeamPM.MCP.Contracts.McpTool.new(attrs)
  end

  test "mcp_tool new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.MCP.Contracts.McpTool.new(%{})
  end


  test "mcp_tool_call_request new/1 succeeds when all fields are present" do
    attrs = %{
      name: "x",
      arguments: %{}
    }

    assert {:ok, _} = BeamPM.MCP.Contracts.McpToolCallRequest.new(attrs)
  end

  test "mcp_tool_call_request new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.MCP.Contracts.McpToolCallRequest.new(%{})
  end


  test "mcp_tool_call_result new/1 succeeds when all fields are present" do
    attrs = %{
      content: %{},
      is_error: true,
      structured_content: %{}
    }

    assert {:ok, _} = BeamPM.MCP.Contracts.McpToolCallResult.new(attrs)
  end

  test "mcp_tool_call_result new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.MCP.Contracts.McpToolCallResult.new(%{})
  end


  test "rpc_error_response new/1 succeeds when all fields are present" do
    attrs = %{
      jsonrpc: "x",
      id: "x",
      error: %{}
    }

    assert {:ok, _} = BeamPM.MCP.Contracts.RpcErrorResponse.new(attrs)
  end

  test "rpc_error_response new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.MCP.Contracts.RpcErrorResponse.new(%{})
  end


  test "rpc_notification new/1 succeeds when all fields are present" do
    attrs = %{
      jsonrpc: "x",
      method: "x",
      params: %{}
    }

    assert {:ok, _} = BeamPM.MCP.Contracts.RpcNotification.new(attrs)
  end

  test "rpc_notification new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.MCP.Contracts.RpcNotification.new(%{})
  end


  test "rpc_request new/1 succeeds when all fields are present" do
    attrs = %{
      jsonrpc: "x",
      id: "x",
      method: "x",
      params: %{}
    }

    assert {:ok, _} = BeamPM.MCP.Contracts.RpcRequest.new(attrs)
  end

  test "rpc_request new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.MCP.Contracts.RpcRequest.new(%{})
  end


  test "rpc_result_response new/1 succeeds when all fields are present" do
    attrs = %{
      jsonrpc: "x",
      id: "x",
      result: %{}
    }

    assert {:ok, _} = BeamPM.MCP.Contracts.RpcResultResponse.new(attrs)
  end

  test "rpc_result_response new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.MCP.Contracts.RpcResultResponse.new(%{})
  end

end
