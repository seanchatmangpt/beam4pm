# GENERATED by ggen from the admitted aic:ContractType graph. Do not edit.
defmodule BeamPM.AI.Contracts.Codec.GeneratedTest do
  use ExUnit.Case, async: true

  test "from_map returns unknown_record for an unadmitted record name" do
    assert {:error, {:unknown_record, :not_a_real_record}} =
             BeamPM.AI.Contracts.Codec.from_map(:not_a_real_record, %{})
  end

  test "decode returns unknown_record for an unadmitted record name" do
    assert {:error, {:unknown_record, :not_a_real_record}} =
             BeamPM.AI.Contracts.Codec.decode(:not_a_real_record, "{}")
  end

  test "decode wraps invalid JSON input in an error tuple" do
    assert {:error, _} = BeamPM.AI.Contracts.Codec.decode(:not_a_real_record, "this is not json")
  end

  test "decode rejects JSON that is not an object" do
    assert {:error, {:not_a_json_object, [1, 2, 3]}} =
             BeamPM.AI.Contracts.Codec.decode(:not_a_real_record, "[1,2,3]")
  end

  test "admission_decision to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      decision_id: "sample_decision_id",
      subject_id: "sample_subject_id",
      principal_id: "sample_principal_id",
      verdict: "sample_verdict",
      typed_reason: "sample_typed_reason",
      policy_id: "sample_policy_id",
      policy_version: "sample_policy_version",
      decided_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.AdmissionDecision.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    assert m["decision_id"] == "sample_decision_id"
    assert m["subject_id"] == "sample_subject_id"
    assert m["principal_id"] == "sample_principal_id"
    assert m["verdict"] == "sample_verdict"
    assert m["typed_reason"] == "sample_typed_reason"
    assert m["policy_id"] == "sample_policy_id"
    assert m["policy_version"] == "sample_policy_version"
    assert m["decided_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 8
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:admission_decision, m)
    assert {:ok, ^rec} =
             BeamPM.AI.Contracts.Codec.from_map(:admission_decision, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "admission_decision encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      decision_id: "sample_decision_id",
      subject_id: "sample_subject_id",
      principal_id: "sample_principal_id",
      verdict: "sample_verdict",
      typed_reason: "sample_typed_reason",
      policy_id: "sample_policy_id",
      policy_version: "sample_policy_version",
      decided_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.AdmissionDecision.new(attrs)
    json = BeamPM.AI.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.decode(:admission_decision, json)
  end

  test "admission_decision from_map reports the first missing required field" do
    assert {:error, {:missing_field, :decision_id}} =
             BeamPM.AI.Contracts.Codec.from_map(:admission_decision, %{})
  end

  test "admission_decision to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      decision_id: "sample_decision_id",
      subject_id: "sample_subject_id",
      verdict: "sample_verdict",
      typed_reason: "sample_typed_reason"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.AdmissionDecision.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "principal_id")
    refute Map.has_key?(m, "policy_id")
    refute Map.has_key?(m, "policy_version")
    refute Map.has_key?(m, "decided_at")
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:admission_decision, m)
  end

  test "candidate_claim to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      claim_id: "sample_claim_id",
      task_id: "sample_task_id",
      statement: "sample_statement",
      support_evidence_ids: ["alpha", "beta"],
      contradict_evidence_ids: ["alpha", "beta"],
      confidence: 3.5,
      semantic_status: "sample_semantic_status"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.CandidateClaim.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    assert m["claim_id"] == "sample_claim_id"
    assert m["task_id"] == "sample_task_id"
    assert m["statement"] == "sample_statement"
    assert m["support_evidence_ids"] == ["alpha", "beta"]
    assert m["contradict_evidence_ids"] == ["alpha", "beta"]
    assert m["confidence"] == 3.5
    assert m["semantic_status"] == "sample_semantic_status"
    assert map_size(m) == 7
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:candidate_claim, m)
    assert {:ok, ^rec} =
             BeamPM.AI.Contracts.Codec.from_map(:candidate_claim, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "candidate_claim encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      claim_id: "sample_claim_id",
      task_id: "sample_task_id",
      statement: "sample_statement",
      support_evidence_ids: ["alpha", "beta"],
      contradict_evidence_ids: ["alpha", "beta"],
      confidence: 3.5,
      semantic_status: "sample_semantic_status"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.CandidateClaim.new(attrs)
    json = BeamPM.AI.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.decode(:candidate_claim, json)
  end

  test "candidate_claim from_map reports the first missing required field" do
    assert {:error, {:missing_field, :claim_id}} =
             BeamPM.AI.Contracts.Codec.from_map(:candidate_claim, %{})
  end

  test "candidate_claim to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      claim_id: "sample_claim_id",
      task_id: "sample_task_id",
      statement: "sample_statement",
      semantic_status: "sample_semantic_status"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.CandidateClaim.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "support_evidence_ids")
    refute Map.has_key?(m, "contradict_evidence_ids")
    refute Map.has_key?(m, "confidence")
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:candidate_claim, m)
  end

  test "evidence_envelope to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      tenant_id: "sample_tenant_id",
      source_uri: "sample_source_uri",
      source_kind: "sample_source_kind",
      content_hash: "sample_content_hash",
      mime_type: "sample_mime_type",
      modality: "sample_modality",
      observed_at: "2026-08-29T12:00:00Z",
      retrieved_at: "2026-08-29T12:00:00Z",
      acl_labels: ["alpha", "beta"],
      canonical_entity_ids: ["alpha", "beta"],
      redaction_class: "sample_redaction_class",
      schema_version: "sample_schema_version"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.EvidenceEnvelope.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["source_uri"] == "sample_source_uri"
    assert m["source_kind"] == "sample_source_kind"
    assert m["content_hash"] == "sample_content_hash"
    assert m["mime_type"] == "sample_mime_type"
    assert m["modality"] == "sample_modality"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert m["retrieved_at"] == "2026-08-29T12:00:00Z"
    assert m["acl_labels"] == ["alpha", "beta"]
    assert m["canonical_entity_ids"] == ["alpha", "beta"]
    assert m["redaction_class"] == "sample_redaction_class"
    assert m["schema_version"] == "sample_schema_version"
    assert map_size(m) == 13
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:evidence_envelope, m)
    assert {:ok, ^rec} =
             BeamPM.AI.Contracts.Codec.from_map(:evidence_envelope, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "evidence_envelope encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      tenant_id: "sample_tenant_id",
      source_uri: "sample_source_uri",
      source_kind: "sample_source_kind",
      content_hash: "sample_content_hash",
      mime_type: "sample_mime_type",
      modality: "sample_modality",
      observed_at: "2026-08-29T12:00:00Z",
      retrieved_at: "2026-08-29T12:00:00Z",
      acl_labels: ["alpha", "beta"],
      canonical_entity_ids: ["alpha", "beta"],
      redaction_class: "sample_redaction_class",
      schema_version: "sample_schema_version"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.EvidenceEnvelope.new(attrs)
    json = BeamPM.AI.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.decode(:evidence_envelope, json)
  end

  test "evidence_envelope from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.AI.Contracts.Codec.from_map(:evidence_envelope, %{})
  end

  test "evidence_envelope to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      source_uri: "sample_source_uri",
      content_hash: "sample_content_hash",
      schema_version: "sample_schema_version"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.EvidenceEnvelope.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "tenant_id")
    refute Map.has_key?(m, "source_kind")
    refute Map.has_key?(m, "mime_type")
    refute Map.has_key?(m, "modality")
    refute Map.has_key?(m, "observed_at")
    refute Map.has_key?(m, "retrieved_at")
    refute Map.has_key?(m, "acl_labels")
    refute Map.has_key?(m, "canonical_entity_ids")
    refute Map.has_key?(m, "redaction_class")
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:evidence_envelope, m)
  end

  test "evidence_hit to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      hit_id: "sample_hit_id",
      query_id: "sample_query_id",
      evidence_id: "sample_evidence_id",
      score: 3.5,
      retrieval_method: "sample_retrieval_method",
      rank: 42
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.EvidenceHit.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    assert m["hit_id"] == "sample_hit_id"
    assert m["query_id"] == "sample_query_id"
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["score"] == 3.5
    assert m["retrieval_method"] == "sample_retrieval_method"
    assert m["rank"] == 42
    assert map_size(m) == 6
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:evidence_hit, m)
    assert {:ok, ^rec} =
             BeamPM.AI.Contracts.Codec.from_map(:evidence_hit, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "evidence_hit encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      hit_id: "sample_hit_id",
      query_id: "sample_query_id",
      evidence_id: "sample_evidence_id",
      score: 3.5,
      retrieval_method: "sample_retrieval_method",
      rank: 42
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.EvidenceHit.new(attrs)
    json = BeamPM.AI.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.decode(:evidence_hit, json)
  end

  test "evidence_hit from_map reports the first missing required field" do
    assert {:error, {:missing_field, :hit_id}} =
             BeamPM.AI.Contracts.Codec.from_map(:evidence_hit, %{})
  end

  test "evidence_hit to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      hit_id: "sample_hit_id",
      evidence_id: "sample_evidence_id",
      score: 3.5,
      rank: 42
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.EvidenceHit.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "query_id")
    refute Map.has_key?(m, "retrieval_method")
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:evidence_hit, m)
  end

  test "model_call_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      call_id: "sample_call_id",
      provider_profile: "sample_provider_profile",
      model_id_or_snapshot: "sample_model_id_or_snapshot",
      request_hash: "sample_request_hash",
      response_hash: "sample_response_hash",
      prompt_template_id: "sample_prompt_template_id",
      prompt_template_version: "sample_prompt_template_version",
      latency_ms: 42,
      trace_id: "sample_trace_id"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.ModelCallReceipt.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    assert m["call_id"] == "sample_call_id"
    assert m["provider_profile"] == "sample_provider_profile"
    assert m["model_id_or_snapshot"] == "sample_model_id_or_snapshot"
    assert m["request_hash"] == "sample_request_hash"
    assert m["response_hash"] == "sample_response_hash"
    assert m["prompt_template_id"] == "sample_prompt_template_id"
    assert m["prompt_template_version"] == "sample_prompt_template_version"
    assert m["latency_ms"] == 42
    assert m["trace_id"] == "sample_trace_id"
    assert map_size(m) == 9
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:model_call_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.AI.Contracts.Codec.from_map(:model_call_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "model_call_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      call_id: "sample_call_id",
      provider_profile: "sample_provider_profile",
      model_id_or_snapshot: "sample_model_id_or_snapshot",
      request_hash: "sample_request_hash",
      response_hash: "sample_response_hash",
      prompt_template_id: "sample_prompt_template_id",
      prompt_template_version: "sample_prompt_template_version",
      latency_ms: 42,
      trace_id: "sample_trace_id"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.ModelCallReceipt.new(attrs)
    json = BeamPM.AI.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.decode(:model_call_receipt, json)
  end

  test "model_call_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :call_id}} =
             BeamPM.AI.Contracts.Codec.from_map(:model_call_receipt, %{})
  end

  test "model_call_receipt to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      call_id: "sample_call_id",
      provider_profile: "sample_provider_profile",
      request_hash: "sample_request_hash",
      response_hash: "sample_response_hash"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.ModelCallReceipt.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "model_id_or_snapshot")
    refute Map.has_key?(m, "prompt_template_id")
    refute Map.has_key?(m, "prompt_template_version")
    refute Map.has_key?(m, "latency_ms")
    refute Map.has_key?(m, "trace_id")
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:model_call_receipt, m)
  end

  test "research_task to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      task_id: "sample_task_id",
      principal_id: "sample_principal_id",
      query: "sample_query",
      freshness_requirement: "sample_freshness_requirement",
      privacy_class: "sample_privacy_class",
      max_tokens: 42,
      max_tool_calls: 42,
      output_contract: "sample_output_contract",
      policy_version: "sample_policy_version",
      correlation_id: "sample_correlation_id"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.ResearchTask.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    assert m["task_id"] == "sample_task_id"
    assert m["principal_id"] == "sample_principal_id"
    assert m["query"] == "sample_query"
    assert m["freshness_requirement"] == "sample_freshness_requirement"
    assert m["privacy_class"] == "sample_privacy_class"
    assert m["max_tokens"] == 42
    assert m["max_tool_calls"] == 42
    assert m["output_contract"] == "sample_output_contract"
    assert m["policy_version"] == "sample_policy_version"
    assert m["correlation_id"] == "sample_correlation_id"
    assert map_size(m) == 10
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:research_task, m)
    assert {:ok, ^rec} =
             BeamPM.AI.Contracts.Codec.from_map(:research_task, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "research_task encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      task_id: "sample_task_id",
      principal_id: "sample_principal_id",
      query: "sample_query",
      freshness_requirement: "sample_freshness_requirement",
      privacy_class: "sample_privacy_class",
      max_tokens: 42,
      max_tool_calls: 42,
      output_contract: "sample_output_contract",
      policy_version: "sample_policy_version",
      correlation_id: "sample_correlation_id"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.ResearchTask.new(attrs)
    json = BeamPM.AI.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.decode(:research_task, json)
  end

  test "research_task from_map reports the first missing required field" do
    assert {:error, {:missing_field, :task_id}} =
             BeamPM.AI.Contracts.Codec.from_map(:research_task, %{})
  end

  test "research_task to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      task_id: "sample_task_id",
      principal_id: "sample_principal_id",
      query: "sample_query",
      policy_version: "sample_policy_version"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.ResearchTask.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "freshness_requirement")
    refute Map.has_key?(m, "privacy_class")
    refute Map.has_key?(m, "max_tokens")
    refute Map.has_key?(m, "max_tool_calls")
    refute Map.has_key?(m, "output_contract")
    refute Map.has_key?(m, "correlation_id")
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:research_task, m)
  end

  test "tool_intent to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      intent_id: "sample_intent_id",
      task_id: "sample_task_id",
      tool_id: "sample_tool_id",
      tool_version: "sample_tool_version",
      canonical_args_hash: "sample_canonical_args_hash",
      side_effect_class: "sample_side_effect_class",
      required_capability: "sample_required_capability",
      dry_run: true,
      idempotency_key: "sample_idempotency_key",
      status: "sample_status"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.ToolIntent.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    assert m["intent_id"] == "sample_intent_id"
    assert m["task_id"] == "sample_task_id"
    assert m["tool_id"] == "sample_tool_id"
    assert m["tool_version"] == "sample_tool_version"
    assert m["canonical_args_hash"] == "sample_canonical_args_hash"
    assert m["side_effect_class"] == "sample_side_effect_class"
    assert m["required_capability"] == "sample_required_capability"
    assert m["dry_run"] == true
    assert m["idempotency_key"] == "sample_idempotency_key"
    assert m["status"] == "sample_status"
    assert map_size(m) == 10
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:tool_intent, m)
    assert {:ok, ^rec} =
             BeamPM.AI.Contracts.Codec.from_map(:tool_intent, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "tool_intent encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      intent_id: "sample_intent_id",
      task_id: "sample_task_id",
      tool_id: "sample_tool_id",
      tool_version: "sample_tool_version",
      canonical_args_hash: "sample_canonical_args_hash",
      side_effect_class: "sample_side_effect_class",
      required_capability: "sample_required_capability",
      dry_run: true,
      idempotency_key: "sample_idempotency_key",
      status: "sample_status"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.ToolIntent.new(attrs)
    json = BeamPM.AI.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.decode(:tool_intent, json)
  end

  test "tool_intent from_map reports the first missing required field" do
    assert {:error, {:missing_field, :intent_id}} =
             BeamPM.AI.Contracts.Codec.from_map(:tool_intent, %{})
  end

  test "tool_intent to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      intent_id: "sample_intent_id",
      task_id: "sample_task_id",
      tool_id: "sample_tool_id",
      canonical_args_hash: "sample_canonical_args_hash",
      side_effect_class: "sample_side_effect_class",
      status: "sample_status"
    }

    assert {:ok, rec} = BeamPM.AI.Contracts.ToolIntent.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "tool_version")
    refute Map.has_key?(m, "required_capability")
    refute Map.has_key?(m, "dry_run")
    refute Map.has_key?(m, "idempotency_key")
    assert map_size(m) == 6
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:tool_intent, m)
  end
end
