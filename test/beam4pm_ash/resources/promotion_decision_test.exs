# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PromotionDecisionTest do
  use ExUnit.Case, async: false

  test "promotion_decision: real Ash ETS create/read round-trips every field" do
    params =
      %{
      decision_id: "sample_decision_id",
      candidate_policy_id: "sample_candidate_policy_id",
      threshold_id: "sample_threshold_id",
      evidence_set_digest: "sample_evidence_set_digest",
      decision: "sample_decision",
      refusal_code: "sample_refusal_code"
      }

    created =
      BeamPM.Ash.Resources.PromotionDecision
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PromotionDecision)
    assert read_back.id == created.id
    assert read_back.decision_id == "sample_decision_id"
    assert read_back.candidate_policy_id == "sample_candidate_policy_id"
    assert read_back.threshold_id == "sample_threshold_id"
    assert read_back.evidence_set_digest == "sample_evidence_set_digest"
    assert read_back.decision == "sample_decision"
    assert read_back.refusal_code == "sample_refusal_code"
  end
end

