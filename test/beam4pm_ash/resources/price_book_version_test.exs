# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PriceBookVersionTest do
  use ExUnit.Case, async: false

  test "price_book_version: real Ash ETS create/read round-trips every field" do
    params =
      %{
      price_book_version_id: "sample_price_book_version_id",
      account_id: "sample_account_id",
      version: "sample_version",
      evidence_digest: "sample_evidence_digest",
      effective_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.PriceBookVersion
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PriceBookVersion)
    assert read_back.id == created.id
    assert read_back.price_book_version_id == "sample_price_book_version_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.version == "sample_version"
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.effective_at == ~U[2026-08-29 12:00:00Z]
  end
end

