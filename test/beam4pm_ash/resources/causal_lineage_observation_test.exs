# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CausalLineageObservationTest do
  use ExUnit.Case, async: false

  test "causal_lineage_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      cause_observation_id: "sample_cause_observation_id",
      effect_observation_id: "sample_effect_observation_id",
      causal_basis: "sample_causal_basis",
      evidence_digest: "sample_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.CausalLineageObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CausalLineageObservation)
    assert read_back.id == created.id
    assert read_back.cause_observation_id == "sample_cause_observation_id"
    assert read_back.effect_observation_id == "sample_effect_observation_id"
    assert read_back.causal_basis == "sample_causal_basis"
    assert read_back.evidence_digest == "sample_evidence_digest"
  end
end

