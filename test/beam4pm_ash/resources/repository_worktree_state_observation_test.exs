# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RepositoryWorktreeStateObservationTest do
  use ExUnit.Case, async: false

  test "repository_worktree_state_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      repository_id: "sample_repository_id",
      worktree_hash: "sample_worktree_hash",
      dirty_path_count: 42,
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.RepositoryWorktreeStateObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RepositoryWorktreeStateObservation)
    assert read_back.id == created.id
    assert read_back.repository_id == "sample_repository_id"
    assert read_back.worktree_hash == "sample_worktree_hash"
    assert read_back.dirty_path_count == 42
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

