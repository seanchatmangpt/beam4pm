# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PsroPopulationTest do
  use ExUnit.Case, async: false

  test "psro_population: real Ash ETS create/read round-trips every field" do
    params =
      %{
      population_id: "sample_population_id",
      policy_ids: "sample_policy_ids",
      population_hash: "sample_population_hash"
      }

    created =
      BeamPM.Ash.Resources.PsroPopulation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PsroPopulation)
    assert read_back.id == created.id
    assert read_back.population_id == "sample_population_id"
    assert read_back.policy_ids == "sample_policy_ids"
    assert read_back.population_hash == "sample_population_hash"
  end
end

