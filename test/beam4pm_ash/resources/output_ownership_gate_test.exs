# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.OutputOwnershipGateTest do
  use ExUnit.Case, async: false

  test "output_ownership_gate: real Ash ETS create/read round-trips every field" do
    params =
      %{
      subject_id: "sample_subject_id",
      ownership_manifest_hash: "sample_ownership_manifest_hash",
      standing: "sample_standing"
      }

    created =
      BeamPM.Ash.Resources.OutputOwnershipGate
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.OutputOwnershipGate)
    assert read_back.id == created.id
    assert read_back.subject_id == "sample_subject_id"
    assert read_back.ownership_manifest_hash == "sample_ownership_manifest_hash"
    assert read_back.standing == "sample_standing"
  end
end

