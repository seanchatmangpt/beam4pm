# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CounterfactualFrontierTest do
  use ExUnit.Case, async: false

  test "counterfactual_frontier: real Ash ETS create/read round-trips every field" do
    params =
      %{
      option_set_id: "sample_option_set_id",
      world_model_hash: "sample_world_model_hash",
      frontier_hash: "sample_frontier_hash"
      }

    created =
      BeamPM.Ash.Resources.CounterfactualFrontier
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CounterfactualFrontier)
    assert read_back.id == created.id
    assert read_back.option_set_id == "sample_option_set_id"
    assert read_back.world_model_hash == "sample_world_model_hash"
    assert read_back.frontier_hash == "sample_frontier_hash"
  end
end

