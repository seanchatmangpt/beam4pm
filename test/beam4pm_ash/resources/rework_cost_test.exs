# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ReworkCostTest do
  use ExUnit.Case, async: false

  test "rework_cost: real Ash ETS create/read round-trips every field" do
    params =
      %{
      case_id: "sample_case_id",
      loop_count: 42,
      weighted_cost: 3.5
      }

    created =
      BeamPM.Ash.Resources.ReworkCost
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ReworkCost)
    assert read_back.id == created.id
    assert read_back.case_id == "sample_case_id"
    assert read_back.loop_count == 42
    assert read_back.weighted_cost == 3.5
  end
end

