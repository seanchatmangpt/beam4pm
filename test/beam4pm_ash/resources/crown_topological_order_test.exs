# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CrownTopologicalOrderTest do
  use ExUnit.Case, async: false

  test "crown_topological_order: real Ash ETS create/read round-trips every field" do
    params =
      %{
      propagation_id: "sample_propagation_id",
      subject_sha: "sample_subject_sha",
      topological_rank: "sample_topological_rank",
      receipt_digest: "sample_receipt_digest"
      }

    created =
      BeamPM.Ash.Resources.CrownTopologicalOrder
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CrownTopologicalOrder)
    assert read_back.id == created.id
    assert read_back.propagation_id == "sample_propagation_id"
    assert read_back.subject_sha == "sample_subject_sha"
    assert read_back.topological_rank == "sample_topological_rank"
    assert read_back.receipt_digest == "sample_receipt_digest"
  end
end

