# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RefusalThresholdTest do
  use ExUnit.Case, async: false

  test "refusal_threshold: real Ash ETS create/read round-trips every field" do
    params =
      %{
      threshold_id: "sample_threshold_id",
      refusal_class: "sample_refusal_class",
      limit: 3.5,
      metric_id: "sample_metric_id",
      authority_binding: "sample_authority_binding"
      }

    created =
      BeamPM.Ash.Resources.RefusalThreshold
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RefusalThreshold)
    assert read_back.id == created.id
    assert read_back.threshold_id == "sample_threshold_id"
    assert read_back.refusal_class == "sample_refusal_class"
    assert read_back.limit == 3.5
    assert read_back.metric_id == "sample_metric_id"
    assert read_back.authority_binding == "sample_authority_binding"
  end
end

