# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RegressionDetectorTest do
  use ExUnit.Case, async: false

  test "regression_detector: real Ash ETS create/read round-trips every field" do
    params =
      %{
      detection_id: "sample_detection_id",
      baseline_policy_id: "sample_baseline_policy_id",
      candidate_policy_id: "sample_candidate_policy_id",
      metric_id: "sample_metric_id",
      regression_delta: 3.5,
      evidence_digest: "sample_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.RegressionDetector
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RegressionDetector)
    assert read_back.id == created.id
    assert read_back.detection_id == "sample_detection_id"
    assert read_back.baseline_policy_id == "sample_baseline_policy_id"
    assert read_back.candidate_policy_id == "sample_candidate_policy_id"
    assert read_back.metric_id == "sample_metric_id"
    assert read_back.regression_delta == 3.5
    assert read_back.evidence_digest == "sample_evidence_digest"
  end
end

