# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CostOfDelayScoreTest do
  use ExUnit.Case, async: false

  test "cost_of_delay_score: real Ash ETS create/read round-trips every field" do
    params =
      %{
      option_id: "sample_option_id",
      horizon: "sample_horizon",
      score: "sample_score"
      }

    created =
      BeamPM.Ash.Resources.CostOfDelayScore
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CostOfDelayScore)
    assert read_back.id == created.id
    assert read_back.option_id == "sample_option_id"
    assert read_back.horizon == "sample_horizon"
    assert read_back.score == "sample_score"
  end
end

