# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.SubmoduleRegistrationObservationTest do
  use ExUnit.Case, async: false

  test "submodule_registration_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      submodule_path: "sample_submodule_path",
      registration_state: "sample_registration_state",
      repository_url: "sample_repository_url",
      evidence_digest: "sample_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.SubmoduleRegistrationObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.SubmoduleRegistrationObservation)
    assert read_back.id == created.id
    assert read_back.submodule_path == "sample_submodule_path"
    assert read_back.registration_state == "sample_registration_state"
    assert read_back.repository_url == "sample_repository_url"
    assert read_back.evidence_digest == "sample_evidence_digest"
  end
end

