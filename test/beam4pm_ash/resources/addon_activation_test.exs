# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.AddonActivationTest do
  use ExUnit.Case, async: false

  test "addon_activation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      addon_activation_id: "sample_addon_activation_id",
      account_id: "sample_account_id",
      addon_id: "sample_addon_id",
      evidence_digest: "sample_evidence_digest",
      effective_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.AddonActivation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.AddonActivation)
    assert read_back.id == created.id
    assert read_back.addon_activation_id == "sample_addon_activation_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.addon_id == "sample_addon_id"
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.effective_at == ~U[2026-08-29 12:00:00Z]
  end
end

