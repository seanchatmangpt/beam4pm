# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PromotionThresholdTest do
  use ExUnit.Case, async: false

  test "promotion_threshold: real Ash ETS create/read round-trips every field" do
    params =
      %{
      threshold_id: "sample_threshold_id",
      objective_id: "sample_objective_id",
      minimum_payoff: 3.5,
      maximum_regression: 3.5,
      minimum_evidence_count: 42,
      authority_binding: "sample_authority_binding"
      }

    created =
      BeamPM.Ash.Resources.PromotionThreshold
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PromotionThreshold)
    assert read_back.id == created.id
    assert read_back.threshold_id == "sample_threshold_id"
    assert read_back.objective_id == "sample_objective_id"
    assert read_back.minimum_payoff == 3.5
    assert read_back.maximum_regression == 3.5
    assert read_back.minimum_evidence_count == 42
    assert read_back.authority_binding == "sample_authority_binding"
  end
end

