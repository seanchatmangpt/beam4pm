# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.BeamSearchCandidateTest do
  use ExUnit.Case, async: false

  test "beam_search_candidate: real Ash ETS create/read round-trips every field" do
    params =
      %{
      plan_id: "sample_plan_id",
      beam_width: "sample_beam_width",
      frontier_hash: "sample_frontier_hash"
      }

    created =
      BeamPM.Ash.Resources.BeamSearchCandidate
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.BeamSearchCandidate)
    assert read_back.id == created.id
    assert read_back.plan_id == "sample_plan_id"
    assert read_back.beam_width == "sample_beam_width"
    assert read_back.frontier_hash == "sample_frontier_hash"
  end
end

