# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PlannerPayoffObservationTest do
  use ExUnit.Case, async: false

  test "planner_payoff_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      observation_id: "sample_observation_id",
      planner_id: "sample_planner_id",
      objective_id: "sample_objective_id",
      payoff: 3.5,
      evidence_digest: "sample_evidence_digest",
      episode_id: "sample_episode_id"
      }

    created =
      BeamPM.Ash.Resources.PlannerPayoffObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PlannerPayoffObservation)
    assert read_back.id == created.id
    assert read_back.observation_id == "sample_observation_id"
    assert read_back.planner_id == "sample_planner_id"
    assert read_back.objective_id == "sample_objective_id"
    assert read_back.payoff == 3.5
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.episode_id == "sample_episode_id"
  end
end

