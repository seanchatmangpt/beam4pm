# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.LateArrivingUsageTest do
  use ExUnit.Case, async: false

  test "late_arriving_usage: real Ash ETS create/read round-trips every field" do
    params =
      %{
      late_arriving_usage_id: "sample_late_arriving_usage_id",
      account_id: "sample_account_id",
      occurred_at: "2026-08-29T12:00:00Z",
      evidence_digest: "sample_evidence_digest",
      effective_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.LateArrivingUsage
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.LateArrivingUsage)
    assert read_back.id == created.id
    assert read_back.late_arriving_usage_id == "sample_late_arriving_usage_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.occurred_at == ~U[2026-08-29 12:00:00Z]
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.effective_at == ~U[2026-08-29 12:00:00Z]
  end
end

