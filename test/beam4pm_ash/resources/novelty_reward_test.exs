# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.NoveltyRewardTest do
  use ExUnit.Case, async: false

  test "novelty_reward: real Ash ETS create/read round-trips every field" do
    params =
      %{
      reward_id: "sample_reward_id",
      hypothesis_id: "sample_hypothesis_id",
      feature_vector_digest: "sample_feature_vector_digest",
      nearest_prior_distance: 3.5,
      reward: 3.5,
      evidence_digest: "sample_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.NoveltyReward
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.NoveltyReward)
    assert read_back.id == created.id
    assert read_back.reward_id == "sample_reward_id"
    assert read_back.hypothesis_id == "sample_hypothesis_id"
    assert read_back.feature_vector_digest == "sample_feature_vector_digest"
    assert read_back.nearest_prior_distance == 3.5
    assert read_back.reward == 3.5
    assert read_back.evidence_digest == "sample_evidence_digest"
  end
end

