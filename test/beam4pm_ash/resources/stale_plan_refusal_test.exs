# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.StalePlanRefusalTest do
  use ExUnit.Case, async: false

  test "stale_plan_refusal: real Ash ETS create/read round-trips every field" do
    params =
      %{
      plan_id: "sample_plan_id",
      admitted_preimage_hash: "sample_admitted_preimage_hash",
      observed_preimage_hash: "sample_observed_preimage_hash"
      }

    created =
      BeamPM.Ash.Resources.StalePlanRefusal
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.StalePlanRefusal)
    assert read_back.id == created.id
    assert read_back.plan_id == "sample_plan_id"
    assert read_back.admitted_preimage_hash == "sample_admitted_preimage_hash"
    assert read_back.observed_preimage_hash == "sample_observed_preimage_hash"
  end
end

