# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RuntimeHealthObservationTest do
  use ExUnit.Case, async: false

  test "runtime_health_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      runtime_id: "sample_runtime_id",
      health_state: "sample_health_state",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.RuntimeHealthObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RuntimeHealthObservation)
    assert read_back.id == created.id
    assert read_back.runtime_id == "sample_runtime_id"
    assert read_back.health_state == "sample_health_state"
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

