# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ManufactureReceiptValidityObservationTest do
  use ExUnit.Case, async: false

  test "manufacture_receipt_validity_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      receipt_digest: "sample_receipt_digest",
      subject_sha: "sample_subject_sha",
      verification_status: "sample_verification_status",
      verifier_identity: "sample_verifier_identity"
      }

    created =
      BeamPM.Ash.Resources.ManufactureReceiptValidityObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ManufactureReceiptValidityObservation)
    assert read_back.id == created.id
    assert read_back.receipt_digest == "sample_receipt_digest"
    assert read_back.subject_sha == "sample_subject_sha"
    assert read_back.verification_status == "sample_verification_status"
    assert read_back.verifier_identity == "sample_verifier_identity"
  end
end

