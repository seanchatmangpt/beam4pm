# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.WipLimitGateTest do
  use ExUnit.Case, async: false

  test "wip_limit_gate: real Ash ETS create/read round-trips every field" do
    params =
      %{
      episode_id: "sample_episode_id",
      wip_limit: "sample_wip_limit",
      standing: "sample_standing"
      }

    created =
      BeamPM.Ash.Resources.WipLimitGate
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.WipLimitGate)
    assert read_back.id == created.id
    assert read_back.episode_id == "sample_episode_id"
    assert read_back.wip_limit == "sample_wip_limit"
    assert read_back.standing == "sample_standing"
  end
end

