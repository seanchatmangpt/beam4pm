# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CrownConvergenceProofTest do
  use ExUnit.Case, async: false

  test "crown_convergence_proof: real Ash ETS create/read round-trips every field" do
    params =
      %{
      propagation_id: "sample_propagation_id",
      subject_sha: "sample_subject_sha",
      convergence_proof_digest: "sample_convergence_proof_digest",
      receipt_digest: "sample_receipt_digest"
      }

    created =
      BeamPM.Ash.Resources.CrownConvergenceProof
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CrownConvergenceProof)
    assert read_back.id == created.id
    assert read_back.propagation_id == "sample_propagation_id"
    assert read_back.subject_sha == "sample_subject_sha"
    assert read_back.convergence_proof_digest == "sample_convergence_proof_digest"
    assert read_back.receipt_digest == "sample_receipt_digest"
  end
end

