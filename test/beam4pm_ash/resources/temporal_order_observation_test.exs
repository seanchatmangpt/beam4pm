# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.TemporalOrderObservationTest do
  use ExUnit.Case, async: false

  test "temporal_order_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      earlier_observation_id: "sample_earlier_observation_id",
      later_observation_id: "sample_later_observation_id",
      ordering_basis: "sample_ordering_basis",
      evidence_digest: "sample_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.TemporalOrderObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.TemporalOrderObservation)
    assert read_back.id == created.id
    assert read_back.earlier_observation_id == "sample_earlier_observation_id"
    assert read_back.later_observation_id == "sample_later_observation_id"
    assert read_back.ordering_basis == "sample_ordering_basis"
    assert read_back.evidence_digest == "sample_evidence_digest"
  end
end

