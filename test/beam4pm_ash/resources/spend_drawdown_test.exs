# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.SpendDrawdownTest do
  use ExUnit.Case, async: false

  test "spend_drawdown: real Ash ETS create/read round-trips every field" do
    params =
      %{
      spend_drawdown_id: "sample_spend_drawdown_id",
      account_id: "sample_account_id",
      consumed_amount: 3.5,
      evidence_digest: "sample_evidence_digest",
      effective_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.SpendDrawdown
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.SpendDrawdown)
    assert read_back.id == created.id
    assert read_back.spend_drawdown_id == "sample_spend_drawdown_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.consumed_amount == 3.5
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.effective_at == ~U[2026-08-29 12:00:00Z]
  end
end

