# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PlannerRoutingUpdateTest do
  use ExUnit.Case, async: false

  test "planner_routing_update: real Ash ETS create/read round-trips every field" do
    params =
      %{
      update_id: "sample_update_id",
      route_id: "sample_route_id",
      prior_planner_id: "sample_prior_planner_id",
      selected_planner_id: "sample_selected_planner_id",
      payoff_basis_digest: "sample_payoff_basis_digest",
      rollback_policy_id: "sample_rollback_policy_id"
      }

    created =
      BeamPM.Ash.Resources.PlannerRoutingUpdate
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PlannerRoutingUpdate)
    assert read_back.id == created.id
    assert read_back.update_id == "sample_update_id"
    assert read_back.route_id == "sample_route_id"
    assert read_back.prior_planner_id == "sample_prior_planner_id"
    assert read_back.selected_planner_id == "sample_selected_planner_id"
    assert read_back.payoff_basis_digest == "sample_payoff_basis_digest"
    assert read_back.rollback_policy_id == "sample_rollback_policy_id"
  end
end

