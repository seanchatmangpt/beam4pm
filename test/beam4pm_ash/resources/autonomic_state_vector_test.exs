# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.AutonomicStateVectorTest do
  use ExUnit.Case, async: false

  test "autonomic_state_vector: real Ash ETS create/read round-trips every field" do
    params =
      %{
      state_vector_id: "sample_state_vector_id",
      subject_id: "sample_subject_id",
      dimension_digest: "sample_dimension_digest",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.AutonomicStateVector
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.AutonomicStateVector)
    assert read_back.id == created.id
    assert read_back.state_vector_id == "sample_state_vector_id"
    assert read_back.subject_id == "sample_subject_id"
    assert read_back.dimension_digest == "sample_dimension_digest"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

