# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RootCauseReuseDecisionTest do
  use ExUnit.Case, async: false

  test "root_cause_reuse_decision: real Ash ETS create/read round-trips every field" do
    params =
      %{
      decision_id: "sample_decision_id",
      pattern_id: "sample_pattern_id",
      current_failure_id: "sample_current_failure_id",
      similarity_score: 3.5,
      applicability_evidence_digest: "sample_applicability_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.RootCauseReuseDecision
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RootCauseReuseDecision)
    assert read_back.id == created.id
    assert read_back.decision_id == "sample_decision_id"
    assert read_back.pattern_id == "sample_pattern_id"
    assert read_back.current_failure_id == "sample_current_failure_id"
    assert read_back.similarity_score == 3.5
    assert read_back.applicability_evidence_digest == "sample_applicability_evidence_digest"
  end
end

