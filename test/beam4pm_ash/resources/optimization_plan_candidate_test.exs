# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.OptimizationPlanCandidateTest do
  use ExUnit.Case, async: false

  test "optimization_plan_candidate: real Ash ETS create/read round-trips every field" do
    params =
      %{
      plan_id: "sample_plan_id",
      objective_id: "sample_objective_id",
      solver_receipt_hash: "sample_solver_receipt_hash"
      }

    created =
      BeamPM.Ash.Resources.OptimizationPlanCandidate
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.OptimizationPlanCandidate)
    assert read_back.id == created.id
    assert read_back.plan_id == "sample_plan_id"
    assert read_back.objective_id == "sample_objective_id"
    assert read_back.solver_receipt_hash == "sample_solver_receipt_hash"
  end
end

