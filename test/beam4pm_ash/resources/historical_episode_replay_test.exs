# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.HistoricalEpisodeReplayTest do
  use ExUnit.Case, async: false

  test "historical_episode_replay: real Ash ETS create/read round-trips every field" do
    params =
      %{
      replay_id: "sample_replay_id",
      episode_id: "sample_episode_id",
      historical_subject_sha: "sample_historical_subject_sha",
      replay_seed: "sample_replay_seed",
      divergence_code: "sample_divergence_code"
      }

    created =
      BeamPM.Ash.Resources.HistoricalEpisodeReplay
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.HistoricalEpisodeReplay)
    assert read_back.id == created.id
    assert read_back.replay_id == "sample_replay_id"
    assert read_back.episode_id == "sample_episode_id"
    assert read_back.historical_subject_sha == "sample_historical_subject_sha"
    assert read_back.replay_seed == "sample_replay_seed"
    assert read_back.divergence_code == "sample_divergence_code"
  end
end

