# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RollbackOutcomeLearningTest do
  use ExUnit.Case, async: false

  test "rollback_outcome_learning: real Ash ETS create/read round-trips every field" do
    params =
      %{
      learning_id: "sample_learning_id",
      rollback_id: "sample_rollback_id",
      pre_rollback_state_digest: "sample_pre_rollback_state_digest",
      post_rollback_state_digest: "sample_post_rollback_state_digest",
      recovery_score: 3.5,
      evidence_digest: "sample_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.RollbackOutcomeLearning
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RollbackOutcomeLearning)
    assert read_back.id == created.id
    assert read_back.learning_id == "sample_learning_id"
    assert read_back.rollback_id == "sample_rollback_id"
    assert read_back.pre_rollback_state_digest == "sample_pre_rollback_state_digest"
    assert read_back.post_rollback_state_digest == "sample_post_rollback_state_digest"
    assert read_back.recovery_score == 3.5
    assert read_back.evidence_digest == "sample_evidence_digest"
  end
end

