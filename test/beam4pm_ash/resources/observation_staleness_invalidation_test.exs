# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ObservationStalenessInvalidationTest do
  use ExUnit.Case, async: false

  test "observation_staleness_invalidation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      observation_id: "sample_observation_id",
      invalidated_at: "2026-08-29T12:00:00Z",
      staleness_reason: "sample_staleness_reason",
      replacement_required: true
      }

    created =
      BeamPM.Ash.Resources.ObservationStalenessInvalidation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ObservationStalenessInvalidation)
    assert read_back.id == created.id
    assert read_back.observation_id == "sample_observation_id"
    assert read_back.invalidated_at == ~U[2026-08-29 12:00:00Z]
    assert read_back.staleness_reason == "sample_staleness_reason"
    assert read_back.replacement_required == true
  end
end

