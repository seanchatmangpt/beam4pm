# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ContainerPlatformDigestObservationTest do
  use ExUnit.Case, async: false

  test "container_platform_digest_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      index_digest: "sample_index_digest",
      platform: "sample_platform",
      platform_digest: "sample_platform_digest",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.ContainerPlatformDigestObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ContainerPlatformDigestObservation)
    assert read_back.id == created.id
    assert read_back.index_digest == "sample_index_digest"
    assert read_back.platform == "sample_platform"
    assert read_back.platform_digest == "sample_platform_digest"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

