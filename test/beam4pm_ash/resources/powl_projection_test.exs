# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PowlProjectionTest do
  use ExUnit.Case, async: false

  test "powl_projection: real Ash ETS create/read round-trips every field" do
    params =
      %{
      plan_id: "sample_plan_id",
      powl_hash: "sample_powl_hash",
      projection_receipt_hash: "sample_projection_receipt_hash"
      }

    created =
      BeamPM.Ash.Resources.PowlProjection
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PowlProjection)
    assert read_back.id == created.id
    assert read_back.plan_id == "sample_plan_id"
    assert read_back.powl_hash == "sample_powl_hash"
    assert read_back.projection_receipt_hash == "sample_projection_receipt_hash"
  end
end

