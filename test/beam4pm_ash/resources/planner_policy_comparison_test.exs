# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PlannerPolicyComparisonTest do
  use ExUnit.Case, async: false

  test "planner_policy_comparison: real Ash ETS create/read round-trips every field" do
    params =
      %{
      comparison_id: "sample_comparison_id",
      planner_id: "sample_planner_id",
      incumbent_policy_id: "sample_incumbent_policy_id",
      challenger_policy_id: "sample_challenger_policy_id",
      payoff_delta: 3.5,
      winner_evidence_digest: "sample_winner_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.PlannerPolicyComparison
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PlannerPolicyComparison)
    assert read_back.id == created.id
    assert read_back.comparison_id == "sample_comparison_id"
    assert read_back.planner_id == "sample_planner_id"
    assert read_back.incumbent_policy_id == "sample_incumbent_policy_id"
    assert read_back.challenger_policy_id == "sample_challenger_policy_id"
    assert read_back.payoff_delta == 3.5
    assert read_back.winner_evidence_digest == "sample_winner_evidence_digest"
  end
end

