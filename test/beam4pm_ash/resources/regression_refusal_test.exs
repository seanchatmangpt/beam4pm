# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RegressionRefusalTest do
  use ExUnit.Case, async: false

  test "regression_refusal: real Ash ETS create/read round-trips every field" do
    params =
      %{
      refusal_id: "sample_refusal_id",
      candidate_policy_id: "sample_candidate_policy_id",
      regression_detection_id: "sample_regression_detection_id",
      acceptance_threshold: 3.5,
      observed_regression: 3.5,
      authority_ceiling: "sample_authority_ceiling"
      }

    created =
      BeamPM.Ash.Resources.RegressionRefusal
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RegressionRefusal)
    assert read_back.id == created.id
    assert read_back.refusal_id == "sample_refusal_id"
    assert read_back.candidate_policy_id == "sample_candidate_policy_id"
    assert read_back.regression_detection_id == "sample_regression_detection_id"
    assert read_back.acceptance_threshold == 3.5
    assert read_back.observed_regression == 3.5
    assert read_back.authority_ceiling == "sample_authority_ceiling"
  end
end

