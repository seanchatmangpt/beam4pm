# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.BeliefStateSnapshotTest do
  use ExUnit.Case, async: false

  test "belief_state_snapshot: real Ash ETS create/read round-trips every field" do
    params =
      %{
      belief_state_id: "sample_belief_state_id",
      subject_id: "sample_subject_id",
      posterior_digest: "sample_posterior_digest",
      uncertainty_status: "sample_uncertainty_status"
      }

    created =
      BeamPM.Ash.Resources.BeliefStateSnapshot
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.BeliefStateSnapshot)
    assert read_back.id == created.id
    assert read_back.belief_state_id == "sample_belief_state_id"
    assert read_back.subject_id == "sample_subject_id"
    assert read_back.posterior_digest == "sample_posterior_digest"
    assert read_back.uncertainty_status == "sample_uncertainty_status"
  end
end

