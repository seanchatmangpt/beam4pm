# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.EvidenceTrainingSampleTest do
  use ExUnit.Case, async: false

  test "evidence_training_sample: real Ash ETS create/read round-trips every field" do
    params =
      %{
      sample_id: "sample_sample_id",
      episode_id: "sample_episode_id",
      feature_digest: "sample_feature_digest",
      label_id: "sample_label_id",
      provenance_digest: "sample_provenance_digest"
      }

    created =
      BeamPM.Ash.Resources.EvidenceTrainingSample
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.EvidenceTrainingSample)
    assert read_back.id == created.id
    assert read_back.sample_id == "sample_sample_id"
    assert read_back.episode_id == "sample_episode_id"
    assert read_back.feature_digest == "sample_feature_digest"
    assert read_back.label_id == "sample_label_id"
    assert read_back.provenance_digest == "sample_provenance_digest"
  end
end

