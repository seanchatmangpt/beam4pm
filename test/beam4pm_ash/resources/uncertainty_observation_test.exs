# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.UncertaintyObservationTest do
  use ExUnit.Case, async: false

  test "uncertainty_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      observation_id: "sample_observation_id",
      uncertainty_kind: "sample_uncertainty_kind",
      confidence_basis: "sample_confidence_basis",
      standing: "sample_standing"
      }

    created =
      BeamPM.Ash.Resources.UncertaintyObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.UncertaintyObservation)
    assert read_back.id == created.id
    assert read_back.observation_id == "sample_observation_id"
    assert read_back.uncertainty_kind == "sample_uncertainty_kind"
    assert read_back.confidence_basis == "sample_confidence_basis"
    assert read_back.standing == "sample_standing"
  end
end

