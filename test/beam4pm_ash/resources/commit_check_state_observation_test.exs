# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CommitCheckStateObservationTest do
  use ExUnit.Case, async: false

  test "commit_check_state_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      commit_sha: "sample_commit_sha",
      check_name: "sample_check_name",
      check_status: "sample_check_status",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.CommitCheckStateObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CommitCheckStateObservation)
    assert read_back.id == created.id
    assert read_back.commit_sha == "sample_commit_sha"
    assert read_back.check_name == "sample_check_name"
    assert read_back.check_status == "sample_check_status"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

