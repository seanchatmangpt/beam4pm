# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.MutualInformationScoreTest do
  use ExUnit.Case, async: false

  test "mutual_information_score: real Ash ETS create/read round-trips every field" do
    params =
      %{
      observation_id: "sample_observation_id",
      target_state_id: "sample_target_state_id",
      score: "sample_score"
      }

    created =
      BeamPM.Ash.Resources.MutualInformationScore
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.MutualInformationScore)
    assert read_back.id == created.id
    assert read_back.observation_id == "sample_observation_id"
    assert read_back.target_state_id == "sample_target_state_id"
    assert read_back.score == "sample_score"
  end
end

