# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PlanLineageTest do
  use ExUnit.Case, async: false

  test "plan_lineage: real Ash ETS create/read round-trips every field" do
    params =
      %{
      plan_id: "sample_plan_id",
      parent_plan_id: "sample_parent_plan_id",
      lineage_hash: "sample_lineage_hash"
      }

    created =
      BeamPM.Ash.Resources.PlanLineage
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PlanLineage)
    assert read_back.id == created.id
    assert read_back.plan_id == "sample_plan_id"
    assert read_back.parent_plan_id == "sample_parent_plan_id"
    assert read_back.lineage_hash == "sample_lineage_hash"
  end
end

