# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.WorkflowJobStateObservationTest do
  use ExUnit.Case, async: false

  test "workflow_job_state_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      job_id: "sample_job_id",
      run_id: "sample_run_id",
      runner_identity: "sample_runner_identity",
      conclusion: "sample_conclusion"
      }

    created =
      BeamPM.Ash.Resources.WorkflowJobStateObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.WorkflowJobStateObservation)
    assert read_back.id == created.id
    assert read_back.job_id == "sample_job_id"
    assert read_back.run_id == "sample_run_id"
    assert read_back.runner_identity == "sample_runner_identity"
    assert read_back.conclusion == "sample_conclusion"
  end
end

