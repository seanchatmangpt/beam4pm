# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ValueOfInformationScoreTest do
  use ExUnit.Case, async: false

  test "value_of_information_score: real Ash ETS create/read round-trips every field" do
    params =
      %{
      option_id: "sample_option_id",
      observation_id: "sample_observation_id",
      score: "sample_score"
      }

    created =
      BeamPM.Ash.Resources.ValueOfInformationScore
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ValueOfInformationScore)
    assert read_back.id == created.id
    assert read_back.option_id == "sample_option_id"
    assert read_back.observation_id == "sample_observation_id"
    assert read_back.score == "sample_score"
  end
end

