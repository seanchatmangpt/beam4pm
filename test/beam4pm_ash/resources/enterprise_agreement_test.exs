# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.EnterpriseAgreementTest do
  use ExUnit.Case, async: false

  test "enterprise_agreement: real Ash ETS create/read round-trips every field" do
    params =
      %{
      enterprise_agreement_id: "sample_enterprise_agreement_id",
      account_id: "sample_account_id",
      agreement_version: "sample_agreement_version",
      evidence_digest: "sample_evidence_digest",
      effective_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.EnterpriseAgreement
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.EnterpriseAgreement)
    assert read_back.id == created.id
    assert read_back.enterprise_agreement_id == "sample_enterprise_agreement_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.agreement_version == "sample_agreement_version"
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.effective_at == ~U[2026-08-29 12:00:00Z]
  end
end

