# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ChallengerCandidateEvaluationTest do
  use ExUnit.Case, async: false

  test "challenger_candidate_evaluation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      evaluation_id: "sample_evaluation_id",
      challenger_policy_id: "sample_challenger_policy_id",
      episode_set_digest: "sample_episode_set_digest",
      evaluation_seed: "sample_evaluation_seed",
      score: 3.5,
      refusal_code: "sample_refusal_code"
      }

    created =
      BeamPM.Ash.Resources.ChallengerCandidateEvaluation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ChallengerCandidateEvaluation)
    assert read_back.id == created.id
    assert read_back.evaluation_id == "sample_evaluation_id"
    assert read_back.challenger_policy_id == "sample_challenger_policy_id"
    assert read_back.episode_set_digest == "sample_episode_set_digest"
    assert read_back.evaluation_seed == "sample_evaluation_seed"
    assert read_back.score == 3.5
    assert read_back.refusal_code == "sample_refusal_code"
  end
end

