# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.MachineActionableDeltaTest do
  use ExUnit.Case, async: false

  test "machine_actionable_delta: real Ash ETS create/read round-trips every field" do
    params =
      %{
      subject_id: "sample_subject_id",
      prior_state_digest: "sample_prior_state_digest",
      delta_digest: "sample_delta_digest",
      recommended_action: "sample_recommended_action"
      }

    created =
      BeamPM.Ash.Resources.MachineActionableDelta
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.MachineActionableDelta)
    assert read_back.id == created.id
    assert read_back.subject_id == "sample_subject_id"
    assert read_back.prior_state_digest == "sample_prior_state_digest"
    assert read_back.delta_digest == "sample_delta_digest"
    assert read_back.recommended_action == "sample_recommended_action"
  end
end

