# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.SubjectFailureSeparationTest do
  use ExUnit.Case, async: false

  test "subject_failure_separation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      assessment_id: "sample_assessment_id",
      exact_subject_sha: "sample_exact_subject_sha",
      capsule_standing: "sample_capsule_standing",
      subject_failure_code: "sample_subject_failure_code",
      independent_reproducer_digest: "sample_independent_reproducer_digest"
      }

    created =
      BeamPM.Ash.Resources.SubjectFailureSeparation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.SubjectFailureSeparation)
    assert read_back.id == created.id
    assert read_back.assessment_id == "sample_assessment_id"
    assert read_back.exact_subject_sha == "sample_exact_subject_sha"
    assert read_back.capsule_standing == "sample_capsule_standing"
    assert read_back.subject_failure_code == "sample_subject_failure_code"
    assert read_back.independent_reproducer_digest == "sample_independent_reproducer_digest"
  end
end

