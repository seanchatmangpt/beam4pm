# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.WorkflowRunStateObservationTest do
  use ExUnit.Case, async: false

  test "workflow_run_state_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      run_id: "sample_run_id",
      workflow_id: "sample_workflow_id",
      head_sha: "sample_head_sha",
      conclusion: "sample_conclusion"
      }

    created =
      BeamPM.Ash.Resources.WorkflowRunStateObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.WorkflowRunStateObservation)
    assert read_back.id == created.id
    assert read_back.run_id == "sample_run_id"
    assert read_back.workflow_id == "sample_workflow_id"
    assert read_back.head_sha == "sample_head_sha"
    assert read_back.conclusion == "sample_conclusion"
  end
end

