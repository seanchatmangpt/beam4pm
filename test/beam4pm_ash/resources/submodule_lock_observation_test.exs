# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.SubmoduleLockObservationTest do
  use ExUnit.Case, async: false

  test "submodule_lock_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      submodule_path: "sample_submodule_path",
      gitlink_sha: "sample_gitlink_sha",
      lock_sha: "sample_lock_sha",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.SubmoduleLockObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.SubmoduleLockObservation)
    assert read_back.id == created.id
    assert read_back.submodule_path == "sample_submodule_path"
    assert read_back.gitlink_sha == "sample_gitlink_sha"
    assert read_back.lock_sha == "sample_lock_sha"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

