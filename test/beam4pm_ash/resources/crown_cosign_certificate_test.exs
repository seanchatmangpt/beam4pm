# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CrownCosignCertificateTest do
  use ExUnit.Case, async: false

  test "crown_cosign_certificate: real Ash ETS create/read round-trips every field" do
    params =
      %{
      propagation_id: "sample_propagation_id",
      subject_sha: "sample_subject_sha",
      certificate_identity: "sample_certificate_identity",
      receipt_digest: "sample_receipt_digest"
      }

    created =
      BeamPM.Ash.Resources.CrownCosignCertificate
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CrownCosignCertificate)
    assert read_back.id == created.id
    assert read_back.propagation_id == "sample_propagation_id"
    assert read_back.subject_sha == "sample_subject_sha"
    assert read_back.certificate_identity == "sample_certificate_identity"
    assert read_back.receipt_digest == "sample_receipt_digest"
  end
end

