# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CurriculumGenerationTest do
  use ExUnit.Case, async: false

  test "curriculum_generation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      curriculum_id: "sample_curriculum_id",
      source_gap_set_digest: "sample_source_gap_set_digest",
      ordered_hypotheses_digest: "sample_ordered_hypotheses_digest",
      diversity_score: 3.5,
      falsifier_coverage: 3.5
      }

    created =
      BeamPM.Ash.Resources.CurriculumGeneration
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CurriculumGeneration)
    assert read_back.id == created.id
    assert read_back.curriculum_id == "sample_curriculum_id"
    assert read_back.source_gap_set_digest == "sample_source_gap_set_digest"
    assert read_back.ordered_hypotheses_digest == "sample_ordered_hypotheses_digest"
    assert read_back.diversity_score == 3.5
    assert read_back.falsifier_coverage == 3.5
  end
end

