# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ContainerManifestDigestObservationTest do
  use ExUnit.Case, async: false

  test "container_manifest_digest_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      image_repository: "sample_image_repository",
      tag: "sample_tag",
      index_digest: "sample_index_digest",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.ContainerManifestDigestObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ContainerManifestDigestObservation)
    assert read_back.id == created.id
    assert read_back.image_repository == "sample_image_repository"
    assert read_back.tag == "sample_tag"
    assert read_back.index_digest == "sample_index_digest"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

