# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ExactSubjectBindingTest do
  use ExUnit.Case, async: false

  test "exact_subject_binding: real Ash ETS create/read round-trips every field" do
    params =
      %{
      subject_id: "sample_subject_id",
      subject_sha: "sample_subject_sha",
      binding_hash: "sample_binding_hash"
      }

    created =
      BeamPM.Ash.Resources.ExactSubjectBinding
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ExactSubjectBinding)
    assert read_back.id == created.id
    assert read_back.subject_id == "sample_subject_id"
    assert read_back.subject_sha == "sample_subject_sha"
    assert read_back.binding_hash == "sample_binding_hash"
  end
end

