# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.SeededEvaluationTest do
  use ExUnit.Case, async: false

  test "seeded_evaluation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      evaluation_id: "sample_evaluation_id",
      seed: "sample_seed",
      dataset_digest: "sample_dataset_digest",
      policy_id: "sample_policy_id",
      score: 3.5,
      replay_digest: "sample_replay_digest"
      }

    created =
      BeamPM.Ash.Resources.SeededEvaluation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.SeededEvaluation)
    assert read_back.id == created.id
    assert read_back.evaluation_id == "sample_evaluation_id"
    assert read_back.seed == "sample_seed"
    assert read_back.dataset_digest == "sample_dataset_digest"
    assert read_back.policy_id == "sample_policy_id"
    assert read_back.score == 3.5
    assert read_back.replay_digest == "sample_replay_digest"
  end
end

