# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CrownPackagePinReconciliationTest do
  use ExUnit.Case, async: false

  test "crown_package_pin_reconciliation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      propagation_id: "sample_propagation_id",
      subject_sha: "sample_subject_sha",
      package_version_digest: "sample_package_version_digest",
      receipt_digest: "sample_receipt_digest"
      }

    created =
      BeamPM.Ash.Resources.CrownPackagePinReconciliation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CrownPackagePinReconciliation)
    assert read_back.id == created.id
    assert read_back.propagation_id == "sample_propagation_id"
    assert read_back.subject_sha == "sample_subject_sha"
    assert read_back.package_version_digest == "sample_package_version_digest"
    assert read_back.receipt_digest == "sample_receipt_digest"
  end
end

