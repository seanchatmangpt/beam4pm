# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.AntiRepeatRefusalTest do
  use ExUnit.Case, async: false

  test "anti_repeat_refusal: real Ash ETS create/read round-trips every field" do
    params =
      %{
      refusal_id: "sample_refusal_id",
      candidate_action_id: "sample_candidate_action_id",
      matching_signature_id: "sample_matching_signature_id",
      recurrence_risk: 3.5,
      alternative_required: true
      }

    created =
      BeamPM.Ash.Resources.AntiRepeatRefusal
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.AntiRepeatRefusal)
    assert read_back.id == created.id
    assert read_back.refusal_id == "sample_refusal_id"
    assert read_back.candidate_action_id == "sample_candidate_action_id"
    assert read_back.matching_signature_id == "sample_matching_signature_id"
    assert read_back.recurrence_risk == 3.5
    assert read_back.alternative_required == true
  end
end

