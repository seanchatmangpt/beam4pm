# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ValueOfInformationEstimateTest do
  use ExUnit.Case, async: false

  test "value_of_information_estimate: real Ash ETS create/read round-trips every field" do
    params =
      %{
      decision_id: "sample_decision_id",
      evidence_candidate_id: "sample_evidence_candidate_id",
      expected_information_gain: 3.5,
      cost_basis: "sample_cost_basis"
      }

    created =
      BeamPM.Ash.Resources.ValueOfInformationEstimate
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ValueOfInformationEstimate)
    assert read_back.id == created.id
    assert read_back.decision_id == "sample_decision_id"
    assert read_back.evidence_candidate_id == "sample_evidence_candidate_id"
    assert read_back.expected_information_gain == 3.5
    assert read_back.cost_basis == "sample_cost_basis"
  end
end

