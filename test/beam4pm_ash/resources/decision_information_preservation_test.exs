# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.DecisionInformationPreservationTest do
  use ExUnit.Case, async: false

  test "decision_information_preservation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      preservation_id: "sample_preservation_id",
      decision_id: "sample_decision_id",
      source_partition_digest: "sample_source_partition_digest",
      projection_digest: "sample_projection_digest",
      preserved_question_set_digest: "sample_preserved_question_set_digest",
      loss_score: 3.5
      }

    created =
      BeamPM.Ash.Resources.DecisionInformationPreservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.DecisionInformationPreservation)
    assert read_back.id == created.id
    assert read_back.preservation_id == "sample_preservation_id"
    assert read_back.decision_id == "sample_decision_id"
    assert read_back.source_partition_digest == "sample_source_partition_digest"
    assert read_back.projection_digest == "sample_projection_digest"
    assert read_back.preserved_question_set_digest == "sample_preserved_question_set_digest"
    assert read_back.loss_score == 3.5
  end
end

