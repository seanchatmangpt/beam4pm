# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PolicyBindingTest do
  use ExUnit.Case, async: false

  test "policy_binding: real Ash ETS create/read round-trips every field" do
    params =
      %{
      policy_id: "sample_policy_id",
      planner_id: "sample_planner_id",
      policy_hash: "sample_policy_hash"
      }

    created =
      BeamPM.Ash.Resources.PolicyBinding
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PolicyBinding)
    assert read_back.id == created.id
    assert read_back.policy_id == "sample_policy_id"
    assert read_back.planner_id == "sample_planner_id"
    assert read_back.policy_hash == "sample_policy_hash"
  end
end

