# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.TrajectoryWindowTest do
  use ExUnit.Case, async: false

  test "trajectory_window: real Ash ETS create/read round-trips every field" do
    params =
      %{
      window_id: "sample_window_id",
      repository_id: "sample_repository_id",
      first_subject_sha: "sample_first_subject_sha",
      last_subject_sha: "sample_last_subject_sha",
      episode_count: 42
      }

    created =
      BeamPM.Ash.Resources.TrajectoryWindow
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.TrajectoryWindow)
    assert read_back.id == created.id
    assert read_back.window_id == "sample_window_id"
    assert read_back.repository_id == "sample_repository_id"
    assert read_back.first_subject_sha == "sample_first_subject_sha"
    assert read_back.last_subject_sha == "sample_last_subject_sha"
    assert read_back.episode_count == 42
  end
end

