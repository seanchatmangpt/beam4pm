# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.EntropyReductionScoreTest do
  use ExUnit.Case, async: false

  test "entropy_reduction_score: real Ash ETS create/read round-trips every field" do
    params =
      %{
      action_id: "sample_action_id",
      prior_entropy: "sample_prior_entropy",
      expected_posterior_entropy: "sample_expected_posterior_entropy"
      }

    created =
      BeamPM.Ash.Resources.EntropyReductionScore
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.EntropyReductionScore)
    assert read_back.id == created.id
    assert read_back.action_id == "sample_action_id"
    assert read_back.prior_entropy == "sample_prior_entropy"
    assert read_back.expected_posterior_entropy == "sample_expected_posterior_entropy"
  end
end

