# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.TrialEntitlementTest do
  use ExUnit.Case, async: false

  test "trial_entitlement: real Ash ETS create/read round-trips every field" do
    params =
      %{
      trial_entitlement_id: "sample_trial_entitlement_id",
      account_id: "sample_account_id",
      trial_expires_at: "2026-08-29T12:00:00Z",
      evidence_digest: "sample_evidence_digest",
      effective_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.TrialEntitlement
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.TrialEntitlement)
    assert read_back.id == created.id
    assert read_back.trial_entitlement_id == "sample_trial_entitlement_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.trial_expires_at == ~U[2026-08-29 12:00:00Z]
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.effective_at == ~U[2026-08-29 12:00:00Z]
  end
end

