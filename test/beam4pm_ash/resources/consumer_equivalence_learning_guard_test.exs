# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ConsumerEquivalenceLearningGuardTest do
  use ExUnit.Case, async: false

  test "consumer_equivalence_learning_guard: real Ash ETS create/read round-trips every field" do
    params =
      %{
      guard_id: "sample_guard_id",
      source_consumer_id: "sample_source_consumer_id",
      target_consumer_id: "sample_target_consumer_id",
      equivalence_evidence_digest: "sample_equivalence_evidence_digest",
      decision: "sample_decision",
      refusal_code: "sample_refusal_code"
      }

    created =
      BeamPM.Ash.Resources.ConsumerEquivalenceLearningGuard
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ConsumerEquivalenceLearningGuard)
    assert read_back.id == created.id
    assert read_back.guard_id == "sample_guard_id"
    assert read_back.source_consumer_id == "sample_source_consumer_id"
    assert read_back.target_consumer_id == "sample_target_consumer_id"
    assert read_back.equivalence_evidence_digest == "sample_equivalence_evidence_digest"
    assert read_back.decision == "sample_decision"
    assert read_back.refusal_code == "sample_refusal_code"
  end
end

