# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.WorkflowDefinitionDigestObservationTest do
  use ExUnit.Case, async: false

  test "workflow_definition_digest_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      workflow_path: "sample_workflow_path",
      definition_sha256: "sample_definition_sha256",
      source_sha: "sample_source_sha",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.WorkflowDefinitionDigestObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.WorkflowDefinitionDigestObservation)
    assert read_back.id == created.id
    assert read_back.workflow_path == "sample_workflow_path"
    assert read_back.definition_sha256 == "sample_definition_sha256"
    assert read_back.source_sha == "sample_source_sha"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

