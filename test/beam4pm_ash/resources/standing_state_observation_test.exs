# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.StandingStateObservationTest do
  use ExUnit.Case, async: false

  test "standing_state_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      subject_id: "sample_subject_id",
      standing: "sample_standing",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.StandingStateObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.StandingStateObservation)
    assert read_back.id == created.id
    assert read_back.subject_id == "sample_subject_id"
    assert read_back.standing == "sample_standing"
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

