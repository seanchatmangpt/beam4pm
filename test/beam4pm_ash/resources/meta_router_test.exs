# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.MetaRouterTest do
  use ExUnit.Case, async: false

  test "meta_router: real Ash ETS create/read round-trips every field" do
    params =
      %{
      portfolio_id: "sample_portfolio_id",
      observation_hash: "sample_observation_hash",
      selected_planner_id: "sample_selected_planner_id"
      }

    created =
      BeamPM.Ash.Resources.MetaRouter
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.MetaRouter)
    assert read_back.id == created.id
    assert read_back.portfolio_id == "sample_portfolio_id"
    assert read_back.observation_hash == "sample_observation_hash"
    assert read_back.selected_planner_id == "sample_selected_planner_id"
  end
end

