# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CustomerSignalObservationTest do
  use ExUnit.Case, async: false

  test "customer_signal_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      customer_id: "sample_customer_id",
      signal_type: "sample_signal_type",
      signal_digest: "sample_signal_digest",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.CustomerSignalObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CustomerSignalObservation)
    assert read_back.id == created.id
    assert read_back.customer_id == "sample_customer_id"
    assert read_back.signal_type == "sample_signal_type"
    assert read_back.signal_digest == "sample_signal_digest"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

