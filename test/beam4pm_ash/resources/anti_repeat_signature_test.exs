# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.AntiRepeatSignatureTest do
  use ExUnit.Case, async: false

  test "anti_repeat_signature: real Ash ETS create/read round-trips every field" do
    params =
      %{
      signature_id: "sample_signature_id",
      failure_class: "sample_failure_class",
      causal_features_digest: "sample_causal_features_digest",
      repair_family: "sample_repair_family",
      first_seen_episode: "sample_first_seen_episode"
      }

    created =
      BeamPM.Ash.Resources.AntiRepeatSignature
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.AntiRepeatSignature)
    assert read_back.id == created.id
    assert read_back.signature_id == "sample_signature_id"
    assert read_back.failure_class == "sample_failure_class"
    assert read_back.causal_features_digest == "sample_causal_features_digest"
    assert read_back.repair_family == "sample_repair_family"
    assert read_back.first_seen_episode == "sample_first_seen_episode"
  end
end

