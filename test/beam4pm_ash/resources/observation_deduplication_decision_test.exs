# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ObservationDeduplicationDecisionTest do
  use ExUnit.Case, async: false

  test "observation_deduplication_decision: real Ash ETS create/read round-trips every field" do
    params =
      %{
      event_id: "sample_event_id",
      event_digest: "sample_event_digest",
      dedup_key: "sample_dedup_key",
      decision: "sample_decision"
      }

    created =
      BeamPM.Ash.Resources.ObservationDeduplicationDecision
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ObservationDeduplicationDecision)
    assert read_back.id == created.id
    assert read_back.event_id == "sample_event_id"
    assert read_back.event_digest == "sample_event_digest"
    assert read_back.dedup_key == "sample_dedup_key"
    assert read_back.decision == "sample_decision"
  end
end

