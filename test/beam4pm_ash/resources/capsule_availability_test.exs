# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CapsuleAvailabilityTest do
  use ExUnit.Case, async: false

  test "capsule_availability: real Ash ETS create/read round-trips every field" do
    params =
      %{
      capsule_id: "sample_capsule_id",
      capsule_digest: "sample_capsule_digest",
      availability: "sample_availability"
      }

    created =
      BeamPM.Ash.Resources.CapsuleAvailability
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CapsuleAvailability)
    assert read_back.id == created.id
    assert read_back.capsule_id == "sample_capsule_id"
    assert read_back.capsule_digest == "sample_capsule_digest"
    assert read_back.availability == "sample_availability"
  end
end

