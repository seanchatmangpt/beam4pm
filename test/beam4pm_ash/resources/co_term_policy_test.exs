# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CoTermPolicyTest do
  use ExUnit.Case, async: false

  test "co_term_policy: real Ash ETS create/read round-trips every field" do
    params =
      %{
      co_term_policy_id: "sample_co_term_policy_id",
      account_id: "sample_account_id",
      coterm_date: "2026-08-29T12:00:00Z",
      evidence_digest: "sample_evidence_digest",
      effective_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.CoTermPolicy
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CoTermPolicy)
    assert read_back.id == created.id
    assert read_back.co_term_policy_id == "sample_co_term_policy_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.coterm_date == ~U[2026-08-29 12:00:00Z]
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.effective_at == ~U[2026-08-29 12:00:00Z]
  end
end

