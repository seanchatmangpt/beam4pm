# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.OptionGenerationTest do
  use ExUnit.Case, async: false

  test "option_generation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      state_id: "sample_state_id",
      generator_id: "sample_generator_id",
      option_set_hash: "sample_option_set_hash"
      }

    created =
      BeamPM.Ash.Resources.OptionGeneration
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.OptionGeneration)
    assert read_back.id == created.id
    assert read_back.state_id == "sample_state_id"
    assert read_back.generator_id == "sample_generator_id"
    assert read_back.option_set_hash == "sample_option_set_hash"
  end
end

