# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.EvaluationSeedBindingTest do
  use ExUnit.Case, async: false

  test "evaluation_seed_binding: real Ash ETS create/read round-trips every field" do
    params =
      %{
      binding_id: "sample_binding_id",
      evaluation_id: "sample_evaluation_id",
      seed: "sample_seed",
      result_digest: "sample_result_digest",
      source_digest: "sample_source_digest",
      deterministic: true
      }

    created =
      BeamPM.Ash.Resources.EvaluationSeedBinding
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.EvaluationSeedBinding)
    assert read_back.id == created.id
    assert read_back.binding_id == "sample_binding_id"
    assert read_back.evaluation_id == "sample_evaluation_id"
    assert read_back.seed == "sample_seed"
    assert read_back.result_digest == "sample_result_digest"
    assert read_back.source_digest == "sample_source_digest"
    assert read_back.deterministic == true
  end
end

