# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.SecondRunIdentityObjectiveTest do
  use ExUnit.Case, async: false

  test "second_run_identity_objective: real Ash ETS create/read round-trips every field" do
    params =
      %{
      subject_id: "sample_subject_id",
      first_tree_hash: "sample_first_tree_hash",
      second_tree_hash: "sample_second_tree_hash"
      }

    created =
      BeamPM.Ash.Resources.SecondRunIdentityObjective
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.SecondRunIdentityObjective)
    assert read_back.id == created.id
    assert read_back.subject_id == "sample_subject_id"
    assert read_back.first_tree_hash == "sample_first_tree_hash"
    assert read_back.second_tree_hash == "sample_second_tree_hash"
  end
end

