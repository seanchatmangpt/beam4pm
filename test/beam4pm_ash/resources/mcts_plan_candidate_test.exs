# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.MctsPlanCandidateTest do
  use ExUnit.Case, async: false

  test "mcts_plan_candidate: real Ash ETS create/read round-trips every field" do
    params =
      %{
      plan_id: "sample_plan_id",
      seed: "sample_seed",
      rollout_hash: "sample_rollout_hash"
      }

    created =
      BeamPM.Ash.Resources.MctsPlanCandidate
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.MctsPlanCandidate)
    assert read_back.id == created.id
    assert read_back.plan_id == "sample_plan_id"
    assert read_back.seed == "sample_seed"
    assert read_back.rollout_hash == "sample_rollout_hash"
  end
end

