# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.SaturationDetectionTest do
  use ExUnit.Case, async: false

  test "saturation_detection: real Ash ETS create/read round-trips every field" do
    params =
      %{
      detection_id: "sample_detection_id",
      curriculum_id: "sample_curriculum_id",
      recent_gain: 3.5,
      gain_slope: 3.5,
      novelty_floor: 3.5,
      coverage_digest: "sample_coverage_digest"
      }

    created =
      BeamPM.Ash.Resources.SaturationDetection
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.SaturationDetection)
    assert read_back.id == created.id
    assert read_back.detection_id == "sample_detection_id"
    assert read_back.curriculum_id == "sample_curriculum_id"
    assert read_back.recent_gain == 3.5
    assert read_back.gain_slope == 3.5
    assert read_back.novelty_floor == 3.5
    assert read_back.coverage_digest == "sample_coverage_digest"
  end
end

