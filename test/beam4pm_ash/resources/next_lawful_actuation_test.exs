# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.NextLawfulActuationTest do
  use ExUnit.Case, async: false

  test "next_lawful_actuation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      episode_id: "sample_episode_id",
      selected_action_id: "sample_selected_action_id",
      selection_receipt_hash: "sample_selection_receipt_hash"
      }

    created =
      BeamPM.Ash.Resources.NextLawfulActuation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.NextLawfulActuation)
    assert read_back.id == created.id
    assert read_back.episode_id == "sample_episode_id"
    assert read_back.selected_action_id == "sample_selected_action_id"
    assert read_back.selection_receipt_hash == "sample_selection_receipt_hash"
  end
end

