# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ValueRealizationFeedbackTest do
  use ExUnit.Case, async: false

  test "value_realization_feedback: real Ash ETS create/read round-trips every field" do
    params =
      %{
      feedback_id: "sample_feedback_id",
      objective_id: "sample_objective_id",
      baseline_value: 3.5,
      realized_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      policy_id: "sample_policy_id"
      }

    created =
      BeamPM.Ash.Resources.ValueRealizationFeedback
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ValueRealizationFeedback)
    assert read_back.id == created.id
    assert read_back.feedback_id == "sample_feedback_id"
    assert read_back.objective_id == "sample_objective_id"
    assert read_back.baseline_value == 3.5
    assert read_back.realized_value == 3.5
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.policy_id == "sample_policy_id"
  end
end

