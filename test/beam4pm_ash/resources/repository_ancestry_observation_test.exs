# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RepositoryAncestryObservationTest do
  use ExUnit.Case, async: false

  test "repository_ancestry_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      ancestor_sha: "sample_ancestor_sha",
      descendant_sha: "sample_descendant_sha",
      relation: "sample_relation",
      evidence_digest: "sample_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.RepositoryAncestryObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RepositoryAncestryObservation)
    assert read_back.id == created.id
    assert read_back.ancestor_sha == "sample_ancestor_sha"
    assert read_back.descendant_sha == "sample_descendant_sha"
    assert read_back.relation == "sample_relation"
    assert read_back.evidence_digest == "sample_evidence_digest"
  end
end

