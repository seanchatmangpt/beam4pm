# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.OrthogonalityRewardTest do
  use ExUnit.Case, async: false

  test "orthogonality_reward: real Ash ETS create/read round-trips every field" do
    params =
      %{
      reward_id: "sample_reward_id",
      candidate_id: "sample_candidate_id",
      comparison_set_digest: "sample_comparison_set_digest",
      minimum_semantic_distance: 3.5,
      reward: 3.5,
      independence_evidence_digest: "sample_independence_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.OrthogonalityReward
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.OrthogonalityReward)
    assert read_back.id == created.id
    assert read_back.reward_id == "sample_reward_id"
    assert read_back.candidate_id == "sample_candidate_id"
    assert read_back.comparison_set_digest == "sample_comparison_set_digest"
    assert read_back.minimum_semantic_distance == 3.5
    assert read_back.reward == 3.5
    assert read_back.independence_evidence_digest == "sample_independence_evidence_digest"
  end
end

