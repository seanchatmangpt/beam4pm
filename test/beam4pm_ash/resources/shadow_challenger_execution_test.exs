# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ShadowChallengerExecutionTest do
  use ExUnit.Case, async: false

  test "shadow_challenger_execution: real Ash ETS create/read round-trips every field" do
    params =
      %{
      execution_id: "sample_execution_id",
      incumbent_policy_id: "sample_incumbent_policy_id",
      challenger_policy_id: "sample_challenger_policy_id",
      shared_observation_digest: "sample_shared_observation_digest",
      shadow_receipt_digest: "sample_shadow_receipt_digest",
      authority_mode: "sample_authority_mode"
      }

    created =
      BeamPM.Ash.Resources.ShadowChallengerExecution
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ShadowChallengerExecution)
    assert read_back.id == created.id
    assert read_back.execution_id == "sample_execution_id"
    assert read_back.incumbent_policy_id == "sample_incumbent_policy_id"
    assert read_back.challenger_policy_id == "sample_challenger_policy_id"
    assert read_back.shared_observation_digest == "sample_shared_observation_digest"
    assert read_back.shadow_receipt_digest == "sample_shadow_receipt_digest"
    assert read_back.authority_mode == "sample_authority_mode"
  end
end

