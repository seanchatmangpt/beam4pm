# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.FxConversionPolicyTest do
  use ExUnit.Case, async: false

  test "fx_conversion_policy: real Ash ETS create/read round-trips every field" do
    params =
      %{
      fx_conversion_policy_id: "sample_fx_conversion_policy_id",
      account_id: "sample_account_id",
      rate_source: "sample_rate_source",
      evidence_digest: "sample_evidence_digest",
      effective_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.FxConversionPolicy
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.FxConversionPolicy)
    assert read_back.id == created.id
    assert read_back.fx_conversion_policy_id == "sample_fx_conversion_policy_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.rate_source == "sample_rate_source"
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.effective_at == ~U[2026-08-29 12:00:00Z]
  end
end

