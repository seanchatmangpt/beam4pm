# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RootCausePatternTest do
  use ExUnit.Case, async: false

  test "root_cause_pattern: real Ash ETS create/read round-trips every field" do
    params =
      %{
      pattern_id: "sample_pattern_id",
      failure_class: "sample_failure_class",
      causal_graph_digest: "sample_causal_graph_digest",
      confirmed_reproducer_digest: "sample_confirmed_reproducer_digest",
      reusable_scope: "sample_reusable_scope"
      }

    created =
      BeamPM.Ash.Resources.RootCausePattern
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RootCausePattern)
    assert read_back.id == created.id
    assert read_back.pattern_id == "sample_pattern_id"
    assert read_back.failure_class == "sample_failure_class"
    assert read_back.causal_graph_digest == "sample_causal_graph_digest"
    assert read_back.confirmed_reproducer_digest == "sample_confirmed_reproducer_digest"
    assert read_back.reusable_scope == "sample_reusable_scope"
  end
end

