# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.DecisionCompressionObservationTest do
  use ExUnit.Case, async: false

  test "decision_compression_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      compression_id: "sample_compression_id",
      input_state_digest: "sample_input_state_digest",
      output_delta_digest: "sample_output_delta_digest",
      loss_bound: "sample_loss_bound"
      }

    created =
      BeamPM.Ash.Resources.DecisionCompressionObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.DecisionCompressionObservation)
    assert read_back.id == created.id
    assert read_back.compression_id == "sample_compression_id"
    assert read_back.input_state_digest == "sample_input_state_digest"
    assert read_back.output_delta_digest == "sample_output_delta_digest"
    assert read_back.loss_bound == "sample_loss_bound"
  end
end

