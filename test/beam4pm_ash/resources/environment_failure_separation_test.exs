# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.EnvironmentFailureSeparationTest do
  use ExUnit.Case, async: false

  test "environment_failure_separation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      assessment_id: "sample_assessment_id",
      exact_subject_sha: "sample_exact_subject_sha",
      capsule_digest: "sample_capsule_digest",
      failure_signal: "sample_failure_signal",
      classification_basis: "sample_classification_basis"
      }

    created =
      BeamPM.Ash.Resources.EnvironmentFailureSeparation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.EnvironmentFailureSeparation)
    assert read_back.id == created.id
    assert read_back.assessment_id == "sample_assessment_id"
    assert read_back.exact_subject_sha == "sample_exact_subject_sha"
    assert read_back.capsule_digest == "sample_capsule_digest"
    assert read_back.failure_signal == "sample_failure_signal"
    assert read_back.classification_basis == "sample_classification_basis"
  end
end

