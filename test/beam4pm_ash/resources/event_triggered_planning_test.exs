# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.EventTriggeredPlanningTest do
  use ExUnit.Case, async: false

  test "event_triggered_planning: real Ash ETS create/read round-trips every field" do
    params =
      %{
      event_id: "sample_event_id",
      world_state_hash: "sample_world_state_hash",
      episode_id: "sample_episode_id"
      }

    created =
      BeamPM.Ash.Resources.EventTriggeredPlanning
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.EventTriggeredPlanning)
    assert read_back.id == created.id
    assert read_back.event_id == "sample_event_id"
    assert read_back.world_state_hash == "sample_world_state_hash"
    assert read_back.episode_id == "sample_episode_id"
  end
end

