# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.DynamicReplanTriggerTest do
  use ExUnit.Case, async: false

  test "dynamic_replan_trigger: real Ash ETS create/read round-trips every field" do
    params =
      %{
      plan_id: "sample_plan_id",
      event_id: "sample_event_id",
      trigger_hash: "sample_trigger_hash"
      }

    created =
      BeamPM.Ash.Resources.DynamicReplanTrigger
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.DynamicReplanTrigger)
    assert read_back.id == created.id
    assert read_back.plan_id == "sample_plan_id"
    assert read_back.event_id == "sample_event_id"
    assert read_back.trigger_hash == "sample_trigger_hash"
  end
end

