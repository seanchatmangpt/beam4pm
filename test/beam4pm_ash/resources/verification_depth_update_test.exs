# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.VerificationDepthUpdateTest do
  use ExUnit.Case, async: false

  test "verification_depth_update: real Ash ETS create/read round-trips every field" do
    params =
      %{
      update_id: "sample_update_id",
      verifier_id: "sample_verifier_id",
      prior_depth: 42,
      new_depth: 42,
      risk_signal_digest: "sample_risk_signal_digest",
      minimum_depth: 42
      }

    created =
      BeamPM.Ash.Resources.VerificationDepthUpdate
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.VerificationDepthUpdate)
    assert read_back.id == created.id
    assert read_back.update_id == "sample_update_id"
    assert read_back.verifier_id == "sample_verifier_id"
    assert read_back.prior_depth == 42
    assert read_back.new_depth == 42
    assert read_back.risk_signal_digest == "sample_risk_signal_digest"
    assert read_back.minimum_depth == 42
  end
end

