# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PlannerCapabilityProfileTest do
  use ExUnit.Case, async: false

  test "planner_capability_profile: real Ash ETS create/read round-trips every field" do
    params =
      %{
      planner_id: "sample_planner_id",
      capability_set: "sample_capability_set",
      profile_hash: "sample_profile_hash"
      }

    created =
      BeamPM.Ash.Resources.PlannerCapabilityProfile
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PlannerCapabilityProfile)
    assert read_back.id == created.id
    assert read_back.planner_id == "sample_planner_id"
    assert read_back.capability_set == "sample_capability_set"
    assert read_back.profile_hash == "sample_profile_hash"
  end
end

