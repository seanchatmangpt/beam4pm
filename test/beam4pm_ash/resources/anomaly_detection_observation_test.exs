# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.AnomalyDetectionObservationTest do
  use ExUnit.Case, async: false

  test "anomaly_detection_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      subject_id: "sample_subject_id",
      baseline_digest: "sample_baseline_digest",
      observation_digest: "sample_observation_digest",
      anomaly_score: 3.5
      }

    created =
      BeamPM.Ash.Resources.AnomalyDetectionObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.AnomalyDetectionObservation)
    assert read_back.id == created.id
    assert read_back.subject_id == "sample_subject_id"
    assert read_back.baseline_digest == "sample_baseline_digest"
    assert read_back.observation_digest == "sample_observation_digest"
    assert read_back.anomaly_score == 3.5
  end
end

