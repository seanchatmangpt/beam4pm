# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ArtifactDigestObservationTest do
  use ExUnit.Case, async: false

  test "artifact_digest_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      artifact_id: "sample_artifact_id",
      artifact_sha256: "sample_artifact_sha256",
      producer_run_id: "sample_producer_run_id",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.ArtifactDigestObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ArtifactDigestObservation)
    assert read_back.id == created.id
    assert read_back.artifact_id == "sample_artifact_id"
    assert read_back.artifact_sha256 == "sample_artifact_sha256"
    assert read_back.producer_run_id == "sample_producer_run_id"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

