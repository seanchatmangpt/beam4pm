# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RepairEffectivenessMeasurementTest do
  use ExUnit.Case, async: false

  test "repair_effectiveness_measurement: real Ash ETS create/read round-trips every field" do
    params =
      %{
      measurement_id: "sample_measurement_id",
      repair_id: "sample_repair_id",
      baseline_failure_rate: 3.5,
      post_repair_failure_rate: 3.5,
      evaluation_window_digest: "sample_evaluation_window_digest"
      }

    created =
      BeamPM.Ash.Resources.RepairEffectivenessMeasurement
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RepairEffectivenessMeasurement)
    assert read_back.id == created.id
    assert read_back.measurement_id == "sample_measurement_id"
    assert read_back.repair_id == "sample_repair_id"
    assert read_back.baseline_failure_rate == 3.5
    assert read_back.post_repair_failure_rate == 3.5
    assert read_back.evaluation_window_digest == "sample_evaluation_window_digest"
  end
end

