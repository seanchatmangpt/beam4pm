# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ObservationProjectionUpdateTest do
  use ExUnit.Case, async: false

  test "observation_projection_update: real Ash ETS create/read round-trips every field" do
    params =
      %{
      update_id: "sample_update_id",
      projection_id: "sample_projection_id",
      prior_digest: "sample_prior_digest",
      new_digest: "sample_new_digest",
      triggering_gap_id: "sample_triggering_gap_id",
      authority_ceiling: "sample_authority_ceiling"
      }

    created =
      BeamPM.Ash.Resources.ObservationProjectionUpdate
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ObservationProjectionUpdate)
    assert read_back.id == created.id
    assert read_back.update_id == "sample_update_id"
    assert read_back.projection_id == "sample_projection_id"
    assert read_back.prior_digest == "sample_prior_digest"
    assert read_back.new_digest == "sample_new_digest"
    assert read_back.triggering_gap_id == "sample_triggering_gap_id"
    assert read_back.authority_ceiling == "sample_authority_ceiling"
  end
end

