# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PlanMemoryTest do
  use ExUnit.Case, async: false

  test "plan_memory: real Ash ETS create/read round-trips every field" do
    params =
      %{
      plan_id: "sample_plan_id",
      evidence_hash: "sample_evidence_hash",
      memory_hash: "sample_memory_hash"
      }

    created =
      BeamPM.Ash.Resources.PlanMemory
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PlanMemory)
    assert read_back.id == created.id
    assert read_back.plan_id == "sample_plan_id"
    assert read_back.evidence_hash == "sample_evidence_hash"
    assert read_back.memory_hash == "sample_memory_hash"
  end
end

