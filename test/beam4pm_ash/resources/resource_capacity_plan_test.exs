# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ResourceCapacityPlanTest do
  use ExUnit.Case, async: false

  test "resource_capacity_plan: real Ash ETS create/read round-trips every field" do
    params =
      %{
      episode_id: "sample_episode_id",
      resource_pool_hash: "sample_resource_pool_hash",
      allocation_hash: "sample_allocation_hash"
      }

    created =
      BeamPM.Ash.Resources.ResourceCapacityPlan
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ResourceCapacityPlan)
    assert read_back.id == created.id
    assert read_back.episode_id == "sample_episode_id"
    assert read_back.resource_pool_hash == "sample_resource_pool_hash"
    assert read_back.allocation_hash == "sample_allocation_hash"
  end
end

