# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.AstarPlanCandidateTest do
  use ExUnit.Case, async: false

  test "astar_plan_candidate: real Ash ETS create/read round-trips every field" do
    params =
      %{
      plan_id: "sample_plan_id",
      heuristic_id: "sample_heuristic_id",
      path_hash: "sample_path_hash"
      }

    created =
      BeamPM.Ash.Resources.AstarPlanCandidate
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.AstarPlanCandidate)
    assert read_back.id == created.id
    assert read_back.plan_id == "sample_plan_id"
    assert read_back.heuristic_id == "sample_heuristic_id"
    assert read_back.path_hash == "sample_path_hash"
  end
end

