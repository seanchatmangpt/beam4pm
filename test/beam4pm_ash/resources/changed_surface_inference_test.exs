# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ChangedSurfaceInferenceTest do
  use ExUnit.Case, async: false

  test "changed_surface_inference: real Ash ETS create/read round-trips every field" do
    params =
      %{
      prior_tree_sha: "sample_prior_tree_sha",
      current_tree_sha: "sample_current_tree_sha",
      changed_surface_digest: "sample_changed_surface_digest",
      inference_status: "sample_inference_status"
      }

    created =
      BeamPM.Ash.Resources.ChangedSurfaceInference
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ChangedSurfaceInference)
    assert read_back.id == created.id
    assert read_back.prior_tree_sha == "sample_prior_tree_sha"
    assert read_back.current_tree_sha == "sample_current_tree_sha"
    assert read_back.changed_surface_digest == "sample_changed_surface_digest"
    assert read_back.inference_status == "sample_inference_status"
  end
end

