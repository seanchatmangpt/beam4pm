# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RefusalBoundaryObservationTest do
  use ExUnit.Case, async: false

  test "refusal_boundary_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      subject_id: "sample_subject_id",
      refusal_code: "sample_refusal_code",
      authority_boundary: "sample_authority_boundary",
      evidence_digest: "sample_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.RefusalBoundaryObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RefusalBoundaryObservation)
    assert read_back.id == created.id
    assert read_back.subject_id == "sample_subject_id"
    assert read_back.refusal_code == "sample_refusal_code"
    assert read_back.authority_boundary == "sample_authority_boundary"
    assert read_back.evidence_digest == "sample_evidence_digest"
  end
end

