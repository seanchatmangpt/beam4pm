# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.IrreversibilityBudgetTest do
  use ExUnit.Case, async: false

  test "irreversibility_budget: real Ash ETS create/read round-trips every field" do
    params =
      %{
      episode_id: "sample_episode_id",
      budget: "sample_budget",
      consumed: "sample_consumed"
      }

    created =
      BeamPM.Ash.Resources.IrreversibilityBudget
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.IrreversibilityBudget)
    assert read_back.id == created.id
    assert read_back.episode_id == "sample_episode_id"
    assert read_back.budget == "sample_budget"
    assert read_back.consumed == "sample_consumed"
  end
end

