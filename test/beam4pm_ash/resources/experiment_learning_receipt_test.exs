# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ExperimentLearningReceiptTest do
  use ExUnit.Case, async: false

  test "experiment_learning_receipt: real Ash ETS create/read round-trips every field" do
    params =
      %{
      receipt_id: "sample_receipt_id",
      experiment_id: "sample_experiment_id",
      exact_subject_sha: "sample_exact_subject_sha",
      evidence_digest: "sample_evidence_digest",
      result_digest: "sample_result_digest",
      authority_ceiling: "sample_authority_ceiling"
      }

    created =
      BeamPM.Ash.Resources.ExperimentLearningReceipt
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ExperimentLearningReceipt)
    assert read_back.id == created.id
    assert read_back.receipt_id == "sample_receipt_id"
    assert read_back.experiment_id == "sample_experiment_id"
    assert read_back.exact_subject_sha == "sample_exact_subject_sha"
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.result_digest == "sample_result_digest"
    assert read_back.authority_ceiling == "sample_authority_ceiling"
  end
end

