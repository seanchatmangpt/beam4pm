# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.BundleConflictTest do
  use ExUnit.Case, async: false

  test "bundle_conflict: real Ash ETS create/read round-trips every field" do
    params =
      %{
      bundle_conflict_id: "sample_bundle_conflict_id",
      account_id: "sample_account_id",
      conflicting_bundle_id: "sample_conflicting_bundle_id",
      evidence_digest: "sample_evidence_digest",
      effective_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.BundleConflict
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.BundleConflict)
    assert read_back.id == created.id
    assert read_back.bundle_conflict_id == "sample_bundle_conflict_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.conflicting_bundle_id == "sample_conflicting_bundle_id"
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.effective_at == ~U[2026-08-29 12:00:00Z]
  end
end

