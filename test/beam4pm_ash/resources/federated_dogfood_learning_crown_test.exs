# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.FederatedDogfoodLearningCrownTest do
  use ExUnit.Case, async: false

  test "federated_dogfood_learning_crown: real Ash ETS create/read round-trips every field" do
    params =
      %{
      crown_id: "sample_crown_id",
      exact_subject_sha: "sample_exact_subject_sha",
      consumer_id: "sample_consumer_id",
      pack_sha: "sample_pack_sha",
      capsule_digest: "sample_capsule_digest",
      manufacture_receipt_digest: "sample_manufacture_receipt_digest",
      ownership_verification_digest: "sample_ownership_verification_digest",
      second_pass_identity_digest: "sample_second_pass_identity_digest",
      workflow_sequence: "sample_workflow_sequence",
      authority_ceiling: "sample_authority_ceiling"
      }

    created =
      BeamPM.Ash.Resources.FederatedDogfoodLearningCrown
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.FederatedDogfoodLearningCrown)
    assert read_back.id == created.id
    assert read_back.crown_id == "sample_crown_id"
    assert read_back.exact_subject_sha == "sample_exact_subject_sha"
    assert read_back.consumer_id == "sample_consumer_id"
    assert read_back.pack_sha == "sample_pack_sha"
    assert read_back.capsule_digest == "sample_capsule_digest"
    assert read_back.manufacture_receipt_digest == "sample_manufacture_receipt_digest"
    assert read_back.ownership_verification_digest == "sample_ownership_verification_digest"
    assert read_back.second_pass_identity_digest == "sample_second_pass_identity_digest"
    assert read_back.workflow_sequence == "sample_workflow_sequence"
    assert read_back.authority_ceiling == "sample_authority_ceiling"
  end
end

