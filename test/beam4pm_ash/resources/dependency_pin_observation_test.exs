# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.DependencyPinObservationTest do
  use ExUnit.Case, async: false

  test "dependency_pin_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      dependency_id: "sample_dependency_id",
      declared_ref: "sample_declared_ref",
      resolved_sha: "sample_resolved_sha",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.DependencyPinObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.DependencyPinObservation)
    assert read_back.id == created.id
    assert read_back.dependency_id == "sample_dependency_id"
    assert read_back.declared_ref == "sample_declared_ref"
    assert read_back.resolved_sha == "sample_resolved_sha"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

