# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ReversibilityWeightTest do
  use ExUnit.Case, async: false

  test "reversibility_weight: real Ash ETS create/read round-trips every field" do
    params =
      %{
      action_id: "sample_action_id",
      rollback_id: "sample_rollback_id",
      weight: "sample_weight"
      }

    created =
      BeamPM.Ash.Resources.ReversibilityWeight
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ReversibilityWeight)
    assert read_back.id == created.id
    assert read_back.action_id == "sample_action_id"
    assert read_back.rollback_id == "sample_rollback_id"
    assert read_back.weight == "sample_weight"
  end
end

