# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.SemanticDriftObservationTest do
  use ExUnit.Case, async: false

  test "semantic_drift_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      subject_id: "sample_subject_id",
      prior_semantic_digest: "sample_prior_semantic_digest",
      current_semantic_digest: "sample_current_semantic_digest",
      drift_class: "sample_drift_class"
      }

    created =
      BeamPM.Ash.Resources.SemanticDriftObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.SemanticDriftObservation)
    assert read_back.id == created.id
    assert read_back.subject_id == "sample_subject_id"
    assert read_back.prior_semantic_digest == "sample_prior_semantic_digest"
    assert read_back.current_semantic_digest == "sample_current_semantic_digest"
    assert read_back.drift_class == "sample_drift_class"
  end
end

