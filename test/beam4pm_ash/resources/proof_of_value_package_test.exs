# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ProofOfValuePackageTest do
  use ExUnit.Case, async: false

  test "proof_of_value_package: real Ash ETS create/read round-trips every field" do
    params =
      %{
      proof_of_value_package_id: "sample_proof_of_value_package_id",
      account_id: "sample_account_id",
      success_metric: "sample_success_metric",
      evidence_digest: "sample_evidence_digest",
      effective_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.ProofOfValuePackage
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ProofOfValuePackage)
    assert read_back.id == created.id
    assert read_back.proof_of_value_package_id == "sample_proof_of_value_package_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.success_metric == "sample_success_metric"
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.effective_at == ~U[2026-08-29 12:00:00Z]
  end
end

