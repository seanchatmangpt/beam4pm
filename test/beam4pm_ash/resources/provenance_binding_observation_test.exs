# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ProvenanceBindingObservationTest do
  use ExUnit.Case, async: false

  test "provenance_binding_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      observation_id: "sample_observation_id",
      source_capsule_digest: "sample_source_capsule_digest",
      evidence_digest: "sample_evidence_digest",
      binding_status: "sample_binding_status"
      }

    created =
      BeamPM.Ash.Resources.ProvenanceBindingObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ProvenanceBindingObservation)
    assert read_back.id == created.id
    assert read_back.observation_id == "sample_observation_id"
    assert read_back.source_capsule_digest == "sample_source_capsule_digest"
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.binding_status == "sample_binding_status"
  end
end

