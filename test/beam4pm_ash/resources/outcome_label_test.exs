# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.OutcomeLabelTest do
  use ExUnit.Case, async: false

  test "outcome_label: real Ash ETS create/read round-trips every field" do
    params =
      %{
      label_id: "sample_label_id",
      episode_id: "sample_episode_id",
      outcome_class: "sample_outcome_class",
      confidence_basis: "sample_confidence_basis",
      independent_evidence_digest: "sample_independent_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.OutcomeLabel
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.OutcomeLabel)
    assert read_back.id == created.id
    assert read_back.label_id == "sample_label_id"
    assert read_back.episode_id == "sample_episode_id"
    assert read_back.outcome_class == "sample_outcome_class"
    assert read_back.confidence_basis == "sample_confidence_basis"
    assert read_back.independent_evidence_digest == "sample_independent_evidence_digest"
  end
end

