# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.NormalizedEventObservationTest do
  use ExUnit.Case, async: false

  test "normalized_event_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      source_system: "sample_source_system",
      event_id: "sample_event_id",
      event_type: "sample_event_type",
      event_time: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.NormalizedEventObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.NormalizedEventObservation)
    assert read_back.id == created.id
    assert read_back.source_system == "sample_source_system"
    assert read_back.event_id == "sample_event_id"
    assert read_back.event_type == "sample_event_type"
    assert read_back.event_time == ~U[2026-08-29 12:00:00Z]
  end
end

