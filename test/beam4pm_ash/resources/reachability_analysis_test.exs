# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ReachabilityAnalysisTest do
  use ExUnit.Case, async: false

  test "reachability_analysis: real Ash ETS create/read round-trips every field" do
    params =
      %{
      state_id: "sample_state_id",
      goal_id: "sample_goal_id",
      reachability_proof_hash: "sample_reachability_proof_hash"
      }

    created =
      BeamPM.Ash.Resources.ReachabilityAnalysis
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ReachabilityAnalysis)
    assert read_back.id == created.id
    assert read_back.state_id == "sample_state_id"
    assert read_back.goal_id == "sample_goal_id"
    assert read_back.reachability_proof_hash == "sample_reachability_proof_hash"
  end
end

