# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CallerLocalConsumerTest do
  use ExUnit.Case, async: false

  test "caller_local_consumer: real Ash ETS create/read round-trips every field" do
    params =
      %{
      consumer_id: "sample_consumer_id",
      subject_sha: "sample_subject_sha",
      consumer_hash: "sample_consumer_hash"
      }

    created =
      BeamPM.Ash.Resources.CallerLocalConsumer
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CallerLocalConsumer)
    assert read_back.id == created.id
    assert read_back.consumer_id == "sample_consumer_id"
    assert read_back.subject_sha == "sample_subject_sha"
    assert read_back.consumer_hash == "sample_consumer_hash"
  end
end

