# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.NoAuthorityLearningGuardTest do
  use ExUnit.Case, async: false

  test "no_authority_learning_guard: real Ash ETS create/read round-trips every field" do
    params =
      %{
      guard_id: "sample_guard_id",
      candidate_update_id: "sample_candidate_update_id",
      current_authority: "sample_current_authority",
      requested_authority: "sample_requested_authority",
      decision: "sample_decision",
      evidence_digest: "sample_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.NoAuthorityLearningGuard
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.NoAuthorityLearningGuard)
    assert read_back.id == created.id
    assert read_back.guard_id == "sample_guard_id"
    assert read_back.candidate_update_id == "sample_candidate_update_id"
    assert read_back.current_authority == "sample_current_authority"
    assert read_back.requested_authority == "sample_requested_authority"
    assert read_back.decision == "sample_decision"
    assert read_back.evidence_digest == "sample_evidence_digest"
  end
end

