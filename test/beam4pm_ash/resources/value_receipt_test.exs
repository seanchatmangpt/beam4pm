# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ValueReceiptTest do
  use ExUnit.Case, async: false

  test "value_receipt: real Ash ETS create/read round-trips every field" do
    params =
      %{
      value_receipt_id: "sample_value_receipt_id",
      account_id: "sample_account_id",
      metric_name: "sample_metric_name",
      baseline_value: 3.5,
      observed_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.ValueReceipt
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ValueReceipt)
    assert read_back.id == created.id
    assert read_back.value_receipt_id == "sample_value_receipt_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.metric_name == "sample_metric_name"
    assert read_back.baseline_value == 3.5
    assert read_back.observed_value == 3.5
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

