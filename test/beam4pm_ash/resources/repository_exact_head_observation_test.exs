# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.RepositoryExactHeadObservationTest do
  use ExUnit.Case, async: false

  test "repository_exact_head_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      repository_id: "sample_repository_id",
      branch_name: "sample_branch_name",
      head_sha: "sample_head_sha",
      previous_head_sha: "sample_previous_head_sha"
      }

    created =
      BeamPM.Ash.Resources.RepositoryExactHeadObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.RepositoryExactHeadObservation)
    assert read_back.id == created.id
    assert read_back.repository_id == "sample_repository_id"
    assert read_back.branch_name == "sample_branch_name"
    assert read_back.head_sha == "sample_head_sha"
    assert read_back.previous_head_sha == "sample_previous_head_sha"
  end
end

