# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.FailureLabelTest do
  use ExUnit.Case, async: false

  test "failure_label: real Ash ETS create/read round-trips every field" do
    params =
      %{
      failure_id: "sample_failure_id",
      episode_id: "sample_episode_id",
      failure_class: "sample_failure_class",
      causal_scope: "sample_causal_scope",
      preserved_evidence_digest: "sample_preserved_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.FailureLabel
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.FailureLabel)
    assert read_back.id == created.id
    assert read_back.failure_id == "sample_failure_id"
    assert read_back.episode_id == "sample_episode_id"
    assert read_back.failure_class == "sample_failure_class"
    assert read_back.causal_scope == "sample_causal_scope"
    assert read_back.preserved_evidence_digest == "sample_preserved_evidence_digest"
  end
end

