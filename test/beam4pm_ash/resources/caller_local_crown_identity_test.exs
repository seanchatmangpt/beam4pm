# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CallerLocalCrownIdentityTest do
  use ExUnit.Case, async: false

  test "caller_local_crown_identity: real Ash ETS create/read round-trips every field" do
    params =
      %{
      propagation_id: "sample_propagation_id",
      subject_sha: "sample_subject_sha",
      consumer_subject_sha: "sample_consumer_subject_sha",
      receipt_digest: "sample_receipt_digest"
      }

    created =
      BeamPM.Ash.Resources.CallerLocalCrownIdentity
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CallerLocalCrownIdentity)
    assert read_back.id == created.id
    assert read_back.propagation_id == "sample_propagation_id"
    assert read_back.subject_sha == "sample_subject_sha"
    assert read_back.consumer_subject_sha == "sample_consumer_subject_sha"
    assert read_back.receipt_digest == "sample_receipt_digest"
  end
end

