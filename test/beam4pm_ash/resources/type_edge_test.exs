# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.TypeEdgeTest do
  use ExUnit.Case, async: false

  test "type_edge: real Ash ETS create/read round-trips every field" do
    params =
      %{
      source_type: "sample_source_type",
      target_type: "sample_target_type",
      qualifier: "sample_qualifier",
      direction: :sample_atom
      }

    created =
      BeamPM.Ash.Resources.TypeEdge
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.TypeEdge)
    assert read_back.id == created.id
    assert read_back.source_type == "sample_source_type"
    assert read_back.target_type == "sample_target_type"
    assert read_back.qualifier == "sample_qualifier"
    assert read_back.direction == :sample_atom
  end
end

