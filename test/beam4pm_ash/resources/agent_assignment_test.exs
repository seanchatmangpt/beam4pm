# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.AgentAssignmentTest do
  use ExUnit.Case, async: false

  test "agent_assignment: real Ash ETS create/read round-trips every field" do
    params =
      %{
      agent_id: "sample_agent_id",
      policy_id: "sample_policy_id",
      assignment_hash: "sample_assignment_hash"
      }

    created =
      BeamPM.Ash.Resources.AgentAssignment
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.AgentAssignment)
    assert read_back.id == created.id
    assert read_back.agent_id == "sample_agent_id"
    assert read_back.policy_id == "sample_policy_id"
    assert read_back.assignment_hash == "sample_assignment_hash"
  end
end

