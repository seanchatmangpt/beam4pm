# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.NegativeFixtureGenerationTest do
  use ExUnit.Case, async: false

  test "negative_fixture_generation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      fixture_id: "sample_fixture_id",
      source_failure_id: "sample_source_failure_id",
      invariant_id: "sample_invariant_id",
      generated_input_digest: "sample_generated_input_digest",
      expected_refusal_code: "sample_expected_refusal_code"
      }

    created =
      BeamPM.Ash.Resources.NegativeFixtureGeneration
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.NegativeFixtureGeneration)
    assert read_back.id == created.id
    assert read_back.fixture_id == "sample_fixture_id"
    assert read_back.source_failure_id == "sample_source_failure_id"
    assert read_back.invariant_id == "sample_invariant_id"
    assert read_back.generated_input_digest == "sample_generated_input_digest"
    assert read_back.expected_refusal_code == "sample_expected_refusal_code"
  end
end

