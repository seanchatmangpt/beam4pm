# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CounterfactualReplayTest do
  use ExUnit.Case, async: false

  test "counterfactual_replay: real Ash ETS create/read round-trips every field" do
    params =
      %{
      replay_id: "sample_replay_id",
      source_episode_id: "sample_source_episode_id",
      intervention_digest: "sample_intervention_digest",
      seed: "sample_seed",
      predicted_outcome: "sample_predicted_outcome",
      comparison_digest: "sample_comparison_digest"
      }

    created =
      BeamPM.Ash.Resources.CounterfactualReplay
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CounterfactualReplay)
    assert read_back.id == created.id
    assert read_back.replay_id == "sample_replay_id"
    assert read_back.source_episode_id == "sample_source_episode_id"
    assert read_back.intervention_digest == "sample_intervention_digest"
    assert read_back.seed == "sample_seed"
    assert read_back.predicted_outcome == "sample_predicted_outcome"
    assert read_back.comparison_digest == "sample_comparison_digest"
  end
end

