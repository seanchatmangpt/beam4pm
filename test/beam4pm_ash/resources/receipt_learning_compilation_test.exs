# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ReceiptLearningCompilationTest do
  use ExUnit.Case, async: false

  test "receipt_learning_compilation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      compilation_id: "sample_compilation_id",
      receipt_chain_head: "sample_receipt_chain_head",
      admitted_receipt_count: 42,
      rejected_receipt_count: 42,
      learning_digest: "sample_learning_digest"
      }

    created =
      BeamPM.Ash.Resources.ReceiptLearningCompilation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ReceiptLearningCompilation)
    assert read_back.id == created.id
    assert read_back.compilation_id == "sample_compilation_id"
    assert read_back.receipt_chain_head == "sample_receipt_chain_head"
    assert read_back.admitted_receipt_count == 42
    assert read_back.rejected_receipt_count == 42
    assert read_back.learning_digest == "sample_learning_digest"
  end
end

