# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PropagationScoreTest do
  use ExUnit.Case, async: false

  test "propagation_score: real Ash ETS create/read round-trips every field" do
    params =
      %{
      option_id: "sample_option_id",
      graph_hash: "sample_graph_hash",
      score: "sample_score"
      }

    created =
      BeamPM.Ash.Resources.PropagationScore
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PropagationScore)
    assert read_back.id == created.id
    assert read_back.option_id == "sample_option_id"
    assert read_back.graph_hash == "sample_graph_hash"
    assert read_back.score == "sample_score"
  end
end

