# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ConsumerEquivalenceProofTest do
  use ExUnit.Case, async: false

  test "consumer_equivalence_proof: real Ash ETS create/read round-trips every field" do
    params =
      %{
      consumer_set_id: "sample_consumer_set_id",
      equivalence_proof_hash: "sample_equivalence_proof_hash",
      standing: "sample_standing"
      }

    created =
      BeamPM.Ash.Resources.ConsumerEquivalenceProof
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ConsumerEquivalenceProof)
    assert read_back.id == created.id
    assert read_back.consumer_set_id == "sample_consumer_set_id"
    assert read_back.equivalence_proof_hash == "sample_equivalence_proof_hash"
    assert read_back.standing == "sample_standing"
  end
end

