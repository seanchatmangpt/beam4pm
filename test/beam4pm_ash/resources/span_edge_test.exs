# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.SpanEdgeTest do
  use ExUnit.Case, async: false

  test "span_edge: real Ash ETS create/read round-trips every field" do
    params =
      %{
      source_service: "sample_source_service",
      target_service: "sample_target_service",
      frequency: 42,
      evidence: :sample_atom
      }

    created =
      BeamPM.Ash.Resources.SpanEdge
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.SpanEdge)
    assert read_back.id == created.id
    assert read_back.source_service == "sample_source_service"
    assert read_back.target_service == "sample_target_service"
    assert read_back.frequency == 42
    assert read_back.evidence == :sample_atom
  end
end

