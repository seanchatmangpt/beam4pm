# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.PlannerIdentityTest do
  use ExUnit.Case, async: false

  test "planner_identity: real Ash ETS create/read round-trips every field" do
    params =
      %{
      planner_id: "sample_planner_id",
      planner_kind: "sample_planner_kind",
      identity_hash: "sample_identity_hash"
      }

    created =
      BeamPM.Ash.Resources.PlannerIdentity
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.PlannerIdentity)
    assert read_back.id == created.id
    assert read_back.planner_id == "sample_planner_id"
    assert read_back.planner_kind == "sample_planner_kind"
    assert read_back.identity_hash == "sample_identity_hash"
  end
end

