# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CreditExpiryPolicyTest do
  use ExUnit.Case, async: false

  test "credit_expiry_policy: real Ash ETS create/read round-trips every field" do
    params =
      %{
      credit_expiry_policy_id: "sample_credit_expiry_policy_id",
      account_id: "sample_account_id",
      expires_at: "2026-08-29T12:00:00Z",
      evidence_digest: "sample_evidence_digest",
      effective_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.CreditExpiryPolicy
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CreditExpiryPolicy)
    assert read_back.id == created.id
    assert read_back.credit_expiry_policy_id == "sample_credit_expiry_policy_id"
    assert read_back.account_id == "sample_account_id"
    assert read_back.expires_at == ~U[2026-08-29 12:00:00Z]
    assert read_back.evidence_digest == "sample_evidence_digest"
    assert read_back.effective_at == ~U[2026-08-29 12:00:00Z]
  end
end

