# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ImmutablePackSelectionTest do
  use ExUnit.Case, async: false

  test "immutable_pack_selection: real Ash ETS create/read round-trips every field" do
    params =
      %{
      pack_id: "sample_pack_id",
      pack_sha: "sample_pack_sha",
      selection_hash: "sample_selection_hash"
      }

    created =
      BeamPM.Ash.Resources.ImmutablePackSelection
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ImmutablePackSelection)
    assert read_back.id == created.id
    assert read_back.pack_id == "sample_pack_id"
    assert read_back.pack_sha == "sample_pack_sha"
    assert read_back.selection_hash == "sample_selection_hash"
  end
end

