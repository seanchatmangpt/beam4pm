# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.CapabilityGapLearningTest do
  use ExUnit.Case, async: false

  test "capability_gap_learning: real Ash ETS create/read round-trips every field" do
    params =
      %{
      learning_id: "sample_learning_id",
      observed_capability_id: "sample_observed_capability_id",
      required_capability_id: "sample_required_capability_id",
      gap_type: "sample_gap_type",
      evidence_digest: "sample_evidence_digest"
      }

    created =
      BeamPM.Ash.Resources.CapabilityGapLearning
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.CapabilityGapLearning)
    assert read_back.id == created.id
    assert read_back.learning_id == "sample_learning_id"
    assert read_back.observed_capability_id == "sample_observed_capability_id"
    assert read_back.required_capability_id == "sample_required_capability_id"
    assert read_back.gap_type == "sample_gap_type"
    assert read_back.evidence_digest == "sample_evidence_digest"
  end
end

