# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ValidationCapsuleDriftObservationTest do
  use ExUnit.Case, async: false

  test "validation_capsule_drift_observation: real Ash ETS create/read round-trips every field" do
    params =
      %{
      expected_digest: "sample_expected_digest",
      observed_digest: "sample_observed_digest",
      drift_status: "sample_drift_status",
      observed_at: "2026-08-29T12:00:00Z"
      }

    created =
      BeamPM.Ash.Resources.ValidationCapsuleDriftObservation
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ValidationCapsuleDriftObservation)
    assert read_back.id == created.id
    assert read_back.expected_digest == "sample_expected_digest"
    assert read_back.observed_digest == "sample_observed_digest"
    assert read_back.drift_status == "sample_drift_status"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
  end
end

