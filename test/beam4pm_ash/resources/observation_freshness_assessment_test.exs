# GENERATED by ggen_igniter. Do not edit.
defmodule BeamPM.Ash.Resources.ObservationFreshnessAssessmentTest do
  use ExUnit.Case, async: false

  test "observation_freshness_assessment: real Ash ETS create/read round-trips every field" do
    params =
      %{
      observation_id: "sample_observation_id",
      observed_at: "2026-08-29T12:00:00Z",
      freshness_deadline: "2026-08-29T12:00:00Z",
      freshness_status: "sample_freshness_status"
      }

    created =
      BeamPM.Ash.Resources.ObservationFreshnessAssessment
      |> Ash.Changeset.for_create(:create, params)
      |> Ash.create!()

    [read_back] = Ash.read!(BeamPM.Ash.Resources.ObservationFreshnessAssessment)
    assert read_back.id == created.id
    assert read_back.observation_id == "sample_observation_id"
    assert read_back.observed_at == ~U[2026-08-29 12:00:00Z]
    assert read_back.freshness_deadline == ~U[2026-08-29 12:00:00Z]
    assert read_back.freshness_status == "sample_freshness_status"
  end
end

