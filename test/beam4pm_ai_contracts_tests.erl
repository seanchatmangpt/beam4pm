%% GENERATED by ggen from the admitted aic:ContractType graph. Do not edit.

-module(beam4pm_ai_contracts_tests).

-include_lib("eunit/include/eunit.hrl").

%% No explicit -export here: EUnit's own parse transform auto-exports every
%% *_test/0 function in this module, and an explicit -export of the same
%% functions produces a real "already exported" compiler warning (which
%% would become a hard failure under warnings_as_errors).

admission_decision_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_ai_contracts:new_admission_decision(#{
        decision_id => <<"x">>,
        subject_id => <<"x">>,
        principal_id => <<"x">>,
        verdict => <<"x">>,
        typed_reason => <<"x">>,
        policy_id => <<"x">>,
        policy_version => <<"x">>,
        decided_at => <<"2026-01-01T00:00:00Z">>
    })).

admission_decision_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_ai_contracts:new_admission_decision(#{})).

candidate_claim_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_ai_contracts:new_candidate_claim(#{
        claim_id => <<"x">>,
        task_id => <<"x">>,
        statement => <<"x">>,
        support_evidence_ids => [<<"a">>],
        contradict_evidence_ids => [<<"a">>],
        confidence => 1.0,
        semantic_status => <<"x">>
    })).

candidate_claim_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_ai_contracts:new_candidate_claim(#{})).

evidence_envelope_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_ai_contracts:new_evidence_envelope(#{
        evidence_id => <<"x">>,
        tenant_id => <<"x">>,
        source_uri => <<"x">>,
        source_kind => <<"x">>,
        content_hash => <<"x">>,
        mime_type => <<"x">>,
        modality => <<"x">>,
        observed_at => <<"2026-01-01T00:00:00Z">>,
        retrieved_at => <<"2026-01-01T00:00:00Z">>,
        acl_labels => [<<"a">>],
        canonical_entity_ids => [<<"a">>],
        redaction_class => <<"x">>,
        schema_version => <<"x">>
    })).

evidence_envelope_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_ai_contracts:new_evidence_envelope(#{})).

evidence_hit_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_ai_contracts:new_evidence_hit(#{
        hit_id => <<"x">>,
        query_id => <<"x">>,
        evidence_id => <<"x">>,
        score => 1.0,
        retrieval_method => <<"x">>,
        rank => 1
    })).

evidence_hit_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_ai_contracts:new_evidence_hit(#{})).

model_call_receipt_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_ai_contracts:new_model_call_receipt(#{
        call_id => <<"x">>,
        provider_profile => <<"x">>,
        model_id_or_snapshot => <<"x">>,
        request_hash => <<"x">>,
        response_hash => <<"x">>,
        prompt_template_id => <<"x">>,
        prompt_template_version => <<"x">>,
        latency_ms => 1,
        trace_id => <<"x">>
    })).

model_call_receipt_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_ai_contracts:new_model_call_receipt(#{})).

research_task_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_ai_contracts:new_research_task(#{
        task_id => <<"x">>,
        principal_id => <<"x">>,
        query => <<"x">>,
        freshness_requirement => <<"x">>,
        privacy_class => <<"x">>,
        max_tokens => 1,
        max_tool_calls => 1,
        output_contract => <<"x">>,
        policy_version => <<"x">>,
        correlation_id => <<"x">>
    })).

research_task_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_ai_contracts:new_research_task(#{})).

tool_intent_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_ai_contracts:new_tool_intent(#{
        intent_id => <<"x">>,
        task_id => <<"x">>,
        tool_id => <<"x">>,
        tool_version => <<"x">>,
        canonical_args_hash => <<"x">>,
        side_effect_class => <<"x">>,
        required_capability => <<"x">>,
        dry_run => true,
        idempotency_key => <<"x">>,
        status => <<"x">>
    })).

tool_intent_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_ai_contracts:new_tool_intent(#{})).

