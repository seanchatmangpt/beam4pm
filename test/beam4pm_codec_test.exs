# GENERATED by ggen from the admitted bpm:RecordType graph. Do not edit.
defmodule BeamPM.Codec.GeneratedTest do
  use ExUnit.Case, async: true

  test "from_map returns unknown_record for an unadmitted record name" do
    assert {:error, {:unknown_record, :not_a_real_record}} =
             BeamPM.Codec.from_map(:not_a_real_record, %{})
  end

  test "decode returns unknown_record for an unadmitted record name" do
    assert {:error, {:unknown_record, :not_a_real_record}} =
             BeamPM.Codec.decode(:not_a_real_record, "{}")
  end

  test "decode wraps invalid JSON input in an error tuple" do
    assert {:error, _} = BeamPM.Codec.decode(:not_a_real_record, "this is not json")
  end

  test "decode rejects JSON that is not an object" do
    assert {:error, {:not_a_json_object, [1, 2, 3]}} =
             BeamPM.Codec.decode(:not_a_real_record, "[1,2,3]")
  end

  test "account_discovery to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      account_discovery_id: "sample_account_discovery_id",
      account_id: "sample_account_id",
      discovery_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.AccountDiscovery.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["account_discovery_id"] == "sample_account_discovery_id"
    assert m["account_id"] == "sample_account_id"
    assert m["discovery_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:account_discovery, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:account_discovery, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "account_discovery encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      account_discovery_id: "sample_account_discovery_id",
      account_id: "sample_account_id",
      discovery_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.AccountDiscovery.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:account_discovery, json)
  end

  test "account_discovery from_map reports the first missing required field" do
    assert {:error, {:missing_field, :account_discovery_id}} =
             BeamPM.Codec.from_map(:account_discovery, %{})
  end


  test "account_master_match to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      source_account_id: "sample_source_account_id",
      canonical_account_id: "sample_canonical_account_id",
      match_evidence_hash: "sample_match_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.AccountMasterMatch.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["source_account_id"] == "sample_source_account_id"
    assert m["canonical_account_id"] == "sample_canonical_account_id"
    assert m["match_evidence_hash"] == "sample_match_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:account_master_match, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:account_master_match, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "account_master_match encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      source_account_id: "sample_source_account_id",
      canonical_account_id: "sample_canonical_account_id",
      match_evidence_hash: "sample_match_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.AccountMasterMatch.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:account_master_match, json)
  end

  test "account_master_match from_map reports the first missing required field" do
    assert {:error, {:missing_field, :source_account_id}} =
             BeamPM.Codec.from_map(:account_master_match, %{})
  end


  test "account_parent_scope to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      account_id: "sample_account_id",
      parent_account_id: "sample_parent_account_id",
      scope_evidence_hash: "sample_scope_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.AccountParentScope.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["account_id"] == "sample_account_id"
    assert m["parent_account_id"] == "sample_parent_account_id"
    assert m["scope_evidence_hash"] == "sample_scope_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:account_parent_scope, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:account_parent_scope, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "account_parent_scope encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      account_id: "sample_account_id",
      parent_account_id: "sample_parent_account_id",
      scope_evidence_hash: "sample_scope_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.AccountParentScope.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:account_parent_scope, json)
  end

  test "account_parent_scope from_map reports the first missing required field" do
    assert {:error, {:missing_field, :account_id}} =
             BeamPM.Codec.from_map(:account_parent_scope, %{})
  end


  test "account_value_realization to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      account_id: "sample_account_id",
      realization_hash: "sample_realization_hash"
    }

    assert {:ok, rec} = BeamPM.Types.AccountValueRealization.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["account_id"] == "sample_account_id"
    assert m["realization_hash"] == "sample_realization_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:account_value_realization, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:account_value_realization, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "account_value_realization encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      account_id: "sample_account_id",
      realization_hash: "sample_realization_hash"
    }

    assert {:ok, rec} = BeamPM.Types.AccountValueRealization.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:account_value_realization, json)
  end

  test "account_value_realization from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:account_value_realization, %{})
  end


  test "action_pin_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      action_sha: "sample_action_sha",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ActionPinEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["action_sha"] == "sample_action_sha"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:action_pin_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:action_pin_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "action_pin_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      action_sha: "sample_action_sha",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ActionPinEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:action_pin_evidence, json)
  end

  test "action_pin_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:action_pin_evidence, %{})
  end


  test "activation_event to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      activation_event_id: "sample_activation_event_id",
      account_id: "sample_account_id",
      activation_type: "sample_activation_type",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ActivationEvent.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["activation_event_id"] == "sample_activation_event_id"
    assert m["account_id"] == "sample_account_id"
    assert m["activation_type"] == "sample_activation_type"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:activation_event, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:activation_event, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "activation_event encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      activation_event_id: "sample_activation_event_id",
      account_id: "sample_account_id",
      activation_type: "sample_activation_type",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ActivationEvent.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:activation_event, json)
  end

  test "activation_event from_map reports the first missing required field" do
    assert {:error, {:missing_field, :activation_event_id}} =
             BeamPM.Codec.from_map(:activation_event, %{})
  end


  test "add_on_bundle to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      add_on_id: "sample_add_on_id",
      name: "sample_name",
      capability_ids: ["alpha", "beta"],
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.AddOnBundle.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["add_on_id"] == "sample_add_on_id"
    assert m["name"] == "sample_name"
    assert m["capability_ids"] == ["alpha", "beta"]
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:add_on_bundle, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:add_on_bundle, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "add_on_bundle encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      add_on_id: "sample_add_on_id",
      name: "sample_name",
      capability_ids: ["alpha", "beta"],
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.AddOnBundle.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:add_on_bundle, json)
  end

  test "add_on_bundle from_map reports the first missing required field" do
    assert {:error, {:missing_field, :add_on_id}} =
             BeamPM.Codec.from_map(:add_on_bundle, %{})
  end


  test "adoption_milestone to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      adoption_milestone_id: "sample_adoption_milestone_id",
      account_id: "sample_account_id",
      milestone_name: "sample_milestone_name",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.AdoptionMilestone.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["adoption_milestone_id"] == "sample_adoption_milestone_id"
    assert m["account_id"] == "sample_account_id"
    assert m["milestone_name"] == "sample_milestone_name"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:adoption_milestone, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:adoption_milestone, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "adoption_milestone encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      adoption_milestone_id: "sample_adoption_milestone_id",
      account_id: "sample_account_id",
      milestone_name: "sample_milestone_name",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.AdoptionMilestone.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:adoption_milestone, json)
  end

  test "adoption_milestone from_map reports the first missing required field" do
    assert {:error, {:missing_field, :adoption_milestone_id}} =
             BeamPM.Codec.from_map(:adoption_milestone, %{})
  end


  test "alignment_move to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      move_type: :sample_atom,
      cost: 42
    }

    assert {:ok, rec} = BeamPM.Types.AlignmentMove.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["move_type"] == "sample_atom"
    assert m["cost"] == 42
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:alignment_move, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:alignment_move, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "alignment_move encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      move_type: :sample_atom,
      cost: 42
    }

    assert {:ok, rec} = BeamPM.Types.AlignmentMove.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:alignment_move, json)
  end

  test "alignment_move from_map reports the first missing required field" do
    assert {:error, {:missing_field, :move_type}} =
             BeamPM.Codec.from_map(:alignment_move, %{})
  end


  test "annual_subscription to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      subscription_id: "sample_subscription_id",
      sku: "sample_sku",
      seat_count: 42,
      renews_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.AnnualSubscription.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["subscription_id"] == "sample_subscription_id"
    assert m["sku"] == "sample_sku"
    assert m["seat_count"] == 42
    assert m["renews_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:annual_subscription, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:annual_subscription, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "annual_subscription encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      subscription_id: "sample_subscription_id",
      sku: "sample_sku",
      seat_count: 42,
      renews_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.AnnualSubscription.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:annual_subscription, json)
  end

  test "annual_subscription from_map reports the first missing required field" do
    assert {:error, {:missing_field, :subscription_id}} =
             BeamPM.Codec.from_map(:annual_subscription, %{})
  end


  test "approval_separation_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      approver_identity: "sample_approver_identity",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ApprovalSeparationEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["approver_identity"] == "sample_approver_identity"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:approval_separation_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:approval_separation_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "approval_separation_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      approver_identity: "sample_approver_identity",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ApprovalSeparationEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:approval_separation_evidence, json)
  end

  test "approval_separation_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:approval_separation_evidence, %{})
  end


  test "architecture_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      architecture_readiness_id: "sample_architecture_readiness_id",
      account_id: "sample_account_id",
      architecture_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ArchitectureReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["architecture_readiness_id"] == "sample_architecture_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["architecture_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:architecture_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:architecture_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "architecture_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      architecture_readiness_id: "sample_architecture_readiness_id",
      account_id: "sample_account_id",
      architecture_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ArchitectureReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:architecture_readiness, json)
  end

  test "architecture_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :architecture_readiness_id}} =
             BeamPM.Codec.from_map(:architecture_readiness, %{})
  end


  test "architecture_review_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      review_decision: "sample_review_decision",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ArchitectureReviewEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["review_decision"] == "sample_review_decision"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:architecture_review_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:architecture_review_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "architecture_review_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      review_decision: "sample_review_decision",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ArchitectureReviewEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:architecture_review_evidence, json)
  end

  test "architecture_review_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:architecture_review_evidence, %{})
  end


  test "artifact_digest_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      digest: "sample_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ArtifactDigestEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["digest"] == "sample_digest"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:artifact_digest_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:artifact_digest_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "artifact_digest_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      digest: "sample_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ArtifactDigestEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:artifact_digest_evidence, json)
  end

  test "artifact_digest_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:artifact_digest_evidence, %{})
  end


  test "attestation_verification_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      predicate_type: "sample_predicate_type",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.AttestationVerificationEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["predicate_type"] == "sample_predicate_type"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:attestation_verification_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:attestation_verification_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "attestation_verification_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      predicate_type: "sample_predicate_type",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.AttestationVerificationEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:attestation_verification_evidence, json)
  end

  test "attestation_verification_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:attestation_verification_evidence, %{})
  end


  test "audit_chain_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      previous_receipt_hash: "sample_previous_receipt_hash",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.AuditChainEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["previous_receipt_hash"] == "sample_previous_receipt_hash"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:audit_chain_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:audit_chain_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "audit_chain_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      previous_receipt_hash: "sample_previous_receipt_hash",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.AuditChainEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:audit_chain_evidence, json)
  end

  test "audit_chain_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:audit_chain_evidence, %{})
  end


  test "availability_observation to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      slo_id: "sample_slo_id",
      observation_hash: "sample_observation_hash"
    }

    assert {:ok, rec} = BeamPM.Types.AvailabilityObservation.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["slo_id"] == "sample_slo_id"
    assert m["observation_hash"] == "sample_observation_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:availability_observation, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:availability_observation, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "availability_observation encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      slo_id: "sample_slo_id",
      observation_hash: "sample_observation_hash"
    }

    assert {:ok, rec} = BeamPM.Types.AvailabilityObservation.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:availability_observation, json)
  end

  test "availability_observation from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:availability_observation, %{})
  end


  test "availability_slo_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      availability_percent: 3.5,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.AvailabilitySloEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["availability_percent"] == 3.5
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:availability_slo_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:availability_slo_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "availability_slo_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      availability_percent: 3.5,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.AvailabilitySloEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:availability_slo_evidence, json)
  end

  test "availability_slo_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:availability_slo_evidence, %{})
  end


  test "backup_restore_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      backup_digest: "sample_backup_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.BackupRestoreEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["backup_digest"] == "sample_backup_digest"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:backup_restore_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:backup_restore_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "backup_restore_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      backup_digest: "sample_backup_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.BackupRestoreEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:backup_restore_evidence, json)
  end

  test "backup_restore_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:backup_restore_evidence, %{})
  end


  test "baseline_metric to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      baseline_metric_id: "sample_baseline_metric_id",
      account_id: "sample_account_id",
      baseline_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.BaselineMetric.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["baseline_metric_id"] == "sample_baseline_metric_id"
    assert m["account_id"] == "sample_account_id"
    assert m["baseline_value"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:baseline_metric, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:baseline_metric, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "baseline_metric encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      baseline_metric_id: "sample_baseline_metric_id",
      account_id: "sample_account_id",
      baseline_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.BaselineMetric.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:baseline_metric, json)
  end

  test "baseline_metric from_map reports the first missing required field" do
    assert {:error, {:missing_field, :baseline_metric_id}} =
             BeamPM.Codec.from_map(:baseline_metric, %{})
  end


  test "beneficial_owner_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      account_id: "sample_account_id",
      owner_id: "sample_owner_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.BeneficialOwnerEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["account_id"] == "sample_account_id"
    assert m["owner_id"] == "sample_owner_id"
    assert m["evidence_hash"] == "sample_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:beneficial_owner_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:beneficial_owner_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "beneficial_owner_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      account_id: "sample_account_id",
      owner_id: "sample_owner_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.BeneficialOwnerEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:beneficial_owner_evidence, json)
  end

  test "beneficial_owner_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :account_id}} =
             BeamPM.Codec.from_map(:beneficial_owner_evidence, %{})
  end


  test "billable_usage_identity to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      billable_usage_id: "sample_billable_usage_id",
      identity_hash: "sample_identity_hash"
    }

    assert {:ok, rec} = BeamPM.Types.BillableUsageIdentity.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["billable_usage_id"] == "sample_billable_usage_id"
    assert m["identity_hash"] == "sample_identity_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:billable_usage_identity, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:billable_usage_identity, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "billable_usage_identity encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      billable_usage_id: "sample_billable_usage_id",
      identity_hash: "sample_identity_hash"
    }

    assert {:ok, rec} = BeamPM.Types.BillableUsageIdentity.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:billable_usage_identity, json)
  end

  test "billable_usage_identity from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:billable_usage_identity, %{})
  end


  test "billing_account to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      billing_account_id: "sample_billing_account_id",
      account_id: "sample_account_id",
      currency: "sample_currency",
      invoice_profile: "sample_invoice_profile"
    }

    assert {:ok, rec} = BeamPM.Types.BillingAccount.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["billing_account_id"] == "sample_billing_account_id"
    assert m["account_id"] == "sample_account_id"
    assert m["currency"] == "sample_currency"
    assert m["invoice_profile"] == "sample_invoice_profile"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:billing_account, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:billing_account, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "billing_account encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      billing_account_id: "sample_billing_account_id",
      account_id: "sample_account_id",
      currency: "sample_currency",
      invoice_profile: "sample_invoice_profile"
    }

    assert {:ok, rec} = BeamPM.Types.BillingAccount.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:billing_account, json)
  end

  test "billing_account from_map reports the first missing required field" do
    assert {:error, {:missing_field, :billing_account_id}} =
             BeamPM.Codec.from_map(:billing_account, %{})
  end


  test "billing_reconciliation to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      entitlement_id: "sample_entitlement_id",
      metric_name: "sample_metric_name",
      total_quantity: 3.5,
      applied_event_ids: ["alpha", "beta"],
      period_start: "2026-08-29T12:00:00Z",
      period_end: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.BillingReconciliation.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["entitlement_id"] == "sample_entitlement_id"
    assert m["metric_name"] == "sample_metric_name"
    assert m["total_quantity"] == 3.5
    assert m["applied_event_ids"] == ["alpha", "beta"]
    assert m["period_start"] == "2026-08-29T12:00:00Z"
    assert m["period_end"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 6
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:billing_reconciliation, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:billing_reconciliation, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "billing_reconciliation encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      entitlement_id: "sample_entitlement_id",
      metric_name: "sample_metric_name",
      total_quantity: 3.5,
      applied_event_ids: ["alpha", "beta"],
      period_start: "2026-08-29T12:00:00Z",
      period_end: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.BillingReconciliation.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:billing_reconciliation, json)
  end

  test "billing_reconciliation from_map reports the first missing required field" do
    assert {:error, {:missing_field, :entitlement_id}} =
             BeamPM.Codec.from_map(:billing_reconciliation, %{})
  end


  test "booking_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      readiness_id: "sample_readiness_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.BookingReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["readiness_id"] == "sample_readiness_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:booking_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:booking_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "booking_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      readiness_id: "sample_readiness_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.BookingReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:booking_readiness, json)
  end

  test "booking_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:booking_readiness, %{})
  end


  test "brce_actuation_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      request_id: "sample_request_id",
      receipt_hash: "sample_receipt_hash"
    }

    assert {:ok, rec} = BeamPM.Types.BrceActuationReceipt.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["request_id"] == "sample_request_id"
    assert m["receipt_hash"] == "sample_receipt_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:brce_actuation_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:brce_actuation_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "brce_actuation_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      request_id: "sample_request_id",
      receipt_hash: "sample_receipt_hash"
    }

    assert {:ok, rec} = BeamPM.Types.BrceActuationReceipt.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:brce_actuation_receipt, json)
  end

  test "brce_actuation_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:brce_actuation_receipt, %{})
  end


  test "brce_actuation_request to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      request_id: "sample_request_id",
      authority_hash: "sample_authority_hash"
    }

    assert {:ok, rec} = BeamPM.Types.BrceActuationRequest.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["request_id"] == "sample_request_id"
    assert m["authority_hash"] == "sample_authority_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:brce_actuation_request, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:brce_actuation_request, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "brce_actuation_request encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      request_id: "sample_request_id",
      authority_hash: "sample_authority_hash"
    }

    assert {:ok, rec} = BeamPM.Types.BrceActuationRequest.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:brce_actuation_request, json)
  end

  test "brce_actuation_request from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:brce_actuation_request, %{})
  end


  test "budget_period_alignment to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      budget_period: "sample_budget_period",
      alignment_result: "sample_alignment_result"
    }

    assert {:ok, rec} = BeamPM.Types.BudgetPeriodAlignment.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["budget_period"] == "sample_budget_period"
    assert m["alignment_result"] == "sample_alignment_result"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:budget_period_alignment, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:budget_period_alignment, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "budget_period_alignment encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      budget_period: "sample_budget_period",
      alignment_result: "sample_alignment_result"
    }

    assert {:ok, rec} = BeamPM.Types.BudgetPeriodAlignment.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:budget_period_alignment, json)
  end

  test "budget_period_alignment from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:budget_period_alignment, %{})
  end


  test "business_continuity_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      continuity_mode: "sample_continuity_mode",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.BusinessContinuityEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["continuity_mode"] == "sample_continuity_mode"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:business_continuity_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:business_continuity_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "business_continuity_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      continuity_mode: "sample_continuity_mode",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.BusinessContinuityEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:business_continuity_evidence, json)
  end

  test "business_continuity_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:business_continuity_evidence, %{})
  end


  test "business_outcome_measurement to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      outcome_id: "sample_outcome_id",
      measurement_hash: "sample_measurement_hash"
    }

    assert {:ok, rec} = BeamPM.Types.BusinessOutcomeMeasurement.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["outcome_id"] == "sample_outcome_id"
    assert m["measurement_hash"] == "sample_measurement_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:business_outcome_measurement, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:business_outcome_measurement, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "business_outcome_measurement encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      outcome_id: "sample_outcome_id",
      measurement_hash: "sample_measurement_hash"
    }

    assert {:ok, rec} = BeamPM.Types.BusinessOutcomeMeasurement.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:business_outcome_measurement, json)
  end

  test "business_outcome_measurement from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:business_outcome_measurement, %{})
  end


  test "buying_committee to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      buying_committee_id: "sample_buying_committee_id",
      account_id: "sample_account_id",
      committee_coverage: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.BuyingCommittee.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["buying_committee_id"] == "sample_buying_committee_id"
    assert m["account_id"] == "sample_account_id"
    assert m["committee_coverage"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:buying_committee, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:buying_committee, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "buying_committee encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      buying_committee_id: "sample_buying_committee_id",
      account_id: "sample_account_id",
      committee_coverage: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.BuyingCommittee.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:buying_committee, json)
  end

  test "buying_committee from_map reports the first missing required field" do
    assert {:error, {:missing_field, :buying_committee_id}} =
             BeamPM.Codec.from_map(:buying_committee, %{})
  end


  test "canary_decision to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      canary_decision_id: "sample_canary_decision_id",
      account_id: "sample_account_id",
      canary_result: "sample_canary_result",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CanaryDecision.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["canary_decision_id"] == "sample_canary_decision_id"
    assert m["account_id"] == "sample_account_id"
    assert m["canary_result"] == "sample_canary_result"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:canary_decision, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:canary_decision, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "canary_decision encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      canary_decision_id: "sample_canary_decision_id",
      account_id: "sample_account_id",
      canary_result: "sample_canary_result",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CanaryDecision.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:canary_decision, json)
  end

  test "canary_decision from_map reports the first missing required field" do
    assert {:error, {:missing_field, :canary_decision_id}} =
             BeamPM.Codec.from_map(:canary_decision, %{})
  end


  test "canary_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      canary_percentage: 3.5,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.CanaryEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["canary_percentage"] == 3.5
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:canary_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:canary_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "canary_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      canary_percentage: 3.5,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.CanaryEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:canary_evidence, json)
  end

  test "canary_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:canary_evidence, %{})
  end


  test "capability_bundle to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      bundle_id: "sample_bundle_id",
      name: "sample_name",
      capability_ids: ["alpha", "beta"],
      version: "sample_version"
    }

    assert {:ok, rec} = BeamPM.Types.CapabilityBundle.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["bundle_id"] == "sample_bundle_id"
    assert m["name"] == "sample_name"
    assert m["capability_ids"] == ["alpha", "beta"]
    assert m["version"] == "sample_version"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:capability_bundle, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:capability_bundle, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "capability_bundle encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      bundle_id: "sample_bundle_id",
      name: "sample_name",
      capability_ids: ["alpha", "beta"],
      version: "sample_version"
    }

    assert {:ok, rec} = BeamPM.Types.CapabilityBundle.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:capability_bundle, json)
  end

  test "capability_bundle from_map reports the first missing required field" do
    assert {:error, {:missing_field, :bundle_id}} =
             BeamPM.Codec.from_map(:capability_bundle, %{})
  end


  test "capability_gap to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      capability_gap_id: "sample_capability_gap_id",
      account_id: "sample_account_id",
      gap_severity: "sample_gap_severity",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CapabilityGap.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["capability_gap_id"] == "sample_capability_gap_id"
    assert m["account_id"] == "sample_account_id"
    assert m["gap_severity"] == "sample_gap_severity"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:capability_gap, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:capability_gap, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "capability_gap encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      capability_gap_id: "sample_capability_gap_id",
      account_id: "sample_account_id",
      gap_severity: "sample_gap_severity",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CapabilityGap.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:capability_gap, json)
  end

  test "capability_gap from_map reports the first missing required field" do
    assert {:error, {:missing_field, :capability_gap_id}} =
             BeamPM.Codec.from_map(:capability_gap, %{})
  end


  test "capsule_identity to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      capsule_id: "sample_capsule_id",
      identity_hash: "sample_identity_hash"
    }

    assert {:ok, rec} = BeamPM.Types.CapsuleIdentity.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["capsule_id"] == "sample_capsule_id"
    assert m["identity_hash"] == "sample_identity_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:capsule_identity, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:capsule_identity, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "capsule_identity encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      capsule_id: "sample_capsule_id",
      identity_hash: "sample_identity_hash"
    }

    assert {:ok, rec} = BeamPM.Types.CapsuleIdentity.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:capsule_identity, json)
  end

  test "capsule_identity from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:capsule_identity, %{})
  end


  test "case_stats to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      case_id: "sample_case_id",
      event_count: 42,
      duration_seconds: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.CaseStats.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["case_id"] == "sample_case_id"
    assert m["event_count"] == 42
    assert m["duration_seconds"] == 3.5
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:case_stats, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:case_stats, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "case_stats encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      case_id: "sample_case_id",
      event_count: 42,
      duration_seconds: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.CaseStats.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:case_stats, json)
  end

  test "case_stats from_map reports the first missing required field" do
    assert {:error, {:missing_field, :case_id}} =
             BeamPM.Codec.from_map(:case_stats, %{})
  end

  test "case_stats to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      case_id: "sample_case_id",
      event_count: 42
    }

    assert {:ok, rec} = BeamPM.Types.CaseStats.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "duration_seconds")
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:case_stats, m)
  end

  test "catalog_release to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      release_id: "sample_release_id",
      version: "sample_version",
      sku_ids: ["alpha", "beta"],
      effective_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CatalogRelease.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["release_id"] == "sample_release_id"
    assert m["version"] == "sample_version"
    assert m["sku_ids"] == ["alpha", "beta"]
    assert m["effective_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:catalog_release, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:catalog_release, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "catalog_release encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      release_id: "sample_release_id",
      version: "sample_version",
      sku_ids: ["alpha", "beta"],
      effective_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CatalogRelease.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:catalog_release, json)
  end

  test "catalog_release from_map reports the first missing required field" do
    assert {:error, {:missing_field, :release_id}} =
             BeamPM.Codec.from_map(:catalog_release, %{})
  end


  test "change_control_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      change_request_id: "sample_change_request_id",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ChangeControlEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["change_request_id"] == "sample_change_request_id"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:change_control_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:change_control_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "change_control_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      change_request_id: "sample_change_request_id",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ChangeControlEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:change_control_evidence, json)
  end

  test "change_control_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:change_control_evidence, %{})
  end


  test "change_order_authority to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      authority_id: "sample_authority_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ChangeOrderAuthority.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["authority_id"] == "sample_authority_id"
    assert m["evidence_hash"] == "sample_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:change_order_authority, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:change_order_authority, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "change_order_authority encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      authority_id: "sample_authority_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ChangeOrderAuthority.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:change_order_authority, json)
  end

  test "change_order_authority from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:change_order_authority, %{})
  end


  test "channel_agreement to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      agreement_id: "sample_agreement_id",
      partner_id: "sample_partner_id",
      territory: "sample_territory",
      valid_until: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ChannelAgreement.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["agreement_id"] == "sample_agreement_id"
    assert m["partner_id"] == "sample_partner_id"
    assert m["territory"] == "sample_territory"
    assert m["valid_until"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:channel_agreement, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:channel_agreement, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "channel_agreement encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      agreement_id: "sample_agreement_id",
      partner_id: "sample_partner_id",
      territory: "sample_territory",
      valid_until: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ChannelAgreement.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:channel_agreement, json)
  end

  test "channel_agreement from_map reports the first missing required field" do
    assert {:error, {:missing_field, :agreement_id}} =
             BeamPM.Codec.from_map(:channel_agreement, %{})
  end


  test "chargeback_rule to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      rule_id: "sample_rule_id",
      cost_center: "sample_cost_center",
      metric_name: "sample_metric_name",
      rate: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ChargebackRule.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["rule_id"] == "sample_rule_id"
    assert m["cost_center"] == "sample_cost_center"
    assert m["metric_name"] == "sample_metric_name"
    assert m["rate"] == 3.5
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:chargeback_rule, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:chargeback_rule, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "chargeback_rule encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      rule_id: "sample_rule_id",
      cost_center: "sample_cost_center",
      metric_name: "sample_metric_name",
      rate: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ChargebackRule.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:chargeback_rule, json)
  end

  test "chargeback_rule from_map reports the first missing required field" do
    assert {:error, {:missing_field, :rule_id}} =
             BeamPM.Codec.from_map(:chargeback_rule, %{})
  end


  test "cluster_quorum_state to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      cluster_id: "sample_cluster_id",
      quorum_hash: "sample_quorum_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ClusterQuorumState.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["cluster_id"] == "sample_cluster_id"
    assert m["quorum_hash"] == "sample_quorum_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:cluster_quorum_state, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:cluster_quorum_state, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "cluster_quorum_state encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      cluster_id: "sample_cluster_id",
      quorum_hash: "sample_quorum_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ClusterQuorumState.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:cluster_quorum_state, json)
  end

  test "cluster_quorum_state from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:cluster_quorum_state, %{})
  end


  test "commercial_approval to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      approval_id: "sample_approval_id",
      quote_id: "sample_quote_id",
      authority: "sample_authority",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.CommercialApproval.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["approval_id"] == "sample_approval_id"
    assert m["quote_id"] == "sample_quote_id"
    assert m["authority"] == "sample_authority"
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:commercial_approval, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:commercial_approval, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "commercial_approval encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      approval_id: "sample_approval_id",
      quote_id: "sample_quote_id",
      authority: "sample_authority",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.CommercialApproval.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:commercial_approval, json)
  end

  test "commercial_approval from_map reports the first missing required field" do
    assert {:error, {:missing_field, :approval_id}} =
             BeamPM.Codec.from_map(:commercial_approval, %{})
  end


  test "commercial_artifact_crown_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      artifact_digest: "sample_artifact_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.CommercialArtifactCrownEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["artifact_digest"] == "sample_artifact_digest"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:commercial_artifact_crown_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:commercial_artifact_crown_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "commercial_artifact_crown_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      artifact_digest: "sample_artifact_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.CommercialArtifactCrownEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:commercial_artifact_crown_evidence, json)
  end

  test "commercial_artifact_crown_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:commercial_artifact_crown_evidence, %{})
  end


  test "commercial_exception to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      exception_id: "sample_exception_id",
      exception_state: "sample_exception_state"
    }

    assert {:ok, rec} = BeamPM.Types.CommercialException.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["exception_id"] == "sample_exception_id"
    assert m["exception_state"] == "sample_exception_state"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:commercial_exception, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:commercial_exception, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "commercial_exception encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      exception_id: "sample_exception_id",
      exception_state: "sample_exception_state"
    }

    assert {:ok, rec} = BeamPM.Types.CommercialException.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:commercial_exception, json)
  end

  test "commercial_exception from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:commercial_exception, %{})
  end


  test "commercial_execution_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      receipt_id: "sample_receipt_id",
      subject_id: "sample_subject_id",
      operation: "sample_operation",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.CommercialExecutionReceipt.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["receipt_id"] == "sample_receipt_id"
    assert m["subject_id"] == "sample_subject_id"
    assert m["operation"] == "sample_operation"
    assert m["evidence_hash"] == "sample_evidence_hash"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:commercial_execution_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:commercial_execution_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "commercial_execution_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      receipt_id: "sample_receipt_id",
      subject_id: "sample_subject_id",
      operation: "sample_operation",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.CommercialExecutionReceipt.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:commercial_execution_receipt, json)
  end

  test "commercial_execution_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :receipt_id}} =
             BeamPM.Codec.from_map(:commercial_execution_receipt, %{})
  end


  test "commercial_forecast to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      forecast_id: "sample_forecast_id",
      account_id: "sample_account_id",
      amount: 3.5,
      confidence: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.CommercialForecast.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["forecast_id"] == "sample_forecast_id"
    assert m["account_id"] == "sample_account_id"
    assert m["amount"] == 3.5
    assert m["confidence"] == 3.5
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:commercial_forecast, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:commercial_forecast, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "commercial_forecast encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      forecast_id: "sample_forecast_id",
      account_id: "sample_account_id",
      amount: 3.5,
      confidence: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.CommercialForecast.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:commercial_forecast, json)
  end

  test "commercial_forecast from_map reports the first missing required field" do
    assert {:error, {:missing_field, :forecast_id}} =
             BeamPM.Codec.from_map(:commercial_forecast, %{})
  end


  test "commercial_outcome to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      commercial_outcome_id: "sample_commercial_outcome_id",
      account_id: "sample_account_id",
      outcome_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CommercialOutcome.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["commercial_outcome_id"] == "sample_commercial_outcome_id"
    assert m["account_id"] == "sample_account_id"
    assert m["outcome_value"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:commercial_outcome, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:commercial_outcome, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "commercial_outcome encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      commercial_outcome_id: "sample_commercial_outcome_id",
      account_id: "sample_account_id",
      outcome_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CommercialOutcome.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:commercial_outcome, json)
  end

  test "commercial_outcome from_map reports the first missing required field" do
    assert {:error, {:missing_field, :commercial_outcome_id}} =
             BeamPM.Codec.from_map(:commercial_outcome, %{})
  end


  test "commercial_quote to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      quote_id: "sample_quote_id",
      account_id: "sample_account_id",
      currency: "sample_currency",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.CommercialQuote.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["quote_id"] == "sample_quote_id"
    assert m["account_id"] == "sample_account_id"
    assert m["currency"] == "sample_currency"
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:commercial_quote, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:commercial_quote, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "commercial_quote encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      quote_id: "sample_quote_id",
      account_id: "sample_account_id",
      currency: "sample_currency",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.CommercialQuote.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:commercial_quote, json)
  end

  test "commercial_quote from_map reports the first missing required field" do
    assert {:error, {:missing_field, :quote_id}} =
             BeamPM.Codec.from_map(:commercial_quote, %{})
  end


  test "commercial_quote_line to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      quote_id: "sample_quote_id",
      sku: "sample_sku",
      quantity: 42,
      unit_price: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.CommercialQuoteLine.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["quote_id"] == "sample_quote_id"
    assert m["sku"] == "sample_sku"
    assert m["quantity"] == 42
    assert m["unit_price"] == 3.5
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:commercial_quote_line, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:commercial_quote_line, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "commercial_quote_line encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      quote_id: "sample_quote_id",
      sku: "sample_sku",
      quantity: 42,
      unit_price: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.CommercialQuoteLine.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:commercial_quote_line, json)
  end

  test "commercial_quote_line from_map reports the first missing required field" do
    assert {:error, {:missing_field, :quote_id}} =
             BeamPM.Codec.from_map(:commercial_quote_line, %{})
  end


  test "commercial_value_realization to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      realization_id: "sample_realization_id",
      baseline_id: "sample_baseline_id",
      realized_value: 3.5,
      measured_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CommercialValueRealization.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["realization_id"] == "sample_realization_id"
    assert m["baseline_id"] == "sample_baseline_id"
    assert m["realized_value"] == 3.5
    assert m["measured_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:commercial_value_realization, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:commercial_value_realization, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "commercial_value_realization encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      realization_id: "sample_realization_id",
      baseline_id: "sample_baseline_id",
      realized_value: 3.5,
      measured_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CommercialValueRealization.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:commercial_value_realization, json)
  end

  test "commercial_value_realization from_map reports the first missing required field" do
    assert {:error, {:missing_field, :realization_id}} =
             BeamPM.Codec.from_map(:commercial_value_realization, %{})
  end


  test "committed_spend to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      commitment_id: "sample_commitment_id",
      amount: 3.5,
      currency: "sample_currency",
      expires_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CommittedSpend.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["commitment_id"] == "sample_commitment_id"
    assert m["amount"] == 3.5
    assert m["currency"] == "sample_currency"
    assert m["expires_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:committed_spend, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:committed_spend, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "committed_spend encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      commitment_id: "sample_commitment_id",
      amount: 3.5,
      currency: "sample_currency",
      expires_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CommittedSpend.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:committed_spend, json)
  end

  test "committed_spend from_map reports the first missing required field" do
    assert {:error, {:missing_field, :commitment_id}} =
             BeamPM.Codec.from_map(:committed_spend, %{})
  end


  test "committed_spend_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      commitment_id: "sample_commitment_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.CommittedSpendAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["commitment_id"] == "sample_commitment_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:committed_spend_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:committed_spend_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "committed_spend_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      commitment_id: "sample_commitment_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.CommittedSpendAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:committed_spend_admission, json)
  end

  test "committed_spend_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:committed_spend_admission, %{})
  end


  test "compatibility_contract to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      contract_id: "sample_contract_id",
      product_version: "sample_product_version",
      schema_version: "sample_schema_version",
      api_version: "sample_api_version"
    }

    assert {:ok, rec} = BeamPM.Types.CompatibilityContract.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["contract_id"] == "sample_contract_id"
    assert m["product_version"] == "sample_product_version"
    assert m["schema_version"] == "sample_schema_version"
    assert m["api_version"] == "sample_api_version"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:compatibility_contract, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:compatibility_contract, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "compatibility_contract encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      contract_id: "sample_contract_id",
      product_version: "sample_product_version",
      schema_version: "sample_schema_version",
      api_version: "sample_api_version"
    }

    assert {:ok, rec} = BeamPM.Types.CompatibilityContract.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:compatibility_contract, json)
  end

  test "compatibility_contract from_map reports the first missing required field" do
    assert {:error, {:missing_field, :contract_id}} =
             BeamPM.Codec.from_map(:compatibility_contract, %{})
  end


  test "configuration_export to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      export_id: "sample_export_id",
      tenant_id: "sample_tenant_id",
      configuration_hash: "sample_configuration_hash",
      exported_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ConfigurationExport.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["export_id"] == "sample_export_id"
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["configuration_hash"] == "sample_configuration_hash"
    assert m["exported_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:configuration_export, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:configuration_export, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "configuration_export encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      export_id: "sample_export_id",
      tenant_id: "sample_tenant_id",
      configuration_hash: "sample_configuration_hash",
      exported_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ConfigurationExport.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:configuration_export, json)
  end

  test "configuration_export from_map reports the first missing required field" do
    assert {:error, {:missing_field, :export_id}} =
             BeamPM.Codec.from_map(:configuration_export, %{})
  end


  test "configuration_import to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      import_id: "sample_import_id",
      tenant_id: "sample_tenant_id",
      configuration_hash: "sample_configuration_hash",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ConfigurationImport.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["import_id"] == "sample_import_id"
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["configuration_hash"] == "sample_configuration_hash"
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:configuration_import, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:configuration_import, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "configuration_import encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      import_id: "sample_import_id",
      tenant_id: "sample_tenant_id",
      configuration_hash: "sample_configuration_hash",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ConfigurationImport.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:configuration_import, json)
  end

  test "configuration_import from_map reports the first missing required field" do
    assert {:error, {:missing_field, :import_id}} =
             BeamPM.Codec.from_map(:configuration_import, %{})
  end


  test "conformance_result to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      trace_id: "sample_trace_id",
      fitness: 3.5,
      precision: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ConformanceResult.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["trace_id"] == "sample_trace_id"
    assert m["fitness"] == 3.5
    assert m["precision"] == 3.5
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:conformance_result, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:conformance_result, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "conformance_result encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      trace_id: "sample_trace_id",
      fitness: 3.5,
      precision: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ConformanceResult.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:conformance_result, json)
  end

  test "conformance_result from_map reports the first missing required field" do
    assert {:error, {:missing_field, :trace_id}} =
             BeamPM.Codec.from_map(:conformance_result, %{})
  end

  test "conformance_result to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      trace_id: "sample_trace_id",
      fitness: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ConformanceResult.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "precision")
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:conformance_result, m)
  end

  test "consumption_pool to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      pool_id: "sample_pool_id",
      account_id: "sample_account_id",
      unit: "sample_unit",
      remaining_quantity: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ConsumptionPool.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["pool_id"] == "sample_pool_id"
    assert m["account_id"] == "sample_account_id"
    assert m["unit"] == "sample_unit"
    assert m["remaining_quantity"] == 3.5
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:consumption_pool, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:consumption_pool, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "consumption_pool encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      pool_id: "sample_pool_id",
      account_id: "sample_account_id",
      unit: "sample_unit",
      remaining_quantity: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ConsumptionPool.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:consumption_pool, json)
  end

  test "consumption_pool from_map reports the first missing required field" do
    assert {:error, {:missing_field, :pool_id}} =
             BeamPM.Codec.from_map(:consumption_pool, %{})
  end


  test "consumption_subscription to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      subscription_id: "sample_subscription_id",
      account_id: "sample_account_id",
      plan_id: "sample_plan_id",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ConsumptionSubscription.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["subscription_id"] == "sample_subscription_id"
    assert m["account_id"] == "sample_account_id"
    assert m["plan_id"] == "sample_plan_id"
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:consumption_subscription, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:consumption_subscription, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "consumption_subscription encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      subscription_id: "sample_subscription_id",
      account_id: "sample_account_id",
      plan_id: "sample_plan_id",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ConsumptionSubscription.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:consumption_subscription, json)
  end

  test "consumption_subscription from_map reports the first missing required field" do
    assert {:error, {:missing_field, :subscription_id}} =
             BeamPM.Codec.from_map(:consumption_subscription, %{})
  end


  test "contracting_entity_identity to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      contracting_entity_id: "sample_contracting_entity_id",
      identity_evidence_hash: "sample_identity_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ContractingEntityIdentity.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["contracting_entity_id"] == "sample_contracting_entity_id"
    assert m["identity_evidence_hash"] == "sample_identity_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:contracting_entity_identity, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:contracting_entity_identity, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "contracting_entity_identity encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      contracting_entity_id: "sample_contracting_entity_id",
      identity_evidence_hash: "sample_identity_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ContractingEntityIdentity.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:contracting_entity_identity, json)
  end

  test "contracting_entity_identity from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:contracting_entity_identity, %{})
  end


  test "cost_to_serve_measurement to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      billing_period_id: "sample_billing_period_id",
      measurement_hash: "sample_measurement_hash"
    }

    assert {:ok, rec} = BeamPM.Types.CostToServeMeasurement.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["billing_period_id"] == "sample_billing_period_id"
    assert m["measurement_hash"] == "sample_measurement_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:cost_to_serve_measurement, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:cost_to_serve_measurement, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "cost_to_serve_measurement encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      billing_period_id: "sample_billing_period_id",
      measurement_hash: "sample_measurement_hash"
    }

    assert {:ok, rec} = BeamPM.Types.CostToServeMeasurement.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:cost_to_serve_measurement, json)
  end

  test "cost_to_serve_measurement from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:cost_to_serve_measurement, %{})
  end


  test "crash_recovery_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      crash_id: "sample_crash_id",
      receipt_hash: "sample_receipt_hash"
    }

    assert {:ok, rec} = BeamPM.Types.CrashRecoveryReceipt.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["crash_id"] == "sample_crash_id"
    assert m["receipt_hash"] == "sample_receipt_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:crash_recovery_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:crash_recovery_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "crash_recovery_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      crash_id: "sample_crash_id",
      receipt_hash: "sample_receipt_hash"
    }

    assert {:ok, rec} = BeamPM.Types.CrashRecoveryReceipt.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:crash_recovery_receipt, json)
  end

  test "crash_recovery_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:crash_recovery_receipt, %{})
  end


  test "credit_risk_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      account_id: "sample_account_id",
      risk_band: "sample_risk_band",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.CreditRiskAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["account_id"] == "sample_account_id"
    assert m["risk_band"] == "sample_risk_band"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:credit_risk_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:credit_risk_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "credit_risk_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      account_id: "sample_account_id",
      risk_band: "sample_risk_band",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.CreditRiskAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:credit_risk_admission, json)
  end

  test "credit_risk_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :account_id}} =
             BeamPM.Codec.from_map(:credit_risk_admission, %{})
  end


  test "cross_sell_fit to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      cross_sell_fit_id: "sample_cross_sell_fit_id",
      account_id: "sample_account_id",
      cross_sell_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CrossSellFit.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["cross_sell_fit_id"] == "sample_cross_sell_fit_id"
    assert m["account_id"] == "sample_account_id"
    assert m["cross_sell_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:cross_sell_fit, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:cross_sell_fit, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "cross_sell_fit encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      cross_sell_fit_id: "sample_cross_sell_fit_id",
      account_id: "sample_account_id",
      cross_sell_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CrossSellFit.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:cross_sell_fit, json)
  end

  test "cross_sell_fit from_map reports the first missing required field" do
    assert {:error, {:missing_field, :cross_sell_fit_id}} =
             BeamPM.Codec.from_map(:cross_sell_fit, %{})
  end


  test "customer_health to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      customer_health_id: "sample_customer_health_id",
      account_id: "sample_account_id",
      health_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CustomerHealth.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["customer_health_id"] == "sample_customer_health_id"
    assert m["account_id"] == "sample_account_id"
    assert m["health_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:customer_health, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:customer_health, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "customer_health encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      customer_health_id: "sample_customer_health_id",
      account_id: "sample_account_id",
      health_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.CustomerHealth.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:customer_health, json)
  end

  test "customer_health from_map reports the first missing required field" do
    assert {:error, {:missing_field, :customer_health_id}} =
             BeamPM.Codec.from_map(:customer_health, %{})
  end


  test "customer_managed_key_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      key_identifier: "sample_key_identifier",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.CustomerManagedKeyEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["key_identifier"] == "sample_key_identifier"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:customer_managed_key_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:customer_managed_key_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "customer_managed_key_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      key_identifier: "sample_key_identifier",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.CustomerManagedKeyEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:customer_managed_key_evidence, json)
  end

  test "customer_managed_key_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:customer_managed_key_evidence, %{})
  end


  test "data_egress_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      egress_bytes: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.DataEgressEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["egress_bytes"] == 42
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:data_egress_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:data_egress_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "data_egress_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      egress_bytes: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.DataEgressEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:data_egress_evidence, json)
  end

  test "data_egress_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:data_egress_evidence, %{})
  end


  test "data_migration_scope_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      migration_scope_id: "sample_migration_scope_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.DataMigrationScopeAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["migration_scope_id"] == "sample_migration_scope_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:data_migration_scope_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:data_migration_scope_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "data_migration_scope_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      migration_scope_id: "sample_migration_scope_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.DataMigrationScopeAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:data_migration_scope_admission, json)
  end

  test "data_migration_scope_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:data_migration_scope_admission, %{})
  end


  test "data_processing_addendum_state to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      addendum_id: "sample_addendum_id",
      addendum_state: "sample_addendum_state"
    }

    assert {:ok, rec} = BeamPM.Types.DataProcessingAddendumState.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["addendum_id"] == "sample_addendum_id"
    assert m["addendum_state"] == "sample_addendum_state"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:data_processing_addendum_state, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:data_processing_addendum_state, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "data_processing_addendum_state encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      addendum_id: "sample_addendum_id",
      addendum_state: "sample_addendum_state"
    }

    assert {:ok, rec} = BeamPM.Types.DataProcessingAddendumState.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:data_processing_addendum_state, json)
  end

  test "data_processing_addendum_state from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:data_processing_addendum_state, %{})
  end


  test "data_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      data_readiness_id: "sample_data_readiness_id",
      account_id: "sample_account_id",
      data_quality_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DataReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["data_readiness_id"] == "sample_data_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["data_quality_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:data_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:data_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "data_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      data_readiness_id: "sample_data_readiness_id",
      account_id: "sample_account_id",
      data_quality_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DataReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:data_readiness, json)
  end

  test "data_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :data_readiness_id}} =
             BeamPM.Codec.from_map(:data_readiness, %{})
  end


  test "data_residency_policy to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      policy_id: "sample_policy_id",
      tenant_id: "sample_tenant_id",
      allowed_regions: ["alpha", "beta"],
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.DataResidencyPolicy.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["policy_id"] == "sample_policy_id"
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["allowed_regions"] == ["alpha", "beta"]
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:data_residency_policy, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:data_residency_policy, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "data_residency_policy encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      policy_id: "sample_policy_id",
      tenant_id: "sample_tenant_id",
      allowed_regions: ["alpha", "beta"],
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.DataResidencyPolicy.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:data_residency_policy, json)
  end

  test "data_residency_policy from_map reports the first missing required field" do
    assert {:error, {:missing_field, :policy_id}} =
             BeamPM.Codec.from_map(:data_residency_policy, %{})
  end


  test "deal_desk_packet to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      packet_id: "sample_packet_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.DealDeskPacket.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["packet_id"] == "sample_packet_id"
    assert m["evidence_hash"] == "sample_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:deal_desk_packet, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:deal_desk_packet, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "deal_desk_packet encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      packet_id: "sample_packet_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.DealDeskPacket.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:deal_desk_packet, json)
  end

  test "deal_desk_packet from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:deal_desk_packet, %{})
  end


  test "deletion_proof_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      deletion_receipt_id: "sample_deletion_receipt_id",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.DeletionProofEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["deletion_receipt_id"] == "sample_deletion_receipt_id"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:deletion_proof_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:deletion_proof_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "deletion_proof_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      deletion_receipt_id: "sample_deletion_receipt_id",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.DeletionProofEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:deletion_proof_evidence, json)
  end

  test "deletion_proof_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:deletion_proof_evidence, %{})
  end


  test "demo_run to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      demo_run_id: "sample_demo_run_id",
      account_id: "sample_account_id",
      demo_result: "sample_demo_result",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DemoRun.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["demo_run_id"] == "sample_demo_run_id"
    assert m["account_id"] == "sample_account_id"
    assert m["demo_result"] == "sample_demo_result"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:demo_run, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:demo_run, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "demo_run encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      demo_run_id: "sample_demo_run_id",
      account_id: "sample_account_id",
      demo_result: "sample_demo_result",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DemoRun.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:demo_run, json)
  end

  test "demo_run from_map reports the first missing required field" do
    assert {:error, {:missing_field, :demo_run_id}} =
             BeamPM.Codec.from_map(:demo_run, %{})
  end


  test "demo_scenario to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      demo_scenario_id: "sample_demo_scenario_id",
      account_id: "sample_account_id",
      scenario_name: "sample_scenario_name",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DemoScenario.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["demo_scenario_id"] == "sample_demo_scenario_id"
    assert m["account_id"] == "sample_account_id"
    assert m["scenario_name"] == "sample_scenario_name"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:demo_scenario, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:demo_scenario, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "demo_scenario encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      demo_scenario_id: "sample_demo_scenario_id",
      account_id: "sample_account_id",
      scenario_name: "sample_scenario_name",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DemoScenario.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:demo_scenario, json)
  end

  test "demo_scenario from_map reports the first missing required field" do
    assert {:error, {:missing_field, :demo_scenario_id}} =
             BeamPM.Codec.from_map(:demo_scenario, %{})
  end


  test "dependency_inventory_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      dependency_count: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.DependencyInventoryEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["dependency_count"] == 42
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:dependency_inventory_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:dependency_inventory_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "dependency_inventory_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      dependency_count: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.DependencyInventoryEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:dependency_inventory_evidence, json)
  end

  test "dependency_inventory_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:dependency_inventory_evidence, %{})
  end


  test "deployment_entitlement to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      entitlement_id: "sample_entitlement_id",
      tenant_id: "sample_tenant_id",
      profile_id: "sample_profile_id",
      valid_until: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DeploymentEntitlement.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["entitlement_id"] == "sample_entitlement_id"
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["profile_id"] == "sample_profile_id"
    assert m["valid_until"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:deployment_entitlement, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:deployment_entitlement, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "deployment_entitlement encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      entitlement_id: "sample_entitlement_id",
      tenant_id: "sample_tenant_id",
      profile_id: "sample_profile_id",
      valid_until: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DeploymentEntitlement.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:deployment_entitlement, json)
  end

  test "deployment_entitlement from_map reports the first missing required field" do
    assert {:error, {:missing_field, :entitlement_id}} =
             BeamPM.Codec.from_map(:deployment_entitlement, %{})
  end


  test "developer_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      developer_readiness_id: "sample_developer_readiness_id",
      account_id: "sample_account_id",
      developer_readiness_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DeveloperReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["developer_readiness_id"] == "sample_developer_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["developer_readiness_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:developer_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:developer_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "developer_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      developer_readiness_id: "sample_developer_readiness_id",
      account_id: "sample_account_id",
      developer_readiness_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DeveloperReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:developer_readiness, json)
  end

  test "developer_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :developer_readiness_id}} =
             BeamPM.Codec.from_map(:developer_readiness, %{})
  end


  test "dfg_edge to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      source_activity: "sample_source_activity",
      target_activity: "sample_target_activity",
      frequency: 42
    }

    assert {:ok, rec} = BeamPM.Types.DfgEdge.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["source_activity"] == "sample_source_activity"
    assert m["target_activity"] == "sample_target_activity"
    assert m["frequency"] == 42
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:dfg_edge, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:dfg_edge, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "dfg_edge encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      source_activity: "sample_source_activity",
      target_activity: "sample_target_activity",
      frequency: 42
    }

    assert {:ok, rec} = BeamPM.Types.DfgEdge.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:dfg_edge, json)
  end

  test "dfg_edge from_map reports the first missing required field" do
    assert {:error, {:missing_field, :source_activity}} =
             BeamPM.Codec.from_map(:dfg_edge, %{})
  end


  test "disaster_recovery_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      recovered_at: "2026-08-29T12:00:00Z",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.DisasterRecoveryEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["recovered_at"] == "2026-08-29T12:00:00Z"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:disaster_recovery_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:disaster_recovery_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "disaster_recovery_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      recovered_at: "2026-08-29T12:00:00Z",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.DisasterRecoveryEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:disaster_recovery_evidence, json)
  end

  test "disaster_recovery_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:disaster_recovery_evidence, %{})
  end


  test "discount_schedule to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      schedule_id: "sample_schedule_id",
      threshold: 3.5,
      discount_percent: 3.5,
      currency: "sample_currency"
    }

    assert {:ok, rec} = BeamPM.Types.DiscountSchedule.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["schedule_id"] == "sample_schedule_id"
    assert m["threshold"] == 3.5
    assert m["discount_percent"] == 3.5
    assert m["currency"] == "sample_currency"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:discount_schedule, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:discount_schedule, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "discount_schedule encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      schedule_id: "sample_schedule_id",
      threshold: 3.5,
      discount_percent: 3.5,
      currency: "sample_currency"
    }

    assert {:ok, rec} = BeamPM.Types.DiscountSchedule.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:discount_schedule, json)
  end

  test "discount_schedule from_map reports the first missing required field" do
    assert {:error, {:missing_field, :schedule_id}} =
             BeamPM.Codec.from_map(:discount_schedule, %{})
  end


  test "discovery_hypothesis to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      discovery_hypothesis_id: "sample_discovery_hypothesis_id",
      account_id: "sample_account_id",
      expected_value: "sample_expected_value",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DiscoveryHypothesis.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["discovery_hypothesis_id"] == "sample_discovery_hypothesis_id"
    assert m["account_id"] == "sample_account_id"
    assert m["expected_value"] == "sample_expected_value"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:discovery_hypothesis, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:discovery_hypothesis, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "discovery_hypothesis encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      discovery_hypothesis_id: "sample_discovery_hypothesis_id",
      account_id: "sample_account_id",
      expected_value: "sample_expected_value",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.DiscoveryHypothesis.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:discovery_hypothesis, json)
  end

  test "discovery_hypothesis from_map reports the first missing required field" do
    assert {:error, {:missing_field, :discovery_hypothesis_id}} =
             BeamPM.Codec.from_map(:discovery_hypothesis, %{})
  end


  test "edition_definition to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      edition_id: "sample_edition_id",
      name: "sample_name",
      bundle_ids: ["alpha", "beta"],
      support_tier: "sample_support_tier"
    }

    assert {:ok, rec} = BeamPM.Types.EditionDefinition.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["edition_id"] == "sample_edition_id"
    assert m["name"] == "sample_name"
    assert m["bundle_ids"] == ["alpha", "beta"]
    assert m["support_tier"] == "sample_support_tier"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:edition_definition, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:edition_definition, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "edition_definition encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      edition_id: "sample_edition_id",
      name: "sample_name",
      bundle_ids: ["alpha", "beta"],
      support_tier: "sample_support_tier"
    }

    assert {:ok, rec} = BeamPM.Types.EditionDefinition.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:edition_definition, json)
  end

  test "edition_definition from_map reports the first missing required field" do
    assert {:error, {:missing_field, :edition_id}} =
             BeamPM.Codec.from_map(:edition_definition, %{})
  end


  test "enterprise_order to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      order_id: "sample_order_id",
      account_id: "sample_account_id",
      quote_id: "sample_quote_id",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.EnterpriseOrder.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["order_id"] == "sample_order_id"
    assert m["account_id"] == "sample_account_id"
    assert m["quote_id"] == "sample_quote_id"
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:enterprise_order, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:enterprise_order, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "enterprise_order encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      order_id: "sample_order_id",
      account_id: "sample_account_id",
      quote_id: "sample_quote_id",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.EnterpriseOrder.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:enterprise_order, json)
  end

  test "enterprise_order from_map reports the first missing required field" do
    assert {:error, {:missing_field, :order_id}} =
             BeamPM.Codec.from_map(:enterprise_order, %{})
  end


  test "enterprise_order_line to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      order_id: "sample_order_id",
      sku: "sample_sku",
      quantity: 42,
      unit_price: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.EnterpriseOrderLine.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["order_id"] == "sample_order_id"
    assert m["sku"] == "sample_sku"
    assert m["quantity"] == 42
    assert m["unit_price"] == 3.5
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:enterprise_order_line, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:enterprise_order_line, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "enterprise_order_line encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      order_id: "sample_order_id",
      sku: "sample_sku",
      quantity: 42,
      unit_price: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.EnterpriseOrderLine.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:enterprise_order_line, json)
  end

  test "enterprise_order_line from_map reports the first missing required field" do
    assert {:error, {:missing_field, :order_id}} =
             BeamPM.Codec.from_map(:enterprise_order_line, %{})
  end


  test "entitlement_denial_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      entitlement_id: "sample_entitlement_id",
      denial_hash: "sample_denial_hash"
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementDenialReceipt.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["entitlement_id"] == "sample_entitlement_id"
    assert m["denial_hash"] == "sample_denial_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:entitlement_denial_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:entitlement_denial_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "entitlement_denial_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      entitlement_id: "sample_entitlement_id",
      denial_hash: "sample_denial_hash"
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementDenialReceipt.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:entitlement_denial_receipt, json)
  end

  test "entitlement_denial_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:entitlement_denial_receipt, %{})
  end


  test "entitlement_event to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      event_id: "sample_event_id",
      entitlement_id: "sample_entitlement_id",
      event_type: "sample_event_type",
      effective_at: "2026-08-29T12:00:00Z",
      payload: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementEvent.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["event_id"] == "sample_event_id"
    assert m["entitlement_id"] == "sample_entitlement_id"
    assert m["event_type"] == "sample_event_type"
    assert m["effective_at"] == "2026-08-29T12:00:00Z"
    assert m["payload"] == %{"k" => "v"}
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:entitlement_event, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:entitlement_event, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "entitlement_event encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      event_id: "sample_event_id",
      entitlement_id: "sample_entitlement_id",
      event_type: "sample_event_type",
      effective_at: "2026-08-29T12:00:00Z",
      payload: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementEvent.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:entitlement_event, json)
  end

  test "entitlement_event from_map reports the first missing required field" do
    assert {:error, {:missing_field, :event_id}} =
             BeamPM.Codec.from_map(:entitlement_event, %{})
  end

  test "entitlement_event to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      event_id: "sample_event_id",
      entitlement_id: "sample_entitlement_id",
      event_type: "sample_event_type",
      effective_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementEvent.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "payload")
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:entitlement_event, m)
  end

  test "entitlement_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      entitlement_id: "sample_entitlement_id",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["entitlement_id"] == "sample_entitlement_id"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:entitlement_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:entitlement_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "entitlement_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      entitlement_id: "sample_entitlement_id",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:entitlement_evidence, json)
  end

  test "entitlement_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:entitlement_evidence, %{})
  end


  test "entitlement_grant to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      grant_id: "sample_grant_id",
      tenant_id: "sample_tenant_id",
      capability_id: "sample_capability_id",
      valid_until: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementGrant.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["grant_id"] == "sample_grant_id"
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["capability_id"] == "sample_capability_id"
    assert m["valid_until"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:entitlement_grant, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:entitlement_grant, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "entitlement_grant encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      grant_id: "sample_grant_id",
      tenant_id: "sample_tenant_id",
      capability_id: "sample_capability_id",
      valid_until: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementGrant.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:entitlement_grant, json)
  end

  test "entitlement_grant from_map reports the first missing required field" do
    assert {:error, {:missing_field, :grant_id}} =
             BeamPM.Codec.from_map(:entitlement_grant, %{})
  end


  test "entitlement_revocation to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      revocation_id: "sample_revocation_id",
      grant_id: "sample_grant_id",
      reason: "sample_reason",
      revoked_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementRevocation.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["revocation_id"] == "sample_revocation_id"
    assert m["grant_id"] == "sample_grant_id"
    assert m["reason"] == "sample_reason"
    assert m["revoked_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:entitlement_revocation, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:entitlement_revocation, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "entitlement_revocation encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      revocation_id: "sample_revocation_id",
      grant_id: "sample_grant_id",
      reason: "sample_reason",
      revoked_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementRevocation.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:entitlement_revocation, json)
  end

  test "entitlement_revocation from_map reports the first missing required field" do
    assert {:error, {:missing_field, :revocation_id}} =
             BeamPM.Codec.from_map(:entitlement_revocation, %{})
  end


  test "entitlement_runtime_check to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      entitlement_id: "sample_entitlement_id",
      check_hash: "sample_check_hash"
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementRuntimeCheck.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["entitlement_id"] == "sample_entitlement_id"
    assert m["check_hash"] == "sample_check_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:entitlement_runtime_check, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:entitlement_runtime_check, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "entitlement_runtime_check encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      entitlement_id: "sample_entitlement_id",
      check_hash: "sample_check_hash"
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementRuntimeCheck.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:entitlement_runtime_check, json)
  end

  test "entitlement_runtime_check from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:entitlement_runtime_check, %{})
  end


  test "entitlement_state to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      entitlement_id: "sample_entitlement_id",
      status: "sample_status",
      last_applied_event_id: "sample_last_applied_event_id",
      updated_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementState.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["entitlement_id"] == "sample_entitlement_id"
    assert m["status"] == "sample_status"
    assert m["last_applied_event_id"] == "sample_last_applied_event_id"
    assert m["updated_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:entitlement_state, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:entitlement_state, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "entitlement_state encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      entitlement_id: "sample_entitlement_id",
      status: "sample_status",
      last_applied_event_id: "sample_last_applied_event_id",
      updated_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.EntitlementState.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:entitlement_state, json)
  end

  test "entitlement_state from_map reports the first missing required field" do
    assert {:error, {:missing_field, :entitlement_id}} =
             BeamPM.Codec.from_map(:entitlement_state, %{})
  end


  test "environment_identity to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      environment_id: "sample_environment_id",
      identity_hash: "sample_identity_hash"
    }

    assert {:ok, rec} = BeamPM.Types.EnvironmentIdentity.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["environment_id"] == "sample_environment_id"
    assert m["identity_hash"] == "sample_identity_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:environment_identity, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:environment_identity, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "environment_identity encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      environment_id: "sample_environment_id",
      identity_hash: "sample_identity_hash"
    }

    assert {:ok, rec} = BeamPM.Types.EnvironmentIdentity.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:environment_identity, json)
  end

  test "environment_identity from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:environment_identity, %{})
  end


  test "environment_profile to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      profile_id: "sample_profile_id",
      environment: :sample_atom,
      region: "sample_region",
      configuration_hash: "sample_configuration_hash"
    }

    assert {:ok, rec} = BeamPM.Types.EnvironmentProfile.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["profile_id"] == "sample_profile_id"
    assert m["environment"] == "sample_atom"
    assert m["region"] == "sample_region"
    assert m["configuration_hash"] == "sample_configuration_hash"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:environment_profile, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:environment_profile, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "environment_profile encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      profile_id: "sample_profile_id",
      environment: :sample_atom,
      region: "sample_region",
      configuration_hash: "sample_configuration_hash"
    }

    assert {:ok, rec} = BeamPM.Types.EnvironmentProfile.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:environment_profile, json)
  end

  test "environment_profile from_map reports the first missing required field" do
    assert {:error, {:missing_field, :profile_id}} =
             BeamPM.Codec.from_map(:environment_profile, %{})
  end


  test "error_budget_state to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      slo_id: "sample_slo_id",
      state_hash: "sample_state_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ErrorBudgetState.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["slo_id"] == "sample_slo_id"
    assert m["state_hash"] == "sample_state_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:error_budget_state, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:error_budget_state, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "error_budget_state encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      slo_id: "sample_slo_id",
      state_hash: "sample_state_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ErrorBudgetState.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:error_budget_state, json)
  end

  test "error_budget_state from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:error_budget_state, %{})
  end


  test "event_log to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      log_id: "sample_log_id",
      name: "sample_name",
      description: "sample_description"
    }

    assert {:ok, rec} = BeamPM.Types.EventLog.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["log_id"] == "sample_log_id"
    assert m["name"] == "sample_name"
    assert m["description"] == "sample_description"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:event_log, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:event_log, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "event_log encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      log_id: "sample_log_id",
      name: "sample_name",
      description: "sample_description"
    }

    assert {:ok, rec} = BeamPM.Types.EventLog.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:event_log, json)
  end

  test "event_log from_map reports the first missing required field" do
    assert {:error, {:missing_field, :log_id}} =
             BeamPM.Codec.from_map(:event_log, %{})
  end

  test "event_log to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      log_id: "sample_log_id",
      name: "sample_name"
    }

    assert {:ok, rec} = BeamPM.Types.EventLog.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "description")
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:event_log, m)
  end

  test "event_type to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      type_name: "sample_type_name",
      attribute_names: ["alpha", "beta"]
    }

    assert {:ok, rec} = BeamPM.Types.EventType.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["type_name"] == "sample_type_name"
    assert m["attribute_names"] == ["alpha", "beta"]
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:event_type, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:event_type, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "event_type encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      type_name: "sample_type_name",
      attribute_names: ["alpha", "beta"]
    }

    assert {:ok, rec} = BeamPM.Types.EventType.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:event_type, json)
  end

  test "event_type from_map reports the first missing required field" do
    assert {:error, {:missing_field, :type_name}} =
             BeamPM.Codec.from_map(:event_type, %{})
  end

  test "event_type to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      type_name: "sample_type_name"
    }

    assert {:ok, rec} = BeamPM.Types.EventType.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "attribute_names")
    assert map_size(m) == 1
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:event_type, m)
  end

  test "evidence_freshness_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      observed_at: "2026-08-29T12:00:00Z",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.EvidenceFreshnessEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:evidence_freshness_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:evidence_freshness_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "evidence_freshness_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      observed_at: "2026-08-29T12:00:00Z",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.EvidenceFreshnessEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:evidence_freshness_evidence, json)
  end

  test "evidence_freshness_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:evidence_freshness_evidence, %{})
  end


  test "exception_authority to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      exception_id: "sample_exception_id",
      authority_id: "sample_authority_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.ExceptionAuthority.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["exception_id"] == "sample_exception_id"
    assert m["authority_id"] == "sample_authority_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:exception_authority, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:exception_authority, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "exception_authority encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      exception_id: "sample_exception_id",
      authority_id: "sample_authority_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.ExceptionAuthority.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:exception_authority, json)
  end

  test "exception_authority from_map reports the first missing required field" do
    assert {:error, {:missing_field, :exception_id}} =
             BeamPM.Codec.from_map(:exception_authority, %{})
  end


  test "executive_business_review to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      executive_business_review_id: "sample_executive_business_review_id",
      account_id: "sample_account_id",
      executive_outcome: "sample_executive_outcome",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ExecutiveBusinessReview.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["executive_business_review_id"] == "sample_executive_business_review_id"
    assert m["account_id"] == "sample_account_id"
    assert m["executive_outcome"] == "sample_executive_outcome"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:executive_business_review, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:executive_business_review, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "executive_business_review encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      executive_business_review_id: "sample_executive_business_review_id",
      account_id: "sample_account_id",
      executive_outcome: "sample_executive_outcome",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ExecutiveBusinessReview.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:executive_business_review, json)
  end

  test "executive_business_review from_map reports the first missing required field" do
    assert {:error, {:missing_field, :executive_business_review_id}} =
             BeamPM.Codec.from_map(:executive_business_review, %{})
  end


  test "executive_sponsor to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      executive_sponsor_id: "sample_executive_sponsor_id",
      account_id: "sample_account_id",
      sponsor_commitment: "sample_sponsor_commitment",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ExecutiveSponsor.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["executive_sponsor_id"] == "sample_executive_sponsor_id"
    assert m["account_id"] == "sample_account_id"
    assert m["sponsor_commitment"] == "sample_sponsor_commitment"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:executive_sponsor, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:executive_sponsor, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "executive_sponsor encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      executive_sponsor_id: "sample_executive_sponsor_id",
      account_id: "sample_account_id",
      sponsor_commitment: "sample_sponsor_commitment",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ExecutiveSponsor.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:executive_sponsor, json)
  end

  test "executive_sponsor from_map reports the first missing required field" do
    assert {:error, {:missing_field, :executive_sponsor_id}} =
             BeamPM.Codec.from_map(:executive_sponsor, %{})
  end


  test "expansion_opportunity to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      expansion_opportunity_id: "sample_expansion_opportunity_id",
      account_id: "sample_account_id",
      expansion_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ExpansionOpportunity.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["expansion_opportunity_id"] == "sample_expansion_opportunity_id"
    assert m["account_id"] == "sample_account_id"
    assert m["expansion_value"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:expansion_opportunity, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:expansion_opportunity, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "expansion_opportunity encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      expansion_opportunity_id: "sample_expansion_opportunity_id",
      account_id: "sample_account_id",
      expansion_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ExpansionOpportunity.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:expansion_opportunity, json)
  end

  test "expansion_opportunity from_map reports the first missing required field" do
    assert {:error, {:missing_field, :expansion_opportunity_id}} =
             BeamPM.Codec.from_map(:expansion_opportunity, %{})
  end


  test "expansion_option to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      option_id: "sample_option_id",
      account_id: "sample_account_id",
      sku: "sample_sku",
      max_quantity: 42
    }

    assert {:ok, rec} = BeamPM.Types.ExpansionOption.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["option_id"] == "sample_option_id"
    assert m["account_id"] == "sample_account_id"
    assert m["sku"] == "sample_sku"
    assert m["max_quantity"] == 42
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:expansion_option, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:expansion_option, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "expansion_option encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      option_id: "sample_option_id",
      account_id: "sample_account_id",
      sku: "sample_sku",
      max_quantity: 42
    }

    assert {:ok, rec} = BeamPM.Types.ExpansionOption.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:expansion_option, json)
  end

  test "expansion_option from_map reports the first missing required field" do
    assert {:error, {:missing_field, :option_id}} =
             BeamPM.Codec.from_map(:expansion_option, %{})
  end


  test "expansion_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      expansion_receipt_id: "sample_expansion_receipt_id",
      account_id: "sample_account_id",
      receipt_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ExpansionReceipt.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["expansion_receipt_id"] == "sample_expansion_receipt_id"
    assert m["account_id"] == "sample_account_id"
    assert m["receipt_value"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:expansion_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:expansion_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "expansion_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      expansion_receipt_id: "sample_expansion_receipt_id",
      account_id: "sample_account_id",
      receipt_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ExpansionReceipt.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:expansion_receipt, json)
  end

  test "expansion_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :expansion_receipt_id}} =
             BeamPM.Codec.from_map(:expansion_receipt, %{})
  end


  test "expansion_signal to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      expansion_signal_id: "sample_expansion_signal_id",
      account_id: "sample_account_id",
      expansion_signal_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ExpansionSignal.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["expansion_signal_id"] == "sample_expansion_signal_id"
    assert m["account_id"] == "sample_account_id"
    assert m["expansion_signal_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:expansion_signal, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:expansion_signal, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "expansion_signal encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      expansion_signal_id: "sample_expansion_signal_id",
      account_id: "sample_account_id",
      expansion_signal_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ExpansionSignal.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:expansion_signal, json)
  end

  test "expansion_signal from_map reports the first missing required field" do
    assert {:error, {:missing_field, :expansion_signal_id}} =
             BeamPM.Codec.from_map(:expansion_signal, %{})
  end


  test "forged_receipt_refusal to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      receipt_id: "sample_receipt_id",
      refusal_hash: "sample_refusal_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ForgedReceiptRefusal.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["receipt_id"] == "sample_receipt_id"
    assert m["refusal_hash"] == "sample_refusal_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:forged_receipt_refusal, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:forged_receipt_refusal, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "forged_receipt_refusal encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      receipt_id: "sample_receipt_id",
      refusal_hash: "sample_refusal_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ForgedReceiptRefusal.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:forged_receipt_refusal, json)
  end

  test "forged_receipt_refusal from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:forged_receipt_refusal, %{})
  end


  test "funding_approval_chain to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      approval_chain_id: "sample_approval_chain_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.FundingApprovalChain.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["approval_chain_id"] == "sample_approval_chain_id"
    assert m["evidence_hash"] == "sample_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:funding_approval_chain, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:funding_approval_chain, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "funding_approval_chain encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      approval_chain_id: "sample_approval_chain_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.FundingApprovalChain.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:funding_approval_chain, json)
  end

  test "funding_approval_chain from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:funding_approval_chain, %{})
  end


  test "heuristic_arc to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      source_activity: "sample_source_activity",
      target_activity: "sample_target_activity",
      dependency_measure: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.HeuristicArc.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["source_activity"] == "sample_source_activity"
    assert m["target_activity"] == "sample_target_activity"
    assert m["dependency_measure"] == 3.5
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:heuristic_arc, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:heuristic_arc, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "heuristic_arc encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      source_activity: "sample_source_activity",
      target_activity: "sample_target_activity",
      dependency_measure: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.HeuristicArc.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:heuristic_arc, json)
  end

  test "heuristic_arc from_map reports the first missing required field" do
    assert {:error, {:missing_field, :source_activity}} =
             BeamPM.Codec.from_map(:heuristic_arc, %{})
  end


  test "implementation_fee_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      fee_id: "sample_fee_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.ImplementationFeeAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["fee_id"] == "sample_fee_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:implementation_fee_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:implementation_fee_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "implementation_fee_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      fee_id: "sample_fee_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.ImplementationFeeAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:implementation_fee_admission, json)
  end

  test "implementation_fee_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:implementation_fee_admission, %{})
  end


  test "incident_acknowledgement to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      incident_id: "sample_incident_id",
      acknowledgement_hash: "sample_acknowledgement_hash"
    }

    assert {:ok, rec} = BeamPM.Types.IncidentAcknowledgement.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["incident_id"] == "sample_incident_id"
    assert m["acknowledgement_hash"] == "sample_acknowledgement_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:incident_acknowledgement, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:incident_acknowledgement, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "incident_acknowledgement encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      incident_id: "sample_incident_id",
      acknowledgement_hash: "sample_acknowledgement_hash"
    }

    assert {:ok, rec} = BeamPM.Types.IncidentAcknowledgement.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:incident_acknowledgement, json)
  end

  test "incident_acknowledgement from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:incident_acknowledgement, %{})
  end


  test "incident_detection_event to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      incident_id: "sample_incident_id",
      detection_hash: "sample_detection_hash"
    }

    assert {:ok, rec} = BeamPM.Types.IncidentDetectionEvent.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["incident_id"] == "sample_incident_id"
    assert m["detection_hash"] == "sample_detection_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:incident_detection_event, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:incident_detection_event, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "incident_detection_event encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      incident_id: "sample_incident_id",
      detection_hash: "sample_detection_hash"
    }

    assert {:ok, rec} = BeamPM.Types.IncidentDetectionEvent.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:incident_detection_event, json)
  end

  test "incident_detection_event from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:incident_detection_event, %{})
  end


  test "incident_response_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      incident_id: "sample_incident_id",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.IncidentResponseEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["incident_id"] == "sample_incident_id"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:incident_response_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:incident_response_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "incident_response_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      incident_id: "sample_incident_id",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.IncidentResponseEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:incident_response_evidence, json)
  end

  test "incident_response_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:incident_response_evidence, %{})
  end


  test "indemnity_scope_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      indemnity_scope_id: "sample_indemnity_scope_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.IndemnityScopeAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["indemnity_scope_id"] == "sample_indemnity_scope_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:indemnity_scope_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:indemnity_scope_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "indemnity_scope_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      indemnity_scope_id: "sample_indemnity_scope_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.IndemnityScopeAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:indemnity_scope_admission, json)
  end

  test "indemnity_scope_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:indemnity_scope_admission, %{})
  end


  test "insurance_requirement to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      coverage_id: "sample_coverage_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.InsuranceRequirement.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["coverage_id"] == "sample_coverage_id"
    assert m["evidence_hash"] == "sample_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:insurance_requirement, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:insurance_requirement, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "insurance_requirement encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      coverage_id: "sample_coverage_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.InsuranceRequirement.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:insurance_requirement, json)
  end

  test "insurance_requirement from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:insurance_requirement, %{})
  end


  test "integration_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      integration_readiness_id: "sample_integration_readiness_id",
      account_id: "sample_account_id",
      integration_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.IntegrationReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["integration_readiness_id"] == "sample_integration_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["integration_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:integration_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:integration_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "integration_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      integration_readiness_id: "sample_integration_readiness_id",
      account_id: "sample_account_id",
      integration_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.IntegrationReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:integration_readiness, json)
  end

  test "integration_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :integration_readiness_id}} =
             BeamPM.Codec.from_map(:integration_readiness, %{})
  end


  test "integration_scope_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      integration_scope_id: "sample_integration_scope_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.IntegrationScopeAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["integration_scope_id"] == "sample_integration_scope_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:integration_scope_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:integration_scope_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "integration_scope_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      integration_scope_id: "sample_integration_scope_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.IntegrationScopeAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:integration_scope_admission, json)
  end

  test "integration_scope_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:integration_scope_admission, %{})
  end


  test "invoice_entity_identity to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      invoice_entity_id: "sample_invoice_entity_id",
      identity_evidence_hash: "sample_identity_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.InvoiceEntityIdentity.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["invoice_entity_id"] == "sample_invoice_entity_id"
    assert m["identity_evidence_hash"] == "sample_identity_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:invoice_entity_identity, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:invoice_entity_identity, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "invoice_entity_identity encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      invoice_entity_id: "sample_invoice_entity_id",
      identity_evidence_hash: "sample_identity_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.InvoiceEntityIdentity.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:invoice_entity_identity, json)
  end

  test "invoice_entity_identity from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:invoice_entity_identity, %{})
  end


  test "invoice_schedule to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      schedule_id: "sample_schedule_id",
      billing_account_id: "sample_billing_account_id",
      cadence: :sample_atom,
      next_invoice_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.InvoiceSchedule.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["schedule_id"] == "sample_schedule_id"
    assert m["billing_account_id"] == "sample_billing_account_id"
    assert m["cadence"] == "sample_atom"
    assert m["next_invoice_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:invoice_schedule, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:invoice_schedule, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "invoice_schedule encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      schedule_id: "sample_schedule_id",
      billing_account_id: "sample_billing_account_id",
      cadence: :sample_atom,
      next_invoice_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.InvoiceSchedule.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:invoice_schedule, json)
  end

  test "invoice_schedule from_map reports the first missing required field" do
    assert {:error, {:missing_field, :schedule_id}} =
             BeamPM.Codec.from_map(:invoice_schedule, %{})
  end


  test "k8s_object_ref to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      kind: "sample_kind",
      name: "sample_name",
      namespace: "sample_namespace"
    }

    assert {:ok, rec} = BeamPM.Types.K8SObjectRef.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["kind"] == "sample_kind"
    assert m["name"] == "sample_name"
    assert m["namespace"] == "sample_namespace"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:k8s_object_ref, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:k8s_object_ref, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "k8s_object_ref encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      kind: "sample_kind",
      name: "sample_name",
      namespace: "sample_namespace"
    }

    assert {:ok, rec} = BeamPM.Types.K8SObjectRef.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:k8s_object_ref, json)
  end

  test "k8s_object_ref from_map reports the first missing required field" do
    assert {:error, {:missing_field, :kind}} =
             BeamPM.Codec.from_map(:k8s_object_ref, %{})
  end

  test "k8s_object_ref to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      kind: "sample_kind",
      name: "sample_name"
    }

    assert {:ok, rec} = BeamPM.Types.K8SObjectRef.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "namespace")
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:k8s_object_ref, m)
  end

  test "latency_budget_observation to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      workload_id: "sample_workload_id",
      observation_hash: "sample_observation_hash"
    }

    assert {:ok, rec} = BeamPM.Types.LatencyBudgetObservation.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["workload_id"] == "sample_workload_id"
    assert m["observation_hash"] == "sample_observation_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:latency_budget_observation, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:latency_budget_observation, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "latency_budget_observation encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      workload_id: "sample_workload_id",
      observation_hash: "sample_observation_hash"
    }

    assert {:ok, rec} = BeamPM.Types.LatencyBudgetObservation.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:latency_budget_observation, json)
  end

  test "latency_budget_observation from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:latency_budget_observation, %{})
  end


  test "leakage_finding to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      case_id: "sample_case_id",
      fitness: 3.5,
      precision: 3.5,
      amount_at_risk: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.LeakageFinding.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["case_id"] == "sample_case_id"
    assert m["fitness"] == 3.5
    assert m["precision"] == 3.5
    assert m["amount_at_risk"] == 3.5
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:leakage_finding, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:leakage_finding, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "leakage_finding encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      case_id: "sample_case_id",
      fitness: 3.5,
      precision: 3.5,
      amount_at_risk: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.LeakageFinding.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:leakage_finding, json)
  end

  test "leakage_finding from_map reports the first missing required field" do
    assert {:error, {:missing_field, :case_id}} =
             BeamPM.Codec.from_map(:leakage_finding, %{})
  end

  test "leakage_finding to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      case_id: "sample_case_id",
      fitness: 3.5,
      precision: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.LeakageFinding.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "amount_at_risk")
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:leakage_finding, m)
  end

  test "least_authority_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      granted_permissions: ["alpha", "beta"],
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.LeastAuthorityEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["granted_permissions"] == ["alpha", "beta"]
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:least_authority_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:least_authority_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "least_authority_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      granted_permissions: ["alpha", "beta"],
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.LeastAuthorityEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:least_authority_evidence, json)
  end

  test "least_authority_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:least_authority_evidence, %{})
  end


  test "legal_blocker to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      blocker_id: "sample_blocker_id",
      refusal_code: "sample_refusal_code"
    }

    assert {:ok, rec} = BeamPM.Types.LegalBlocker.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["blocker_id"] == "sample_blocker_id"
    assert m["refusal_code"] == "sample_refusal_code"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:legal_blocker, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:legal_blocker, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "legal_blocker encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      blocker_id: "sample_blocker_id",
      refusal_code: "sample_refusal_code"
    }

    assert {:ok, rec} = BeamPM.Types.LegalBlocker.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:legal_blocker, json)
  end

  test "legal_blocker from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:legal_blocker, %{})
  end


  test "liability_cap_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      liability_cap_id: "sample_liability_cap_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.LiabilityCapAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["liability_cap_id"] == "sample_liability_cap_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:liability_cap_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:liability_cap_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "liability_cap_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      liability_cap_id: "sample_liability_cap_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.LiabilityCapAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:liability_cap_admission, json)
  end

  test "liability_cap_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:liability_cap_admission, %{})
  end


  test "license_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      license_expression: "sample_license_expression",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.LicenseEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["license_expression"] == "sample_license_expression"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:license_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:license_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "license_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      license_expression: "sample_license_expression",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.LicenseEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:license_evidence, json)
  end

  test "license_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:license_evidence, %{})
  end


  test "log_trace to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      case_id: "sample_case_id",
      activity_sequence: ["alpha", "beta"]
    }

    assert {:ok, rec} = BeamPM.Types.LogTrace.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["case_id"] == "sample_case_id"
    assert m["activity_sequence"] == ["alpha", "beta"]
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:log_trace, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:log_trace, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "log_trace encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      case_id: "sample_case_id",
      activity_sequence: ["alpha", "beta"]
    }

    assert {:ok, rec} = BeamPM.Types.LogTrace.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:log_trace, json)
  end

  test "log_trace from_map reports the first missing required field" do
    assert {:error, {:missing_field, :case_id}} =
             BeamPM.Codec.from_map(:log_trace, %{})
  end


  test "master_service_agreement_state to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      agreement_id: "sample_agreement_id",
      agreement_state: "sample_agreement_state"
    }

    assert {:ok, rec} = BeamPM.Types.MasterServiceAgreementState.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["agreement_id"] == "sample_agreement_id"
    assert m["agreement_state"] == "sample_agreement_state"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:master_service_agreement_state, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:master_service_agreement_state, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "master_service_agreement_state encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      agreement_id: "sample_agreement_id",
      agreement_state: "sample_agreement_state"
    }

    assert {:ok, rec} = BeamPM.Types.MasterServiceAgreementState.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:master_service_agreement_state, json)
  end

  test "master_service_agreement_state from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:master_service_agreement_state, %{})
  end


  test "metered_usage_sample to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      usage_sample_id: "sample_usage_sample_id",
      sample_hash: "sample_sample_hash"
    }

    assert {:ok, rec} = BeamPM.Types.MeteredUsageSample.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["usage_sample_id"] == "sample_usage_sample_id"
    assert m["sample_hash"] == "sample_sample_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:metered_usage_sample, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:metered_usage_sample, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "metered_usage_sample encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      usage_sample_id: "sample_usage_sample_id",
      sample_hash: "sample_sample_hash"
    }

    assert {:ok, rec} = BeamPM.Types.MeteredUsageSample.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:metered_usage_sample, json)
  end

  test "metered_usage_sample from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:metered_usage_sample, %{})
  end


  test "migration_contract to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      migration_id: "sample_migration_id",
      from_version: "sample_from_version",
      to_version: "sample_to_version",
      rollback_plan: "sample_rollback_plan"
    }

    assert {:ok, rec} = BeamPM.Types.MigrationContract.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["migration_id"] == "sample_migration_id"
    assert m["from_version"] == "sample_from_version"
    assert m["to_version"] == "sample_to_version"
    assert m["rollback_plan"] == "sample_rollback_plan"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:migration_contract, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:migration_contract, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "migration_contract encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      migration_id: "sample_migration_id",
      from_version: "sample_from_version",
      to_version: "sample_to_version",
      rollback_plan: "sample_rollback_plan"
    }

    assert {:ok, rec} = BeamPM.Types.MigrationContract.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:migration_contract, json)
  end

  test "migration_contract from_map reports the first missing required field" do
    assert {:error, {:missing_field, :migration_id}} =
             BeamPM.Codec.from_map(:migration_contract, %{})
  end


  test "migration_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      migration_readiness_id: "sample_migration_readiness_id",
      account_id: "sample_account_id",
      migration_effort_days: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.MigrationReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["migration_readiness_id"] == "sample_migration_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["migration_effort_days"] == 42
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:migration_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:migration_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "migration_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      migration_readiness_id: "sample_migration_readiness_id",
      account_id: "sample_account_id",
      migration_effort_days: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.MigrationReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:migration_readiness, json)
  end

  test "migration_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :migration_readiness_id}} =
             BeamPM.Codec.from_map(:migration_readiness, %{})
  end


  test "minimum_term_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      minimum_term: "sample_minimum_term",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.MinimumTermAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["minimum_term"] == "sample_minimum_term"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:minimum_term_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:minimum_term_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "minimum_term_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      minimum_term: "sample_minimum_term",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.MinimumTermAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:minimum_term_admission, json)
  end

  test "minimum_term_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:minimum_term_admission, %{})
  end


  test "multiarch_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      platforms: ["alpha", "beta"],
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.MultiarchEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["platforms"] == ["alpha", "beta"]
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:multiarch_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:multiarch_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "multiarch_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      platforms: ["alpha", "beta"],
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.MultiarchEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:multiarch_evidence, json)
  end

  test "multiarch_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:multiarch_evidence, %{})
  end


  test "mutable_identity_refusal_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      mutable_reference: "sample_mutable_reference",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.MutableIdentityRefusalEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["mutable_reference"] == "sample_mutable_reference"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:mutable_identity_refusal_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:mutable_identity_refusal_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "mutable_identity_refusal_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      mutable_reference: "sample_mutable_reference",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.MutableIdentityRefusalEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:mutable_identity_refusal_evidence, json)
  end

  test "mutable_identity_refusal_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:mutable_identity_refusal_evidence, %{})
  end


  test "node_failover_event to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      node_id: "sample_node_id",
      failover_hash: "sample_failover_hash"
    }

    assert {:ok, rec} = BeamPM.Types.NodeFailoverEvent.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["node_id"] == "sample_node_id"
    assert m["failover_hash"] == "sample_failover_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:node_failover_event, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:node_failover_event, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "node_failover_event encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      node_id: "sample_node_id",
      failover_hash: "sample_failover_hash"
    }

    assert {:ok, rec} = BeamPM.Types.NodeFailoverEvent.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:node_failover_event, json)
  end

  test "node_failover_event from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:node_failover_event, %{})
  end


  test "object_attribute_change to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      object_id: "sample_object_id",
      attribute_name: "sample_attribute_name",
      old_value: "sample_old_value",
      new_value: "sample_new_value",
      changed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ObjectAttributeChange.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["object_id"] == "sample_object_id"
    assert m["attribute_name"] == "sample_attribute_name"
    assert m["old_value"] == "sample_old_value"
    assert m["new_value"] == "sample_new_value"
    assert m["changed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:object_attribute_change, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:object_attribute_change, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "object_attribute_change encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      object_id: "sample_object_id",
      attribute_name: "sample_attribute_name",
      old_value: "sample_old_value",
      new_value: "sample_new_value",
      changed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ObjectAttributeChange.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:object_attribute_change, json)
  end

  test "object_attribute_change from_map reports the first missing required field" do
    assert {:error, {:missing_field, :object_id}} =
             BeamPM.Codec.from_map(:object_attribute_change, %{})
  end

  test "object_attribute_change to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      object_id: "sample_object_id",
      attribute_name: "sample_attribute_name",
      new_value: "sample_new_value",
      changed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ObjectAttributeChange.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "old_value")
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:object_attribute_change, m)
  end

  test "object_type to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      type_name: "sample_type_name",
      attribute_names: ["alpha", "beta"]
    }

    assert {:ok, rec} = BeamPM.Types.ObjectType.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["type_name"] == "sample_type_name"
    assert m["attribute_names"] == ["alpha", "beta"]
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:object_type, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:object_type, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "object_type encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      type_name: "sample_type_name",
      attribute_names: ["alpha", "beta"]
    }

    assert {:ok, rec} = BeamPM.Types.ObjectType.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:object_type, json)
  end

  test "object_type from_map reports the first missing required field" do
    assert {:error, {:missing_field, :type_name}} =
             BeamPM.Codec.from_map(:object_type, %{})
  end

  test "object_type to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      type_name: "sample_type_name"
    }

    assert {:ok, rec} = BeamPM.Types.ObjectType.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "attribute_names")
    assert map_size(m) == 1
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:object_type, m)
  end

  test "objection to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      objection_id: "sample_objection_id",
      account_id: "sample_account_id",
      objection_type: "sample_objection_type",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.Objection.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["objection_id"] == "sample_objection_id"
    assert m["account_id"] == "sample_account_id"
    assert m["objection_type"] == "sample_objection_type"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:objection, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:objection, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "objection encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      objection_id: "sample_objection_id",
      account_id: "sample_account_id",
      objection_type: "sample_objection_type",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.Objection.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:objection, json)
  end

  test "objection from_map reports the first missing required field" do
    assert {:error, {:missing_field, :objection_id}} =
             BeamPM.Codec.from_map(:objection, %{})
  end


  test "objection_resolution to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      objection_resolution_id: "sample_objection_resolution_id",
      account_id: "sample_account_id",
      resolution_status: "sample_resolution_status",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ObjectionResolution.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["objection_resolution_id"] == "sample_objection_resolution_id"
    assert m["account_id"] == "sample_account_id"
    assert m["resolution_status"] == "sample_resolution_status"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:objection_resolution, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:objection_resolution, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "objection_resolution encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      objection_resolution_id: "sample_objection_resolution_id",
      account_id: "sample_account_id",
      resolution_status: "sample_resolution_status",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ObjectionResolution.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:objection_resolution, json)
  end

  test "objection_resolution from_map reports the first missing required field" do
    assert {:error, {:missing_field, :objection_resolution_id}} =
             BeamPM.Codec.from_map(:objection_resolution, %{})
  end


  test "oc_declare_constraint to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      constraint_id: "sample_constraint_id",
      source_activity: "sample_source_activity",
      target_activity: "sample_target_activity",
      constraint_type: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.OcDeclareConstraint.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["constraint_id"] == "sample_constraint_id"
    assert m["source_activity"] == "sample_source_activity"
    assert m["target_activity"] == "sample_target_activity"
    assert m["constraint_type"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:oc_declare_constraint, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:oc_declare_constraint, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "oc_declare_constraint encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      constraint_id: "sample_constraint_id",
      source_activity: "sample_source_activity",
      target_activity: "sample_target_activity",
      constraint_type: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.OcDeclareConstraint.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:oc_declare_constraint, json)
  end

  test "oc_declare_constraint from_map reports the first missing required field" do
    assert {:error, {:missing_field, :constraint_id}} =
             BeamPM.Codec.from_map(:oc_declare_constraint, %{})
  end


  test "ocel_attribute to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      attribute_name: "sample_attribute_name",
      attribute_value: "sample_attribute_value",
      recorded_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.OcelAttribute.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["attribute_name"] == "sample_attribute_name"
    assert m["attribute_value"] == "sample_attribute_value"
    assert m["recorded_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:ocel_attribute, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:ocel_attribute, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "ocel_attribute encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      attribute_name: "sample_attribute_name",
      attribute_value: "sample_attribute_value",
      recorded_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.OcelAttribute.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:ocel_attribute, json)
  end

  test "ocel_attribute from_map reports the first missing required field" do
    assert {:error, {:missing_field, :attribute_name}} =
             BeamPM.Codec.from_map(:ocel_attribute, %{})
  end

  test "ocel_attribute to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      attribute_name: "sample_attribute_name",
      attribute_value: "sample_attribute_value"
    }

    assert {:ok, rec} = BeamPM.Types.OcelAttribute.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "recorded_at")
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:ocel_attribute, m)
  end

  test "ocel_event to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      event_id: "sample_event_id",
      event_type: "sample_event_type",
      event_time: "2026-08-29T12:00:00Z",
      attributes: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.Types.OcelEvent.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["event_id"] == "sample_event_id"
    assert m["event_type"] == "sample_event_type"
    assert m["event_time"] == "2026-08-29T12:00:00Z"
    assert m["attributes"] == %{"k" => "v"}
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:ocel_event, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:ocel_event, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "ocel_event encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      event_id: "sample_event_id",
      event_type: "sample_event_type",
      event_time: "2026-08-29T12:00:00Z",
      attributes: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.Types.OcelEvent.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:ocel_event, json)
  end

  test "ocel_event from_map reports the first missing required field" do
    assert {:error, {:missing_field, :event_id}} =
             BeamPM.Codec.from_map(:ocel_event, %{})
  end

  test "ocel_event to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      event_id: "sample_event_id",
      event_type: "sample_event_type",
      event_time: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.OcelEvent.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "attributes")
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:ocel_event, m)
  end

  test "ocel_object to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      object_id: "sample_object_id",
      object_type: "sample_object_type",
      attributes: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.Types.OcelObject.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["object_id"] == "sample_object_id"
    assert m["object_type"] == "sample_object_type"
    assert m["attributes"] == %{"k" => "v"}
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:ocel_object, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:ocel_object, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "ocel_object encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      object_id: "sample_object_id",
      object_type: "sample_object_type",
      attributes: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.Types.OcelObject.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:ocel_object, json)
  end

  test "ocel_object from_map reports the first missing required field" do
    assert {:error, {:missing_field, :object_id}} =
             BeamPM.Codec.from_map(:ocel_object, %{})
  end

  test "ocel_object to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      object_id: "sample_object_id",
      object_type: "sample_object_type"
    }

    assert {:ok, rec} = BeamPM.Types.OcelObject.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "attributes")
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:ocel_object, m)
  end

  test "ocel_relationship to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      qualifier: "sample_qualifier",
      object_id: "sample_object_id"
    }

    assert {:ok, rec} = BeamPM.Types.OcelRelationship.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["qualifier"] == "sample_qualifier"
    assert m["object_id"] == "sample_object_id"
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:ocel_relationship, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:ocel_relationship, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "ocel_relationship encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      qualifier: "sample_qualifier",
      object_id: "sample_object_id"
    }

    assert {:ok, rec} = BeamPM.Types.OcelRelationship.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:ocel_relationship, json)
  end

  test "ocel_relationship from_map reports the first missing required field" do
    assert {:error, {:missing_field, :qualifier}} =
             BeamPM.Codec.from_map(:ocel_relationship, %{})
  end


  test "oci_manifest_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      manifest_digest: "sample_manifest_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.OciManifestEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["manifest_digest"] == "sample_manifest_digest"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:oci_manifest_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:oci_manifest_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "oci_manifest_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      manifest_digest: "sample_manifest_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.OciManifestEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:oci_manifest_evidence, json)
  end

  test "oci_manifest_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:oci_manifest_evidence, %{})
  end


  test "offline_bundle_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      bundle_digest: "sample_bundle_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.OfflineBundleEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["bundle_digest"] == "sample_bundle_digest"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:offline_bundle_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:offline_bundle_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "offline_bundle_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      bundle_digest: "sample_bundle_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.OfflineBundleEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:offline_bundle_evidence, json)
  end

  test "offline_bundle_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:offline_bundle_evidence, %{})
  end


  test "operator_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      operator_readiness_id: "sample_operator_readiness_id",
      account_id: "sample_account_id",
      operator_readiness_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.OperatorReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["operator_readiness_id"] == "sample_operator_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["operator_readiness_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:operator_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:operator_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "operator_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      operator_readiness_id: "sample_operator_readiness_id",
      account_id: "sample_account_id",
      operator_readiness_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.OperatorReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:operator_readiness, json)
  end

  test "operator_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :operator_readiness_id}} =
             BeamPM.Codec.from_map(:operator_readiness, %{})
  end


  test "opportunity_currency_contract to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      currency_code: "sample_currency_code",
      fx_basis_id: "sample_fx_basis_id"
    }

    assert {:ok, rec} = BeamPM.Types.OpportunityCurrencyContract.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["currency_code"] == "sample_currency_code"
    assert m["fx_basis_id"] == "sample_fx_basis_id"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:opportunity_currency_contract, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:opportunity_currency_contract, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "opportunity_currency_contract encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      currency_code: "sample_currency_code",
      fx_basis_id: "sample_fx_basis_id"
    }

    assert {:ok, rec} = BeamPM.Types.OpportunityCurrencyContract.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:opportunity_currency_contract, json)
  end

  test "opportunity_currency_contract from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:opportunity_currency_contract, %{})
  end


  test "opportunity_value_range to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      minimum_value: "sample_minimum_value",
      maximum_value: "sample_maximum_value"
    }

    assert {:ok, rec} = BeamPM.Types.OpportunityValueRange.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["minimum_value"] == "sample_minimum_value"
    assert m["maximum_value"] == "sample_maximum_value"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:opportunity_value_range, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:opportunity_value_range, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "opportunity_value_range encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      minimum_value: "sample_minimum_value",
      maximum_value: "sample_maximum_value"
    }

    assert {:ok, rec} = BeamPM.Types.OpportunityValueRange.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:opportunity_value_range, json)
  end

  test "opportunity_value_range from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:opportunity_value_range, %{})
  end


  test "order_form_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      order_form_id: "sample_order_form_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.OrderFormAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["order_form_id"] == "sample_order_form_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:order_form_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:order_form_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "order_form_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      order_form_id: "sample_order_form_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.OrderFormAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:order_form_admission, json)
  end

  test "order_form_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:order_form_admission, %{})
  end


  test "overage_policy to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      policy_id: "sample_policy_id",
      quota_id: "sample_quota_id",
      unit_price: 3.5,
      behavior: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.OveragePolicy.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["policy_id"] == "sample_policy_id"
    assert m["quota_id"] == "sample_quota_id"
    assert m["unit_price"] == 3.5
    assert m["behavior"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:overage_policy, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:overage_policy, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "overage_policy encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      policy_id: "sample_policy_id",
      quota_id: "sample_quota_id",
      unit_price: 3.5,
      behavior: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.OveragePolicy.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:overage_policy, json)
  end

  test "overage_policy from_map reports the first missing required field" do
    assert {:error, {:missing_field, :policy_id}} =
             BeamPM.Codec.from_map(:overage_policy, %{})
  end


  test "paid_workload_outcome_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      workload_id: "sample_workload_id",
      outcome_receipt_hash: "sample_outcome_receipt_hash"
    }

    assert {:ok, rec} = BeamPM.Types.PaidWorkloadOutcomeReceipt.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["workload_id"] == "sample_workload_id"
    assert m["outcome_receipt_hash"] == "sample_outcome_receipt_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:paid_workload_outcome_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:paid_workload_outcome_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "paid_workload_outcome_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      workload_id: "sample_workload_id",
      outcome_receipt_hash: "sample_outcome_receipt_hash"
    }

    assert {:ok, rec} = BeamPM.Types.PaidWorkloadOutcomeReceipt.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:paid_workload_outcome_receipt, json)
  end

  test "paid_workload_outcome_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:paid_workload_outcome_receipt, %{})
  end


  test "path_schema to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      schema_id: "sample_schema_id",
      source_type: "sample_source_type",
      target_type: "sample_target_type",
      support: 42
    }

    assert {:ok, rec} = BeamPM.Types.PathSchema.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["schema_id"] == "sample_schema_id"
    assert m["source_type"] == "sample_source_type"
    assert m["target_type"] == "sample_target_type"
    assert m["support"] == 42
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:path_schema, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:path_schema, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "path_schema encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      schema_id: "sample_schema_id",
      source_type: "sample_source_type",
      target_type: "sample_target_type",
      support: 42
    }

    assert {:ok, rec} = BeamPM.Types.PathSchema.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:path_schema, json)
  end

  test "path_schema from_map reports the first missing required field" do
    assert {:error, {:missing_field, :schema_id}} =
             BeamPM.Codec.from_map(:path_schema, %{})
  end


  test "path_schema_query to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      source_type: "sample_source_type",
      target_type: "sample_target_type",
      max_length: 42
    }

    assert {:ok, rec} = BeamPM.Types.PathSchemaQuery.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["source_type"] == "sample_source_type"
    assert m["target_type"] == "sample_target_type"
    assert m["max_length"] == 42
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:path_schema_query, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:path_schema_query, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "path_schema_query encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      source_type: "sample_source_type",
      target_type: "sample_target_type",
      max_length: 42
    }

    assert {:ok, rec} = BeamPM.Types.PathSchemaQuery.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:path_schema_query, json)
  end

  test "path_schema_query from_map reports the first missing required field" do
    assert {:error, {:missing_field, :source_type}} =
             BeamPM.Codec.from_map(:path_schema_query, %{})
  end


  test "payment_terms to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      terms_id: "sample_terms_id",
      net_days: 42,
      late_policy: "sample_late_policy",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.PaymentTerms.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["terms_id"] == "sample_terms_id"
    assert m["net_days"] == 42
    assert m["late_policy"] == "sample_late_policy"
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:payment_terms, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:payment_terms, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "payment_terms encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      terms_id: "sample_terms_id",
      net_days: 42,
      late_policy: "sample_late_policy",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.PaymentTerms.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:payment_terms, json)
  end

  test "payment_terms from_map reports the first missing required field" do
    assert {:error, {:missing_field, :terms_id}} =
             BeamPM.Codec.from_map(:payment_terms, %{})
  end


  test "payment_terms_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      payment_terms: "sample_payment_terms",
      authority_evidence_hash: "sample_authority_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.PaymentTermsAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["payment_terms"] == "sample_payment_terms"
    assert m["authority_evidence_hash"] == "sample_authority_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:payment_terms_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:payment_terms_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "payment_terms_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      payment_terms: "sample_payment_terms",
      authority_evidence_hash: "sample_authority_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.PaymentTermsAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:payment_terms_admission, json)
  end

  test "payment_terms_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:payment_terms_admission, %{})
  end


  test "performance_slo_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      latency_p95_ms: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.PerformanceSloEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["latency_p95_ms"] == 42
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:performance_slo_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:performance_slo_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "performance_slo_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      latency_p95_ms: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.PerformanceSloEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:performance_slo_evidence, json)
  end

  test "performance_slo_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:performance_slo_evidence, %{})
  end


  test "petri_arc to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      source_id: "sample_source_id",
      target_id: "sample_target_id",
      weight: 42
    }

    assert {:ok, rec} = BeamPM.Types.PetriArc.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["source_id"] == "sample_source_id"
    assert m["target_id"] == "sample_target_id"
    assert m["weight"] == 42
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:petri_arc, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:petri_arc, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "petri_arc encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      source_id: "sample_source_id",
      target_id: "sample_target_id",
      weight: 42
    }

    assert {:ok, rec} = BeamPM.Types.PetriArc.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:petri_arc, json)
  end

  test "petri_arc from_map reports the first missing required field" do
    assert {:error, {:missing_field, :source_id}} =
             BeamPM.Codec.from_map(:petri_arc, %{})
  end


  test "petri_place to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      place_id: "sample_place_id",
      tokens: 42
    }

    assert {:ok, rec} = BeamPM.Types.PetriPlace.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["place_id"] == "sample_place_id"
    assert m["tokens"] == 42
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:petri_place, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:petri_place, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "petri_place encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      place_id: "sample_place_id",
      tokens: 42
    }

    assert {:ok, rec} = BeamPM.Types.PetriPlace.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:petri_place, json)
  end

  test "petri_place from_map reports the first missing required field" do
    assert {:error, {:missing_field, :place_id}} =
             BeamPM.Codec.from_map(:petri_place, %{})
  end


  test "petri_transition to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      transition_id: "sample_transition_id",
      label: "sample_label"
    }

    assert {:ok, rec} = BeamPM.Types.PetriTransition.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["transition_id"] == "sample_transition_id"
    assert m["label"] == "sample_label"
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:petri_transition, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:petri_transition, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "petri_transition encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      transition_id: "sample_transition_id",
      label: "sample_label"
    }

    assert {:ok, rec} = BeamPM.Types.PetriTransition.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:petri_transition, json)
  end

  test "petri_transition from_map reports the first missing required field" do
    assert {:error, {:missing_field, :transition_id}} =
             BeamPM.Codec.from_map(:petri_transition, %{})
  end

  test "petri_transition to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      transition_id: "sample_transition_id"
    }

    assert {:ok, rec} = BeamPM.Types.PetriTransition.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "label")
    assert map_size(m) == 1
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:petri_transition, m)
  end

  test "planning_action to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      action_name: "sample_action_name",
      preconditions: ["alpha", "beta"],
      effects: ["alpha", "beta"]
    }

    assert {:ok, rec} = BeamPM.Types.PlanningAction.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["action_name"] == "sample_action_name"
    assert m["preconditions"] == ["alpha", "beta"]
    assert m["effects"] == ["alpha", "beta"]
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:planning_action, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:planning_action, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "planning_action encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      action_name: "sample_action_name",
      preconditions: ["alpha", "beta"],
      effects: ["alpha", "beta"]
    }

    assert {:ok, rec} = BeamPM.Types.PlanningAction.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:planning_action, json)
  end

  test "planning_action from_map reports the first missing required field" do
    assert {:error, {:missing_field, :action_name}} =
             BeamPM.Codec.from_map(:planning_action, %{})
  end

  test "planning_action to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      action_name: "sample_action_name"
    }

    assert {:ok, rec} = BeamPM.Types.PlanningAction.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "preconditions")
    refute Map.has_key?(m, "effects")
    assert map_size(m) == 1
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:planning_action, m)
  end

  test "planning_state to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      state_id: "sample_state_id",
      facts: ["alpha", "beta"]
    }

    assert {:ok, rec} = BeamPM.Types.PlanningState.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["state_id"] == "sample_state_id"
    assert m["facts"] == ["alpha", "beta"]
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:planning_state, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:planning_state, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "planning_state encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      state_id: "sample_state_id",
      facts: ["alpha", "beta"]
    }

    assert {:ok, rec} = BeamPM.Types.PlanningState.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:planning_state, json)
  end

  test "planning_state from_map reports the first missing required field" do
    assert {:error, {:missing_field, :state_id}} =
             BeamPM.Codec.from_map(:planning_state, %{})
  end


  test "poc_exit_criteria to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      poc_exit_criteria_id: "sample_poc_exit_criteria_id",
      account_id: "sample_account_id",
      criteria_pass_rate: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.PocExitCriteria.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["poc_exit_criteria_id"] == "sample_poc_exit_criteria_id"
    assert m["account_id"] == "sample_account_id"
    assert m["criteria_pass_rate"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:poc_exit_criteria, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:poc_exit_criteria, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "poc_exit_criteria encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      poc_exit_criteria_id: "sample_poc_exit_criteria_id",
      account_id: "sample_account_id",
      criteria_pass_rate: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.PocExitCriteria.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:poc_exit_criteria, json)
  end

  test "poc_exit_criteria from_map reports the first missing required field" do
    assert {:error, {:missing_field, :poc_exit_criteria_id}} =
             BeamPM.Codec.from_map(:poc_exit_criteria, %{})
  end


  test "poc_risk to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      poc_risk_id: "sample_poc_risk_id",
      account_id: "sample_account_id",
      risk_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.PocRisk.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["poc_risk_id"] == "sample_poc_risk_id"
    assert m["account_id"] == "sample_account_id"
    assert m["risk_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:poc_risk, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:poc_risk, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "poc_risk encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      poc_risk_id: "sample_poc_risk_id",
      account_id: "sample_account_id",
      risk_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.PocRisk.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:poc_risk, json)
  end

  test "poc_risk from_map reports the first missing required field" do
    assert {:error, {:missing_field, :poc_risk_id}} =
             BeamPM.Codec.from_map(:poc_risk, %{})
  end


  test "poc_scope to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      poc_scope_id: "sample_poc_scope_id",
      account_id: "sample_account_id",
      use_case_count: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.PocScope.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["poc_scope_id"] == "sample_poc_scope_id"
    assert m["account_id"] == "sample_account_id"
    assert m["use_case_count"] == 42
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:poc_scope, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:poc_scope, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "poc_scope encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      poc_scope_id: "sample_poc_scope_id",
      account_id: "sample_account_id",
      use_case_count: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.PocScope.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:poc_scope, json)
  end

  test "poc_scope from_map reports the first missing required field" do
    assert {:error, {:missing_field, :poc_scope_id}} =
             BeamPM.Codec.from_map(:poc_scope, %{})
  end


  test "poc_timeline to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      poc_timeline_id: "sample_poc_timeline_id",
      account_id: "sample_account_id",
      days_to_value: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.PocTimeline.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["poc_timeline_id"] == "sample_poc_timeline_id"
    assert m["account_id"] == "sample_account_id"
    assert m["days_to_value"] == 42
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:poc_timeline, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:poc_timeline, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "poc_timeline encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      poc_timeline_id: "sample_poc_timeline_id",
      account_id: "sample_account_id",
      days_to_value: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.PocTimeline.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:poc_timeline, json)
  end

  test "poc_timeline from_map reports the first missing required field" do
    assert {:error, {:missing_field, :poc_timeline_id}} =
             BeamPM.Codec.from_map(:poc_timeline, %{})
  end


  test "policy_decision to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      decision_id: "sample_decision_id",
      verdict: :sample_atom,
      reason: "sample_reason"
    }

    assert {:ok, rec} = BeamPM.Types.PolicyDecision.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["decision_id"] == "sample_decision_id"
    assert m["verdict"] == "sample_atom"
    assert m["reason"] == "sample_reason"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:policy_decision, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:policy_decision, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "policy_decision encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      decision_id: "sample_decision_id",
      verdict: :sample_atom,
      reason: "sample_reason"
    }

    assert {:ok, rec} = BeamPM.Types.PolicyDecision.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:policy_decision, json)
  end

  test "policy_decision from_map reports the first missing required field" do
    assert {:error, {:missing_field, :decision_id}} =
             BeamPM.Codec.from_map(:policy_decision, %{})
  end

  test "policy_decision to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      decision_id: "sample_decision_id",
      verdict: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.PolicyDecision.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "reason")
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:policy_decision, m)
  end

  test "pricing_basis_contract to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      pricing_basis_id: "sample_pricing_basis_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.PricingBasisContract.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["pricing_basis_id"] == "sample_pricing_basis_id"
    assert m["evidence_hash"] == "sample_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:pricing_basis_contract, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:pricing_basis_contract, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "pricing_basis_contract encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      pricing_basis_id: "sample_pricing_basis_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.PricingBasisContract.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:pricing_basis_contract, json)
  end

  test "pricing_basis_contract from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:pricing_basis_contract, %{})
  end


  test "privacy_classification_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      privacy_class: "sample_privacy_class",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.PrivacyClassificationEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["privacy_class"] == "sample_privacy_class"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:privacy_classification_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:privacy_classification_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "privacy_classification_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      privacy_class: "sample_privacy_class",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.PrivacyClassificationEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:privacy_classification_evidence, json)
  end

  test "privacy_classification_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:privacy_classification_evidence, %{})
  end


  test "private_offer to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      offer_id: "sample_offer_id",
      account_id: "sample_account_id",
      total_price: 3.5,
      expires_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.PrivateOffer.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["offer_id"] == "sample_offer_id"
    assert m["account_id"] == "sample_account_id"
    assert m["total_price"] == 3.5
    assert m["expires_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:private_offer, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:private_offer, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "private_offer encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      offer_id: "sample_offer_id",
      account_id: "sample_account_id",
      total_price: 3.5,
      expires_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.PrivateOffer.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:private_offer, json)
  end

  test "private_offer from_map reports the first missing required field" do
    assert {:error, {:missing_field, :offer_id}} =
             BeamPM.Codec.from_map(:private_offer, %{})
  end


  test "private_registry_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      registry_digest: "sample_registry_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.PrivateRegistryEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["registry_digest"] == "sample_registry_digest"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:private_registry_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:private_registry_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "private_registry_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      registry_digest: "sample_registry_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.PrivateRegistryEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:private_registry_evidence, json)
  end

  test "private_registry_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:private_registry_evidence, %{})
  end


  test "process_variant to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      variant_id: "sample_variant_id",
      activity_sequence: ["alpha", "beta"],
      frequency: 42
    }

    assert {:ok, rec} = BeamPM.Types.ProcessVariant.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["variant_id"] == "sample_variant_id"
    assert m["activity_sequence"] == ["alpha", "beta"]
    assert m["frequency"] == 42
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:process_variant, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:process_variant, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "process_variant encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      variant_id: "sample_variant_id",
      activity_sequence: ["alpha", "beta"],
      frequency: 42
    }

    assert {:ok, rec} = BeamPM.Types.ProcessVariant.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:process_variant, json)
  end

  test "process_variant from_map reports the first missing required field" do
    assert {:error, {:missing_field, :variant_id}} =
             BeamPM.Codec.from_map(:process_variant, %{})
  end


  test "procurement_acceptance_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      acceptance_decision: "sample_acceptance_decision",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ProcurementAcceptanceEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["acceptance_decision"] == "sample_acceptance_decision"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:procurement_acceptance_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:procurement_acceptance_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "procurement_acceptance_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      acceptance_decision: "sample_acceptance_decision",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ProcurementAcceptanceEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:procurement_acceptance_evidence, json)
  end

  test "procurement_acceptance_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:procurement_acceptance_evidence, %{})
  end


  test "procurement_blocker to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      blocker_id: "sample_blocker_id",
      refusal_code: "sample_refusal_code"
    }

    assert {:ok, rec} = BeamPM.Types.ProcurementBlocker.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["blocker_id"] == "sample_blocker_id"
    assert m["refusal_code"] == "sample_refusal_code"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:procurement_blocker, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:procurement_blocker, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "procurement_blocker encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      blocker_id: "sample_blocker_id",
      refusal_code: "sample_refusal_code"
    }

    assert {:ok, rec} = BeamPM.Types.ProcurementBlocker.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:procurement_blocker, json)
  end

  test "procurement_blocker from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:procurement_blocker, %{})
  end


  test "procurement_channel_selection to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      channel_id: "sample_channel_id",
      selection_evidence_hash: "sample_selection_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ProcurementChannelSelection.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["channel_id"] == "sample_channel_id"
    assert m["selection_evidence_hash"] == "sample_selection_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:procurement_channel_selection, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:procurement_channel_selection, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "procurement_channel_selection encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      channel_id: "sample_channel_id",
      selection_evidence_hash: "sample_selection_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ProcurementChannelSelection.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:procurement_channel_selection, json)
  end

  test "procurement_channel_selection from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:procurement_channel_selection, %{})
  end


  test "procurement_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      procurement_readiness_id: "sample_procurement_readiness_id",
      account_id: "sample_account_id",
      procurement_stage: "sample_procurement_stage",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ProcurementReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["procurement_readiness_id"] == "sample_procurement_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["procurement_stage"] == "sample_procurement_stage"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:procurement_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:procurement_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "procurement_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      procurement_readiness_id: "sample_procurement_readiness_id",
      account_id: "sample_account_id",
      procurement_stage: "sample_procurement_stage",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ProcurementReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:procurement_readiness, json)
  end

  test "procurement_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :procurement_readiness_id}} =
             BeamPM.Codec.from_map(:procurement_readiness, %{})
  end


  test "production_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      production_readiness_id: "sample_production_readiness_id",
      account_id: "sample_account_id",
      production_readiness_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ProductionReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["production_readiness_id"] == "sample_production_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["production_readiness_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:production_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:production_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "production_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      production_readiness_id: "sample_production_readiness_id",
      account_id: "sample_account_id",
      production_readiness_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ProductionReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:production_readiness, json)
  end

  test "production_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :production_readiness_id}} =
             BeamPM.Codec.from_map(:production_readiness, %{})
  end


  test "proof_of_value_budget to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      budget_id: "sample_budget_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.ProofOfValueBudget.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["budget_id"] == "sample_budget_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:proof_of_value_budget, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:proof_of_value_budget, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "proof_of_value_budget encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      budget_id: "sample_budget_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.ProofOfValueBudget.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:proof_of_value_budget, json)
  end

  test "proof_of_value_budget from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:proof_of_value_budget, %{})
  end


  test "proof_of_value_exit_gate to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      pov_id: "sample_pov_id",
      exit_gate_id: "sample_exit_gate_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.ProofOfValueExitGate.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["pov_id"] == "sample_pov_id"
    assert m["exit_gate_id"] == "sample_exit_gate_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:proof_of_value_exit_gate, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:proof_of_value_exit_gate, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "proof_of_value_exit_gate encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      pov_id: "sample_pov_id",
      exit_gate_id: "sample_exit_gate_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.ProofOfValueExitGate.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:proof_of_value_exit_gate, json)
  end

  test "proof_of_value_exit_gate from_map reports the first missing required field" do
    assert {:error, {:missing_field, :pov_id}} =
             BeamPM.Codec.from_map(:proof_of_value_exit_gate, %{})
  end


  test "provenance_binding_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      builder_identity: "sample_builder_identity",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ProvenanceBindingEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["builder_identity"] == "sample_builder_identity"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:provenance_binding_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:provenance_binding_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "provenance_binding_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      builder_identity: "sample_builder_identity",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ProvenanceBindingEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:provenance_binding_evidence, json)
  end

  test "provenance_binding_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:provenance_binding_evidence, %{})
  end


  test "purchase_order_binding to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      binding_id: "sample_binding_id",
      order_id: "sample_order_id",
      purchase_order_number: "sample_purchase_order_number",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.PurchaseOrderBinding.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["binding_id"] == "sample_binding_id"
    assert m["order_id"] == "sample_order_id"
    assert m["purchase_order_number"] == "sample_purchase_order_number"
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:purchase_order_binding, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:purchase_order_binding, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "purchase_order_binding encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      binding_id: "sample_binding_id",
      order_id: "sample_order_id",
      purchase_order_number: "sample_purchase_order_number",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.PurchaseOrderBinding.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:purchase_order_binding, json)
  end

  test "purchase_order_binding from_map reports the first missing required field" do
    assert {:error, {:missing_field, :binding_id}} =
             BeamPM.Codec.from_map(:purchase_order_binding, %{})
  end


  test "purchase_order_requirement to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      requirement_id: "sample_requirement_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.PurchaseOrderRequirement.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["requirement_id"] == "sample_requirement_id"
    assert m["evidence_hash"] == "sample_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:purchase_order_requirement, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:purchase_order_requirement, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "purchase_order_requirement encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      requirement_id: "sample_requirement_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.PurchaseOrderRequirement.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:purchase_order_requirement, json)
  end

  test "purchase_order_requirement from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:purchase_order_requirement, %{})
  end


  test "purchasing_entity_identity to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      purchasing_entity_id: "sample_purchasing_entity_id",
      identity_evidence_hash: "sample_identity_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.PurchasingEntityIdentity.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["purchasing_entity_id"] == "sample_purchasing_entity_id"
    assert m["identity_evidence_hash"] == "sample_identity_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:purchasing_entity_identity, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:purchasing_entity_identity, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "purchasing_entity_identity encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      purchasing_entity_id: "sample_purchasing_entity_id",
      identity_evidence_hash: "sample_identity_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.PurchasingEntityIdentity.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:purchasing_entity_identity, json)
  end

  test "purchasing_entity_identity from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:purchasing_entity_identity, %{})
  end


  test "queue_snapshot to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      queue_name: "sample_queue_name",
      depth: 42,
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.QueueSnapshot.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["queue_name"] == "sample_queue_name"
    assert m["depth"] == 42
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:queue_snapshot, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:queue_snapshot, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "queue_snapshot encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      queue_name: "sample_queue_name",
      depth: 42,
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.QueueSnapshot.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:queue_snapshot, json)
  end

  test "queue_snapshot from_map reports the first missing required field" do
    assert {:error, {:missing_field, :queue_name}} =
             BeamPM.Codec.from_map(:queue_snapshot, %{})
  end


  test "quota_policy to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      quota_id: "sample_quota_id",
      metric_name: "sample_metric_name",
      limit: 3.5,
      window: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.QuotaPolicy.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["quota_id"] == "sample_quota_id"
    assert m["metric_name"] == "sample_metric_name"
    assert m["limit"] == 3.5
    assert m["window"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:quota_policy, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:quota_policy, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "quota_policy encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      quota_id: "sample_quota_id",
      metric_name: "sample_metric_name",
      limit: 3.5,
      window: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.QuotaPolicy.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:quota_policy, json)
  end

  test "quota_policy from_map reports the first missing required field" do
    assert {:error, {:missing_field, :quota_id}} =
             BeamPM.Codec.from_map(:quota_policy, %{})
  end


  test "ramp_commitment to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      ramp_id: "sample_ramp_id",
      phase: 42,
      committed_amount: 3.5,
      effective_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RampCommitment.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["ramp_id"] == "sample_ramp_id"
    assert m["phase"] == 42
    assert m["committed_amount"] == 3.5
    assert m["effective_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:ramp_commitment, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:ramp_commitment, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "ramp_commitment encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      ramp_id: "sample_ramp_id",
      phase: 42,
      committed_amount: 3.5,
      effective_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RampCommitment.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:ramp_commitment, json)
  end

  test "ramp_commitment from_map reports the first missing required field" do
    assert {:error, {:missing_field, :ramp_id}} =
             BeamPM.Codec.from_map(:ramp_commitment, %{})
  end


  test "receipt_replay_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      replay_result_hash: "sample_replay_result_hash",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ReceiptReplayEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["replay_result_hash"] == "sample_replay_result_hash"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:receipt_replay_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:receipt_replay_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "receipt_replay_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      replay_result_hash: "sample_replay_result_hash",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ReceiptReplayEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:receipt_replay_evidence, json)
  end

  test "receipt_replay_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:receipt_replay_evidence, %{})
  end


  test "receipt_replay_request to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      receipt_id: "sample_receipt_id",
      replay_request_hash: "sample_replay_request_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ReceiptReplayRequest.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["receipt_id"] == "sample_receipt_id"
    assert m["replay_request_hash"] == "sample_replay_request_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:receipt_replay_request, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:receipt_replay_request, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "receipt_replay_request encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      receipt_id: "sample_receipt_id",
      replay_request_hash: "sample_replay_request_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ReceiptReplayRequest.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:receipt_replay_request, json)
  end

  test "receipt_replay_request from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:receipt_replay_request, %{})
  end


  test "receipt_signature to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      receipt_id: "sample_receipt_id",
      signature_hash: "sample_signature_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ReceiptSignature.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["receipt_id"] == "sample_receipt_id"
    assert m["signature_hash"] == "sample_signature_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:receipt_signature, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:receipt_signature, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "receipt_signature encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      receipt_id: "sample_receipt_id",
      signature_hash: "sample_signature_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ReceiptSignature.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:receipt_signature, json)
  end

  test "receipt_signature from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:receipt_signature, %{})
  end


  test "receipt_subject_binding to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      workload_id: "sample_workload_id",
      binding_hash: "sample_binding_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ReceiptSubjectBinding.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["workload_id"] == "sample_workload_id"
    assert m["binding_hash"] == "sample_binding_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:receipt_subject_binding, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:receipt_subject_binding, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "receipt_subject_binding encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      workload_id: "sample_workload_id",
      binding_hash: "sample_binding_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ReceiptSubjectBinding.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:receipt_subject_binding, json)
  end

  test "receipt_subject_binding from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:receipt_subject_binding, %{})
  end


  test "receipt_verification to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      receipt_id: "sample_receipt_id",
      verification_hash: "sample_verification_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ReceiptVerification.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["receipt_id"] == "sample_receipt_id"
    assert m["verification_hash"] == "sample_verification_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:receipt_verification, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:receipt_verification, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "receipt_verification encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      receipt_id: "sample_receipt_id",
      verification_hash: "sample_verification_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ReceiptVerification.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:receipt_verification, json)
  end

  test "receipt_verification from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:receipt_verification, %{})
  end


  test "recovery_plan to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      recovery_plan_id: "sample_recovery_plan_id",
      account_id: "sample_account_id",
      recovery_time_hours: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RecoveryPlan.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["recovery_plan_id"] == "sample_recovery_plan_id"
    assert m["account_id"] == "sample_account_id"
    assert m["recovery_time_hours"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:recovery_plan, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:recovery_plan, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "recovery_plan encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      recovery_plan_id: "sample_recovery_plan_id",
      account_id: "sample_account_id",
      recovery_time_hours: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RecoveryPlan.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:recovery_plan, json)
  end

  test "recovery_plan from_map reports the first missing required field" do
    assert {:error, {:missing_field, :recovery_plan_id}} =
             BeamPM.Codec.from_map(:recovery_plan, %{})
  end


  test "recovery_point_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      incident_id: "sample_incident_id",
      recovery_hash: "sample_recovery_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RecoveryPointReceipt.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["incident_id"] == "sample_incident_id"
    assert m["recovery_hash"] == "sample_recovery_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:recovery_point_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:recovery_point_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "recovery_point_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      incident_id: "sample_incident_id",
      recovery_hash: "sample_recovery_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RecoveryPointReceipt.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:recovery_point_receipt, json)
  end

  test "recovery_point_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:recovery_point_receipt, %{})
  end


  test "recovery_time_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      incident_id: "sample_incident_id",
      recovery_hash: "sample_recovery_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RecoveryTimeReceipt.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["incident_id"] == "sample_incident_id"
    assert m["recovery_hash"] == "sample_recovery_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:recovery_time_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:recovery_time_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "recovery_time_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      incident_id: "sample_incident_id",
      recovery_hash: "sample_recovery_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RecoveryTimeReceipt.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:recovery_time_receipt, json)
  end

  test "recovery_time_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:recovery_time_receipt, %{})
  end


  test "remediation_sla_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      remediation_due_at: "2026-08-29T12:00:00Z",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.RemediationSlaEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["remediation_due_at"] == "2026-08-29T12:00:00Z"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:remediation_sla_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:remediation_sla_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "remediation_sla_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      remediation_due_at: "2026-08-29T12:00:00Z",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.RemediationSlaEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:remediation_sla_evidence, json)
  end

  test "remediation_sla_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:remediation_sla_evidence, %{})
  end


  test "renewal_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      renewal_evidence_id: "sample_renewal_evidence_id",
      account_id: "sample_account_id",
      renewal_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RenewalEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["renewal_evidence_id"] == "sample_renewal_evidence_id"
    assert m["account_id"] == "sample_account_id"
    assert m["renewal_value"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:renewal_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:renewal_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "renewal_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      renewal_evidence_id: "sample_renewal_evidence_id",
      account_id: "sample_account_id",
      renewal_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RenewalEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:renewal_evidence, json)
  end

  test "renewal_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :renewal_evidence_id}} =
             BeamPM.Codec.from_map(:renewal_evidence, %{})
  end


  test "renewal_health to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      renewal_health_id: "sample_renewal_health_id",
      account_id: "sample_account_id",
      renewal_health_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RenewalHealth.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["renewal_health_id"] == "sample_renewal_health_id"
    assert m["account_id"] == "sample_account_id"
    assert m["renewal_health_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:renewal_health, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:renewal_health, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "renewal_health encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      renewal_health_id: "sample_renewal_health_id",
      account_id: "sample_account_id",
      renewal_health_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RenewalHealth.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:renewal_health, json)
  end

  test "renewal_health from_map reports the first missing required field" do
    assert {:error, {:missing_field, :renewal_health_id}} =
             BeamPM.Codec.from_map(:renewal_health, %{})
  end


  test "renewal_option to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      option_id: "sample_option_id",
      subscription_id: "sample_subscription_id",
      term_months: 42,
      notice_by: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RenewalOption.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["option_id"] == "sample_option_id"
    assert m["subscription_id"] == "sample_subscription_id"
    assert m["term_months"] == 42
    assert m["notice_by"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:renewal_option, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:renewal_option, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "renewal_option encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      option_id: "sample_option_id",
      subscription_id: "sample_subscription_id",
      term_months: 42,
      notice_by: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RenewalOption.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:renewal_option, json)
  end

  test "renewal_option from_map reports the first missing required field" do
    assert {:error, {:missing_field, :option_id}} =
             BeamPM.Codec.from_map(:renewal_option, %{})
  end


  test "renewal_risk to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      renewal_risk_id: "sample_renewal_risk_id",
      account_id: "sample_account_id",
      renewal_risk_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RenewalRisk.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["renewal_risk_id"] == "sample_renewal_risk_id"
    assert m["account_id"] == "sample_account_id"
    assert m["renewal_risk_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:renewal_risk, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:renewal_risk, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "renewal_risk encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      renewal_risk_id: "sample_renewal_risk_id",
      account_id: "sample_account_id",
      renewal_risk_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RenewalRisk.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:renewal_risk, json)
  end

  test "renewal_risk from_map reports the first missing required field" do
    assert {:error, {:missing_field, :renewal_risk_id}} =
             BeamPM.Codec.from_map(:renewal_risk, %{})
  end


  test "renewal_term_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      renewal_term: "sample_renewal_term",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.RenewalTermAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["renewal_term"] == "sample_renewal_term"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:renewal_term_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:renewal_term_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "renewal_term_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      renewal_term: "sample_renewal_term",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.RenewalTermAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:renewal_term_admission, json)
  end

  test "renewal_term_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:renewal_term_admission, %{})
  end


  test "replay_environment_identity to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      environment_id: "sample_environment_id",
      identity_hash: "sample_identity_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ReplayEnvironmentIdentity.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["environment_id"] == "sample_environment_id"
    assert m["identity_hash"] == "sample_identity_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:replay_environment_identity, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:replay_environment_identity, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "replay_environment_identity encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      environment_id: "sample_environment_id",
      identity_hash: "sample_identity_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ReplayEnvironmentIdentity.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:replay_environment_identity, json)
  end

  test "replay_environment_identity from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:replay_environment_identity, %{})
  end


  test "reproducible_build_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      build_digest: "sample_build_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ReproducibleBuildEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["build_digest"] == "sample_build_digest"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:reproducible_build_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:reproducible_build_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "reproducible_build_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      build_digest: "sample_build_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ReproducibleBuildEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:reproducible_build_evidence, json)
  end

  test "reproducible_build_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:reproducible_build_evidence, %{})
  end


  test "reseller_authorization to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      authorization_id: "sample_authorization_id",
      reseller_id: "sample_reseller_id",
      sku: "sample_sku",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ResellerAuthorization.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["authorization_id"] == "sample_authorization_id"
    assert m["reseller_id"] == "sample_reseller_id"
    assert m["sku"] == "sample_sku"
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:reseller_authorization, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:reseller_authorization, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "reseller_authorization encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      authorization_id: "sample_authorization_id",
      reseller_id: "sample_reseller_id",
      sku: "sample_sku",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ResellerAuthorization.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:reseller_authorization, json)
  end

  test "reseller_authorization from_map reports the first missing required field" do
    assert {:error, {:missing_field, :authorization_id}} =
             BeamPM.Codec.from_map(:reseller_authorization, %{})
  end


  test "residency_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      region: "sample_region",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ResidencyEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["region"] == "sample_region"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:residency_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:residency_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "residency_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      region: "sample_region",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ResidencyEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:residency_evidence, json)
  end

  test "residency_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:residency_evidence, %{})
  end


  test "resource_allocation to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      resource_id: "sample_resource_id",
      activity: "sample_activity",
      event_id: "sample_event_id"
    }

    assert {:ok, rec} = BeamPM.Types.ResourceAllocation.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["resource_id"] == "sample_resource_id"
    assert m["activity"] == "sample_activity"
    assert m["event_id"] == "sample_event_id"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:resource_allocation, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:resource_allocation, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "resource_allocation encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      resource_id: "sample_resource_id",
      activity: "sample_activity",
      event_id: "sample_event_id"
    }

    assert {:ok, rec} = BeamPM.Types.ResourceAllocation.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:resource_allocation, json)
  end

  test "resource_allocation from_map reports the first missing required field" do
    assert {:error, {:missing_field, :resource_id}} =
             BeamPM.Codec.from_map(:resource_allocation, %{})
  end


  test "retention_policy_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      retention_days: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.RetentionPolicyEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["retention_days"] == 42
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:retention_policy_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:retention_policy_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "retention_policy_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      retention_days: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.RetentionPolicyEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:retention_policy_evidence, json)
  end

  test "retention_policy_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:retention_policy_evidence, %{})
  end


  test "revenue_attribution to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      revenue_attribution_id: "sample_revenue_attribution_id",
      account_id: "sample_account_id",
      attributed_revenue: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RevenueAttribution.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["revenue_attribution_id"] == "sample_revenue_attribution_id"
    assert m["account_id"] == "sample_account_id"
    assert m["attributed_revenue"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:revenue_attribution, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:revenue_attribution, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "revenue_attribution encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      revenue_attribution_id: "sample_revenue_attribution_id",
      account_id: "sample_account_id",
      attributed_revenue: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RevenueAttribution.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:revenue_attribution, json)
  end

  test "revenue_attribution from_map reports the first missing required field" do
    assert {:error, {:missing_field, :revenue_attribution_id}} =
             BeamPM.Codec.from_map(:revenue_attribution, %{})
  end


  test "revenue_contract_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      contract_id: "sample_contract_id",
      admission_receipt_hash: "sample_admission_receipt_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RevenueContractAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["contract_id"] == "sample_contract_id"
    assert m["admission_receipt_hash"] == "sample_admission_receipt_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:revenue_contract_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:revenue_contract_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "revenue_contract_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      contract_id: "sample_contract_id",
      admission_receipt_hash: "sample_admission_receipt_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RevenueContractAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:revenue_contract_admission, json)
  end

  test "revenue_contract_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:revenue_contract_admission, %{})
  end


  test "revenue_schedule_assumption to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      schedule_id: "sample_schedule_id",
      assumption_evidence_hash: "sample_assumption_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RevenueScheduleAssumption.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["schedule_id"] == "sample_schedule_id"
    assert m["assumption_evidence_hash"] == "sample_assumption_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:revenue_schedule_assumption, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:revenue_schedule_assumption, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "revenue_schedule_assumption encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      schedule_id: "sample_schedule_id",
      assumption_evidence_hash: "sample_assumption_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RevenueScheduleAssumption.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:revenue_schedule_assumption, json)
  end

  test "revenue_schedule_assumption from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:revenue_schedule_assumption, %{})
  end


  test "rework_cost to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      case_id: "sample_case_id",
      loop_count: 42,
      weighted_cost: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ReworkCost.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["case_id"] == "sample_case_id"
    assert m["loop_count"] == 42
    assert m["weighted_cost"] == 3.5
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:rework_cost, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:rework_cost, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "rework_cost encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      case_id: "sample_case_id",
      loop_count: 42,
      weighted_cost: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ReworkCost.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:rework_cost, json)
  end

  test "rework_cost from_map reports the first missing required field" do
    assert {:error, {:missing_field, :case_id}} =
             BeamPM.Codec.from_map(:rework_cost, %{})
  end


  test "rfp_response_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      answer_set_hash: "sample_answer_set_hash",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.RfpResponseEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["answer_set_hash"] == "sample_answer_set_hash"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:rfp_response_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:rfp_response_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "rfp_response_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      answer_set_hash: "sample_answer_set_hash",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.RfpResponseEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:rfp_response_evidence, json)
  end

  test "rfp_response_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:rfp_response_evidence, %{})
  end


  test "rollback_checkpoint to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      checkpoint_id: "sample_checkpoint_id",
      state_hash: "sample_state_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RollbackCheckpoint.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["checkpoint_id"] == "sample_checkpoint_id"
    assert m["state_hash"] == "sample_state_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:rollback_checkpoint, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:rollback_checkpoint, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "rollback_checkpoint encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      checkpoint_id: "sample_checkpoint_id",
      state_hash: "sample_state_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RollbackCheckpoint.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:rollback_checkpoint, json)
  end

  test "rollback_checkpoint from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:rollback_checkpoint, %{})
  end


  test "rollback_decision to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      rollback_decision_id: "sample_rollback_decision_id",
      account_id: "sample_account_id",
      rollback_result: "sample_rollback_result",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RollbackDecision.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["rollback_decision_id"] == "sample_rollback_decision_id"
    assert m["account_id"] == "sample_account_id"
    assert m["rollback_result"] == "sample_rollback_result"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:rollback_decision, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:rollback_decision, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "rollback_decision encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      rollback_decision_id: "sample_rollback_decision_id",
      account_id: "sample_account_id",
      rollback_result: "sample_rollback_result",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.RollbackDecision.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:rollback_decision, json)
  end

  test "rollback_decision from_map reports the first missing required field" do
    assert {:error, {:missing_field, :rollback_decision_id}} =
             BeamPM.Codec.from_map(:rollback_decision, %{})
  end


  test "rollback_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      rollback_digest: "sample_rollback_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.RollbackEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["rollback_digest"] == "sample_rollback_digest"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:rollback_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:rollback_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "rollback_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      rollback_digest: "sample_rollback_digest",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.RollbackEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:rollback_evidence, json)
  end

  test "rollback_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:rollback_evidence, %{})
  end


  test "rolling_upgrade_plan to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      upgrade_id: "sample_upgrade_id",
      plan_hash: "sample_plan_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RollingUpgradePlan.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["upgrade_id"] == "sample_upgrade_id"
    assert m["plan_hash"] == "sample_plan_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:rolling_upgrade_plan, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:rolling_upgrade_plan, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "rolling_upgrade_plan encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      upgrade_id: "sample_upgrade_id",
      plan_hash: "sample_plan_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RollingUpgradePlan.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:rolling_upgrade_plan, json)
  end

  test "rolling_upgrade_plan from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:rolling_upgrade_plan, %{})
  end


  test "runtime_policy_decision to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      decision_id: "sample_decision_id",
      policy_hash: "sample_policy_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RuntimePolicyDecision.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["decision_id"] == "sample_decision_id"
    assert m["policy_hash"] == "sample_policy_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:runtime_policy_decision, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:runtime_policy_decision, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "runtime_policy_decision encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      decision_id: "sample_decision_id",
      policy_hash: "sample_policy_hash"
    }

    assert {:ok, rec} = BeamPM.Types.RuntimePolicyDecision.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:runtime_policy_decision, json)
  end

  test "runtime_policy_decision from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:runtime_policy_decision, %{})
  end


  test "sanctions_screening_result to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      account_id: "sample_account_id",
      screening_id: "sample_screening_id",
      screening_result: "sample_screening_result"
    }

    assert {:ok, rec} = BeamPM.Types.SanctionsScreeningResult.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["account_id"] == "sample_account_id"
    assert m["screening_id"] == "sample_screening_id"
    assert m["screening_result"] == "sample_screening_result"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:sanctions_screening_result, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:sanctions_screening_result, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "sanctions_screening_result encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      account_id: "sample_account_id",
      screening_id: "sample_screening_id",
      screening_result: "sample_screening_result"
    }

    assert {:ok, rec} = BeamPM.Types.SanctionsScreeningResult.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:sanctions_screening_result, json)
  end

  test "sanctions_screening_result from_map reports the first missing required field" do
    assert {:error, {:missing_field, :account_id}} =
             BeamPM.Codec.from_map(:sanctions_screening_result, %{})
  end


  test "sbom_inventory_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      component_count: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SbomInventoryEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["component_count"] == 42
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:sbom_inventory_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:sbom_inventory_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "sbom_inventory_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      component_count: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SbomInventoryEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:sbom_inventory_evidence, json)
  end

  test "sbom_inventory_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:sbom_inventory_evidence, %{})
  end


  test "secret_boundary_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      secret_source: "sample_secret_source",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SecretBoundaryEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["secret_source"] == "sample_secret_source"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:secret_boundary_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:secret_boundary_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "secret_boundary_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      secret_source: "sample_secret_source",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SecretBoundaryEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:secret_boundary_evidence, json)
  end

  test "secret_boundary_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:secret_boundary_evidence, %{})
  end


  test "security_addendum_state to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      addendum_id: "sample_addendum_id",
      addendum_state: "sample_addendum_state"
    }

    assert {:ok, rec} = BeamPM.Types.SecurityAddendumState.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["addendum_id"] == "sample_addendum_id"
    assert m["addendum_state"] == "sample_addendum_state"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:security_addendum_state, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:security_addendum_state, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "security_addendum_state encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      addendum_id: "sample_addendum_id",
      addendum_state: "sample_addendum_state"
    }

    assert {:ok, rec} = BeamPM.Types.SecurityAddendumState.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:security_addendum_state, json)
  end

  test "security_addendum_state from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:security_addendum_state, %{})
  end


  test "security_blocker to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      blocker_id: "sample_blocker_id",
      refusal_code: "sample_refusal_code"
    }

    assert {:ok, rec} = BeamPM.Types.SecurityBlocker.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["blocker_id"] == "sample_blocker_id"
    assert m["refusal_code"] == "sample_refusal_code"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:security_blocker, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:security_blocker, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "security_blocker encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      blocker_id: "sample_blocker_id",
      refusal_code: "sample_refusal_code"
    }

    assert {:ok, rec} = BeamPM.Types.SecurityBlocker.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:security_blocker, json)
  end

  test "security_blocker from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:security_blocker, %{})
  end


  test "security_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      security_readiness_id: "sample_security_readiness_id",
      account_id: "sample_account_id",
      control_coverage: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.SecurityReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["security_readiness_id"] == "sample_security_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["control_coverage"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:security_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:security_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "security_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      security_readiness_id: "sample_security_readiness_id",
      account_id: "sample_account_id",
      control_coverage: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.SecurityReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:security_readiness, json)
  end

  test "security_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :security_readiness_id}} =
             BeamPM.Codec.from_map(:security_readiness, %{})
  end


  test "service_credit to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      credit_id: "sample_credit_id",
      slo_id: "sample_slo_id",
      amount: 3.5,
      currency: "sample_currency"
    }

    assert {:ok, rec} = BeamPM.Types.ServiceCredit.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["credit_id"] == "sample_credit_id"
    assert m["slo_id"] == "sample_slo_id"
    assert m["amount"] == 3.5
    assert m["currency"] == "sample_currency"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:service_credit, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:service_credit, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "service_credit encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      credit_id: "sample_credit_id",
      slo_id: "sample_slo_id",
      amount: 3.5,
      currency: "sample_currency"
    }

    assert {:ok, rec} = BeamPM.Types.ServiceCredit.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:service_credit, json)
  end

  test "service_credit from_map reports the first missing required field" do
    assert {:error, {:missing_field, :credit_id}} =
             BeamPM.Codec.from_map(:service_credit, %{})
  end


  test "service_credit_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      service_credit_id: "sample_service_credit_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.ServiceCreditAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["service_credit_id"] == "sample_service_credit_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:service_credit_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:service_credit_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "service_credit_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      service_credit_id: "sample_service_credit_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.ServiceCreditAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:service_credit_admission, json)
  end

  test "service_credit_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:service_credit_admission, %{})
  end


  test "service_health_snapshot to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      service_id: "sample_service_id",
      snapshot_hash: "sample_snapshot_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ServiceHealthSnapshot.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["service_id"] == "sample_service_id"
    assert m["snapshot_hash"] == "sample_snapshot_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:service_health_snapshot, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:service_health_snapshot, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "service_health_snapshot encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      service_id: "sample_service_id",
      snapshot_hash: "sample_snapshot_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ServiceHealthSnapshot.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:service_health_snapshot, json)
  end

  test "service_health_snapshot from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:service_health_snapshot, %{})
  end


  test "service_level_objective to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      slo_id: "sample_slo_id",
      contract_id: "sample_contract_id",
      target_percent: 3.5,
      measurement_window: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ServiceLevelObjective.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["slo_id"] == "sample_slo_id"
    assert m["contract_id"] == "sample_contract_id"
    assert m["target_percent"] == 3.5
    assert m["measurement_window"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:service_level_objective, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:service_level_objective, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "service_level_objective encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      slo_id: "sample_slo_id",
      contract_id: "sample_contract_id",
      target_percent: 3.5,
      measurement_window: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.ServiceLevelObjective.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:service_level_objective, json)
  end

  test "service_level_objective from_map reports the first missing required field" do
    assert {:error, {:missing_field, :slo_id}} =
             BeamPM.Codec.from_map(:service_level_objective, %{})
  end


  test "service_slo_contract to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      slo_id: "sample_slo_id",
      contract_hash: "sample_contract_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ServiceSloContract.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["slo_id"] == "sample_slo_id"
    assert m["contract_hash"] == "sample_contract_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:service_slo_contract, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:service_slo_contract, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "service_slo_contract encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      slo_id: "sample_slo_id",
      contract_hash: "sample_contract_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ServiceSloContract.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:service_slo_contract, json)
  end

  test "service_slo_contract from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:service_slo_contract, %{})
  end


  test "service_span to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      span_id: "sample_span_id",
      service_name: "sample_service_name",
      duration_ms: 42,
      parent_span_id: "sample_parent_span_id"
    }

    assert {:ok, rec} = BeamPM.Types.ServiceSpan.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["span_id"] == "sample_span_id"
    assert m["service_name"] == "sample_service_name"
    assert m["duration_ms"] == 42
    assert m["parent_span_id"] == "sample_parent_span_id"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:service_span, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:service_span, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "service_span encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      span_id: "sample_span_id",
      service_name: "sample_service_name",
      duration_ms: 42,
      parent_span_id: "sample_parent_span_id"
    }

    assert {:ok, rec} = BeamPM.Types.ServiceSpan.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:service_span, json)
  end

  test "service_span from_map reports the first missing required field" do
    assert {:error, {:missing_field, :span_id}} =
             BeamPM.Codec.from_map(:service_span, %{})
  end

  test "service_span to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      span_id: "sample_span_id",
      service_name: "sample_service_name",
      duration_ms: 42
    }

    assert {:ok, rec} = BeamPM.Types.ServiceSpan.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "parent_span_id")
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:service_span, m)
  end

  test "showback_allocation to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      allocation_id: "sample_allocation_id",
      project_id: "sample_project_id",
      metric_name: "sample_metric_name",
      quantity: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ShowbackAllocation.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["allocation_id"] == "sample_allocation_id"
    assert m["project_id"] == "sample_project_id"
    assert m["metric_name"] == "sample_metric_name"
    assert m["quantity"] == 3.5
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:showback_allocation, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:showback_allocation, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "showback_allocation encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      allocation_id: "sample_allocation_id",
      project_id: "sample_project_id",
      metric_name: "sample_metric_name",
      quantity: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ShowbackAllocation.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:showback_allocation, json)
  end

  test "showback_allocation from_map reports the first missing required field" do
    assert {:error, {:missing_field, :allocation_id}} =
             BeamPM.Codec.from_map(:showback_allocation, %{})
  end


  test "signature_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      signature_identity: "sample_signature_identity",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SignatureEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["signature_identity"] == "sample_signature_identity"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:signature_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:signature_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "signature_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      signature_identity: "sample_signature_identity",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SignatureEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:signature_evidence, json)
  end

  test "signature_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:signature_evidence, %{})
  end


  test "sku_definition to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      sku: "sample_sku",
      edition_id: "sample_edition_id",
      billing_model: :sample_atom,
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SkuDefinition.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["sku"] == "sample_sku"
    assert m["edition_id"] == "sample_edition_id"
    assert m["billing_model"] == "sample_atom"
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:sku_definition, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:sku_definition, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "sku_definition encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      sku: "sample_sku",
      edition_id: "sample_edition_id",
      billing_model: :sample_atom,
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SkuDefinition.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:sku_definition, json)
  end

  test "sku_definition from_map reports the first missing required field" do
    assert {:error, {:missing_field, :sku}} =
             BeamPM.Codec.from_map(:sku_definition, %{})
  end


  test "sla_offer_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      sla_offer_id: "sample_sla_offer_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.SlaOfferAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["sla_offer_id"] == "sample_sla_offer_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:sla_offer_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:sla_offer_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "sla_offer_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      sla_offer_id: "sample_sla_offer_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.SlaOfferAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:sla_offer_admission, json)
  end

  test "sla_offer_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:sla_offer_admission, %{})
  end


  test "sojourn_time to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      object_id: "sample_object_id",
      event_type: "sample_event_type",
      seconds: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.SojournTime.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["object_id"] == "sample_object_id"
    assert m["event_type"] == "sample_event_type"
    assert m["seconds"] == 3.5
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:sojourn_time, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:sojourn_time, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "sojourn_time encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      object_id: "sample_object_id",
      event_type: "sample_event_type",
      seconds: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.SojournTime.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:sojourn_time, json)
  end

  test "sojourn_time from_map reports the first missing required field" do
    assert {:error, {:missing_field, :object_id}} =
             BeamPM.Codec.from_map(:sojourn_time, %{})
  end


  test "solution_fit to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      solution_fit_id: "sample_solution_fit_id",
      account_id: "sample_account_id",
      fit_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.SolutionFit.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["solution_fit_id"] == "sample_solution_fit_id"
    assert m["account_id"] == "sample_account_id"
    assert m["fit_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:solution_fit, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:solution_fit, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "solution_fit encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      solution_fit_id: "sample_solution_fit_id",
      account_id: "sample_account_id",
      fit_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.SolutionFit.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:solution_fit, json)
  end

  test "solution_fit from_map reports the first missing required field" do
    assert {:error, {:missing_field, :solution_fit_id}} =
             BeamPM.Codec.from_map(:solution_fit, %{})
  end


  test "stakeholder_map to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      stakeholder_map_id: "sample_stakeholder_map_id",
      account_id: "sample_account_id",
      stakeholder_count: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.StakeholderMap.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["stakeholder_map_id"] == "sample_stakeholder_map_id"
    assert m["account_id"] == "sample_account_id"
    assert m["stakeholder_count"] == 42
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:stakeholder_map, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:stakeholder_map, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "stakeholder_map encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      stakeholder_map_id: "sample_stakeholder_map_id",
      account_id: "sample_account_id",
      stakeholder_count: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.StakeholderMap.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:stakeholder_map, json)
  end

  test "stakeholder_map from_map reports the first missing required field" do
    assert {:error, {:missing_field, :stakeholder_map_id}} =
             BeamPM.Codec.from_map(:stakeholder_map, %{})
  end


  test "stale_receipt_refusal to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      receipt_id: "sample_receipt_id",
      refusal_hash: "sample_refusal_hash"
    }

    assert {:ok, rec} = BeamPM.Types.StaleReceiptRefusal.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["receipt_id"] == "sample_receipt_id"
    assert m["refusal_hash"] == "sample_refusal_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:stale_receipt_refusal, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:stale_receipt_refusal, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "stale_receipt_refusal encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      receipt_id: "sample_receipt_id",
      refusal_hash: "sample_refusal_hash"
    }

    assert {:ok, rec} = BeamPM.Types.StaleReceiptRefusal.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:stale_receipt_refusal, json)
  end

  test "stale_receipt_refusal from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:stale_receipt_refusal, %{})
  end


  test "stale_subject_refusal_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      stale_sha: "sample_stale_sha",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.StaleSubjectRefusalEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["stale_sha"] == "sample_stale_sha"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:stale_subject_refusal_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:stale_subject_refusal_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "stale_subject_refusal_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      stale_sha: "sample_stale_sha",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.StaleSubjectRefusalEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:stale_subject_refusal_evidence, json)
  end

  test "stale_subject_refusal_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:stale_subject_refusal_evidence, %{})
  end


  test "success_plan to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      success_plan_id: "sample_success_plan_id",
      account_id: "sample_account_id",
      success_target: "sample_success_target",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.SuccessPlan.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["success_plan_id"] == "sample_success_plan_id"
    assert m["account_id"] == "sample_account_id"
    assert m["success_target"] == "sample_success_target"
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:success_plan, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:success_plan, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "success_plan encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      success_plan_id: "sample_success_plan_id",
      account_id: "sample_account_id",
      success_target: "sample_success_target",
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.SuccessPlan.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:success_plan, json)
  end

  test "success_plan from_map reports the first missing required field" do
    assert {:error, {:missing_field, :success_plan_id}} =
             BeamPM.Codec.from_map(:success_plan, %{})
  end


  test "supervisor_restart_policy to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      supervisor_id: "sample_supervisor_id",
      policy_hash: "sample_policy_hash"
    }

    assert {:ok, rec} = BeamPM.Types.SupervisorRestartPolicy.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["supervisor_id"] == "sample_supervisor_id"
    assert m["policy_hash"] == "sample_policy_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:supervisor_restart_policy, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:supervisor_restart_policy, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "supervisor_restart_policy encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      supervisor_id: "sample_supervisor_id",
      policy_hash: "sample_policy_hash"
    }

    assert {:ok, rec} = BeamPM.Types.SupervisorRestartPolicy.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:supervisor_restart_policy, json)
  end

  test "supervisor_restart_policy from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:supervisor_restart_policy, %{})
  end


  test "support_contract to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      contract_id: "sample_contract_id",
      account_id: "sample_account_id",
      tier: :sample_atom,
      valid_until: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.SupportContract.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["contract_id"] == "sample_contract_id"
    assert m["account_id"] == "sample_account_id"
    assert m["tier"] == "sample_atom"
    assert m["valid_until"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:support_contract, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:support_contract, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "support_contract encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      contract_id: "sample_contract_id",
      account_id: "sample_account_id",
      tier: :sample_atom,
      valid_until: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.SupportContract.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:support_contract, json)
  end

  test "support_contract from_map reports the first missing required field" do
    assert {:error, {:missing_field, :contract_id}} =
             BeamPM.Codec.from_map(:support_contract, %{})
  end


  test "support_diagnostic_bundle to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      incident_id: "sample_incident_id",
      bundle_hash: "sample_bundle_hash"
    }

    assert {:ok, rec} = BeamPM.Types.SupportDiagnosticBundle.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["incident_id"] == "sample_incident_id"
    assert m["bundle_hash"] == "sample_bundle_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:support_diagnostic_bundle, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:support_diagnostic_bundle, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "support_diagnostic_bundle encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      incident_id: "sample_incident_id",
      bundle_hash: "sample_bundle_hash"
    }

    assert {:ok, rec} = BeamPM.Types.SupportDiagnosticBundle.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:support_diagnostic_bundle, json)
  end

  test "support_diagnostic_bundle from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:support_diagnostic_bundle, %{})
  end


  test "support_escalation_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      escalation_owner: "sample_escalation_owner",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SupportEscalationEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["escalation_owner"] == "sample_escalation_owner"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:support_escalation_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:support_escalation_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "support_escalation_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      escalation_owner: "sample_escalation_owner",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SupportEscalationEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:support_escalation_evidence, json)
  end

  test "support_escalation_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:support_escalation_evidence, %{})
  end


  test "support_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      support_readiness_id: "sample_support_readiness_id",
      account_id: "sample_account_id",
      support_readiness_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.SupportReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["support_readiness_id"] == "sample_support_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["support_readiness_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:support_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:support_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "support_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      support_readiness_id: "sample_support_readiness_id",
      account_id: "sample_account_id",
      support_readiness_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.SupportReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:support_readiness, json)
  end

  test "support_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :support_readiness_id}} =
             BeamPM.Codec.from_map(:support_readiness, %{})
  end


  test "support_sla_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      response_minutes: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SupportSlaEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["response_minutes"] == 42
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:support_sla_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:support_sla_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "support_sla_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      response_minutes: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SupportSlaEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:support_sla_evidence, json)
  end

  test "support_sla_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:support_sla_evidence, %{})
  end


  test "support_tier_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      support_tier_id: "sample_support_tier_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.SupportTierAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["support_tier_id"] == "sample_support_tier_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:support_tier_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:support_tier_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "support_tier_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      support_tier_id: "sample_support_tier_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.SupportTierAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:support_tier_admission, json)
  end

  test "support_tier_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:support_tier_admission, %{})
  end


  test "support_window_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      support_channel: "sample_support_channel",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SupportWindowEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["support_channel"] == "sample_support_channel"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:support_window_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:support_window_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "support_window_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      support_channel: "sample_support_channel",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.SupportWindowEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:support_window_evidence, json)
  end

  test "support_window_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:support_window_evidence, %{})
  end


  test "sync_time to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      object_id: "sample_object_id",
      delaying_object_id: "sample_delaying_object_id",
      seconds: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.SyncTime.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["object_id"] == "sample_object_id"
    assert m["delaying_object_id"] == "sample_delaying_object_id"
    assert m["seconds"] == 3.5
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:sync_time, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:sync_time, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "sync_time encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      object_id: "sample_object_id",
      delaying_object_id: "sample_delaying_object_id",
      seconds: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.SyncTime.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:sync_time, json)
  end

  test "sync_time from_map reports the first missing required field" do
    assert {:error, {:missing_field, :object_id}} =
             BeamPM.Codec.from_map(:sync_time, %{})
  end

  test "sync_time to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      object_id: "sample_object_id",
      seconds: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.SyncTime.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    refute Map.has_key?(m, "delaying_object_id")
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:sync_time, m)
  end

  test "target_metric to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      target_metric_id: "sample_target_metric_id",
      account_id: "sample_account_id",
      target_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.TargetMetric.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["target_metric_id"] == "sample_target_metric_id"
    assert m["account_id"] == "sample_account_id"
    assert m["target_value"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:target_metric, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:target_metric, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "target_metric encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      target_metric_id: "sample_target_metric_id",
      account_id: "sample_account_id",
      target_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.TargetMetric.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:target_metric, json)
  end

  test "target_metric from_map reports the first missing required field" do
    assert {:error, {:missing_field, :target_metric_id}} =
             BeamPM.Codec.from_map(:target_metric, %{})
  end


  test "tax_jurisdiction_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      contracting_entity_id: "sample_contracting_entity_id",
      tax_jurisdiction: "sample_tax_jurisdiction",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.TaxJurisdictionEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["contracting_entity_id"] == "sample_contracting_entity_id"
    assert m["tax_jurisdiction"] == "sample_tax_jurisdiction"
    assert m["evidence_hash"] == "sample_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:tax_jurisdiction_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:tax_jurisdiction_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "tax_jurisdiction_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      contracting_entity_id: "sample_contracting_entity_id",
      tax_jurisdiction: "sample_tax_jurisdiction",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.TaxJurisdictionEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:tax_jurisdiction_evidence, json)
  end

  test "tax_jurisdiction_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :contracting_entity_id}} =
             BeamPM.Codec.from_map(:tax_jurisdiction_evidence, %{})
  end


  test "technical_blocker to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      blocker_id: "sample_blocker_id",
      refusal_code: "sample_refusal_code"
    }

    assert {:ok, rec} = BeamPM.Types.TechnicalBlocker.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["blocker_id"] == "sample_blocker_id"
    assert m["refusal_code"] == "sample_refusal_code"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:technical_blocker, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:technical_blocker, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "technical_blocker encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      blocker_id: "sample_blocker_id",
      refusal_code: "sample_refusal_code"
    }

    assert {:ok, rec} = BeamPM.Types.TechnicalBlocker.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:technical_blocker, json)
  end

  test "technical_blocker from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:technical_blocker, %{})
  end


  test "tenant_account to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      account_id: "sample_account_id",
      home_region: "sample_home_region",
      edition_id: "sample_edition_id"
    }

    assert {:ok, rec} = BeamPM.Types.TenantAccount.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["account_id"] == "sample_account_id"
    assert m["home_region"] == "sample_home_region"
    assert m["edition_id"] == "sample_edition_id"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:tenant_account, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:tenant_account, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "tenant_account encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      account_id: "sample_account_id",
      home_region: "sample_home_region",
      edition_id: "sample_edition_id"
    }

    assert {:ok, rec} = BeamPM.Types.TenantAccount.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:tenant_account, json)
  end

  test "tenant_account from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:tenant_account, %{})
  end


  test "tenant_data_partition to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      partition_id: "sample_partition_id",
      isolation_hash: "sample_isolation_hash"
    }

    assert {:ok, rec} = BeamPM.Types.TenantDataPartition.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["partition_id"] == "sample_partition_id"
    assert m["isolation_hash"] == "sample_isolation_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:tenant_data_partition, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:tenant_data_partition, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "tenant_data_partition encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      partition_id: "sample_partition_id",
      isolation_hash: "sample_isolation_hash"
    }

    assert {:ok, rec} = BeamPM.Types.TenantDataPartition.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:tenant_data_partition, json)
  end

  test "tenant_data_partition from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:tenant_data_partition, %{})
  end


  test "tenant_key_scope to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      key_scope_id: "sample_key_scope_id",
      key_attestation_hash: "sample_key_attestation_hash"
    }

    assert {:ok, rec} = BeamPM.Types.TenantKeyScope.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["key_scope_id"] == "sample_key_scope_id"
    assert m["key_attestation_hash"] == "sample_key_attestation_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:tenant_key_scope, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:tenant_key_scope, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "tenant_key_scope encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      key_scope_id: "sample_key_scope_id",
      key_attestation_hash: "sample_key_attestation_hash"
    }

    assert {:ok, rec} = BeamPM.Types.TenantKeyScope.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:tenant_key_scope, json)
  end

  test "tenant_key_scope from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:tenant_key_scope, %{})
  end


  test "tenant_project to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      project_id: "sample_project_id",
      tenant_id: "sample_tenant_id",
      cost_center: "sample_cost_center",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.TenantProject.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["project_id"] == "sample_project_id"
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["cost_center"] == "sample_cost_center"
    assert m["status"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:tenant_project, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:tenant_project, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "tenant_project encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      project_id: "sample_project_id",
      tenant_id: "sample_tenant_id",
      cost_center: "sample_cost_center",
      status: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.TenantProject.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:tenant_project, json)
  end

  test "tenant_project from_map reports the first missing required field" do
    assert {:error, {:missing_field, :project_id}} =
             BeamPM.Codec.from_map(:tenant_project, %{})
  end


  test "tenant_resource_quota to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      quota_id: "sample_quota_id",
      utilization_hash: "sample_utilization_hash"
    }

    assert {:ok, rec} = BeamPM.Types.TenantResourceQuota.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["quota_id"] == "sample_quota_id"
    assert m["utilization_hash"] == "sample_utilization_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:tenant_resource_quota, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:tenant_resource_quota, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "tenant_resource_quota encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      quota_id: "sample_quota_id",
      utilization_hash: "sample_utilization_hash"
    }

    assert {:ok, rec} = BeamPM.Types.TenantResourceQuota.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:tenant_resource_quota, json)
  end

  test "tenant_resource_quota from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:tenant_resource_quota, %{})
  end


  test "tenant_runtime_boundary to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      boundary_id: "sample_boundary_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.TenantRuntimeBoundary.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["boundary_id"] == "sample_boundary_id"
    assert m["evidence_hash"] == "sample_evidence_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:tenant_runtime_boundary, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:tenant_runtime_boundary, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "tenant_runtime_boundary encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      boundary_id: "sample_boundary_id",
      evidence_hash: "sample_evidence_hash"
    }

    assert {:ok, rec} = BeamPM.Types.TenantRuntimeBoundary.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:tenant_runtime_boundary, json)
  end

  test "tenant_runtime_boundary from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:tenant_runtime_boundary, %{})
  end


  test "term_subscription to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      subscription_id: "sample_subscription_id",
      sku: "sample_sku",
      starts_at: "2026-08-29T12:00:00Z",
      ends_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.TermSubscription.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["subscription_id"] == "sample_subscription_id"
    assert m["sku"] == "sample_sku"
    assert m["starts_at"] == "2026-08-29T12:00:00Z"
    assert m["ends_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:term_subscription, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:term_subscription, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "term_subscription encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      subscription_id: "sample_subscription_id",
      sku: "sample_sku",
      starts_at: "2026-08-29T12:00:00Z",
      ends_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.TermSubscription.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:term_subscription, json)
  end

  test "term_subscription from_map reports the first missing required field" do
    assert {:error, {:missing_field, :subscription_id}} =
             BeamPM.Codec.from_map(:term_subscription, %{})
  end


  test "termination_right_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      termination_right_id: "sample_termination_right_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.TerminationRightAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["termination_right_id"] == "sample_termination_right_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:termination_right_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:termination_right_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "termination_right_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      termination_right_id: "sample_termination_right_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.TerminationRightAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:termination_right_admission, json)
  end

  test "termination_right_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:termination_right_admission, %{})
  end


  test "time_to_value to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      time_to_value_id: "sample_time_to_value_id",
      account_id: "sample_account_id",
      verified_days: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.TimeToValue.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["time_to_value_id"] == "sample_time_to_value_id"
    assert m["account_id"] == "sample_account_id"
    assert m["verified_days"] == 42
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:time_to_value, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:time_to_value, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "time_to_value encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      time_to_value_id: "sample_time_to_value_id",
      account_id: "sample_account_id",
      verified_days: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.TimeToValue.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:time_to_value, json)
  end

  test "time_to_value from_map reports the first missing required field" do
    assert {:error, {:missing_field, :time_to_value_id}} =
             BeamPM.Codec.from_map(:time_to_value, %{})
  end


  test "toolchain_identity to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      toolchain_id: "sample_toolchain_id",
      identity_hash: "sample_identity_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ToolchainIdentity.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["toolchain_id"] == "sample_toolchain_id"
    assert m["identity_hash"] == "sample_identity_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:toolchain_identity, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:toolchain_identity, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "toolchain_identity encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      toolchain_id: "sample_toolchain_id",
      identity_hash: "sample_identity_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ToolchainIdentity.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:toolchain_identity, json)
  end

  test "toolchain_identity from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:toolchain_identity, %{})
  end


  test "training_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      training_readiness_id: "sample_training_readiness_id",
      account_id: "sample_account_id",
      training_completion_rate: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.TrainingReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["training_readiness_id"] == "sample_training_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["training_completion_rate"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:training_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:training_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "training_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      training_readiness_id: "sample_training_readiness_id",
      account_id: "sample_account_id",
      training_completion_rate: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.TrainingReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:training_readiness, json)
  end

  test "training_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :training_readiness_id}} =
             BeamPM.Codec.from_map(:training_readiness, %{})
  end


  test "training_scope_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      training_scope_id: "sample_training_scope_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.TrainingScopeAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["training_scope_id"] == "sample_training_scope_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:training_scope_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:training_scope_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "training_scope_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      training_scope_id: "sample_training_scope_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.TrainingScopeAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:training_scope_admission, json)
  end

  test "training_scope_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:training_scope_admission, %{})
  end


  test "true_up_policy to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      policy_id: "sample_policy_id",
      commitment_id: "sample_commitment_id",
      cadence: :sample_atom,
      shortfall_behavior: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.TrueUpPolicy.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["policy_id"] == "sample_policy_id"
    assert m["commitment_id"] == "sample_commitment_id"
    assert m["cadence"] == "sample_atom"
    assert m["shortfall_behavior"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:true_up_policy, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:true_up_policy, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "true_up_policy encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      policy_id: "sample_policy_id",
      commitment_id: "sample_commitment_id",
      cadence: :sample_atom,
      shortfall_behavior: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.TrueUpPolicy.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:true_up_policy, json)
  end

  test "true_up_policy from_map reports the first missing required field" do
    assert {:error, {:missing_field, :policy_id}} =
             BeamPM.Codec.from_map(:true_up_policy, %{})
  end


  test "type_edge to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      source_type: "sample_source_type",
      target_type: "sample_target_type",
      qualifier: "sample_qualifier",
      direction: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.TypeEdge.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["source_type"] == "sample_source_type"
    assert m["target_type"] == "sample_target_type"
    assert m["qualifier"] == "sample_qualifier"
    assert m["direction"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:type_edge, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:type_edge, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "type_edge encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      source_type: "sample_source_type",
      target_type: "sample_target_type",
      qualifier: "sample_qualifier",
      direction: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.TypeEdge.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:type_edge, json)
  end

  test "type_edge from_map reports the first missing required field" do
    assert {:error, {:missing_field, :source_type}} =
             BeamPM.Codec.from_map(:type_edge, %{})
  end


  test "unsupported_capability_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      capability_name: "sample_capability_name",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.UnsupportedCapabilityEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["capability_name"] == "sample_capability_name"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:unsupported_capability_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:unsupported_capability_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "unsupported_capability_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      capability_name: "sample_capability_name",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.UnsupportedCapabilityEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:unsupported_capability_evidence, json)
  end

  test "unsupported_capability_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:unsupported_capability_evidence, %{})
  end


  test "upgrade_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      from_version: "sample_from_version",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.UpgradeEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["from_version"] == "sample_from_version"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:upgrade_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:upgrade_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "upgrade_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      from_version: "sample_from_version",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.UpgradeEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:upgrade_evidence, json)
  end

  test "upgrade_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:upgrade_evidence, %{})
  end


  test "upsell_readiness to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      upsell_readiness_id: "sample_upsell_readiness_id",
      account_id: "sample_account_id",
      upsell_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.UpsellReadiness.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["upsell_readiness_id"] == "sample_upsell_readiness_id"
    assert m["account_id"] == "sample_account_id"
    assert m["upsell_score"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:upsell_readiness, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:upsell_readiness, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "upsell_readiness encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      upsell_readiness_id: "sample_upsell_readiness_id",
      account_id: "sample_account_id",
      upsell_score: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.UpsellReadiness.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:upsell_readiness, json)
  end

  test "upsell_readiness from_map reports the first missing required field" do
    assert {:error, {:missing_field, :upsell_readiness_id}} =
             BeamPM.Codec.from_map(:upsell_readiness, %{})
  end


  test "usage_event to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      event_id: "sample_event_id",
      entitlement_id: "sample_entitlement_id",
      quantity: 3.5,
      metric_name: "sample_metric_name",
      occurred_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.UsageEvent.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["event_id"] == "sample_event_id"
    assert m["entitlement_id"] == "sample_entitlement_id"
    assert m["quantity"] == 3.5
    assert m["metric_name"] == "sample_metric_name"
    assert m["occurred_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:usage_event, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:usage_event, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "usage_event encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      event_id: "sample_event_id",
      entitlement_id: "sample_entitlement_id",
      quantity: 3.5,
      metric_name: "sample_metric_name",
      occurred_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.UsageEvent.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:usage_event, json)
  end

  test "usage_event from_map reports the first missing required field" do
    assert {:error, {:missing_field, :event_id}} =
             BeamPM.Codec.from_map(:usage_event, %{})
  end


  test "usage_plan to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      plan_id: "sample_plan_id",
      metric_name: "sample_metric_name",
      unit: "sample_unit",
      billing_mode: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.UsagePlan.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["plan_id"] == "sample_plan_id"
    assert m["metric_name"] == "sample_metric_name"
    assert m["unit"] == "sample_unit"
    assert m["billing_mode"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:usage_plan, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:usage_plan, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "usage_plan encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      plan_id: "sample_plan_id",
      metric_name: "sample_metric_name",
      unit: "sample_unit",
      billing_mode: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.UsagePlan.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:usage_plan, json)
  end

  test "usage_plan from_map reports the first missing required field" do
    assert {:error, {:missing_field, :plan_id}} =
             BeamPM.Codec.from_map(:usage_plan, %{})
  end


  test "usage_reconciliation_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      billing_period_id: "sample_billing_period_id",
      reconciliation_hash: "sample_reconciliation_hash"
    }

    assert {:ok, rec} = BeamPM.Types.UsageReconciliationReceipt.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["billing_period_id"] == "sample_billing_period_id"
    assert m["reconciliation_hash"] == "sample_reconciliation_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:usage_reconciliation_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:usage_reconciliation_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "usage_reconciliation_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      billing_period_id: "sample_billing_period_id",
      reconciliation_hash: "sample_reconciliation_hash"
    }

    assert {:ok, rec} = BeamPM.Types.UsageReconciliationReceipt.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:usage_reconciliation_receipt, json)
  end

  test "usage_reconciliation_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:usage_reconciliation_receipt, %{})
  end


  test "usage_signal to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      usage_signal_id: "sample_usage_signal_id",
      account_id: "sample_account_id",
      active_user_count: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.UsageSignal.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["usage_signal_id"] == "sample_usage_signal_id"
    assert m["account_id"] == "sample_account_id"
    assert m["active_user_count"] == 42
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:usage_signal, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:usage_signal, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "usage_signal encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      usage_signal_id: "sample_usage_signal_id",
      account_id: "sample_account_id",
      active_user_count: 42,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.UsageSignal.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:usage_signal, json)
  end

  test "usage_signal from_map reports the first missing required field" do
    assert {:error, {:missing_field, :usage_signal_id}} =
             BeamPM.Codec.from_map(:usage_signal, %{})
  end


  test "value_baseline to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      baseline_id: "sample_baseline_id",
      account_id: "sample_account_id",
      metric_name: "sample_metric_name",
      baseline_value: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ValueBaseline.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["baseline_id"] == "sample_baseline_id"
    assert m["account_id"] == "sample_account_id"
    assert m["metric_name"] == "sample_metric_name"
    assert m["baseline_value"] == 3.5
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:value_baseline, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:value_baseline, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "value_baseline encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      baseline_id: "sample_baseline_id",
      account_id: "sample_account_id",
      metric_name: "sample_metric_name",
      baseline_value: 3.5
    }

    assert {:ok, rec} = BeamPM.Types.ValueBaseline.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:value_baseline, json)
  end

  test "value_baseline from_map reports the first missing required field" do
    assert {:error, {:missing_field, :baseline_id}} =
             BeamPM.Codec.from_map(:value_baseline, %{})
  end


  test "value_driver to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      value_driver_id: "sample_value_driver_id",
      account_id: "sample_account_id",
      annual_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ValueDriver.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["value_driver_id"] == "sample_value_driver_id"
    assert m["account_id"] == "sample_account_id"
    assert m["annual_value"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:value_driver, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:value_driver, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "value_driver encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      value_driver_id: "sample_value_driver_id",
      account_id: "sample_account_id",
      annual_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ValueDriver.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:value_driver, json)
  end

  test "value_driver from_map reports the first missing required field" do
    assert {:error, {:missing_field, :value_driver_id}} =
             BeamPM.Codec.from_map(:value_driver, %{})
  end


  test "value_realization to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      value_realization_id: "sample_value_realization_id",
      account_id: "sample_account_id",
      realized_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ValueRealization.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["value_realization_id"] == "sample_value_realization_id"
    assert m["account_id"] == "sample_account_id"
    assert m["realized_value"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:value_realization, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:value_realization, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "value_realization encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      value_realization_id: "sample_value_realization_id",
      account_id: "sample_account_id",
      realized_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ValueRealization.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:value_realization, json)
  end

  test "value_realization from_map reports the first missing required field" do
    assert {:error, {:missing_field, :value_realization_id}} =
             BeamPM.Codec.from_map(:value_realization, %{})
  end


  test "value_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      value_receipt_id: "sample_value_receipt_id",
      account_id: "sample_account_id",
      metric_name: "sample_metric_name",
      baseline_value: 3.5,
      observed_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ValueReceipt.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["value_receipt_id"] == "sample_value_receipt_id"
    assert m["account_id"] == "sample_account_id"
    assert m["metric_name"] == "sample_metric_name"
    assert m["baseline_value"] == 3.5
    assert m["observed_value"] == 3.5
    assert m["evidence_digest"] == "sample_evidence_digest"
    assert m["observed_at"] == "2026-08-29T12:00:00Z"
    assert map_size(m) == 7
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:value_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:value_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "value_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      value_receipt_id: "sample_value_receipt_id",
      account_id: "sample_account_id",
      metric_name: "sample_metric_name",
      baseline_value: 3.5,
      observed_value: 3.5,
      evidence_digest: "sample_evidence_digest",
      observed_at: "2026-08-29T12:00:00Z"
    }

    assert {:ok, rec} = BeamPM.Types.ValueReceipt.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:value_receipt, json)
  end

  test "value_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :value_receipt_id}} =
             BeamPM.Codec.from_map(:value_receipt, %{})
  end


  test "value_telemetry_sample to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      outcome_id: "sample_outcome_id",
      telemetry_hash: "sample_telemetry_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ValueTelemetrySample.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["outcome_id"] == "sample_outcome_id"
    assert m["telemetry_hash"] == "sample_telemetry_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:value_telemetry_sample, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:value_telemetry_sample, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "value_telemetry_sample encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      outcome_id: "sample_outcome_id",
      telemetry_hash: "sample_telemetry_hash"
    }

    assert {:ok, rec} = BeamPM.Types.ValueTelemetrySample.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:value_telemetry_sample, json)
  end

  test "value_telemetry_sample from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:value_telemetry_sample, %{})
  end


  test "vendor_registration_state to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      account_id: "sample_account_id",
      registration_id: "sample_registration_id",
      registration_state: "sample_registration_state"
    }

    assert {:ok, rec} = BeamPM.Types.VendorRegistrationState.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["account_id"] == "sample_account_id"
    assert m["registration_id"] == "sample_registration_id"
    assert m["registration_state"] == "sample_registration_state"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:vendor_registration_state, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:vendor_registration_state, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "vendor_registration_state encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      account_id: "sample_account_id",
      registration_id: "sample_registration_id",
      registration_state: "sample_registration_state"
    }

    assert {:ok, rec} = BeamPM.Types.VendorRegistrationState.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:vendor_registration_state, json)
  end

  test "vendor_registration_state from_map reports the first missing required field" do
    assert {:error, {:missing_field, :account_id}} =
             BeamPM.Codec.from_map(:vendor_registration_state, %{})
  end


  test "vendor_risk_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      risk_score: 3.5,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.VendorRiskEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["risk_score"] == 3.5
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:vendor_risk_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:vendor_risk_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "vendor_risk_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      risk_score: 3.5,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.VendorRiskEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:vendor_risk_evidence, json)
  end

  test "vendor_risk_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:vendor_risk_evidence, %{})
  end


  test "version_lifecycle_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      supported_until: "2026-08-29T12:00:00Z",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.VersionLifecycleEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["supported_until"] == "2026-08-29T12:00:00Z"
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:version_lifecycle_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:version_lifecycle_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "version_lifecycle_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      supported_until: "2026-08-29T12:00:00Z",
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.VersionLifecycleEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:version_lifecycle_evidence, json)
  end

  test "version_lifecycle_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:version_lifecycle_evidence, %{})
  end


  test "volume_tier_admission to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      volume_tier_id: "sample_volume_tier_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.VolumeTierAdmission.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["opportunity_id"] == "sample_opportunity_id"
    assert m["volume_tier_id"] == "sample_volume_tier_id"
    assert m["decision"] == "sample_decision"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:volume_tier_admission, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:volume_tier_admission, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "volume_tier_admission encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      opportunity_id: "sample_opportunity_id",
      volume_tier_id: "sample_volume_tier_id",
      decision: "sample_decision"
    }

    assert {:ok, rec} = BeamPM.Types.VolumeTierAdmission.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:volume_tier_admission, json)
  end

  test "volume_tier_admission from_map reports the first missing required field" do
    assert {:error, {:missing_field, :opportunity_id}} =
             BeamPM.Codec.from_map(:volume_tier_admission, %{})
  end


  test "vulnerability_scan_evidence to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      vulnerability_count: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.VulnerabilityScanEvidence.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["evidence_id"] == "sample_evidence_id"
    assert m["subject_sha"] == "sample_subject_sha"
    assert m["vulnerability_count"] == 42
    assert m["observed_result"] == "sample_atom"
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:vulnerability_scan_evidence, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:vulnerability_scan_evidence, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "vulnerability_scan_evidence encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      evidence_id: "sample_evidence_id",
      subject_sha: "sample_subject_sha",
      vulnerability_count: 42,
      observed_result: :sample_atom
    }

    assert {:ok, rec} = BeamPM.Types.VulnerabilityScanEvidence.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:vulnerability_scan_evidence, json)
  end

  test "vulnerability_scan_evidence from_map reports the first missing required field" do
    assert {:error, {:missing_field, :evidence_id}} =
             BeamPM.Codec.from_map(:vulnerability_scan_evidence, %{})
  end


  test "workload_backpressure_signal to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      signal_id: "sample_signal_id",
      measurement_hash: "sample_measurement_hash"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadBackpressureSignal.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["signal_id"] == "sample_signal_id"
    assert m["measurement_hash"] == "sample_measurement_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:workload_backpressure_signal, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:workload_backpressure_signal, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "workload_backpressure_signal encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      signal_id: "sample_signal_id",
      measurement_hash: "sample_measurement_hash"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadBackpressureSignal.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:workload_backpressure_signal, json)
  end

  test "workload_backpressure_signal from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:workload_backpressure_signal, %{})
  end


  test "workload_cancellation_receipt to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      execution_id: "sample_execution_id",
      receipt_hash: "sample_receipt_hash"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadCancellationReceipt.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["execution_id"] == "sample_execution_id"
    assert m["receipt_hash"] == "sample_receipt_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:workload_cancellation_receipt, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:workload_cancellation_receipt, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "workload_cancellation_receipt encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      execution_id: "sample_execution_id",
      receipt_hash: "sample_receipt_hash"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadCancellationReceipt.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:workload_cancellation_receipt, json)
  end

  test "workload_cancellation_receipt from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:workload_cancellation_receipt, %{})
  end


  test "workload_execution_identity to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      execution_id: "sample_execution_id",
      subject_digest: "sample_subject_digest"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadExecutionIdentity.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["execution_id"] == "sample_execution_id"
    assert m["subject_digest"] == "sample_subject_digest"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:workload_execution_identity, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:workload_execution_identity, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "workload_execution_identity encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      execution_id: "sample_execution_id",
      subject_digest: "sample_subject_digest"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadExecutionIdentity.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:workload_execution_identity, json)
  end

  test "workload_execution_identity from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:workload_execution_identity, %{})
  end


  test "workload_idempotency_key to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      execution_id: "sample_execution_id",
      idempotency_key: "sample_idempotency_key"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadIdempotencyKey.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["execution_id"] == "sample_execution_id"
    assert m["idempotency_key"] == "sample_idempotency_key"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:workload_idempotency_key, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:workload_idempotency_key, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "workload_idempotency_key encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      execution_id: "sample_execution_id",
      idempotency_key: "sample_idempotency_key"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadIdempotencyKey.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:workload_idempotency_key, json)
  end

  test "workload_idempotency_key from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:workload_idempotency_key, %{})
  end


  test "workload_queue_depth to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      queue_id: "sample_queue_id",
      measurement_hash: "sample_measurement_hash"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadQueueDepth.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["queue_id"] == "sample_queue_id"
    assert m["measurement_hash"] == "sample_measurement_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:workload_queue_depth, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:workload_queue_depth, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "workload_queue_depth encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      queue_id: "sample_queue_id",
      measurement_hash: "sample_measurement_hash"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadQueueDepth.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:workload_queue_depth, json)
  end

  test "workload_queue_depth from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:workload_queue_depth, %{})
  end


  test "workload_retry_policy to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      policy_id: "sample_policy_id",
      policy_hash: "sample_policy_hash"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadRetryPolicy.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["policy_id"] == "sample_policy_id"
    assert m["policy_hash"] == "sample_policy_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:workload_retry_policy, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:workload_retry_policy, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "workload_retry_policy encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      policy_id: "sample_policy_id",
      policy_hash: "sample_policy_hash"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadRetryPolicy.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:workload_retry_policy, json)
  end

  test "workload_retry_policy from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:workload_retry_policy, %{})
  end


  test "workload_timeout_budget to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      budget_id: "sample_budget_id",
      budget_hash: "sample_budget_hash"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadTimeoutBudget.new(attrs)
    m = BeamPM.Codec.to_map(rec)
    assert m["tenant_id"] == "sample_tenant_id"
    assert m["budget_id"] == "sample_budget_id"
    assert m["budget_hash"] == "sample_budget_hash"
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.Codec.from_map(:workload_timeout_budget, m)
    assert {:ok, ^rec} =
             BeamPM.Codec.from_map(:workload_timeout_budget, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "workload_timeout_budget encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      tenant_id: "sample_tenant_id",
      budget_id: "sample_budget_id",
      budget_hash: "sample_budget_hash"
    }

    assert {:ok, rec} = BeamPM.Types.WorkloadTimeoutBudget.new(attrs)
    json = BeamPM.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.Codec.decode(:workload_timeout_budget, json)
  end

  test "workload_timeout_budget from_map reports the first missing required field" do
    assert {:error, {:missing_field, :tenant_id}} =
             BeamPM.Codec.from_map(:workload_timeout_budget, %{})
  end

end
