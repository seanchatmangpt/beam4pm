# GENERATED by ggen from the admitted mcp:MessageType graph. Do not edit.
defmodule BeamPM.MCP.Contracts.Codec.GeneratedTest do
  use ExUnit.Case, async: true

  test "from_map returns unknown_record for an unadmitted record name" do
    assert {:error, {:unknown_record, :not_a_real_record}} =
             BeamPM.MCP.Contracts.Codec.from_map(:not_a_real_record, %{})
  end

  test "decode returns unknown_record for an unadmitted record name" do
    assert {:error, {:unknown_record, :not_a_real_record}} =
             BeamPM.MCP.Contracts.Codec.decode(:not_a_real_record, "{}")
  end

  test "decode wraps invalid JSON input in an error tuple" do
    assert {:error, _} = BeamPM.MCP.Contracts.Codec.decode(:not_a_real_record, "this is not json")
  end

  test "decode rejects JSON that is not an object" do
    assert {:error, {:not_a_json_object, [1, 2, 3]}} =
             BeamPM.MCP.Contracts.Codec.decode(:not_a_real_record, "[1,2,3]")
  end

  test "mcp_resource to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      uri: "sample_uri",
      name: "sample_name",
      title: "sample_title",
      description: "sample_description",
      mime_type: "sample_mime_type",
      size: 42
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpResource.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    assert m["uri"] == "sample_uri"
    assert m["name"] == "sample_name"
    assert m["title"] == "sample_title"
    assert m["description"] == "sample_description"
    assert m["mimeType"] == "sample_mime_type"
    assert m["size"] == 42
    assert map_size(m) == 6
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:mcp_resource, m)
    assert {:ok, ^rec} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_resource, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "mcp_resource encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      uri: "sample_uri",
      name: "sample_name",
      title: "sample_title",
      description: "sample_description",
      mime_type: "sample_mime_type",
      size: 42
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpResource.new(attrs)
    json = BeamPM.MCP.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.decode(:mcp_resource, json)
  end

  test "mcp_resource from_map reports the first missing required field" do
    assert {:error, {:missing_field, :uri}} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_resource, %{})
  end

  test "mcp_resource to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      uri: "sample_uri",
      name: "sample_name"
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpResource.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "title")
    refute Map.has_key?(m, "description")
    refute Map.has_key?(m, "mimeType")
    refute Map.has_key?(m, "size")
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:mcp_resource, m)
  end

  test "mcp_resource_read_request to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      uri: "sample_uri"
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpResourceReadRequest.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    assert m["uri"] == "sample_uri"
    assert map_size(m) == 1
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:mcp_resource_read_request, m)
    assert {:ok, ^rec} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_resource_read_request, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "mcp_resource_read_request encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      uri: "sample_uri"
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpResourceReadRequest.new(attrs)
    json = BeamPM.MCP.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.decode(:mcp_resource_read_request, json)
  end

  test "mcp_resource_read_request from_map reports the first missing required field" do
    assert {:error, {:missing_field, :uri}} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_resource_read_request, %{})
  end


  test "mcp_resource_read_result to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      contents: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpResourceReadResult.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    assert m["contents"] == %{"k" => "v"}
    assert map_size(m) == 1
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:mcp_resource_read_result, m)
    assert {:ok, ^rec} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_resource_read_result, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "mcp_resource_read_result encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      contents: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpResourceReadResult.new(attrs)
    json = BeamPM.MCP.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.decode(:mcp_resource_read_result, json)
  end

  test "mcp_resource_read_result from_map reports the first missing required field" do
    assert {:error, {:missing_field, :contents}} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_resource_read_result, %{})
  end


  test "mcp_tool to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      name: "sample_name",
      title: "sample_title",
      description: "sample_description",
      input_schema: %{"k" => "v"},
      output_schema: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpTool.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    assert m["name"] == "sample_name"
    assert m["title"] == "sample_title"
    assert m["description"] == "sample_description"
    assert m["inputSchema"] == %{"k" => "v"}
    assert m["outputSchema"] == %{"k" => "v"}
    assert map_size(m) == 5
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool, m)
    assert {:ok, ^rec} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "mcp_tool encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      name: "sample_name",
      title: "sample_title",
      description: "sample_description",
      input_schema: %{"k" => "v"},
      output_schema: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpTool.new(attrs)
    json = BeamPM.MCP.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.decode(:mcp_tool, json)
  end

  test "mcp_tool from_map reports the first missing required field" do
    assert {:error, {:missing_field, :name}} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool, %{})
  end

  test "mcp_tool to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      name: "sample_name",
      input_schema: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpTool.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "title")
    refute Map.has_key?(m, "description")
    refute Map.has_key?(m, "outputSchema")
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool, m)
  end

  test "mcp_tool_call_request to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      name: "sample_name",
      arguments: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpToolCallRequest.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    assert m["name"] == "sample_name"
    assert m["arguments"] == %{"k" => "v"}
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool_call_request, m)
    assert {:ok, ^rec} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool_call_request, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "mcp_tool_call_request encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      name: "sample_name",
      arguments: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpToolCallRequest.new(attrs)
    json = BeamPM.MCP.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.decode(:mcp_tool_call_request, json)
  end

  test "mcp_tool_call_request from_map reports the first missing required field" do
    assert {:error, {:missing_field, :name}} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool_call_request, %{})
  end

  test "mcp_tool_call_request to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      name: "sample_name"
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpToolCallRequest.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "arguments")
    assert map_size(m) == 1
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool_call_request, m)
  end

  test "mcp_tool_call_result to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      content: %{"k" => "v"},
      is_error: true,
      structured_content: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpToolCallResult.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    assert m["content"] == %{"k" => "v"}
    assert m["isError"] == true
    assert m["structuredContent"] == %{"k" => "v"}
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool_call_result, m)
    assert {:ok, ^rec} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool_call_result, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "mcp_tool_call_result encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      content: %{"k" => "v"},
      is_error: true,
      structured_content: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpToolCallResult.new(attrs)
    json = BeamPM.MCP.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.decode(:mcp_tool_call_result, json)
  end

  test "mcp_tool_call_result from_map reports the first missing required field" do
    assert {:error, {:missing_field, :content}} =
             BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool_call_result, %{})
  end

  test "mcp_tool_call_result to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      content: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpToolCallResult.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "isError")
    refute Map.has_key?(m, "structuredContent")
    assert map_size(m) == 1
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool_call_result, m)
  end
  test "mcp_tool_call_result to_map/from_map keeps an explicit false is_error (not the same as absent)" do
    attrs = %{
      content: %{"k" => "v"},
      is_error: false,
      structured_content: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.McpToolCallResult.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    assert Map.has_key?(m, "isError")
    assert m["isError"] == false
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:mcp_tool_call_result, m)
  end

  test "rpc_error_response to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      jsonrpc: "sample_jsonrpc",
      id: "sample_id",
      error: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.RpcErrorResponse.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    assert m["jsonrpc"] == "sample_jsonrpc"
    assert m["id"] == "sample_id"
    assert m["error"] == %{"k" => "v"}
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:rpc_error_response, m)
    assert {:ok, ^rec} =
             BeamPM.MCP.Contracts.Codec.from_map(:rpc_error_response, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "rpc_error_response encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      jsonrpc: "sample_jsonrpc",
      id: "sample_id",
      error: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.RpcErrorResponse.new(attrs)
    json = BeamPM.MCP.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.decode(:rpc_error_response, json)
  end

  test "rpc_error_response from_map reports the first missing required field" do
    assert {:error, {:missing_field, :jsonrpc}} =
             BeamPM.MCP.Contracts.Codec.from_map(:rpc_error_response, %{})
  end


  test "rpc_notification to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      jsonrpc: "sample_jsonrpc",
      method: "sample_method",
      params: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.RpcNotification.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    assert m["jsonrpc"] == "sample_jsonrpc"
    assert m["method"] == "sample_method"
    assert m["params"] == %{"k" => "v"}
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:rpc_notification, m)
    assert {:ok, ^rec} =
             BeamPM.MCP.Contracts.Codec.from_map(:rpc_notification, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "rpc_notification encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      jsonrpc: "sample_jsonrpc",
      method: "sample_method",
      params: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.RpcNotification.new(attrs)
    json = BeamPM.MCP.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.decode(:rpc_notification, json)
  end

  test "rpc_notification from_map reports the first missing required field" do
    assert {:error, {:missing_field, :jsonrpc}} =
             BeamPM.MCP.Contracts.Codec.from_map(:rpc_notification, %{})
  end

  test "rpc_notification to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      jsonrpc: "sample_jsonrpc",
      method: "sample_method"
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.RpcNotification.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "params")
    assert map_size(m) == 2
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:rpc_notification, m)
  end

  test "rpc_request to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      jsonrpc: "sample_jsonrpc",
      id: "sample_id",
      method: "sample_method",
      params: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.RpcRequest.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    assert m["jsonrpc"] == "sample_jsonrpc"
    assert m["id"] == "sample_id"
    assert m["method"] == "sample_method"
    assert m["params"] == %{"k" => "v"}
    assert map_size(m) == 4
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:rpc_request, m)
    assert {:ok, ^rec} =
             BeamPM.MCP.Contracts.Codec.from_map(:rpc_request, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "rpc_request encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      jsonrpc: "sample_jsonrpc",
      id: "sample_id",
      method: "sample_method",
      params: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.RpcRequest.new(attrs)
    json = BeamPM.MCP.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.decode(:rpc_request, json)
  end

  test "rpc_request from_map reports the first missing required field" do
    assert {:error, {:missing_field, :jsonrpc}} =
             BeamPM.MCP.Contracts.Codec.from_map(:rpc_request, %{})
  end

  test "rpc_request to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
      jsonrpc: "sample_jsonrpc",
      id: "sample_id",
      method: "sample_method"
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.RpcRequest.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    refute Map.has_key?(m, "params")
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:rpc_request, m)
  end

  test "rpc_result_response to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
      jsonrpc: "sample_jsonrpc",
      id: "sample_id",
      result: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.RpcResultResponse.new(attrs)
    m = BeamPM.MCP.Contracts.Codec.to_map(rec)
    assert m["jsonrpc"] == "sample_jsonrpc"
    assert m["id"] == "sample_id"
    assert m["result"] == %{"k" => "v"}
    assert map_size(m) == 3
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.from_map(:rpc_result_response, m)
    assert {:ok, ^rec} =
             BeamPM.MCP.Contracts.Codec.from_map(:rpc_result_response, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "rpc_result_response encode/decode JSON roundtrip (full variant)" do
    attrs = %{
      jsonrpc: "sample_jsonrpc",
      id: "sample_id",
      result: %{"k" => "v"}
    }

    assert {:ok, rec} = BeamPM.MCP.Contracts.RpcResultResponse.new(attrs)
    json = BeamPM.MCP.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.MCP.Contracts.Codec.decode(:rpc_result_response, json)
  end

  test "rpc_result_response from_map reports the first missing required field" do
    assert {:error, {:missing_field, :jsonrpc}} =
             BeamPM.MCP.Contracts.Codec.from_map(:rpc_result_response, %{})
  end

end
