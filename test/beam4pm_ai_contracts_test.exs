# GENERATED by ggen from the admitted aic:ContractType graph. Do not edit.
defmodule BeamPM.AI.Contracts.GeneratedTest do
  use ExUnit.Case, async: true

  test "admission_decision new/1 succeeds when all fields are present" do
    attrs = %{
      decision_id: "x",
      subject_id: "x",
      principal_id: "x",
      verdict: "x",
      typed_reason: "x",
      policy_id: "x",
      policy_version: "x",
      decided_at: "2026-01-01T00:00:00Z"
    }

    assert {:ok, _} = BeamPM.AI.Contracts.AdmissionDecision.new(attrs)
  end

  test "admission_decision new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.AI.Contracts.AdmissionDecision.new(%{})
  end


  test "candidate_claim new/1 succeeds when all fields are present" do
    attrs = %{
      claim_id: "x",
      task_id: "x",
      statement: "x",
      support_evidence_ids: ["a"],
      contradict_evidence_ids: ["a"],
      confidence: 1.0,
      semantic_status: "x"
    }

    assert {:ok, _} = BeamPM.AI.Contracts.CandidateClaim.new(attrs)
  end

  test "candidate_claim new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.AI.Contracts.CandidateClaim.new(%{})
  end


  test "evidence_envelope new/1 succeeds when all fields are present" do
    attrs = %{
      evidence_id: "x",
      tenant_id: "x",
      source_uri: "x",
      source_kind: "x",
      content_hash: "x",
      mime_type: "x",
      modality: "x",
      observed_at: "2026-01-01T00:00:00Z",
      retrieved_at: "2026-01-01T00:00:00Z",
      acl_labels: ["a"],
      canonical_entity_ids: ["a"],
      redaction_class: "x",
      schema_version: "x"
    }

    assert {:ok, _} = BeamPM.AI.Contracts.EvidenceEnvelope.new(attrs)
  end

  test "evidence_envelope new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.AI.Contracts.EvidenceEnvelope.new(%{})
  end


  test "evidence_hit new/1 succeeds when all fields are present" do
    attrs = %{
      hit_id: "x",
      query_id: "x",
      evidence_id: "x",
      score: 1.0,
      retrieval_method: "x",
      rank: 1
    }

    assert {:ok, _} = BeamPM.AI.Contracts.EvidenceHit.new(attrs)
  end

  test "evidence_hit new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.AI.Contracts.EvidenceHit.new(%{})
  end


  test "model_call_receipt new/1 succeeds when all fields are present" do
    attrs = %{
      call_id: "x",
      provider_profile: "x",
      model_id_or_snapshot: "x",
      request_hash: "x",
      response_hash: "x",
      prompt_template_id: "x",
      prompt_template_version: "x",
      latency_ms: 1,
      trace_id: "x"
    }

    assert {:ok, _} = BeamPM.AI.Contracts.ModelCallReceipt.new(attrs)
  end

  test "model_call_receipt new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.AI.Contracts.ModelCallReceipt.new(%{})
  end


  test "research_task new/1 succeeds when all fields are present" do
    attrs = %{
      task_id: "x",
      principal_id: "x",
      query: "x",
      freshness_requirement: "x",
      privacy_class: "x",
      max_tokens: 1,
      max_tool_calls: 1,
      output_contract: "x",
      policy_version: "x",
      correlation_id: "x"
    }

    assert {:ok, _} = BeamPM.AI.Contracts.ResearchTask.new(attrs)
  end

  test "research_task new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.AI.Contracts.ResearchTask.new(%{})
  end


  test "tool_intent new/1 succeeds when all fields are present" do
    attrs = %{
      intent_id: "x",
      task_id: "x",
      tool_id: "x",
      tool_version: "x",
      canonical_args_hash: "x",
      side_effect_class: "x",
      required_capability: "x",
      dry_run: true,
      idempotency_key: "x",
      status: "x"
    }

    assert {:ok, _} = BeamPM.AI.Contracts.ToolIntent.new(attrs)
  end

  test "tool_intent new/1 returns an error when a required field is missing" do
    assert {:error, {:missing_field, _}} = BeamPM.AI.Contracts.ToolIntent.new(%{})
  end

end
