%% GENERATED by ggen from the admitted mcp:MessageType graph. Do not edit.

-module(beam4pm_mcp_contracts_tests).

-include_lib("eunit/include/eunit.hrl").

%% No explicit -export here: EUnit's own parse transform auto-exports every
%% *_test/0 function in this module, and an explicit -export of the same
%% functions produces a real "already exported" compiler warning (which
%% would become a hard failure under warnings_as_errors).

mcp_resource_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_mcp_contracts:new_mcp_resource(#{
        uri => <<"x">>,
        name => <<"x">>,
        title => <<"x">>,
        description => <<"x">>,
        mime_type => <<"x">>,
        size => 1
    })).

mcp_resource_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_mcp_contracts:new_mcp_resource(#{})).

mcp_resource_read_request_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_mcp_contracts:new_mcp_resource_read_request(#{
        uri => <<"x">>
    })).

mcp_resource_read_request_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_mcp_contracts:new_mcp_resource_read_request(#{})).

mcp_resource_read_result_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_mcp_contracts:new_mcp_resource_read_result(#{
        contents => #{}
    })).

mcp_resource_read_result_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_mcp_contracts:new_mcp_resource_read_result(#{})).

mcp_tool_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_mcp_contracts:new_mcp_tool(#{
        name => <<"x">>,
        title => <<"x">>,
        description => <<"x">>,
        input_schema => #{},
        output_schema => #{}
    })).

mcp_tool_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_mcp_contracts:new_mcp_tool(#{})).

mcp_tool_call_request_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_mcp_contracts:new_mcp_tool_call_request(#{
        name => <<"x">>,
        arguments => #{}
    })).

mcp_tool_call_request_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_mcp_contracts:new_mcp_tool_call_request(#{})).

mcp_tool_call_result_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_mcp_contracts:new_mcp_tool_call_result(#{
        content => #{},
        is_error => true,
        structured_content => #{}
    })).

mcp_tool_call_result_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_mcp_contracts:new_mcp_tool_call_result(#{})).

rpc_error_response_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_mcp_contracts:new_rpc_error_response(#{
        jsonrpc => <<"x">>,
        id => <<"x">>,
        error => #{}
    })).

rpc_error_response_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_mcp_contracts:new_rpc_error_response(#{})).

rpc_notification_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_mcp_contracts:new_rpc_notification(#{
        jsonrpc => <<"x">>,
        method => <<"x">>,
        params => #{}
    })).

rpc_notification_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_mcp_contracts:new_rpc_notification(#{})).

rpc_request_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_mcp_contracts:new_rpc_request(#{
        jsonrpc => <<"x">>,
        id => <<"x">>,
        method => <<"x">>,
        params => #{}
    })).

rpc_request_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_mcp_contracts:new_rpc_request(#{})).

rpc_result_response_ok_test() ->
    ?assertMatch({ok, _}, beam4pm_mcp_contracts:new_rpc_result_response(#{
        jsonrpc => <<"x">>,
        id => <<"x">>,
        result => #{}
    })).

rpc_result_response_missing_field_test() ->
    ?assertMatch({error, {missing_field, _}}, beam4pm_mcp_contracts:new_rpc_result_response(#{})).

