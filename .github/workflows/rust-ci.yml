# GENERATED by ggen (github-actions-pack/templates/workflow.yml.tmpl). Do not edit.
# Source of truth: this consumer's own admitted gha:Workflow/gha:Job/gha:Step facts.
name: test native Rust engine crates (native/ferroplan)
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:


concurrency:
  group: rust-ci-${{ github.ref }}
  cancel-in-progress: true


permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read

    name: native/ferroplan cargo build + test + clippy

    steps:
      - name: Checkout (with vendor/ggen-marketplace submodule)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          submodules: recursive
        
      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          components: clippy
          toolchain: stable
        
      - name: Cache Cargo dependencies for native/ferroplan
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          restore-keys: "${{ runner.os }}-cargo-\n"
          key: ${{ runner.os }}-cargo-${{ hashFiles('native/ferroplan/Cargo.lock') }}
          path: "native/ferroplan/target"
        
      - name: cargo build --workspace (native/ferroplan)
        shell: bash
        run: |
          cd native/ferroplan && cargo build --workspace

      - name: cargo test --workspace (native/ferroplan)
        shell: bash
        run: |
          cd native/ferroplan && cargo test --workspace

      - name: cargo clippy --workspace --all-targets -- -D warnings (native/ferroplan)
        shell: bash
        run: |
          cd native/ferroplan && cargo clippy --workspace --all-targets -- -D warnings

      - name: cargo audit (advisory, non-blocking)
        shell: bash
        run: |
          cargo install cargo-audit --locked || true
          cd native/ferroplan && cargo audit || echo "::warning::pre-existing RUSTSEC debt remains advisory"
          

