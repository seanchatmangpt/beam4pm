// GENERATED by ggen from the admitted bpm:Engine graph (tract). Do not edit.
//// Facade over the tract native wasm engine hosted by Elixir/wasmex --
//// A real ONNX-inference engine (sonos/tract -- the same crate Wasmtime's own WASI-NN implementation uses for this exact target, native/tract-wasm): graph optimization, the compiled runnable plan and the actual tensor ops all happen inside the wasm module; this side only frames base64 ONNX bytes and JSON tensor descriptors. A model is compiled to a runnable plan at load time, so a malformed or unsupported-op ONNX file fails loudly at load, never on first inference; only f32 tensors are supported and anything else is refused explicitly, never coerced.
////
//// Every function here is an `@external` delegation to `Elixir.BeamPM.Tract`
//// (lib/beam4pm_tract.ex), which drives the single wasm32-wasip1 engine
//// (native/tract-wasm/target/wasm32-wasip1/release/tract_wasm.wasm). No computation happens in Gleam -- this module is a
//// typed facade over the one engine. This file, that wrapper and the Erlang
//// facade src/beam4pm_tract.erl are all rendered from the same admitted
//// bpm:Engine / bpm:EngineOp / bpm:EngineArg facts.
////
//// Compile-checked by `gleam build` (externals are not resolved at compile
//// time); runtime-tested ONLY from the mix context
//// (test/beam4pm_tract_facades_test.exs appends gleam/build/dev/erlang/
//// beam4pm/ebin to the code path), because `gleam test`/gleeunit runs
//// without wasmex on its path. Documented, not faked.
////
//// Runtime contract (inherited verbatim from BeamPM.Tract):
////   - Elixir's `{:ok, map} | {:error, reason}` tuples ARE Gleam's `Result`
////     runtime representation, so every call types as
////     `Result(Dynamic, Dynamic)`: Ok carries a binary-keyed map decoded
////     from the engine's JSON response; Error carries
////     `{:engine, msg}` / `{:wasmex, term}` tagged tuples.
////   - `start()` must have been called (or the engine supervised) in the
////     hosting BEAM before any other function; a restart produces a fresh
////     wasm store, so ALL previously returned model handles are invalid.
////   - Ops with optional trailing args in Elixir (an optional positional, a
////     keyword-list option, the `opts` timeout list) are bound here at their
////     minimal-arity default-argument heads only; there is no clean Gleam
////     representation of an Elixir keyword list, so no extra-arity variant
////     is invented.
////   - Arguments typed `Dynamic` (maps, tensor lists) are shapes Gleam's type
////     system cannot express without a bespoke record mirrored on both sides
////     of the boundary; callers build the shape themselves, the same
////     honesty-over-invention convention as the rest of the facade family.

import gleam/dynamic.{type Dynamic}

/// Start (or find already started) the named singleton wasmex engine
/// hosting tract_wasm.wasm. Idempotent; `Ok` carries the engine pid.
@external(erlang, "Elixir.BeamPM.Tract", "start")
pub fn start() -> Result(Dynamic, Dynamic)

/// `{"op":"load_model","model_b64":...}` -- `model_bytes` is the raw ONNX protobuf (base64-encoded host-side; JSON strings cannot carry raw protobuf). Optimizes and compiles to a runnable plan immediately. `{:ok, %{"handle" => n, "inputs" => [%{"name" => s, "shape" => [dims|nil]}], "outputs" => [...]}}` -- a `nil` dim is a symbolic/dynamic axis tract could not fully resolve at load time, reported honestly rather than guessed.
@external(erlang, "Elixir.BeamPM.Tract", "load_model")
pub fn load_model(model_bytes: BitArray) -> Result(Dynamic, Dynamic)

/// `File.read!/1` host-side, then `load_model/2` (raises if the path is absent).
@external(erlang, "Elixir.BeamPM.Tract", "load_model_path")
pub fn load_model_path(path: String) -> Result(Dynamic, Dynamic)

/// `{"op":"run","handle":h,"inputs":[{"shape":[dims],"data":[f32,...]}]}` -- one f32 tensor per declared model input, in order (`inputs` a list of `%{shape: [dims], data: [floats]}`, converted to the wire's string-keyed shape here). `{:ok, %{"outputs" => [%{"shape" => [dims], "data" => [floats]}]}}`, one per declared model output, in order. Real tensor inference, even on a tiny model, draws the heavy timeout budget.
@external(erlang, "Elixir.BeamPM.Tract", "run")
pub fn run(handle: Int, inputs: Dynamic) -> Result(Dynamic, Dynamic)

/// `{"op":"model_info","handle":h}` -- same input/output fact shape as `load_model/2`.
@external(erlang, "Elixir.BeamPM.Tract", "model_info")
pub fn model_info(handle: Int) -> Result(Dynamic, Dynamic)

/// `{"op":"free_model","handle":h}` -- releases the model handle; subsequent use errors with an unknown-handle message.
@external(erlang, "Elixir.BeamPM.Tract", "free_model")
pub fn free_model(handle: Int) -> Result(Dynamic, Dynamic)
